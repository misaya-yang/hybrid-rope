# Tools

## Active utility

- `sync_results_data_only.ps1`
  - Pulls remote `results/` data without model/checkpoint weights.
  - Uses `plink` + `pscp` + remote `tar` excludes.

## Archived remote scripts

- `remote_legacy/`
  - Historical one-off upload/download/run helpers moved from project root.


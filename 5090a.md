# 5090a — Phase 12 r-sweep 交接文档

> 创建时间: 2026-03-03
> 服务器: 5090 32GB (AutoDL westd)
> 状态: **实验正在跑，预计 ~1h 后全部完成**

---

## 1. 服务器信息

```
SSH:  ssh -p 48787 root@connect.westd.seetacloud.com
密码: yIKaPV0fHS/Q
GPU:  NVIDIA GeForce RTX 5090, 32GB VRAM, CUDA 13.0
Python: /root/miniconda3/bin/python (3.12.3, torch 2.8.0+cu128)
```

**重要**: 服务器上 `python3` 不在 PATH，需要：
```bash
export PATH="/root/miniconda3/bin:$PATH"
export HF_ENDPOINT=https://hf-mirror.com
```

---

## 2. 已完成的工作

### 2.1 代码改动 (本地 `scripts/m4_evq_sweep/run_evq_sweep.py`)

在原有 τ-sweep 基础上添加了 r-sweep 支持：

1. **新函数 `hybrid_evq_inv_freq(head_dim, tau, r, base)`** — 前 r 通道保持 Geometric，剩余通道用 EVQ warp
2. **新 CLI 参数**:
   - `--r_values`: 逗号分隔的 r 值列表（如 `0,4,8,12,14,16,20,24,32`）
   - `--fixed_tau`: r-sweep 时固定的 τ 值
   - `--train_tokens`: 覆盖 tier 默认训练 token 数
3. **修改 `run_single()`**: 新增 `override_inv_freq` 和 `override_run_id` 可选参数
4. **修改 `main()`**: 检测 `--r_values` 后进入 r-sweep 模式，waterbed 分析以 r=32 为基线

代码已同步到服务器 `/root/autodl-tmp/dfrope/hybrid-rope/scripts/m4_evq_sweep/`。

### 2.2 实验运行

**Pilot** (r=0,14,16,32, seed=42) — ✅ 已完成
**Full 9-point sweep** (r=0,4,8,12,14,16,20,24,32, seed=42) — 🔄 进行中 (7/9 完成)

服务器进程 PID=5727，后台运行中：
```bash
# 查看日志
tail -f /root/autodl-tmp/evq_phase12_r_sweep/full_seed42.log

# 查看已完成结果
cat /root/autodl-tmp/evq_phase12_r_sweep/results_checkpoint.json | python3 -m json.tool
```

---

## 3. 当前实验结果 (7/9 runs, seed=42)

### 3.1 PPL 数据

| r  | 含义          | PPL@2K | PPL@4K | PPL@8K | PPL@16K | Δ@2K   | Δ@16K   |
|----|---------------|--------|--------|--------|---------|--------|---------|
| 0  | Full EVQ      | 97.1   | 124.2  | 162.3  | 251.6   | +4.5%  | -13.6%  |
| 4  | 极重 warp     | 96.5   | 125.0  | 163.3  | **247.1** | +3.8%  | **-15.1%** |
| 8  | 重 warp       | 95.5   | 125.1  | 168.4  | 254.5   | +2.8%  | -12.5%  |
| 12 | 中等          | 96.8   | 128.4  | 177.8  | 273.2   | +4.1%  | -6.1%   |
| 14 | r* 理论预测   | 95.5   | 129.5  | 175.7  | 261.2   | +2.7%  | -10.2%  |
| 16 | 当前默认      | 94.4   | 125.7  | 175.4  | 270.4   | +1.5%  | -7.1%   |
| 20 | 轻 warp       | 🔄 训练中 |
| 24 | 极轻 warp     | ⏳ 待跑 |
| 32 | Geometric     | 92.9   | 127.5  | 185.6  | 291.1   | 基线   | 基线    |

Δ 列以 r=32 (Geometric) 为基线。

### 3.2 Passkey 检索率

| r  | PK全局 |
|----|--------|
| 0  | 55%    |
| 4  | 55%    |
| 8  | 52%    |
| 12 | 46%    |
| 14 | 45%    |
| 16 | 50%    |
| 32 | 40%    |

### 3.3 初步结论

1. **Waterbed 完美单调**: 更多 EVQ 通道 → 更好的长程 PPL，更差的短程 PPL
2. **r=4 是 PPL@16K 最优点** (-15.1%)，而非理论预测的 r=14
3. **r=14 与 r=16 非常接近**，差距 <3%
4. 所有 EVQ 变体的 passkey 都优于 Geometric (40%)
5. r=4 PPL 曲线比 r=0 更优（短程代价更低，长程改善更大）

---

## 4. 服务器文件结构

```
/root/autodl-tmp/evq_phase12_r_sweep/
├── results_checkpoint.json    # 实时结果（每个 run 完成后更新）
├── results_final.json         # pilot 的最终结果
├── pilot_seed42.log           # pilot 日志
├── full_seed42.log            # 当前 full sweep 日志
├── train_fineweb-edu_50000000_2048.pt   # 训练数据缓存 (400MB)
├── val_fineweb-edu_5000000.pt           # 验证数据缓存 (40MB)
├── 350m_r0_tau1.50_seed42/    # 各 run 的 checkpoint 目录
│   ├── model.pt
│   ├── inv_freq.npy
│   └── passkey_nll.json
├── 350m_r4_tau1.50_seed42/
├── 350m_r8_tau1.50_seed42/
├── 350m_r12_tau1.50_seed42/
├── 350m_r14_tau1.50_seed42/
├── 350m_r16_tau1.50_seed42/
├── 350m_r32_tau1.50_seed42/
└── ... (r=20, r=24 will appear when done)
```

---

## 5. 实验执行计划 (全自动，已排队)

服务器上已部署链式脚本 `run_all.sh`，**无需手动干预**，会自动依次执行：

| 阶段 | 内容 | 预计时间 | 状态 |
|------|------|----------|------|
| 1/3 | seed=42 full 9 点 r-sweep (r=20,24) | ~1h | 🔄 进行中 |
| 2/3 | seed=137 关键 r 值 (0,4,14,16,32) | ~3h | ⏳ 排队 |
| 3/3 | τ-r 联合验证: r=14, τ=0.5,1.0,2.0,2.5 | ~2.5h | ⏳ 排队 |
| | **总计** | **~6.5h** | |

**验证 τ* 的意义**: 理论预测 τ*(L=2048) = 64/√2048 ≈ 1.41，实验用 τ=1.5。扫描 τ=0.5,1.0,1.5,2.0,2.5 可以验证 τ=1.5 附近确实是最优的。

```bash
# 监控总进度
tail -f /root/autodl-tmp/evq_phase12_r_sweep/run_all.log
# 看到 "ALL PHASE 12 EXPERIMENTS DONE" 就全部跑完了

# 或者分别看各阶段日志
tail -f /root/autodl-tmp/evq_phase12_r_sweep/full_seed42.log     # 阶段1
tail -f /root/autodl-tmp/evq_phase12_r_sweep/seed137.log          # 阶段2
tail -f /root/autodl-tmp/evq_phase12_r_sweep/tau_sweep_r14.log    # 阶段3
```

### 5.1 接手后：拉取完整结果到本地

等 `run_all.log` 出现 "ALL PHASE 12 EXPERIMENTS DONE" 后：

```bash
# 在本地执行
mkdir -p results/phase12_r_sweep
sshpass -p 'yIKaPV0fHS/Q' scp -P 48787 \
    root@connect.westd.seetacloud.com:/root/autodl-tmp/evq_phase12_r_sweep/results_checkpoint.json \
    results/phase12_r_sweep/

# 拉 inv_freq（论文用）
for r in 0 4 8 12 14 16 20 24 32; do
    sshpass -p 'yIKaPV0fHS/Q' scp -P 48787 \
        root@connect.westd.seetacloud.com:/root/autodl-tmp/evq_phase12_r_sweep/350m_r${r}_tau1.50_seed42/inv_freq.npy \
        results/phase12_r_sweep/inv_freq_r${r}.npy 2>/dev/null
done
```

### 5.2 写分析报告

位置: `docs/exp/2026-03-03_phase12_r_sweep_report.md`

需要包含:
- 完整 9 点 PPL 表 + Passkey 表
- Pareto 曲线图 (x=PPL@2K, y=PPL@16K, 标注 r 值和 r*)
- 理论验证检查清单 (见 `docs/prompts/PROMPT_PHASE12_R_SWEEP.md` §6.4)
- r=4 意外最优的解释

### 5.5 更新论文

- 新增 r-sweep Table（参考 `docs/prompts/PROMPT_PHASE12_R_SWEEP.md` §9.2 模板）
- 新增 Pareto Figure
- 更新 `docs/paperdraft/CORE_THEORY.md` §11.6 补充 r* 验证数据
- 调整 r* 公式的 claim（如果 r=4 确实优于 r=14，需要讨论）

---

## 6. 关键参考文件

| 文件 | 用途 |
|------|------|
| `docs/prompts/PROMPT_PHASE12_R_SWEEP.md` | 完整实验计划（553行），含理论背景、分析模板、决策树 |
| `scripts/m4_evq_sweep/run_evq_sweep.py` | 主实验脚本（已改动，含 r-sweep 支持）|
| `scripts/m4_evq_sweep/eval_passkey_scratch.py` | Passkey 评估脚本 |
| `docs/paperdraft/CORE_THEORY.md` | 核心理论文档（r* 公式在 §7）|
| `docs/exp/2026-03-03_phase9f_50pct_checkpoint_report.md` | Phase9F 750M 完整报告 |
| `results/phase9f_750m_2k_1b/` | Phase9F 已拉回的结果 |

---

## 7. 注意事项

1. **不要关服务器** — 实验还在跑，r=20 和 r=24 未完成
2. **results_checkpoint.json 是实时的** — 每个 run 完成后自动更新
3. **数据缓存已存好** — train/val `.pt` 文件在 work_dir，新 run 不需要重新下载
4. **`--resume` 会跳过已完成 run** — 可以安全重跑命令
5. **5090 是 32GB 卡** — 350m 模型 batch=2，16K eval 有 OOM 风险但代码有保护
6. **r=12 的数据** 在 checkpoint.json 里但本文档写时未来得及展示，直接读 JSON 即可

Re-reading your review against our response, two things you asked for were already answered in the submitted version and we pointed to neither.

1. Your first point — the operating rule. Our response separated the scaling form from the constant and stopped there. The submission also checks the analysis against the exact kernel directly, without any trained model in the loop (Table 6).
2. Your second point — one base frequency. The submission contains a trained base sweep spanning two and a half orders of magnitude (Table 17).

We take each below, and then the scale evidence, which answered another review and so never reached this thread.

**1. The analysis checked against the exact kernel (Table 6).**

Under the λ = 1 unit convention the analysis yields a predicted prefactor \\(c_{\\rm pred} = \\sqrt{45\\,Q_1(L,b)}\\). The submission compares it with \\(c_{\\rm coll}\\), the exact-kernel collision-score minimizer, over nine configurations crossing \\(d_{\\rm head} \\in \\{32,64,128\\}\\) and \\(L \\in \\{256,512,1024\\}\\) — the head dimensions your review asks about. The ratio is **0.981 ± 0.003 (CV 0.28%)**, agreement at the 2% level, with leave-one-out cross-validation giving a maximum prediction error of **0.008** (RMSE 0.004). For contrast the best points of a PPL sweep show CV near 14%, so the tightness reflects the smoothness of the collision score rather than a quantity fitted to results.

What this establishes is narrow and we state it narrowly: the curvature the analysis produces is checkable against an independent object rather than free-floating, and it holds across head dimensions. It says nothing about a trained-model optimum, and your τ ≈ 4 observation stands as our response described it.

**2. Base coverage in the submitted version (Table 17).**

The trained text models use b = 500K, and the post-submission factorial extends that to 500K/1M crossed with three head dimensions. The submission also contains a trained base sweep in the video-DiT family, from base 100 to 50,000, over which the dead-channel count rises from 0/16 to about 9/16 — the mechanism the allocation acts on, varied across the sweep. EVQ is stable across base ≥ 500, which is how the table describes it. Different modality and a denoising-MSE endpoint at one to two seeds, so we offer it as base coverage present in the submitted version rather than as a text result.

**3. Scale.**

On scale the evidence is 8B and 1.485B. At 8B, under identical physical-8K supervision over all 13 RULER families, 16K official macro goes from **0.295% to 14.03%**; the submitted version reported extrapolated PPL going from **176.3 to 21.5** at 16K (Table 23). A separate matched 8B adaptation gives NLL of 1.919/2.309 at 8K and **4.691/3.181** at 16K, with the direction holding in all 24 evaluation packs and all three domains. At true 16K, deleting the remote gold block from every head worsens EVQ NLL by **1.506** while leaving the matched Native control essentially unchanged, and median target-block hit@16 rises from **18.75% to 64.06%** — consistent with the model using the distant content rather than reconstructing it locally.

At 1.485B, matched Native and EVQ arms took an identical downstream chain with every backward pass capped at 4,096 physical tokens. By literal equality of the complete answer string plus a terminal EOS token, read from raw token IDs, Native/EVQ is 95/100 versus 100/100 at 4K, **18/100 versus 98/100** at 8K, and **0/100 versus 60/100** at 16K. The single controlled intervention is the fixed frequency table. Both are matched single-trajectory comparisons, at the tier our response assigned them.

If any of the attribution controls still looks confounded, we can answer before the deadline.

Thank you for separating the exact statement from the finite-τ operating regime, and for reading Table 1's epistemic labeling closely. We answer your five requests in order.

**1. The approximation chain and finite τ.** You are right that the small-τ analysis does not determine a finite optimum, and that the submission let the shape and operating-point rationales sit too close together. We now separate four links: (i) Cosh is the stationary density of C_app — exact given that surrogate and nothing more; (ii) the quadratic surrogate, the discrete grid and the pure-tether branch are modeling choices, none forced; (iii) the derivation motivates only the scaling τ ∝ d_eff / √L_train; (iv) setting the proportionality constant to one is an empirical operating convention, not a theorem and not a point optimum. Your observation that experiments sit near τ ≈ 4 while the expansion is small-τ is exactly right, and (iii)/(iv) is where that gap lives.

The rule is useful and fallible. In a direct sweep the independently selected τ = 5 and the rule value 5.657 differ by 0.0119 selection NLL. Across the 99-run reanalysis the rule beats Geo in 7/9 configuration means but beats a neighboring empirical pilot in only 3/9. We will describe it as a basin prior, not as approximately optimal.

**2. Independently tuned τ and matched non-Cosh schedules.** We built one experiment delivering both, under a stricter control than requested: a three-seed exact-range factorial over two bases (500K/1M), two training lengths (256/1024) and three head dimensions (32/64/128), giving 12 structural configurations, in which every schedule shares the same highest sampled frequency, lowest sampled frequency and log span — so the spectral range is identical by construction and only interior spacing differs.

It separates the two claims you asked us to separate. The axis survives: the preregistered 1.25× Cosh and a deformation-matched exponential improve weighted OOD NLL over Geo by 0.0121 and 0.0106, favoring the non-geometric allocation in 10/12 and 9/12 structural configurations respectively, so the historical gains are not an implicit base/range change. The specific formula does not: it is the best preregistered Cosh multiplier in only 4/12 configurations, its own mean against Geo has a configuration-bootstrap interval crossing zero, and formula-Cosh versus matched exponential is +0.0007 NLL with exact sign-flip p = 0.836. At the boundary arms the optimum moves inward from both ends of the formula's range. So allocation is an identifiable axis, robust across base, training length and head dimension; Cosh is one closed-form point on it, not a demonstrated optimum.

**3. Base, head dimension, and lineage.** You asked specifically about b ≥ 500K and larger head dimensions, and we have a suite held out from the submitted calibration that matches that request directly: base 1M, d_head = 128, L_train = 512, three seeds, 49,995,776 tokens. Mean tail NLL, EVQ minus Paper-Geo, with paired 95% intervals:

| length | delta | paired 95% CI |
| ---: | ---: | --- |
| 512 | +0.0694 | [+0.0503, +0.0885] |
| 1K | −0.8018 | [−0.9351, −0.6684] |
| 2K | −0.6640 | [−0.7310, −0.5971] |
| 4K | −0.4329 | [−0.6761, −0.1897] |
| 8K | −0.2871 | [−0.4387, −0.1354] |
| 16K | −0.2117 | [−0.3029, −0.1205] |

Every interval excludes zero and all three seeds agree at every length: EVQ pays a small in-domain cost at the training length and improves every extrapolation length through 16K. The magnitude declines with distance rather than growing, so we claim no universal shape for the gain — only that the effect is not confined to base 500K or d_head = 64. This is a 151.9M model and does not answer your separate production-scale request; the factorial in (2) covers the same base and head dimensions under the stricter exact-range control, and scale is answered in (5).

**4. Allocation shape versus parameterization and optimization effort.** We think you are right, and we ran the experiment you specified: keeping the positional operator unchanged and varying only fixed frequency schedules — geometric, EVQ, and several alternative analytic allocations. We did that at three levels of control, each removing one more of the confounds you identified.

Level 1 removes learned capacity and tuning budget: same operator, same training protocol, and every arm a fixed zero-parameter analytic schedule, so parameter count and optimization effort are identical by construction. Three seeds, mean tail NLL, EVQ-Cosh minus Paper-Geo: −0.256/−0.305/−0.223/−0.238 at 1K/2K/4K/8K, all three seeds agreeing. A span-matched uniform arm and RMS-deformation-matched power and exponential arms are included; EVQ also beats the span-matched uniform one.

Level 2 additionally removes the reference-grid and deviation-magnitude questions: all arms use the native Std-RoPE endpoint grid rather than our midpoint convention, share the same span, and every non-Geo arm shares the same RMS deformation from native Geo (0.2557), so no arm is simply further from geometric. Native EVQ minus native Std-RoPE is −0.113/−0.149/−0.207/−0.190/−0.099 NLL at 512/1K/2K/4K/8K, with seed-level paired 95% intervals [−0.200,−0.026], [−0.183,−0.115], [−0.241,−0.174], [−0.260,−0.121], [−0.141,−0.058], and all three seeds agreeing at every length.

Level 3 additionally removes any implicit base/range change: the exact-range factorial of (2).

Taken together these say what you asked whether the original comparison could say, and no more: with operator, protocol, parameter count, tuning budget, reference grid, deviation magnitude and spectral range all fixed, changing only the interior allocation still changes trained NLL. Allocation is a real and separately identifiable design axis. It does not say Cosh is optimal — at Levels 1 and 2 the matched exponential beats it at several lengths, and an attention-derived two-band schedule beats it by 0.116 and 0.211 NLL at 4K and 8K.

On the row that prompted the question: yes, it was tuned. The row labeled "DAPE" in the submission is a layer-shared learnable inverse-frequency baseline with 32 free parameters, and it received a dedicated positional learning-rate sweep at 10× and 100×, with the better of the two reported (455.3 versus 477.7 PPL@8K, seed 42). We should have stated that budget, and the revision will state both it and the comparator's identity precisely. Your learned-τ observation is well taken: a 1-parameter arm at 437.9 against a 32-parameter arm at 455.3 is fair reason to doubt that any comparison involving learned positional parameters settles a question about allocation shape — which is why the attribution above rests entirely on the zero-parameter ladder.

**5. Held-out base and a larger pre-specified run.** Both branches were run: the held-out base branch is in (3), and for the scale branch we trained OLMo-2 (1,484,916,736 parameters, d_head = 128, base 500K) from the public step-0 initialization for 1,000 steps at global batch 512 and exactly 2,097,152,000 counted tokens, under the pinned recipe and data-order prefix. The protocol was specified in a dated design document before the run — checkpoint identity, token budget, evaluation lengths, metric list and five numbered success criteria — and the run meets all five, including the pre-set requirements that at least one length improve by ≥ 0.05 NLL and that the 4K train-range cost stay under 0.05.

Geo/EVQ PPL on the same 128 document-disjoint PG-19 documents is 161.19/167.45 at 4K, 163.88/156.87 at 8K, 182.73/159.64 at 16K. Paired document-level bootstrap 95% intervals exclude zero at every length: the EVQ−Geo NLL delta is +0.0381 [+0.0332, +0.0428] at 4K, −0.0437 [−0.0494, −0.0380] at 8K and −0.1351 [−0.1420, −0.1281] at 16K, with 122/128 and 126/128 documents favoring EVQ at 8K/16K. These intervals measure held-out document sampling, not training-seed uncertainty, and we do not present them as the latter.

The 4K regression is not incidental: it is the in-window cost of reallocating a finite channel budget toward long range. The 1.485B scratch branch shows a strict monotone relationship between relative gain and extrapolation ratio, and the same in-window-cost / long-range-gain ordering reappears in the 1.485B and 8B adaptation studies.

**Scope.** The step-1,000 comparison is a matched-initialization early-training probe, not a converged one, and the two arms used different trainer implementations — matched on initialization, recipe, data-order prefix, counted tokens and evaluation rows, but not bitwise paired and not multi-seed. The factorial is a 50.9M short-budget natural-text NLL control: mechanistic, not capability.

**Revision plan.** (1) §2 will cite and discuss Oka et al., with a table contrasting the parameterization levels and the stage at which each acts. (2) The row currently marked "DAPE" will be relabeled as the layer-shared learnable inverse-frequency baseline it is, with its 10×/100× tuning budget reported. (3) A new appendix will report the exact-range control and the three-seed base/length/head factorial, including the retargeted and matched-exponential results that constrain our claim. (4) A second appendix will report the 1.485B and 8B results with their single-seed and task-family limits stated in line, and the step-0→1,000 run labeled as an early-training probe. (5) §3.3 and Table 1 will restate the τ rule as an empirical operating convention and record that the formula point is best in only 4/12 tested configurations. (6) §6 will add the unseen-task, length-transfer and metric-dependence limits, and the in-window cost.

"""R4' mature OLMo retrofit preparation.

This package is a no-GPU protocol and contract bundle.  It deliberately does
not import a model, download assets, start training, or enable a GPU action.
"""

METHOD_ID = "olmo2_demand_retrofit_r4prime_v1"
STATUS = "PREPARED_NO_GPU_DEFAULT_DISABLED"

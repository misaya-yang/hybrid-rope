"""CPU-only preparation for the demand-companding R1'/R2' experiments.

This package deliberately contains no training entry point. It constructs and
audits finite frequency tables, then writes a dry-run manifest for a separately
authorised GPU run.
"""


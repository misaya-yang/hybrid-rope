# Data Preparation

This directory keeps only the data-preparation helpers that are still needed for reproducibility or next-step experiments.

- `prepare_longbench_local_data.py`: local LongBench preparation helper
- `prepare_mixed_prior_dataset_v1.py`: mixed-prior dataset builder

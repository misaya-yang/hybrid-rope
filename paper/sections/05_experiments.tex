\section{Experiments}
\label{sec:experiments}

\subsection{Setup and evidence tiering}
\label{sec:exp-setup}

\paragraph{Evaluation scope.} This is a positional-encoding mechanism study. PE-diagnostic endpoints (extrapolation PPL, passkey, NIAH/RULER-style retrieval, gold-answer NLL) are our causal targets; downstream accuracy at these scales is confounded with semantic capacity and is reported as a non-regression check rather than a primary claim. EVQ-Cosh modifies only the training-time allocation and adds no learned parameters---it is neither an inference-time rescaler (YaRN/NTK/LongRoPE) nor a learned positional operator (DAPE/FIRE).

Experiments test three questions derived from the variational characterization: (Q1)~does EVQ improve over Geo at fixed training length? (Q2)~does EVQ compose with inference-time scaling, i.e., does EVQ+YaRN outperform Geo+YaRN at extrapolated lengths under matched YaRN scale? (Q3)~does the $\tau^*$ rule land in the best observed sweep basin across architectures and scales? Core experiments train from scratch with fixed architecture, modifying only the RoPE inverse-frequency initialization. We use $b{=}500\mathrm{K}$, the predicted $\tau^*(L)$ for each training length, and standard GPT-style decoder stacks \citep{radford2019gpt2} with $d_{\mathrm{head}}{=}64$ across $50$M--$750$M. The primary datasets are TinyStories, FineWeb-Edu, and controlled passkey-mix settings. Full hyperparameters are in Appendix~\ref{sec:supporting-experiments}.

Evidence is organized in three tiers (Table~\ref{tab:evidence-tier}). \emph{Primary} (3-seed, directly tied to central claims): EVQ$\times$YaRN composition (\S\ref{sec:exp-yarn}), PE-dominant extrapolation (\S\ref{sec:exp-pe}), and MLA (\S\ref{sec:exp-mla}; full table in Appendix~\ref{sec:mla-results}). \emph{Robustness} (3-seed, single architecture): FineWeb-Edu consistency, base sweep consistency, and capability preservation (\S\ref{sec:exp-robust}). \emph{Supporting / exploratory} (1--2 seed): the one-seed rows in the multi-scale raw-PPL table, video DiT, LoRA into LLaMA-3-8B, progressive training, and the MLA 1B-token run (\S\ref{sec:exp-robust}; full results in Appendix~\ref{sec:supporting-experiments}). No claim made in the abstract or introduction rests on tier-3 evidence.

\input{tables/table_evidence_tier}

%% =========================================================================
%% PRIMARY ANCHOR 1
%% =========================================================================
\subsection{Primary I: matched-scale substrate test (EVQ vs.\ YaRN)}
\label{sec:exp-yarn}

\emph{Substrate vs.\ range stress test.} If EVQ only changed effective range, applying the same YaRN scale should largely erase its advantage; if it changes the training-time substrate, the same rescaling rule should have higher leverage on EVQ than Geo (Table~\ref{tab:evq-yarn}, Fig.~\ref{fig:evq-yarn}).

In the $454$M $10\%$ passkey-mix setting ($3$~seeds per config), EVQ+YaRN reaches $100\%$ passkey retrieval at $8\mathrm{K}$ while Geo+YaRN remains at $61\%$, with the advantage persisting at $12$/$16\mathrm{K}$ ($79\%/68\%$ vs.\ $59\%/51\%$; Table~\ref{tab:evq-yarn}); PPL drops from $82.9$ to $70.9$ at $8\mathrm{K}$ and from $157.7$ to $107.5$ at $16\mathrm{K}$. EVQ alone improves over Geo without saturating retrieval, so the jump to $100\%$ under YaRN is not a ceiling effect. Table~\ref{tab:phase11-leverage} (Appendix) quantifies the interaction: YaRN gives Geo only $-4.5\%/-3.1\%$ at $4$/$8\mathrm{K}$ but gives EVQ ($\tau{=}4$) $-32.5\%/-40.7\%$; naive NTK-aware scaling becomes destructive at large $\tau$. This is consistent with substrate--range complementarity: EVQ corrects shape, YaRN corrects range, and their corrections are additive (Figure~\ref{fig:evq-yarn}; Appendix Fig.~\ref{fig:orthogonal}). We read the result as EVQ increasing YaRN's leverage under matched scale; we do not claim dominance over every tuned-scale YaRN baseline.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.68\linewidth]{fig2_evq_yarn_synergy.pdf}
  \caption{EVQ and YaRN correct orthogonal deficiencies. EVQ expands low-frequency spacing at training time; YaRN rescales positions at inference time. Their combination under matched YaRN scale substantially outperforms Geo+YaRN on both retrieval and PPL.}
  \label{fig:evq-yarn}
\end{figure}

\input{tables/table2_evq_yarn_main}
%% Table~\ref{tab:phase11-leverage} moved to Appendix to save body space; still referenced from body.

%% =========================================================================
%% PRIMARY ANCHOR 2
%% =========================================================================
\subsection{Primary II: PE-dominant allocation stress test}
\label{sec:exp-pe}

\emph{Allocation-shape stress test.} In a DAPE-style $128{\to}8\mathrm{K}$ protocol \citep{zheng2024dape} where model capacity is deliberately limited, we ask whether allocation shape alone can close a PE-dominant extrapolation gap that a learned positional operator cannot. Table~\ref{tab:pe-dominant}: EVQ reaches $333.7$ PPL (vs.\ Geo $513.7$, DAPE $455.3$) with no learned parameters; the closed-form reallocation (zero learned parameters) closes more of the gap than a $32$-parameter learned positional operator on the tested DAPE-style protocol. The predicted $\tau^{*}{=}4.0$ is preferred at both $125$M and $454$M (Appendix Fig.~\ref{fig:pe-dominant}).

\input{tables/table4_pe_dominant.tex}

%% =========================================================================
%% PRIMARY ANCHOR 3: MLA (channels-are-scarce regime)
%% =========================================================================
\subsection{Primary III: scarce-channel stress test (MLA)}
\label{sec:exp-mla}

\emph{Scarce-channel stress test.} MLA \citep{deepseekv2, deepseekv3} compresses RoPE to $d_{\mathrm{rope}}{=}32$ ($16$ channels after pairing) vs.\ $64$ in MHA at $d_{\mathrm{head}}{=}128$; the finite-budget view predicts EVQ's relative effect should \emph{grow} here. On a $432$M MLA transformer trained for $500$M tokens at $L_{\mathrm{train}}{=}8192$ ($3$~seeds), EVQ reduces $2\times$ extrapolation PPL from $138.8$ to $95.6$ ($-31.1\%$) with $+1.1\%$ in-distribution cost, exceeding Geo+YaRN ($117.9$; Appendix~\ref{sec:mla-results}). The $31.1\%$ effect is larger than the $13$--$15\%$ MHA range, i.e.\ the hypothesis strengthens where it predicts.

\paragraph{Training-saturation test (preliminary).} A standard objection is that standalone wins could be undertraining artifacts. Extending to 1B tokens (single seed; Table~\ref{tab:mla-reversal}): EVQ's raw advantage reverses ($-31.1\%\to{+}11.1\%$), but the YaRN+FT composition boost \emph{grows} ($+8.6\to{+}13.6$~pp)---the opposite of the undertraining-artifact signature. Preliminary single-seed evidence that the training-time substrate remains the binding constraint on inference-time scaling.

\begin{table}[ht]
\centering\small
\setlength{\tabcolsep}{4pt}
\caption{MLA training-saturation test (432M, single seed): the YaRN+FT composition boost \emph{grows} from $+8.6$ to $+13.6$~pp with full training, inconsistent with an undertraining-artifact account.}
\label{tab:mla-reversal}
\begin{tabular}{@{}l r r r@{}}
\toprule
Training & EVQ raw ($\Delta$PPL) & EVQ+YaRN[+FT] ($\Delta$PPL) & YaRN boost \\
\midrule
500M tokens ($L_{\mathrm{train}}{=}8$K, 2$\times$) & $-31.1\%$ & $-39.7\%$ (YaRN inf.) & $+8.6$~pp \\
1B tokens ($L_{\mathrm{train}}{=}4$K, target)     & $+11.1\%$ & $-2.5\%$ (YaRN$+$FT) & $+13.6$~pp \\
\bottomrule
\end{tabular}
\end{table}

%% =========================================================================
%% ROBUSTNESS + SUPPORTING EVIDENCE (merged; full results in Appendix)
%% =========================================================================
\subsection{Robustness and supporting evidence}
\label{sec:exp-robust}

Multi-scale raw extrapolation (Table~\ref{tab:multiscale}, Appendix) separates 3-seed FineWeb-Edu rows from single-seed rows: in-range PPL stays within ${\pm}1.7\%$ while long-range PPL improves by $10$--$46\%$; base sweeps ($b{=}10\mathrm{K}/500\mathrm{K}$) and capability preservation checks (Table~\ref{tab:capability}) show EVQ is non-destructive on retrieval-free tasks. Supporting rows extend scope without anchoring abstract claims: video DiT ($1$--$2$ seeds, Rectified-Flow; $0.53\times$ correction matches $(1/\sqrt{2})\sqrt{\mathbb{E}[\alpha_t]}{\approx}0.500$ to $6\%$ under $m{=}1$), LoRA into LLaMA-3-8B-Instruct at $r{=}d_{\mathrm{head}}/2$ recovering $8$--$19\times$ extrapolation-PPL, and single-seed progressive training (Appendix~\ref{sec:supporting-experiments}).

\paragraph{Signal gradient.} Across these diagnostics, EVQ's effect attenuates through abstraction layers---raw PPL $-46\%$, gold-answer NLL $-30\%$, passkey $+59$~pp, QuALITY accuracy $+2.2$~pp---consistent with the intervention being concentrated at the PE layer and attenuating where model capacity becomes the binding constraint.

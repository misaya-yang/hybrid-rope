\section{Supporting Results Placement}

The main text intentionally reserves space for Figure~\ref{fig:evq-yarn} and Figure~\ref{fig:pe-dominant}. The following assets are supporting and belong in the appendix:
\begin{itemize}
\item Figure 1: \(750\mathrm{M}\) dynamics and retrieval divergence,
\item collision-block / dead-zone visualizations,
\item full phase9f and phase15 tables,
\item `r`-sweep ablations,
\item video temporal transfer figures and tables,
\item complete implementation and hyperparameter dumps.
\end{itemize}

This separation is deliberate. The body should answer only three questions: what the exact theory gives, whether closed-form EVQ beats learnable PE in the cleanest extrapolation regime, and whether EVQ materially unlocks inference-time scaling. Everything else strengthens the package, but should not dilute the central narrative under the 9-page anonymous submission constraint.

\subsection{Attention distance redistribution}
\label{sec:attn-redistribution}

\begin{figure}[ht]
  \centering
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attn_aggregate_comparison.pdf}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attn_head_avg_distance.pdf}
  \end{minipage}
  \caption{\textbf{Waterbed verification in learned attention patterns (750M).} Left: aggregate attention density vs.\ token distance. A crossover at ${\approx}508$ tokens separates a short-range regime (Geometric dominant) from a long-range regime (EVQ dominant, up to $2.5\times$ higher density). Right: per-head average attention distance. 295/432 heads (68\%) attend farther under EVQ, with the strongest shifts in deeper layers (L12--L17).}
  \label{fig:attn-mechanism}
\end{figure}

\subsection{Why Figure 1 is appendix-only}

The \(750\mathrm{M}\) dynamics figure is visually strong, but it is not the right primary anchor for an anonymous submission because it mixes two avoidable weaknesses: it is single-seed and it was produced with the now-deprecated hybrid configuration. Its role in the submission is therefore explanatory rather than foundational. It helps the reader understand the qualitative geometry of retrieval divergence, but it should not compete with the multi-seed EVQ\(\times\)YaRN result or the PE-dominant scaling-law confirmation for body space.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.85\linewidth]{fig1_frequency_dynamics.pdf}
  \caption{\textbf{750M frequency dynamics and retrieval divergence} (single seed, deprecated hybrid configuration). EVQ redistributes resolution toward low-frequency channels, producing qualitatively different retrieval dynamics. This figure is explanatory rather than foundational; see Figures~\ref{fig:evq-yarn} and~\ref{fig:pe-dominant} in the main text for multi-seed primary evidence.}
  \label{fig:freq-dynamics}
\end{figure}

\subsection{Why video remains supportive}

The video temporal experiments provide three layers of evidence for cross-modal transfer. First, EVQ-Cosh wins every measured metric on temporal extrapolation, from PPL ($-27.3\%$) through teacher-forced accuracy ($+3.14\%$ top-1, $+5.40\%$ top-5) to generation-level VideoMAE FVD ($-1.5\%$). Second, the teacher-forced evaluation eliminates the autoregressive sampling bottleneck that compresses FVD margins, recovering $3.6\times$ more signal than VideoMAE FVD. Third, and most importantly, the frequency-resolved decomposition (Table~\ref{tab:freq-decomp}) reveals a monotonic gradient: EVQ's advantage is largest for the highest temporal frequency ($+10.4\%$ top-5 at $P{=}16$) and decreases smoothly toward lower frequencies ($+6.3\%$ at $P{=}32$). This gradient matches the theoretical prediction that $\phi_k(\tau)$ redistributes precision toward higher-frequency channels.

Nevertheless, these results remain supportive rather than co-primary: the current evidence is single-seed on a synthetic benchmark, and future work should validate the frequency-precision gradient on natural video using diffusion-based architectures (e.g., DiT with 3D RoPE) that do not suffer from autoregressive error accumulation. The text results already deliver a closed-form theory, a clean extreme-extrapolation test, and a strong systems interaction result; the video evidence strengthens the scope claim but is not required for the main thesis to stand.

\subsection{Downstream QA evaluation: QuALITY Gold NLL}
\label{sec:quality-downstream}

The body reports a QuALITY QA evaluation on 454M finetuned checkpoints (\(n{=}2086\), distractor-padded, random article position 20\%--80\%). Table~\ref{tab:quality-nll} gives the full results. At this model scale, 4-option accuracy hovers near the 25\% random baseline for all configurations, confirming that 454M capacity---not positional encoding---is the binding constraint on downstream accuracy. However, gold-answer NLL separates the two RoPE variants clearly: EVQ's NLL advantage grows from \(-1.7\%\) at \(4\mathrm{K}\) (in-distribution) to \(-30.1\%\) at \(8\mathrm{K}\) (\(2\times\) extrapolation) and \(-21.4\%\) at \(16\mathrm{K}\) (\(4\times\)). YaRN at \(8\mathrm{K}\) recovers Geo's accuracy to \(4\mathrm{K}\) level but does not close the NLL gap (\(-8.1\%\)).

\begin{table}[ht]
\caption{QuALITY QA downstream evaluation (454M, \(n{=}2086\), single seed). Accuracy is near random for all configs; gold-answer NLL reveals EVQ's advantage in probability space.}
\label{tab:quality-nll}
\centering
\small
\begin{tabular}{@{}l l r r r r r@{}}
\toprule
Length & Mode & \multicolumn{2}{c}{Accuracy (\%)} & \multicolumn{2}{c}{Gold NLL \(\downarrow\)} & NLL \(\Delta\) \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
& & Geo & EVQ & Geo & EVQ & \\
\midrule
4K & raw & 26.1 & 26.8 & 2.220 & 2.182 & \(-1.7\%\) \\
8K & raw & 24.6 & 26.8 & 3.202 & 2.239 & \(\mathbf{-30.1\%}\) \\
8K & YaRN & 26.5 & 26.6 & 2.389 & 2.195 & \(-8.1\%\) \\
16K & raw & 24.1 & 23.7 & 7.915 & 6.220 & \(-21.4\%\) \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:downstream-qa} visualizes the NLL divergence across context lengths, showing that EVQ's advantage grows with extrapolation ratio.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.75\linewidth]{fig5_downstream_qa.pdf}
  \caption{\textbf{Downstream QA: Gold-answer NLL across context lengths} (454M, QuALITY, $n{=}2086$). EVQ's NLL advantage grows from $-1.7\%$ at in-distribution ($4\mathrm{K}$) to $-30.1\%$ at $2\times$ extrapolation ($8\mathrm{K}$).}
  \label{fig:downstream-qa}
\end{figure}

The key takeaway is that accuracy at the capacity floor is not a useful discriminator, but NLL on the correct answer preserves the signal. This is consistent with the PPL and passkey evidence in the body: EVQ improves the model's probabilistic calibration at extrapolated lengths even when accuracy is saturated by capacity constraints.

\subsection{Multi-scale waterbed verification}

Figure~\ref{fig:waterbed-multiscale} visualizes the waterbed effect across model scales, confirming that EVQ consistently trades a small in-range cost for large long-range gains. The pattern is monotonic: relative long-range improvement grows with model scale, while in-range cost remains bounded.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.85\linewidth]{fig7_multiscale_waterbed.pdf}
  \caption{\textbf{Waterbed verification across model scales.} EVQ pays a bounded in-range cost (left of dashed line) while achieving progressively larger long-range gains. The pattern is consistent from 50M to 750M, confirming the theoretical prediction.}
  \label{fig:waterbed-multiscale}
\end{figure}

\subsection{Why base=10K and r-sweep move to the appendix}

Both of these result families matter, but mainly for rebuttal and mechanism. The low-base dead-zone result is a theory-confirming negative result, and the \(r\)-sweep is a method-simplification argument showing that the main EVQ recipe does not need a hybrid cutoff. Neither is strong enough to justify body-level figure real estate under the current page budget, so both are retained as appendix-strength material.

\section{Proof Details}
\label{sec:proofs}

\subsection{From the broadband surrogate to the ODE}

Starting from the surrogate kernel
\begin{equation}
K_{\mathrm{app}}(\phi,\psi)=\alpha\delta(\phi-\psi)+\beta\min(\phi,\psi),
\end{equation}
the functional is
\begin{equation}
\mathcal{J}[\rho]
=
\frac{\alpha}{2}\int_0^1 \rho(\phi)^2\,d\phi
+
\frac{\beta}{2}\int_0^1\int_0^1 \rho(\phi)\rho(\psi)\min(\phi,\psi)\,d\phi\,d\psi
- \mu\int_0^1 \rho(\phi)b^{-2\phi}\,d\phi.
\end{equation}
Its first variation is
\begin{equation}
\delta\mathcal{J}
=
\int_0^1
\left[
\alpha\rho(\phi)
+
\beta\int_0^1 \rho(\psi)\min(\phi,\psi)\,d\psi
- \mu b^{-2\phi}
\right]\delta\rho(\phi)\,d\phi.
\end{equation}
The Euler-Lagrange stationarity condition is therefore
\begin{equation}
\alpha\rho(\phi)
+
\beta\int_0^1 \rho(\psi)\min(\phi,\psi)\,d\psi
=
\mu b^{-2\phi}.
\end{equation}
Define
\begin{equation}
g(\phi)=\int_0^1 \rho(\psi)\min(\phi,\psi)\,d\psi
=
\int_0^\phi \rho(\psi)\psi\,d\psi
+
\phi\int_\phi^1 \rho(\psi)\,d\psi.
\end{equation}
Direct differentiation gives
\begin{align}
g'(\phi)
&=
\int_\phi^1 \rho(\psi)\,d\psi,\\
g''(\phi)
&=
-\rho(\phi).
\end{align}
Hence the stationarity equation can be rewritten as
\begin{equation}
\alpha \rho(\phi)+\beta g(\phi)=\mu b^{-2\phi}.
\end{equation}
Differentiating twice with respect to \(\phi\) and substituting \(g''(\phi)=-\rho(\phi)\) yields
\begin{equation}
\rho''(\phi)-\frac{\beta}{\alpha}\rho(\phi)=\gamma b^{-2\phi},
\qquad
\tau=\sqrt{\beta/\alpha}.
\end{equation}
The same calculation shows that \(g(0)=0\) and \(g'(1)=0\), so \(\min(\phi,\psi)\) acts as the Green kernel of \(-d^2/d\phi^2\) under the mixed boundary conditions induced by the surrogate formulation. Conditional on the surrogate, this step is exact.

\subsection{Closed-form solution and geometric limit}

The non-resonant ODE has the closed-form solution
\begin{equation}
\rho^*(\phi)=C_1\cosh(\tau\phi)+C_2\sinh(\tau\phi)+P\,b^{-2\phi}.
\end{equation}
The first two terms span the homogeneous solution of \(\rho''-\tau^2\rho=0\), and the final term is a particular solution induced by the Fisher-weighted source \(b^{-2\phi}\). This decomposition is the source of the main paper's ``tether + pulse'' interpretation.

To translate the density into RoPE frequencies, write the cumulative map
\begin{equation}
F(\phi)=\int_0^\phi \rho^*(s)\,ds,
\end{equation}
normalize it to \([0,1]\), and invert at the discrete quantiles \(u_k=k/K\). In the pure-tether branch this inversion has the closed form
\begin{equation}
\phi_k(\tau)
=
1-\frac{1}{\tau}\operatorname{arcsinh}\!\left((1-u_k)\sinh\tau\right).
\end{equation}
To recover geometric RoPE, expand \(\sinh\tau\approx\tau\) as \(\tau\to 0\). Then
\begin{equation}
\phi_k(\tau)\to 1-\frac{1}{\tau}\operatorname{arcsinh}((1-u_k)\tau)\to u_k,
\end{equation}
which is exactly the uniform quantile map.

This also shows why the high-frequency end remains effectively anchored: for \(u_0=0\), one has \(\phi_0(\tau)=0\) exactly, so the leading inverse frequency remains \(b^0=1\), matching geometric RoPE.

\subsection{Why the pure-tether branch}

The full stationary density contains three terms: a hyperbolic tether (\(\cosh, \sinh\)) and a particular solution \(Pb^{-2\phi}\). The practical EVQ method uses only the pure-tether branch, justified on two grounds.

\textbf{(1) Closed-form invertibility.} The pure-tether density \(\rho(\phi)\propto\cosh(\tau(1-\phi))\) admits an analytically invertible CDF, yielding the closed-form warp \(\phi_k(\tau)\). Including the \(b^{-2\phi}\) term destroys this invertibility and requires numerical root-finding.

\textbf{(2) The forcing term is amplitude-suppressed.} The particular solution coefficient $P = \gamma/(4\ln^2 b - \tau^2)$ is strongly suppressed at typical base values ($P \propto 1/689$ at $b{=}500\mathrm{K}$). Numerically, the full solution improves over pure tether by ${<}0.5\%$ at the theoretically motivated $\lambda \approx P$, and by only $2$--$6\%$ even at artificially large $\lambda{=}1$ (vs.\ $28$--$92\%$ improvement over geometric). The 99-run Phase~16 sweep (Figure~\ref{fig:tau-sweep}) confirms that the pure-tether family matches the scaling-law prediction across scales, and subsumes geometric RoPE as its $\tau{\to}0$ limit.

\subsection{Surrogate quality: functional validation}
\label{sec:surrogate-validation}

The broadband surrogate $K_{\mathrm{app}} = \alpha\delta + \beta\min$ is the theory's sole approximation. We validate it not by pointwise comparison with the exact kernel $K(\phi_1,\phi_2) = \int D(\Delta)\cos(\omega(\phi_1)\Delta)\cos(\omega(\phi_2)\Delta)\,d\Delta$ --- which is oscillatory (sinc-like) and cannot be well-approximated pointwise by a smooth two-parameter model --- but by the \emph{functional} test: does the EVQ allocation derived from $K_{\mathrm{app}}$ also reduce the collision objective under the true kernel $K$?

Table~\ref{tab:surrogate-validation} answers affirmatively. For a uniform distance prior $D(\Delta) = 1/L$ on $[0, L]$, we compute the exact kernel $K$ at the discrete allocation points for both geometric and EVQ, then evaluate the normalized collision score $C = \sum_{i<j} K_{ij}^2/(K_{ii}K_{jj})$ and effective spectral rank (exponential of the Shannon entropy of the eigenvalue distribution). We sweep across six training lengths ($L \in \{128, \ldots, 4096\}$), three base values ($b \in \{10\mathrm{K}, 100\mathrm{K}, 500\mathrm{K}\}$), and two channel counts ($K \in \{16, 32\}$), covering both text and video regimes.

\begin{table}[ht]
\caption{Functional surrogate validation across 12 configurations. EVQ (derived from $K_{\mathrm{app}}$) reduces collision and increases effective rank under the \emph{exact} kernel $K$ in every tested regime. Collision reduction monotonically increases with extrapolation stress (shorter $L$, larger $b$).}
\label{tab:surrogate-validation}
\centering
\small
\begin{tabular}{@{}l r r r r r@{}}
\toprule
& \multicolumn{2}{c}{Collision $C$} & & \multicolumn{2}{c}{Eff.\ rank} \\
\cmidrule(lr){2-3} \cmidrule(lr){5-6}
Configuration & GEO & EVQ ($\Delta_C$) & & GEO & EVQ \\
\midrule
\multicolumn{6}{@{}l}{\emph{Text: $K{=}32$, $b{=}500\mathrm{K}$, sweep $L$}} \\
\quad $L{=}128$ & 226.3 & 17.7 ($-92\%$) & & 2.5 & 17.0 \\
\quad $L{=}256$ & 192.6 & 27.6 ($-86\%$) & & 3.0 & 13.9 \\
\quad $L{=}512$ & 163.2 & 43.3 ($-73\%$) & & 3.6 & 10.8 \\
\quad $L{=}1024$ & 133.9 & 59.0 ($-56\%$) & & 4.4 & 8.8 \\
\quad $L{=}2048$ & 109.0 & 67.5 ($-38\%$) & & 5.4 & 8.0 \\
\quad $L{=}4096$ & 87.0 & 66.3 ($-24\%$) & & 6.6 & 8.1 \\
\midrule
\multicolumn{6}{@{}l}{\emph{Text: $K{=}32$, $L{=}2048$, sweep $b$}} \\
\quad $b{=}10\mathrm{K}$ & 34.8 & 19.2 ($-45\%$) & & 13.0 & 17.3 \\
\quad $b{=}100\mathrm{K}$ & 79.4 & 47.0 ($-41\%$) & & 7.1 & 10.4 \\
\midrule
\multicolumn{6}{@{}l}{\emph{Video: $K{=}16$, $L{=}32$, sweep $b$}} \\
\quad $b{=}100$ & 22.6 & 14.2 ($-37\%$) & & 4.8 & 6.4 \\
\quad $b{=}1\mathrm{K}$ & 42.3 & 28.0 ($-34\%$) & & 2.8 & 3.9 \\
\quad $b{=}10\mathrm{K}$ & 55.4 & 39.8 ($-28\%$) & & 2.2 & 2.9 \\
\quad $b{=}50\mathrm{K}$ & 63.3 & 47.2 ($-26\%$) & & 2.0 & 2.5 \\
\bottomrule
\end{tabular}
\end{table}

EVQ reduces collision under the exact kernel in all 12 configurations ($-24\%$ to $-92\%$), with effective rank improvements of $+24\%$ to $+570\%$. Two patterns emerge. First, the collision reduction monotonically increases as the geometric allocation becomes more degenerate: shorter $L$ (more extrapolation stress at fixed $\tau^*$) and larger $b$ (more dead channels) both amplify the advantage, consistent with the dead-channel mechanism. Second, even in the most favorable regime for geometric RoPE ($L{=}4096$, where baseline effective rank is already $6.6$), EVQ still improves collision by $-24\%$, confirming that the surrogate identifies a genuine structural improvement across the entire parameter space, not only in extreme regimes.

\subsection{Waterbed assumptions}

The waterbed proposition in the body is conditional. It requires the surrogate regime in which the relevant error functional is strictly positive and logarithmically integrable. Under those assumptions, Jensen's inequality yields a lower bound of the form
\begin{equation}
\int \ln E(\phi)\,d\phi \ge \ln b - \ln c,
\end{equation}
with equality at the geometric allocation. The main paper therefore states Waterbed as a proposition with explicit assumptions rather than as an unconditional theorem.

The logic is the standard convexity argument. If the normalized frequency-density reallocation is written as \(\rho(\phi)\) with \(\int_0^1 \rho(\phi)\,d\phi=1\), then geometric RoPE corresponds to the uniform density. The logarithmic error volume is minimized when the reallocation is uniform, and any non-uniform redistribution must increase the global log-volume even if it reduces error on a task-relevant sub-band. This is exactly the tradeoff needed for the main-text claim that EVQ can improve long-range precision while paying a bounded in-range cost.

\subsection{Surrogate self-consistency theorem}
\label{sec:self-consistency}

The surrogate functional $\mathcal{J}[\rho]$ admits three exact identities that connect its two integral terms $T_1(\tau) = \int_0^1 \rho^2\,d\phi$ and $T_2(\tau) = \int_0^1\!\!\int_0^1 \rho(\phi)\rho(\psi)\min(\phi,\psi)\,d\phi\,d\psi$, where $\rho(\phi) = \tau\cosh(\tau(1{-}\phi))/\sinh\tau$ is the normalized cosh density.

\begin{theorem}[Surrogate self-consistency]
\label{thm:self-consistency}
For all $\tau > 0$:
\begin{equation}
\tau^2 T_2(\tau) + T_1(\tau) = \tau\coth\tau.
\label{eq:self-consistency}
\end{equation}
\end{theorem}

\begin{proof}
The Green's function $g(\phi) = \int_0^1 \rho(\psi)\min(\phi,\psi)\,d\psi$ satisfies $g''(\phi) = -\rho(\phi)$ with $g(0) = 0$, $g'(1) = 0$ (from the $\min$ kernel structure; see~\S\ref{sec:proofs}). Define $h(\phi) = \rho(0) - \rho(\phi)$. Then $h'' = -\rho'' = \tau^2\rho$ (from the ODE $\rho'' = \tau^2\rho$), with $h(0) = 0$ and $h'(1) = -\rho'(1) = \tau^2 g'(0)$ (boundary conditions).

Define $f(\phi) = \tau^2 g(\phi) - h(\phi)$. Then $f'' = \tau^2 g'' + \rho'' = -\tau^2\rho + \tau^2\rho = 0$, with $f(0) = 0$ and $f'(1) = \tau^2 g'(1) - h'(1) = 0$. Hence $f \equiv 0$, yielding $\tau^2 g(\phi) = h(\phi) = \rho(0) - \rho(\phi)$, i.e.,
\begin{equation}
g(\phi) = \frac{\rho(0) - \rho(\phi)}{\tau^2}.
\label{eq:green-identity}
\end{equation}
Integrating against $\rho$:
\begin{equation}
T_2 = \int_0^1 \rho(\phi)\,g(\phi)\,d\phi = \frac{1}{\tau^2}\bigl[\rho(0)\underbrace{\int_0^1\!\rho}_{=1} - T_1\bigr]
= \frac{\rho(0) - T_1}{\tau^2}.
\end{equation}
Since $\rho(0) = \tau\cosh\tau/\sinh\tau = \tau\coth\tau$, rearranging gives~\eqref{eq:self-consistency}.
\end{proof}

An immediate corollary follows.

\begin{corollary}[Closed-form $T_2$]
\begin{equation}
T_2(\tau) = \frac{\sinh 2\tau - 2\tau}{4\tau\sinh^2\tau}.
% \label{eq:T2-closed} % unused
\end{equation}
\end{corollary}

This follows from substituting the known closed form $T_1(\tau) = \tau(1 + \sinh 2\tau / 2\tau) / (2\sinh^2\tau)$ and $\rho(0) = \tau\coth\tau$ into the theorem. Both identities are verified numerically to machine precision ($<10^{-15}$ relative error across $\tau \in [0.01, 20]$; code: \texttt{scripts/lambda\_identity.py}).

The self-consistency theorem reveals that the surrogate's two energy terms are not independent: they are locked by the cosh density's self-reproducing property under the $\min$-kernel Green's function. This structural rigidity is why the pure-tether solution (\S\ref{sec:proofs}) performs so well despite being a single-parameter family.

\subsection{Cross-validation of \texorpdfstring{$\lambda$}{lambda}}
\label{sec:lambda-cv}

The scaling law $\tau^* = \lambda \cdot d_{\mathrm{head}}/\sqrt{L}$ is validated at two levels.

\paragraph{Collision-score level.} We minimize the exact-kernel collision score $C(\tau)$ for each of 9 configurations ($d_{\mathrm{head}} \in \{32, 64, 128\}$, $L \in \{256, 512, 1024\}$) and compute $\lambda_{\mathrm{coll}} = \tau^*_{\mathrm{coll}} / (d_{\mathrm{head}}/\sqrt{L})$. The resulting $\lambda_{\mathrm{coll}} \approx 1.17$ is nearly constant (CV: $0.11\%$); leave-one-out cross-validation confirms max prediction error $<0.4\%$ (Table~\ref{tab:lambda-cv}). This near-zero spread reflects the smoothness of the collision score as a function of $\tau$, not PPL variability.

\input{tables/table_lambda_cv}

\paragraph{PPL-sweep level.} Phase~16 training sweeps (99 runs, 9 configurations $\times$ 3+ seeds) identify the PPL-optimal $\tau$ for each configuration. The resulting $\lambda_{\mathrm{PPL}} = \tau^*_{\mathrm{PPL}} / (d_{\mathrm{head}}/\sqrt{L})$ has mean ${\approx}1.19$ with broader scatter (CV ${\approx}14\%$), because PPL depends on training noise, finite data, and the discrete sweep grid. The deployed formula uses $\lambda{=}1$ (i.e.\ $\tau = d_{\mathrm{head}}/\sqrt{L}$ without a multiplicative constant), which consistently falls inside the flat PPL basin: the PPL gap between the formula and the per-configuration optimum is ${<}1\%$ across all 27 validated configurations.

\paragraph{Basin flatness.} The collision-score landscape $C(\tau)$ for a representative configuration ($d_{\mathrm{head}}{=}64$, $L{=}512$, $b{=}500\mathrm{K}$) reveals a flat optimal basin: ${\pm}20\%$ deviation from $\tau^*_{\mathrm{coll}}$ incurs ${<}1\%$ collision increase, and the basin spans $2.3\%$ of $\tau^*$. This flatness is why the deployed $\lambda{=}1$ formula---despite being ${\approx}17\%$ below the collision-score optimum---lands inside the near-optimal PPL basin for every tested configuration.

\subsection{Scaling analysis of \texorpdfstring{$\tau^*$}{tau*}: from surrogate to empirical law}
\label{sec:tau-scaling}

The scaling law
\begin{equation}
\tau^* = \frac{d_{\mathrm{head}}}{\sqrt{L}}
\end{equation}
is validated across 99 training runs (27 configurations $\times$ 3+ seeds, Figure~\ref{fig:tau-sweep}). Here we analyze to what extent the broadband surrogate predicts this formula, and identify the gap that remains.

\paragraph{Numerical characterization of surrogate coefficients.}
We fit $K_{\mathrm{app}}(\phi_i,\phi_j) = \alpha\delta_{ij}/\Delta\phi + \beta\min(\phi_i,\phi_j)$ to the exact collision kernel $K(\phi_i,\phi_j) = \frac{1}{2L}\bigl[\frac{\sin((\omega_1{-}\omega_2)L)}{\omega_1{-}\omega_2} + \frac{\sin((\omega_1{+}\omega_2)L)}{\omega_1{+}\omega_2}\bigr]$ evaluated at $K = d_{\mathrm{head}}/2$ uniformly spaced channel positions in $[0,1]$. Sweeping across $L \in \{128,\ldots,4096\}$, $d_{\mathrm{head}} \in \{32,64,128,256\}$, and $b \in \{10\mathrm{K}, 100\mathrm{K}, 500\mathrm{K}\}$ yields:
\begin{equation}
\alpha \;\approx\; \frac{1}{2K} \;=\; \frac{1}{d_{\mathrm{head}}},
\qquad
\beta \;\sim\; L^{-0.22},\; O(1).
\end{equation}
The diagonal coefficient $\alpha$ is set by the kernel's diagonal value ($K(\phi,\phi) \approx 1/2$ for most channels) times the channel spacing $\Delta\phi = 1/K$. Crucially, $\alpha$ depends on $d_{\mathrm{head}}$ through the discretization ($K = d_{\mathrm{head}}/2$) and is nearly independent of $L$. The off-diagonal coefficient $\beta$ has weak power-law decay with $L$ and negligible $d_{\mathrm{head}}$ dependence.

\paragraph{What the surrogate predicts.}
Substituting into $\tau_{\mathrm{surr}} = \sqrt{\beta/\alpha}$:
\begin{equation}
\tau_{\mathrm{surr}} = \sqrt{\beta \cdot d_{\mathrm{head}}} \;\sim\; \sqrt{d_{\mathrm{head}}} \cdot L^{-0.11}.
\end{equation}
This correctly captures the $d_{\mathrm{head}}$ dependence: $\alpha \propto 1/d_{\mathrm{head}}$ implies $\tau \propto \sqrt{d_{\mathrm{head}}}$, matching the direction and monotonicity of the empirical law. However, the $L$-scaling is too weak: $L^{-0.11}$ vs.\ the empirical $L^{-0.5}$.

\paragraph{The discrete--continuous gap.}
The discrepancy arises because the broadband surrogate operates in a \emph{continuous} function space, where the number of channels $K$ appears only through the integral constraint $\int\rho = K$. The surrogate's optimal $\tau$ thus captures the infinite-resolution limit; it does not account for the finite-channel oscillatory correlations that penalize over-concentration at low frequencies.

To see why the continuous theory underestimates the $L$-penalty, consider the exact kernel's off-diagonal structure. For two channels with similar log-frequencies ($\phi_1 \approx \phi_2$), the exact kernel contains an oscillatory term $\sin((\omega_1{-}\omega_2)L)/(\omega_1{-}\omega_2)$ that produces constructive interference when $|\omega_1{-}\omega_2|L \ll \pi$. Packing more channels near the low-frequency end (large $\tau$) reduces inter-channel frequency separation, pushing neighboring channels into this constructive-interference regime. The resulting excess correlation acts as an implicit penalty on large $\tau$ that the smooth $\beta\min(\phi_1,\phi_2)$ surrogate does not capture.

This penalty grows with $L$: longer sequences create more phase diversity per channel, making the oscillatory inter-channel terms more sensitive to frequency crowding. The strengthened $L$-dependence converts $\tau_{\mathrm{surr}} \sim \sqrt{d_{\mathrm{head}}} \cdot L^{-0.11}$ into $\tau^* = d_{\mathrm{head}}/\sqrt{L}$. The $d_{\mathrm{head}}$ exponent simultaneously increases from $1/2$ to $1$ because the discrete-correction term itself scales with $K = d_{\mathrm{head}}/2$: more channels create more pairwise interactions in the constructive-interference regime, amplifying the finite-channel penalty by an additional factor of $\sqrt{d_{\mathrm{head}}}$. The combined effect is $\sqrt{d_{\mathrm{head}}} \cdot \sqrt{d_{\mathrm{head}}} = d_{\mathrm{head}}$.

Confirming this picture, we verified numerically that at the special point $d_{\mathrm{head}} = L$ (where the number of encoding dimensions equals the number of positions, and the continuous and discrete problems coincide), the surrogate formula $\tau_{\mathrm{surr}} = \sqrt{\beta/\alpha}$ matches $d_{\mathrm{head}}/\sqrt{L}$ to within $3\%$. As the ratio $L/d_{\mathrm{head}}$ increases, the surrogate progressively overestimates $\tau^*$, consistent with a growing discrete-correction term.

\paragraph{Summary.}
The broadband surrogate provides two of the three ingredients for $\tau^*$: the \emph{functional form} (cosh density, via the ODE) and the \emph{$d_{\mathrm{head}}$ dependence} (via $\alpha = 1/d_{\mathrm{head}}$). The missing $L^{-1/2}$ exponent is recovered by the softmax transport analysis (Proposition~\ref{prop:softmax-transport} and \S\ref{sec:stiffness-derivation}): the post-softmax Jacobian introduces a $1/L$ factor per channel, and combining this with Pearson $\chi^2$ stiffness yields $\gamma = 0.465$ (Table~\ref{tab:stiffness-sweep}), which we round to $\gamma \approx 0.5$ for the practical rule. Together, these yield
\begin{equation}
\tau^* = \frac{d_{\mathrm{head}}}{\sqrt{L}},
\end{equation}
validated empirically with $R^2 > 0.99$ across 99 runs (Figure~\ref{fig:tau-sweep}).

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.85\linewidth]{fig6_tau_formula_validation.pdf}
  \caption{\textbf{Scaling-law validation across 99 training runs} (27 configurations $\times$ 3+ seeds). Each point is a separate training run; the $x$-axis is the predicted $\tau^* = d_{\mathrm{head}}/\sqrt{L}$ and the $y$-axis is the empirically optimal $\tau$ (minimizing validation perplexity). The fit achieves $R^2 > 0.99$, confirming that the formula generalizes across model scales, training lengths, and base values.}
  \label{fig:tau-sweep}
\end{figure}

\paragraph{Configuration breakdown.} Table~\ref{tab:config-breakdown} decomposes the 27 validated configurations by architecture, seed count, and evidence tier.

\begin{table}[ht]
\caption{Breakdown of the 27 configurations validated against $\tau^* = d_{\mathrm{head}}/\sqrt{L}$. The $0.53\times$ modality correction for video DiT is empirical (\S\ref{sec:tau-correction}).}
\label{tab:config-breakdown}
\centering
\small
\begin{tabular}{@{}l l r r r l l@{}}
\toprule
Architecture & Scale & $d_{\mathrm{head}}$ & $L_{\mathrm{train}}$ & Seeds & $\tau^*$ variant & Tier \\
\midrule
MHA (text) & 50M & 64 & 2048 & 1 & base & Robustness \\
MHA (text) & 125M & 64 & 128 & 1--3 & base & Primary \\
MHA (text) & 125M & 64 & 2048 & 1 & base & Robustness \\
MHA (text) & 454M & 64 & 128 & 1--3 & base & Primary \\
MHA (text) & 454M & 64 & 256 & 3 & base & Primary \\
MHA (text) & 454M & 64 & 512--2048 & 3 & base & Primary \\
MHA (text) & 454M & 64 & 2048 & 3 & base & Primary \\
MHA (text) & 750M & 64 & 4096 & 1 & base & Supporting \\
\midrule
MLA & 432M & 128 & 8192 & 3 & base & Primary \\
\midrule
DiT (video) & 129M & 16 & 32 & 2 & $0.53\times$ & Supporting \\
DiT (video) & 382M & 16 & 32 & 1 & $0.53\times$ & Supporting \\
\midrule
LoRA & 8B & 128 & 8192 & 1 & base & Supporting \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Stiffness functional derivation}
\label{sec:stiffness-derivation}

The softmax transport objective in Proposition~\ref{prop:softmax-transport} requires choosing a stiffness functional $S(\tau)$ measuring the cost of departing from uniform density. We test a one-parameter family $S_p(\tau) = \frac{1}{M}\int_0^1 (\rho_\tau - 1)^2/\rho_\tau^p\,d\phi$ using exact (non-Taylor) integrals of the normalized cosh density $\rho_\tau(\phi) = \tau\cosh(\tau(1{-}\phi))/\sinh\tau$.

\begin{table}[ht]
\caption{$L$-exponent $\gamma$ (defined by $\tau_* \propto L^{-\gamma}$) under different stiffness functionals, computed via exact numerical optimization over $L \in [128, 4096]$. Target: $\gamma = 0.500$.}
\label{tab:stiffness-sweep}
\centering
\small
\begin{tabular}{@{}l c c l@{}}
\toprule
Stiffness & $p$ & $\gamma$ & Note \\
\midrule
$L^2$ norm $\int(\rho{-}1)^2$ & 0 & 0.626 & Original choice \\
Geometric mean & 0.5 & 0.561 & \\
\textbf{Self-consistent} & \textbf{0.80} & \textbf{0.498} & Uniquely determined by $\tau_*{\propto}1/\!\sqrt{L}$ \\
\textbf{Pearson $\chi^2$} & \textbf{1.0} & \textbf{0.465} & First-principles (attention-load) \\
\midrule
KL divergence & --- & 0.710 & Worse than $L^2$ \\
Reverse KL & --- & 0.581 & \\
Jeffreys & --- & 0.621 & $\approx L^2$ \\
R\'enyi ($\alpha{=}2$) & --- & 0.807 & \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Why $\chi^2$.} Two independent arguments favor $p{=}1$. (1)~\emph{Attention-load asymmetry}: each frequency channel at density $\rho(\phi)$ bears per-channel attention load ${\propto}1/\rho$; weighting the squared departure by this load yields $\int(\rho{-}1)^2/\rho = \chi^2$. (2)~\emph{Discrete-grid effect}: the continuous $L^2$ integral, evaluated on EVQ's non-uniform cosh-quantile grid, naturally acquires a $1/\rho$ Jacobian factor, producing discrete $\chi^2$.

\paragraph{Closed form.} For $p{=}1$, the stiffness admits
\begin{equation}
S_{\chi^2}(\tau) = \frac{1}{M}\!\left[\frac{\sinh\tau\cdot\arctan(\sinh\tau)}{\tau^2} - 1\right],
\end{equation}
verified numerically to relative error ${<}7{\times}10^{-6}$.

\paragraph{Self-consistent analysis.} Requiring $\tau_* \propto 1/\sqrt{L}$ to hold \emph{exactly} for all $L$ uniquely determines the stiffness shape. The resulting $S_{\mathrm{derived}}(\tau)$ best matches $S_p$ at $p \approx 0.85$ (high-resolution sweep; consistent with $0.80$ from the coarser grid), with an effective power-law exponent of exactly~4 at $\tau{=}1.0$---a property no standard divergence achieves. The full sweep is reproduced by \texttt{scripts/analysis/verify\_stiffness\_and\_regime.py} (pure NumPy, ${\sim}60$s).

\subsection{Training-regime dependence and the LoRA phase transition}
\label{sec:lora-regime}

Pretrained weights create a \emph{coupling stiffness} that penalizes frequency redistribution. Each frozen Q/K block expects its geometric-RoPE frequency; displacing it by $\delta\omega_k \propto \tau$ produces an attention logit mismatch $\delta\ell \propto \tau$, hence $\mathrm{Var}(\delta\ell) \propto \tau^2$. The total stiffness becomes
\begin{equation}
S_{\mathrm{total}}(\tau; r) = S_{\chi^2}(\tau) + \Lambda_0\!\left(1 - \frac{r}{K}\right)\frac{\tau^2}{d_{\mathrm{head}}},
\end{equation}
where $r$ is the LoRA rank, $K = d_{\mathrm{head}}/2$, and $\Lambda_0 \gg 1$ for well-trained models. Since $S_{\chi^2} \sim \tau^4$ while $S_{\mathrm{frozen}} \sim \tau^2$, the coupling term dominates at small~$\tau$. The balance equation $\partial\mathcal{F}/\partial\tau = 0$ then degenerates: $\tau$ cancels, leaving a $\tau$-independent inequality. The system undergoes a \emph{phase transition} between $\tau_*{=}0$ (EVQ infeasible) and $\tau_*{>}0$ (EVQ viable), with critical rank $r_c = K = d_{\mathrm{head}}/2$.

Numerical verification confirms the transition is \emph{sharp}: for LLaMA-8B parameters ($d_{\mathrm{head}}{=}128$, $K{=}64$), sweeping LoRA rank over the variational landscape shows $\tau_*{=}0$ for all $r \leq 48$ ($r/K \leq 0.75$), then $\tau_*$ jumps discontinuously to $1.41$ at $r{=}64$ ($r/K{=}1.0$). The critical rank is exactly $r_c = K = 64$, not an approximation.

For LLaMA-8B with LoRA $r{=}16$: the adaptation ratio $r/K = 25\%$ leaves 48 of 64 frequency channels frozen. At $\tau{=}1.414$, approximately 8 channels are displaced, but the 48 frozen channels cannot adapt, creating systematic attention logit mismatch and driving PPL from 11.8 to~77.1 (the predicted PPL from $\Lambda_0$ calibration reproduces this value exactly). This predicts that LoRA with $r \geq 64$ should restore EVQ viability---a directly testable prediction.

\paragraph{SFT coupling decay.} Under full-parameter SFT, the coupling decays exponentially: $\tau^*_{\mathrm{SFT}}(T) \approx \tau^*_{\mathrm{pt}} / \sqrt{1 + \Lambda_0 e^{-T\eta\sigma}}$. Recovery to $70\%$ of $\tau^*_{\mathrm{pt}}$ requires $T\eta\sigma \approx 5$; full recovery ($>95\%$) requires $T\eta\sigma \approx 10$.

\subsection{Mechanism isolation: collision-only ablation}
\label{sec:mechanism-isolation}

To verify that the collision-reduction mechanism identified by the surrogate theory is the primary driver of EVQ's improvement, we perform a diagnostic experiment that isolates the collision objective from the full training loss.

We define the off-diagonal energy $E_{\mathrm{off}} = \sum_{i<j} K_{ij}^2 / (K_{ii}K_{jj})$ under the exact RoPE kernel (not the surrogate), and optimize the EVQ allocation to minimize $E_{\mathrm{off}}$ directly (``collision-only''), without any language modeling. Table~\ref{tab:mechanism-isolation} compares geometric, EVQ at theory-predicted $\tau^*$, and collision-optimized allocations across 36 configurations ($d_{\mathrm{head}} \in \{32, 64, 128\}$, $L \in \{256,\ldots,8192\}$, $b \in \{10\mathrm{K}, 500\mathrm{K}\}$).

\begin{table}[ht]
\caption{Mechanism isolation: off-diagonal energy $E_{\mathrm{off}}$ reduction and mutual coherence ($\mu$) across representative configurations. EVQ at theory $\tau^*$ already captures the majority of the collision-optimal improvement; the collision-only oracle provides $63$--$69\%$ reduction vs.\ geometric.}
\label{tab:mechanism-isolation}
\centering
\small
\begin{tabular}{@{}l r r r r r@{}}
\toprule
& \multicolumn{3}{c}{$E_{\mathrm{off}}$ ($\downarrow$)} & \multicolumn{2}{c}{Mutual coherence $\mu$} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-6}
Configuration & GEO & EVQ($\tau^*$) & Optimal & GEO & Optimal \\
\midrule
\multicolumn{6}{@{}l}{\emph{$b{=}10\mathrm{K}$}} \\
\quad $d{=}32,\, L{=}512$ & 653 & 516 & 220 ($-66\%$) & .999 & .708 \\
\quad $d{=}64,\, L{=}512$ & 2887 & 1533 & 1000 ($-65\%$) & .999 & .814 \\
\quad $d{=}128,\, L{=}512$ & 12120 & 3958 & 4283 ($-65\%$) & .999 & .973 \\
\midrule
\multicolumn{6}{@{}l}{\emph{$b{=}500\mathrm{K}$}} \\
\quad $d{=}32,\, L{=}512$ & 850 & 690 & 286 ($-66\%$) & 1.00 & .862 \\
\quad $d{=}64,\, L{=}512$ & 3715 & 2069 & 1305 ($-65\%$) & 1.00 & .995 \\
\quad $d{=}128,\, L{=}2048$ & 16473 & 8899 & 5574 ($-66\%$) & 1.00 & .998 \\
\bottomrule
\end{tabular}
\end{table}

Three findings emerge. First, the collision-optimal allocation reduces $E_{\mathrm{off}}$ by a remarkably stable $63$--$69\%$ across all configurations, confirming that the collision mechanism is robust and not a small-scale artifact. Second, mutual coherence $\mu$ (the maximum off-diagonal entry of the normalized kernel Gram matrix) drops from ${\geq}0.99$ under geometric to $0.55$--$0.99$ under the collision-optimal allocation, indicating that EVQ breaks the near-degeneracy of the geometric kernel. Third, EVQ at the theory-predicted $\tau^*$ captures a substantial fraction of the collision-optimal improvement (the ratio varies by configuration), with the gap attributable to the continuous-to-discrete transport that the surrogate does not model (cf.\ \S\ref{sec:tau-scaling}).

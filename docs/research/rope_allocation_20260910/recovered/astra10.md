# Astra10：当轮回传内容整理

状态：原综合报告和阅读回执未完成落盘。本文件由主任务根据已回传内容整理，不是该代理的原报告，也不是新一轮代理工作。

## 已保留下来的贡献

- 复核其他代理的数学条件：相关 C 下的非负最优解不能用 `[C^-1 h]_+` 直接裁剪代替 active-set 求解；独立通道噪声与共享频率 bin 噪声必须区分；正均值／阈值条件不能从尾界里省略。
- 指出自由系数适配不能自动导出唯一 Cosh 密度；密度、整数通道、幅度三者不能互换。
- 提醒实际注意力存在罕见高分 key 时，均值／协方差近似可能错误排序；精确 logsumexp 与二阶近似应分开。
- 指出已有少文档、高维直接频率拟合未建立稳健迁移；新的 whole-model CE 测量或拟合不能仅因用了梯度就称为新的统一理论。
- 提供 LeRoPE 先行工作定位：频率损失梯度及 downstream cotangent 并非本轮首创。原回传引用 https://arxiv.org/html/2607.10134v1 §3.2；归档保留为文献入口。
- 对 Mr 锚定的联合模式投影，强调 `/4` 的模式目标仍是假设；梯度仅可筛选方向，有限变化必须实际计算。固定 R、以 native 定义的投影半群性质，不能自动移植到每次重新锚定 Mr 的操作。
- 对新 YaRN/Mr 附件独立复算：公式、交叉槽位、局部扰动数值吻合；尺度导数须固定 native 和边界。
- 四格判读：F00=YaRN，F10=max，F01=min，F11=Mr。min>Mr 只改变 A 段；Mr 优于两个混合不足以证明协同。交互应看 F11−F10−F01+F00，并注明指标尺度。

- 另回传了生成配置核对项：部分既有自然文本结果的顶层 max_new_tokens 与个别行长度可能不一致，需按实际运行来源核定，不能只读一个顶层字段。此项保留为待核对线索，不作为新数据错误结论。

## 覆盖状态

代理曾回传已读完原分配的六文件及多数其他报告，但没有最终逐文件回执。归档不将这条回传升级为完整验证清单。完整原稿缺失这一事实保留在 archive_manifest.json。

当前研究目标以 ../../ROPE_ALLOCATION_THEORY_CORE_20260910.md 为准；以上历史建议不自动构成执行计划。

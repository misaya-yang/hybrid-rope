# Qwen3B 128K：前缀形成 × 读取旋转交叉诊断

状态：已完成，以下保留事前协议与执行中修订。继续两条既有反例 `niah_multikey_2_131072_1`、`vt_131072_1`；
这两例已经看过输出，属于回溯机制诊断，不是独立验证集或新方法收益。

问题：BM的退化更依赖原始长背景形成的前缀状态，还是最终读取使用的旋转表？
前次C/P/L说明背景形成的KV参与失败，但没有隔离这两类因素。

固定官方Qwen2.5-3B权重、S4、原始完整输入、所有可见key、标量gain、greedy预算和EOS。
二维因素为前缀形成表（MrPro/BM）与读取表（MrPro/BM）。每个来源先完整生成，
保存各层旋转前k_proj输出；去掉最后prompt token及生成token的缓存，再重算最后query。
交叉时从保存的原始K按目标表重新旋转全部前缀key，V保持来源预填充结果；
最后query及后续生成使用目标表。没有oracle token筛选，没有BF16逆旋转。

GPU解释资格：原完整输出逐token复现保存基线；从捕获K按原表重建缓存逐位一致；
同表缓存query回放逐token复现完整生成。任一失败则停止，不把工程不一致解释为机制。
实际远端Transformers 5.15.1 Qwen2Attention在k_proj之后直接reshape/transpose并旋转，
没有额外key归一化；已审核该路径。执行源码另存目录，不改旧快照或manifest。

共4次完整预填充、12次生成（含4次原始回放、4次同表资格和4次交叉），
预计数分钟，成本以实际记录为准。主读数是完整生成、EOS及既定任务评分；
逐步top-5/EOS margin辅助解释，不替代能力结果。

结果分支：若BM前缀+MrPro读取恢复而反向退化，支持读取规则在这些固定状态上
有因果影响；若结果跟随前缀来源，支持来源状态更关键；若混合则逐例报告交互。
不能把任一分支宣布为唯一机制：改变读取表也改变最后query和生成轨迹各层状态，
改变来源表同时改变上游内容表示、K和V。本实验不是纯key效应的完整识别。
只有结果需要进一步定位时才做有限层组干预；不扩展13任务或重启1.5B。

## 执行中修订：VT缓存读取建立独立配对基线

run_01的多键双向交叉完成且严格资格通过。VT/MrPro完整O复现旧结果，但
同表cached-query只在第30个（预算最后）token从`assigned`变成`only`，前29个
token一致，均已列出全部5变量，均未EOS。严格逐token资格因此正确停止。
不能称VT的新路径与原输出完全一致，也不把30-token预算输出称EOS完整正确。

后续run_02只执行VT，明确以cached-query输出为新配对基线：增加一次同表
K重建后的完整输出回放，必须逐token复现cached-query基线；交叉使用完全相同
K重建和query路径，只改读取表。原O仍需逐token复现旧基线，O与cached的差异
单独保留。此修订发生在VT交叉输出之前，不重跑已完成的多键案例。

## 完成结果

| 前缀形成表 | 读取表 | 多键输出（均有EOS） | VT官方召回 | VT结束方式 |
| --- | --- | --- | ---: | --- |
| MrPro | MrPro | 3954314，正确 | 100% | 30-token预算，无EOS |
| MrPro | BM | 3954314，正确 | 100% | 30-token预算，无EOS |
| BM | BM | 9289114，错误 | 20% | ` ZOFPD.` 后EOS |
| BM | MrPro | 9289114，错误 | 20% | ` ZOFPD.` 后EOS |

多键交叉输出分别与相应来源的同表完整输出逐token一致。VT的BM来源同样保持
原6-token输出含EOS。VT/MrPro来源换BM读取仍列出全部5变量，但后续说明改成
`However, it seems there is a mistake in the text.`，因此只是官方召回保持，
不是完整输出一致，也不是完整字符串加EOS正确率100%。

两例的成败/召回均跟随前缀来源；这排除了“只切换最后query和生成期的全局读取表，
并匹配重旋转所有前缀K，就能修复这两例”的方案。读取表可以改变后续文本，
不能说它完全没有影响。BM读取也能从MrPro形成的前缀中取出正确答案。
更值得定位的是预填充形成的状态，而非继续调一个统一decode表。

尚未分离K内容与V内容，也未定位层组。结果不证明任何来源状态不可修复，
不证明全部128K任务都由相同机制控制。下一项可判定实验是有限层组的来源状态
替换/形成干预，必须分别说明保留的K/V来源及是否为oracle；本轮未运行层组矩阵。

原run_01保留FAILED状态（VT逐token资格停止），其多键两臂资格均通过、结果可用。
run_02只重做VT并正常COMPLETE；两个来源的同表重建均逐token匹配cached基线。
两次累计16次生成、5次完整预填充；记录的生成时间合计179.08秒，不含全部加载/
重建/传输开销。run_02总耗时78.62秒，峰值分配24.30GB。5项相关CPU测试通过。
已从原始输出重新计算两例召回和EOS，并核对下载的执行源码与运行回执SHA。

[紧凑结果与完整token IDs](ROPE_BM_CROSS_CACHE_RESULT_20260908.json)随Git保存。
原始逐步top-5/EOS margin、资格、runtime保留在本地与远端
`results/bm_transfer_20260908/cross_cache_run_01/`、`cross_cache_run_02/`（远端根目录
为`/root/autodl-tmp/bm_transfer_20260908/`）。执行源码分别为`code_cross_cache_01/`
和`code_cross_cache_02/`；本地这两个目录保留对应运行器，远端保留依赖源码快照。
GPU进程已退出，没有后续自动队列。

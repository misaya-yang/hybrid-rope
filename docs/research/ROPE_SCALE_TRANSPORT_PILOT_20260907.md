# Scale-transport 冻结试验：首轮两小时执行协议

> **2026-09-08 当前状态：** 本夜实验及旧队列已收尾。下文授权、运行中与启动
> 顺序仅保留历史语境，不恢复执行；结果见[整夜报告](ROPE_OVERNIGHT_EXPERIMENT_REVIEW_20260908.md)，
> 后续解释与判据见[失败谱系](ROPE_LOCAL_FAILURE_SYNTHESIS_20260908.md)。现场授权只看HANDOFF。

> **2026-09-07 家用机接替后的授权更新：** 作者现明确允许高 ROI GPU
> 实验与 CPU/理论分析并行；随后又明确整夜算力授权、三次是效率期待而非
> 硬上限，实时预算见 HANDOFF。以下旧作业结果
> 和停止记录保留，当前新比较定义见文末，不把旧清单重新执行。

- **状态 / 日期：** 2026-09-07，构造与两项生成诊断均已完成；小型开发结果，不是 SOTA 确认。本文件是本轮实际执行范围，替代四臂/五臂先跑基线的提议。
- **授权：** 作者要求本任务接管，首轮约两小时、零训练、优先本方方法并复用论文数字；允许无卡准备后自行切回同一有卡实例。累计 GPU 试验上限 7200 秒，不训练旧 Z 或对手，不租新实例。
- **问题：** 一版模型响应驱动的中低频分配能否生成不同于 MrPro 的可用静态表，并在小型自然问答面板上提供值得继续的结果？
- **输入：** [Pro 原文](../../paper-2027/research/external-reviews/ROPE_SCALE_TRANSPORT_METHOD_AND_CODEX_20260907.md)、[独立评议](ROPE_SCALE_TRANSPORT_REVIEW_20260907.md)、[代码](../../scripts/experiments/scale_transport/)。这是降成本的本方 pilot，不是 Pro 全层原版或已验证 SOTA。
- **证据身份：** 模型版本、实际代码/数据/数组哈希和运行回执写入服务器私有 plan、prepared manifest 和 run 输出；下载完成由官方 revision/hash 核验的 model_ready.json 确认。完成后在本文件补结果摘要；文件存在不代表运行发生。

## 最新解释修正

后续逐行分析确认：尾频方案相对 Native 的短面板 F1 增量，全部来自两行的措辞/词面差异；不足以认定新增正确能力。8 条短题和 8 条长题各只覆盖 5 个独立上下文。原 F1 数字不改，解释应使用文末“继续研究”段落，不能把 0.523>0.498 当作 Native 改善证据。

## 固定范围

| 项目 | 本轮定义 |
| --- | --- |
| 模型 | Qwen/Qwen2.5-3B-Instruct，官方 revision `aa8e72537993ba99e69dfaafa59ed015b17504d1`；冻结 BF16 权重 |
| 源/目标窗口 | L0=32768，固定 s=4、目标相位长度 131072；当前效果面板实际输入最长 65408，不能宣称测得 128K 能力 |
| 采集 | 层索引 5、11、17、23、29、35，所有 16 个 query heads；每段末尾 8 个查询，相邻间隔 8 tokens；每个查询对全部可见 key 归一化 |
| 校准文本 | 本地 FineWeb-Edu 已有第一份 parquet 中按原行顺序选首 16 篇 ≥32769 个 Qwen tokens 的不同文本；每篇固定首 32769 tokens，8 篇 C、8 篇 V |
| 可见长度 | 对同一末尾锚点取 8192/16384/32768 tokens；名义输入 917504 tokens；最终 1 token 作尾部 NLL 标签，不加入输入 |
| 控制 | 在 32K 的固定 Q/K/V 上截断 keys 至 8K/16K，重算响应并保存；真实短窗与固定内容裁切分别报告 |
| 构造 | C 上 16K→32K 分位数估 β；高段 j≤23 保持 Mr，零响应槽位回退 Mr；其他槽位 ω/4≤ν≤ω，保槽位的有界降序投影 |
| 机制检查 | C 的 8K→16K β 预测 V 的 16K→32K 分布；比较固定/同比增长与 Mr 指数解释；属于诊断，不把成功当因果证明 |
| 缓存约束 | C 文档固定源状态；Native 残差变化与其他 C 文档 128 个 key 在固定抽样长距离上的 log-mean-exp；完整 sin/cos，全头输出先加后取平方 |
| 固定选择 | λ=0、.25、.5、.75、1；取两个缓存量均不超过 Mr 参考的最大 λ，浮点相对容差 1e−6；无额外参数搜索 |
| 部署 | 一张 FP32 inv_freq，gain=1+.1 log4；同一表用于本方所有输入长度、prefill、decode |
| Native 小检查 | LongBench narrativeqa 中首 8 条完整输入为 4K–16K 的行；原模型与本方各生成一次 |
| 本方长任务 | 同数据中首 8 条完整输入 >32K 且≤65408 的行，仅测试本方，不重跑对手矩阵；保留完整上下文，不截取证据 |
| 生成/评分 | Qwen chat template、greedy、最多 128 新 tokens；保存原答案/输出/IDs/EOS、QA token F1。属于小子集，不是完整 LongBench 或 RULER 榜单 |

Published MrRoPE 数字用于研究背景和初步目标参照，当前子集分数不能直接与其 RULER 总分相减。这里没有因这种不可比性追加完整对手评测；仅在后续决策确需时补最小比较。

## 输出与停止

- CPU 阶段先完成模型下载、文档和评测行冻结、三个数学测试及必要运行检查；无卡时不加载完整模型到 CPU。
- 一个独立监督器持有 GPU 锁，核对 plan/code/data 身份和显卡，使用正的硬超时；运行失败保留日志与原始输出，不自动重试或重置两小时预算。
- GPU 路径限定 BF16 Flash SDPA。第一个实际采集前向同时检查自算 attention replay 与模型输出，相对范数误差大于 0.05 就停止查实现。这是 BF16 工程容差，不是能力门槛。
- OOM、非 Flash、数组/输入身份漂移、非有限数值或硬超时立即停当前试验。硬超时允许 15 秒退出清理，主作业超时设置不超过总上限减清理余量。
- λ=0 输出 REFERENCE_ONLY：本轮未得到新表，不对相同 Mr 数组重复生成，不宣布新方法有效或方法类失败。
- λ>0 才测本方生成。若 Native 明显退化或长任务无可用输出，结束本轮，分析已存输出，不追加训练/扫参。
- 采集、裁切控制、提案、五点缓存记录、最终数组和逐行生成均保存。缓存分数不是能力选择证据；pilot 只有单模型、小样本、固定六层，未建立跨层代表性或跨模型迁移。
- 若无法开回 GPU，则留无卡/停止状态等作者。完成后报告并暂停监控；关机/模式状态以平台或 SSH 回读为准。

## 本轮验证状态

四个工作机 CPU 测试通过（最终回读 13.63 秒）：直接旋转+causal GQA 对照、分位尺度恢复与可见窗口反例、有界投影与独立小型优化器对照、真实小型 Qwen attention hook/输出布局与最后 logits 接口对照。准备阶段发现 Transformers 返回 BatchEncoding，已显式使用 return_dict=False，冻结行数为 8 Native/8 long、实际输入 8399–60445 tokens。CLI 检查通过。正式大模型 hook 和最终任务改善未验证；它们由受预算限制的本轮运行给出，不在本文件提前写成成功。

## 首轮构造回读与预算内的能力诊断

构造运行已返回 REFERENCE_ONLY（146.44 秒）：λ=0 的 DN=22.44445、背景 log-mean-exp=5.38381；原始 λ=1 提案的 DN=23.44834、背景=5.28431。所有正 λ 都因缓存 DN 较高而未通过。原结果与五点规则保留，不事后放宽阈值。

作者在本轮明确要求优先研究自己的方法，避免严谨性流程挤占实际效果判断。因此在同一两小时总预算内，追加**已冻结原始提案的生成诊断**，最多 15 分钟：复用同一模型、原数组、gain、16 条任务行；仅 Native 的 8 条短问答作必要能力参照，本方提案测 8 短+8 长。不再跑 Mr/YaRN/CoPE，不修改 β/投影/参数，也不按生成成绩选择另一张表。

该臂单独命名 ScaleTransportUnguarded，输出到不同目录；它不是通过约束的 Pro 最终方法。问题是“缓存拒绝是否伴随实际能力损伤”。若无收益或损伤明显，结束这轮并分析已有文本；如有可用效果，后续再解释 guard 与能力的差别，不把此 post-construction 诊断冒充预注册独立确认。

原始提案的生成诊断已完成（66.97 秒）：Native 短面板 F1=0.49846，本方短面板=0.425，本方长面板=0.22202；每格 8 行，全部 EOS。它不能支持获益主张。

随后仅作一个已声明的中/尾段归因比较：MrMiddleScaleTail 保持 Mr 的 j<40 原频率，中低尾 j≥40 使用**同一已经固定的原始提案**，没有重新估计、投影、排序或调 gain。复用已核验的 8 条 Native 输出，只新增本方 8 短+8 长，最多 15 分钟且不突破同一个两小时截止。它检验大幅改变中段是否是短任务退化的来源，属于同开发集的 post-outcome 归因，不作为独立确认。第一轮 evaluator 的代码已在修改前归档，原 plan/hash 与原结果保留。

## 完成结果与证据

| 已执行配置 | Native 内问答 F1 | >32K 问答 F1 | 完成/EOS |
| --- | --- | --- | --- |
| 原始 Native | 0.498465（8 行） | 未追加 | 8/8 |
| ScaleTransportUnguarded | 0.425000（同 8 行） | 0.222024（8 行） | 16/16 |
| MrMiddleScaleTail | 0.523214（同 8 行） | 0.225000（同 8 行） | 16/16 |

**Observation（此开发面板）：** 固定本方尾频、恢复 Mr 中段，短问答比全中低频改动提高 0.09821 F1；长面板仅差 0.00298。支持进一步定位中段改动的代价，尚不支持长任务优势、总体 Native 保持或超过 MrRoPE。没有重新跑 Mr/YaRN 的整套论文；论文数字与本面板不属于同一评分终点。

原始构造因为缓存约束返回 REFERENCE_ONLY。后两项是明确标记的未通过该 guard 的能力/归因诊断，不能合并称“Pro 原方法成功”。C 集的共有中低频槽位平均早期 β 为 0.82778，固定内容裁切控制为 0.91970，说明可见范围改变本身足以产生很强的增长信号；不据此声称所有真实关系都由裁切解释。

监督器计时：构造 155.41 秒、原提案生成 75.01 秒、尾部归因 70.01 秒，合计约 300.44 秒；程序内部时间分别为 146.44、66.97、59.47 秒。计时不包含下载、CPU 准备、控制台切换和 GPU 空闲时间，不冒充云账单。三个作业均 exit 0；没有权重训练、参数扫描或对手训练。

原始输出已回传本地私有维护目录，并独立重算 QA F1、EOS、行数及数组哈希：

- 原提案 examples.jsonl SHA-256：`a8edd9830848eb04ab01fdae0f5431672b0876efe2621fd4cb08774146c7ccfa`。
- 尾部归因 examples.jsonl SHA-256：`52bd2c2ba6b89913056efec81f040debf676862c2679857043fc09832f443d59`。
- 紧凑回读包 SHA-256：`3f1db0dffeb1fb97155de3fd07b7f78046e046236f6f08c5a48b3fc21aa43f9c`；保存完整逐行输出、公式/数组身份、模型下载核验、plan 和作业状态。大缓存与原始响应留服务器。
- 第一项生成 evaluator 的原代码在修改前另存，原 plan/hash 保留。两张诊断数组从冻结输入重构，并与各运行实际记录的 tensor SHA 对齐；不把重构时间冒充执行时保存时间。

本轮到此结束，不为用满两小时继续消耗 GPU。后续建议优先保留有效 Mr 中段框架，研究更有依据的尾频/衔接；新的强结论需要新样本或可比公开终点，避免在这 16 条开发问题上反复挑表。论文 TeX/PDF 与历史分支未修改，未提交或推送 Git。


## 继续研究：先保留有效部分，再稳定最慢尾部

**状态 / 日期：** 2026-09-07；既有输出的只读复核，以及一张未运行的组合候选。没有新 GPU 结果。计算入口为 [标准库分析程序](../../scripts/analysis/analyze_scale_transport_pilot.py)，输出为 [机器可读分析与候选表](ROPE_SCALE_TRANSPORT_FOLLOWUP_20260907.json)。

### 逐题结果改变了哪些判断

尾频组合相对 Native 的平均 F1 +0.024749，仅由两行组成：narrativeqa_3 贡献 −0.010965，narrativeqa_13 贡献 +0.035714，其余六行相同。后者 Native 已输出人名 Virginie Vervelle，尾频组合输出更长的“他们的女儿 Virginie”表述，参考答案包含 daughter 和另一种人名拼写。这不足以说明多学会了一题，不能把 F1 上升当作能力增加。

原始全中低频提案把 narrativeqa_19 的答案由正确的 Two 改为 One，恢复 Mr 中段后又输出 Two；它还把另一行的人名换成了不同人物。这给保留中段提供了具体开发证据。数量词 Fifty/50 的 F1 差异则不能当推理进步。这些是对已保存答案与参考的解释，没有修改官方词面评分，也没有另造语义分数。

长面板 0.222→0.225 同样没有显示清楚的增益；错误答案中的少量共有词也会拿到非零 F1。每个面板 8 行各有 5 个上下文，不应把相关问题当成 8 份独立文档证据。接下来不继续围绕这 16 行挑表。

### 原始尺度分配的具体问题

Mr 在 j=24 的累计指数约 0.0065，本方原始提案却给到 0.912，接近把该槽位从几乎不动改为 `/4`。类似变化遍布中段。因此它实际重写了 Mr 有效的高到低渐进分配，而不是在该分配上作小修正。

“真实增长减去固定内容裁切增长”的差分思路有消除纯可见性效应的优点，但现有未截断尺度比给出的差分范围为 −1.537 到 +0.541，40 个待考察槽位中 7 个绝对值超过 0.5。没有充分依据把它立即加到 Mr 指数上；那会启动又一轮剧烈移动。这次只将差分作为诊断，不把它包装成新最优规则交给 GPU。

### 只保留一个新组合

令 T 为本轮已经测试的 MrMiddleScaleTail 数组。使用 CoPE 官方推理代码的末 20 槽余弦衰减思路：

\[
c_j=1\quad(j<44),\qquad
c_j=\frac{1+\cos(\pi(j-44)/19)}2\quad(44\le j\le63),
\qquad \nu_j=T_jc_j.
\]

名称仅作实验标识：`MR_SCALE_TAIL_COPE20`。不改统计、gain、槽位数或边界；当前参考计算约定为 Python float64 cosine 后存 IEEE float32，不能称与 Torch cosine 逐位相同。[CoPE 官方实现](https://github.com/hrlics/CoPE/blob/f8957a1c7e4891f31266ca79299437a672175a1a/modeling_cope.py)

这里三部分各有用途：Mr 中段保留已有效的累计分配；现有本方尾频允许部分较快的低频保留位置变化；CoPE 衰减进一步稳定深尾。末频率变零仍保留该 Q/K 子空间的内容内积，不等于删除通道。组合不是已验证的新颖性，仅仅把两个已有方法相乘也不足以支持论文主贡献。

CPU 结果：前 44 槽逐元素不变，末槽精确为零，所有频率有限、非负、非增；继承的等频对如实保留。最终 FP32 tensor SHA-256：`a63fe2714b37a30b4387fff8af500c270b62770a5b90677302083569af9c2eb2`。

对比已测试的 T，最大附加频率变化出现在槽位 51，约 2.11834e−6。由二维旋转恒等式和块对角范数，连续距离区间 [0,L] 上有

\[
\sup_\Delta\|R_{\nu}(\Delta)-R_T(\Delta)\|_2
=\max_j2\sin\!\left(\frac{\min\{\pi,L|\nu_j-T_j|\}}2\right).
\]

在 Native 32K、目标 128K 上分别约为 0.06940、0.27676。这里不含共同 gain，比较的是相对旋转算子；不是最终 logits/生成误差界，也不能修复 T 已有的损伤。它说明这次新增干预集中在深尾部，不再像原始提案一样大幅重写中段。

### 下一步用什么实际终点

上一轮 NarrativeQA 小面板适合抓明显故障，却与 MrRoPE 原文的 RULER、InfiniteBench、LongBench-v2 不同，无法直接利用论文分数作方法对比。这一点应改进，而不是再增加许多同类自定义小面板。[MrRoPE §4.1](https://arxiv.org/html/2601.22181v1)

下一项具体比较应优先使用 Qwen2.5-3B、固定 s=4、真实 128K 输入，在原文对应的官方 RULER 任务上测**本方组合**。先少量、固定每任务样本进行开发筛查，保留任务宏平均，注明样本数/代码版本/精度与论文差异；论文对手结果先复用，不自动重跑完整基线。真实计时后再定下一批预算，不把先前约 60K 的吞吐直接外推成 128K 保证。

若组合出现有用结果，再补最小的 `MrPro×CoPE` 去掉本方统计的消融，判断自身分配是否提供了额外价值；这比先跑完整对手矩阵更直接。若只有恢复中段有效、长任务仍无收益，就保留发现并结束此候选，不以新 gain、更多窗口或未声明重拟合救分。

此阶段已经得到具体数组与可证的局部改动范围，但尚未得到新模型结果、可比论文分数或 SOTA 证据。服务器与监控保持停止，论文与 Git 历史未改。

## 作者“启动”后的组合试验范围

作者已重新开卡并明确要求启动。执行固定组合 `MR_SCALE_TAIL_COPE20`，不重算频率，不跑对手矩阵。继续遵守此前两小时累计作业预算；上一轮已用约 300.44 秒，本轮总上限不超过剩余约 6899 秒，并从本次开卡时间再设墙钟截止。

首先使用 NVIDIA/RULER 固定 commit `c3f5e3b4f87f97e048793bb510a3a6b19a46bf3a` 的原始生成器和原始 string_match_all 评分，任务为 niah_single_1、niah_multikey_3、vt，seed137。每项 2 条 4K 正控制、8 条 128K 长度档，共 30 行；Qwen 原聊天模板与任务 answer prefix 在生成数据时加入，保留各任务原生成预算（NIAH128、VT30）。物理输入长度逐条记录，不用表的 s4 冒充 128K 实测。

这些是官方任务的开发子集，不能将其三任务平均与 MrRoPE 的完整十三任务总分直接比较；论文成绩用于目标参考。先取得本方在实际 128K 的输出，再决定是否值得扩至其余任务，不为基线复现阻塞本方试验。

每项短控制至少一条全参考答案命中且正常 EOS 后才跑该项长格；未通过则保留短输出，标记该项长格未测，不只挑已通过任务报“总分”。OOM、非 Flash、静态表或数据身份漂移、硬超时停止；不自动重试或改窗口/增益。所有结果均为开发诊断，不以词面小样本领先直接宣称 SOTA。

## 组合 RULER 结果回读

该作业已结束：监督器 575.11 秒，程序 568.15 秒，22 条生成输出与 8 条预先规则下未测行；不是完整三任务或十三任务总分。

| 任务 | 短控制 | 128K 档 | EOS |
| --- | --- | --- | --- |
| niah_single_1 | 2 条，100% | 8 条，100% | 长格 8/8 |
| niah_multikey_3 | 2 条，50% | 8 条，0% | 长格 7/8 |
| vt | 2 条，80% | 8 条未测 | 短格 0/2，未满足本轮全答案+EOS 的长格资格规则 |

VT 的两条输出均覆盖 4/5 个变量，随后进入解释，在 30-token 预算处结束。它有部分追踪能力；本轮未测的 128K 格不能记零分，也不能据此关闭方法类。UUID 检索在一个短样本也失败，因此长格的 0% 尚不能全部归因于窗外频率。普通 needle 的 100% 不等于解决了这些更难任务或达到 SOTA。

逐行官方评分、EOS 与 raw SHA 已核验，完整紧凑回执已复制到本地维护目录。examples SHA：`76e82341411ad81e31d3e9cb7033febd8fe351f503d56339ede0b41ebfcfeb66`；回读包 SHA：`8ea220cf73825d105b1163505fec6e58d115e8bce7e8817bc4bb3538490a4200`。下一项分析应优先区分 UUID 短程错误与长度退化、VT 答案预算与能力限制；不因简单 needle 成功就扩大整套实验。

## 续研实验 1：有效 p2 中段的当前权重判别

**状态：** 2026-09-07，运行前冻结；一个候选，不扫描。作者最新授权允许
GPU 与 CPU 分析并行。问题是旧 Qwen1.5B 有效分配的中段，在当前 3B 上
相对 MrPro 是否仍有价值；不是把跨模型旧分数当当前结果。

- **固定干预：** `P2Middle` 在 j=24..39 使用恢复 JSON 的 FP32 p2；其余
  槽位逐位保留实际 MrPro。避免继承已知 legacy 高频尖峰，尾段均为 `/4`。
  两臂共同 gain=`1+.1 ln4`。这是一项新中段移植，不冒称旧 p2 原方法
  （其 gain 系数为 .074）的完整复现；没有 CoPE、gain 或边界搜索。
- **比较与数据：** 同一 Qwen3B revision、BF16/Flash、静态整请求表，
  复用既有 seed137 RULER `niah_multikey_3` / `vt`，每任务 2 条 4K、
  8 条 128K 档。两臂各 20 条；Native 只补相同 4 条短控制，共 44 次生成。
  这些已暴露开发行不充当独立确认。不因某臂短格不过关而只跳其长格。
- **协议修复：** VT 完整生成预算为 128，同时另报前 30 tokens 的原预算
  string-match-all 分数。保留原 prompt、不截证据；总 token 可超过旧档位
  最多 98，逐行记录真实输入/输出/总长度。官方覆盖率、全答案和 EOS 分开。
- **判别与下一步：** 若 p2 中段带来配对长任务收益且短控制无新增实质错误，
  保留为当前模型的有据起点，下一实验才做独立行确认或一项有依据的修正。
  若损伤或不改善，不在这些输出上扫混合率/边界；结合冻结状态重放解释
  具体改动。若两臂在困难任务同处地板，结论为该格不可区分，不能判方法类
  失败或用普通 needle 的成绩补成成功。两题短格只抓显著故障，不是 Native
  全窗口保持证明。
- **成本和停止：** 复用前次实测约 575 秒/22 行，当前预计约 15–25 分钟，
  单次硬上限 1800 秒（含准备后的模型加载）。共同阶段截止保守从本次 goal
  建立时 13:53:27 UTC 起计，扣旧监督器约 875.55 秒，最迟 15:38:51 UTC；
  此累计口径不是云账单复原。OOM、非 Flash、非有限输出 logits（生成异常）、
  数据/表身份漂移、硬截止停当前尝试并保留回执；普通工程故障修复后在同一
  实验身份及剩余额度内恢复，不能重置计数或数据。监督器沿用现有 jobs.py。

执行入口：[paired_ruler_run.py](../../scripts/experiments/scale_transport/paired_ruler_run.py)。
数组、输入、代码、评分器及 plan 的确切 SHA 在运行前保存；当前尚无本比较
结果。CPU 同时只分析已有 32K Q/K/V，不据缓存损伤大小自动选部署表。

### 实验 1 完成回读

44 条生成已完成，监督器耗时 1165.211 秒，程序 1154.658 秒。数组及
实际 gain 与冻结 plan 相符；本机重算了完整输出覆盖率、行数、配对及原始
SHA。这是当前 3B 的中段移植比较，不是旧 1.5B p2 整表的复现。

| 任务 / 输入档 | Native 短参照 | MrPro | P2Middle | P2−Mr 配对胜/平/负 |
| --- | ---: | ---: | ---: | --- |
| UUID / 4K，2条 | 100 | 100 | 50 | 0/1/1 |
| UUID / 128K，8条 | — | 12.5 | 0 | 0/7/1 |
| VT / 4K，2条 | 90 | 90 | 50 | 0/0/2 |
| VT / 128K，8条 | — | 62.5 | 65 | 2/5/1 |

分数为官方 string-match-all 百分数，非完整答案/EOS 联合分数。长 VT 的
配对行变化为 `[-60,0,+20,0,+60,0,0,0]`；+2.5点均值不是稳定优势，短格
同时损伤。Mr/P2 的长 VT 全答案为2/8、3/8，EOS为4/8、6/8；长UUID
全答案1/8、0/8，EOS为7/8、8/8。不能用较多EOS把错误答案算成功。

VT 从原预算30扩为128后，Mr长格由60变62.5、P2由62.5变65；Mr短格
由80变90、P2仍为50。输出中确有后来才提到的变量，也有从头到尾遗漏或
改写的赋值链，不能把全部问题归为输出预算。原30-token结果未覆盖。

短UUID第二行的P2输出 `42161567-d5fb-42a2-a59d-6e8af6546470` 是原文中
另一个键对应的真实值，目标为另一条记录；Native和Mr复制正确目标。这是
可直接核对的键值错配，而不是仅仅词面格式差异。该事实本身还未定位具体
层/head机制，也不支持从一个错例直接拟合频率。

**当前判断：** 这张中段移植不能作为改进晋级；旧p2仍拥有其原模型/整表/
gain/任务下的结果。本轮没有解释出checkpoint之间功能兼容的充分条件，
不能把“同几何、有旧成功”当作当前移植必然成功的推导。下一步不在当前行
上扫混合率或边界；只对相同条件做数值状态回读，检验直接换频和隐藏状态
传播分别造成了什么，再决定主方案。

原始 examples SHA：`05aba1e9f3dae7937e6445ec13b845390ce02f69297a53d59f481bc8c3c26bc0`。
P2Middle tensor SHA：`9c33e9ae47b69e43b630f3776bd986a61e722e06a9e57bc905808c951050c7f3`。
plan SHA：`4d7d857bd04e15f1a815d291cb9b92a77c085c58e6afa10ff79166c07db8b2b6`。

数值回读复用同一四条短输入及同一两张表，8次短前向保存全36层的原始
Q/K/V及输出；不生成新答案或新候选。监督器25.005秒，原始资产另存于
`frequency_e1_states_01`。入口为
[capture_paired_states.py](../../scripts/experiments/scale_transport/capture_paired_states.py)，
后续分析为 [analyze_binding_states.py](../../scripts/analysis/analyze_binding_states.py)。
这些数值诊断不计新的核心效果实验，但计入实际耗时；研究目标仍未完成。

# 新 Sol session：5090直接接管

更新：2026-09-12。先读本页和[准备记录](5090_recovery_preparation.md)，需要原计划时读[原文](ICLR2027_ROPE_EXPERIMENT_PLAN_20260912.md)。

## 最新执行目标：1–sL全范围，包含原生短段

**最新顺序覆盖：先零训练选表，再用所选方法微调。** 当前健康作业正常完成；
下一阶段优先补对称Beta增量gamma=1.5与3，复用Uni(gamma1)、BM(gamma2)和
其他已有强基线，不做二维乱扫或重训全矩阵。代码复用、全范围面板及结果选择
见[研究计划顶部](index.md)。附件的原生/端点非劣不是执行门槛。

作者最新指导以[当前研究计划顶部](index.md)为准：BM是当前适配主线，
目标从最短可用上下文到16K整体可靠，包含4K以内，允许可解释的小幅短端损失。
当前健康E3/E2和此前安排的32K/64K小面板正常完成，后者不升级为新的极远端
主目标。下一项复用BM frozen、BM-8K200及现有短指令结果，补
512/1K/2K/4K/6K/8K/10K/12K/14K/16K区间曲线；各任务从实际可容纳的最短长度开始。
小面板与自然QA/完整任务评价均用于记录收益和损失，不是后续实验的准入门槛。
不默认五臂重新训练。
原生零退化不是门禁，固定频率端点也不是实用方案门禁；需要改变范围或gain
时按组合方法标明，不能以形式纯度阻塞有价值的实验。当前不因此自动增加架构
或大模型训练。后续16K200、Llama等依作者在执行任务中的最新授权与顺序推进，
研究代理不得以AUC、最差点、单针或原生保持等指标另设“通过后才继续”的条件。
以下早期Cosh-only、暂停或旧队列段落为历史，不覆盖本节。

## 最新执行约定：可信克隆，不做SHA门禁

用户已把实例同步到24904并授权执行；该端口是当前入口，后续克隆改端口时以用户最新消息为准。**不做模型、数据、tokenizer、代码、manifest或adapter的SHA校验，不因历史字节身份变化重建数据包或阻塞训练/恢复/评价/统计。** 用户保证克隆资产正确。已有历史SHA保留即可，不重算；保留文件存在、模型/输入长度、arm/预算/恢复位置、解码与样本对应、显存/有限loss等执行检查。

当前运行由任务“5090实验负责agent-5.6sol”管理，E2 GPU生成与LoRA/E3 CPU准备并行，实例可用配额据用户说明约25核/90GB。不要重启健康进程或重复G1。下面原无卡约束只适用于无卡准备阶段，当前有卡资源由执行任务调度。

## 目标与顺序

**使用现有OLMo，全部5090，小步快跑，不等4080。** 四条线是：Top1 LoRA恢复、E2七配置QA、E3两表独立确认、G1实际Q/K系数GPU诊断。E2/G1可以先跑，LoRA/E3数据处理好后直接接；旧EVQ精确字节或S1未完成checkpoint不阻塞本轮。

2026-09-12最新范围：LoRA只比较Native与anchored Cosh τ2，真实8K/16K混合、两臂32Mi CPT。YaRN官方依赖全参数长窗SFT，当前LoRA版本不再作为训练对照；已完成的YaRN零训练结果仅保留历史参考。旧精确复播只是可选入口。Llama等OLMo稳定后再考虑。

OLMo原生4K；2×/4×必须落实在训练数据本身。8K/16K各占一半CPT tokens，长指令prompt至少7K/14K且保留完整答案。Native是频率表对照，同样训练长输入；4K以内Dolly只用于防遗忘。见准备记录的长度配方。

## 资源与权限

历史无卡只有0.5核/2GiB；当前有卡实例使用其约25核/90GB配额，本机仍不做重活。**用户新授权原始数据提前下载到服务器**，所以下载可以继续；分词、案例生成、模型CPU/GPU计算留到开卡后的资源。不要把旧文档“禁止下载”覆盖这项新授权。不在本机下载大文件或运行模型。

保留模型权重，不做清理。4080的主训练独立，别启动同一S1、别迁移其进程。用户已在执行任务授权开跑；此任务负责总设计和后续代码准备，避免启动第二套调度。

当前工作区是 `/Users/yang/projects/hybrid-rope`。接管先看Git状态，不丢弃dirty work。其他任务已在本轮期间推进HEAD至 `468db7c2`；其后还有新改动/未跟踪文件，不假设全都在新worktree中。本session没有提交或推送。

## 服务器路径

以 `/root/autodl-tmp` 为数据盘根：

| 资产 | 相对位置 |
|---|---|
| 后续无SHA门禁源码 | `rope_fast_5090_20260912/prepared_code_no_sha_20260912/` |
| 当前健康进程原源码 | `rope_fast_5090_20260912/prepared_code/`，不中断或覆盖 |
| OLMo模型+tokenizer | `olmo2_1b_longalign_assets/models/OLMo-2-0425-1B-Instruct/` |
| LoRA原始数据 | `olmo_recovery_20260912/sources/`；小回执 `acquisition.json` |
| LoRA处理后数据/plan | `olmo_recovery_20260912/data/`、`olmo_recovery_20260912/paired32m/`，待生成 |
| E2输入 | `rope_fast_5090_20260912/e2_prepared/` |
| E3旧350开发面板 | `olmo_fast_screen_20260908/prepared_ruler_newtasks_02/` |
| RULER上游 | `rope_qwen_baseline_20260907/ruler_upstream/RULER-c3f5e3b4f87f97e048793bb510a3a6b19a46bf3a/` |

SSH：`ssh -p 24904 root@connect.westd.seetacloud.com`；Python：`/root/miniconda3/bin/python`。后续从无SHA门禁源码根用 `python -m` 运行，沿用原绝对数据/输出路径。避免用更早的 `.../code/` 快照。

## 先做两件事

1. 原始数据已下载完成：`sources/acquisition.json`为`RAW_SOURCES_READY`，180文件共777,255,900 bytes、0失败，另复用RULER四个源文件。下载进程已结束；无需重复下载。训练包和新评测实例尚待有资源后生成。
2. 后续新任务从`prepared_code_no_sha_20260912/`启动；当前已运行的E2与CPU准备继续使用原`prepared_code/`，不为切换源码重启。E2原输入不变；无需重绑SHA；克隆数据沿用，按实际arm、长度、预算和解码设置继续。

无卡阶段只准备；当前有卡时做最短必要显存/有限loss检查，然后进入有效实验，不逐项搭审批门。

## 实际入口

- 原始数据下载：
  `python -m experiments.olmo_recovery_20260912.download_sources --root SOURCES --ruler-upstream RULER --execute`
- LoRA数据：`data_prepare --sources SOURCES --model MODEL --output DATA`。有旧native_rows才需要source-tokenizer；否则自动使用下载好的Dolly新replay，不等旧文件。
- LoRA配置：
  `python -m experiments.olmo_recovery_20260912.make_plan --model MODEL --data-manifest DATA/data_manifest.json --out RUN`
  默认 `olmo_fixed_support_tau2`，不再要求旧数组。
- LoRA显存：`probe --root RUN --arm Cosh --execute`。
- 0-step评价：`evaluate --root RUN --arm Native --base-native --split dev --output NEW_DIR --execute`；Cosh用 `--unadapted`。不再启动YaRN训练或新评价。
- LoRA训练：`train --root RUN --arm ARM --until-cpt-tokens 33554432 --execute`；恢复加 `--resume RUN/runs/ARM/resume`。模块前缀均为 `experiments.olmo_recovery_20260912.`。
- E2：`python -m experiments.rope_fast_5090_20260912.e2_run --prepared E2 --model MODEL --output NEW_DIR --execute`；统计用同包 `e2_score`。
- E3准备：`e3_prepare --reuse-prepared OLD350 --upstream RULER --development-screen OLD350/screen.jsonl --out E3`；额外prior-registry可选，旧350始终排除；不存在/不完整的旧registry不阻塞。运行用 `e3_run --prepared E3 --development-screen OLD350/screen.jsonl --out NEW_DIR --execute`，统计用 `e3_postprocess`。
- G1：`python -m experiments.rope_fast_5090_20260912.qk_diagnostic --prepared E2 --out NEW_DIR --execute`。

以上是手动模板，MODEL/SOURCES等替换为真实路径；同卡一次一个GPU工作负载。没有已启动的跨实验守护进程，不要以为列了命令就会自己接下一项。

## 追加队列与LoRA v2

三个追加面板已在24904准备：p1=384、source=768、layers=360次新生成（复用E2两条全层基线；否则480）。从当前无SHA源码目录运行：

`python -m experiments.rope_fast_5090_20260912.supplement_queue` 列出具体命令；`--job p1|source|layers --execute`由本执行任务选择一个空闲GPU时段执行，默认不会自启动。数据目录和机制边界见准备记录。不要抢占LoRA或重复启动另一条GPU调度。

[LoRA v2设计](OLMO_LORA_V2_20260912.md)已结合新的Pro方案：两者都训练全层Attention+FFN LoRA；Pro改的是rank和LR分配。当前R0仅70条16K长指令，因此正在下载公开训练集并扩大真实长池。扩展数据另存，别直接覆盖当前训练manifest；v2已新增独立data/runtime/train/probe/eval入口，具体命令见设计页；不把Pro研究YAML直接交给R0训练器。下载/数据跟进已通过任务消息交由执行Sol，估剩余下载10–20分钟、处理20–40分钟（规划值，不要求持续轮询）。

## 最新队列覆盖：暂停LoRA调参

### 夜间决策交接：按原目标完成短阶段，不增加临时门禁

**最新研究安排：** 渐进安装已完成且单针仍全零，停止该日程。复用旧`evq_deploy_t1`的`CoshDeploy_tau1_g4`已完成真实8K200：4K/8K official及规范化exact+EOS均16/16，16K仍0/16；没有加NIAH训练标签。研究任务已指示Sol继承checkpoint-200和Adam做真实16K200，保持当前部署表、scale=4与gain不变，新增明确`16k_coshdeploy`阶段；**不要调用会切回旧anchored tau1的`16k`阶段**。保持前200步实际长SFT来源比例，short replay offset200，其余r32/七模块/LR/loss不动。先看本轮实际任务变化，不机械加8K100。具体事实、结果路径和归因边界以[机制owner顶部](LORA_MECHANISM_AND_RECIPE_20260912.md)为准。以下两阶段叙述是此前anchored配方的历史，不覆盖新部署构造的进展。

**该链现已完成。** Native8K300与Cosh8K300→16K200及阶段评测均结束；[机制owner](LORA_MECHANISM_AND_RECIPE_20260912.md)已记录真实结果。简单单针仍失败，停止本配方机械续训，不重复下列已完成步骤。Cosh最后500步对Native300步的长LM差异不是同预算方法比较；同预算层是8K300对照。只继续已授权且未完成的非LoRA任务，后续LoRA实质改动由研究任务依据证据安排，不默认专项监督、扫描或Llama。

用户要求研究任务阅读历史transcript并自主给执行Sol方案。已回读7月25/26日原始用户消息及[9月提示词记录](../USER_PROMPT_TRANSCRIPT_20260909.md)T3-P08/P11、T2-P04/P12、T4-P14：核心是实际功能、机制与成功先例、最小有判断价值的比较，不以忙碌/文档替代、不要求指标全胜。历史“只训4K”已被本轮真实8K/16K授权替代；旧附件推断不是当前事实。

当前Cosh8K300步约5.03M主输入，建模改善但4K/8K/16K简单单针仍复读；tau1零步亦0。已知改变不足以证明LoRA容量、通用数据或整个Cosh族不可行。下一执行顺序明确如下：

1. 同扩充数据、同300步、同索引/LoRA/loss/LR完成Native对照，复用Cosh300；不扩大成训练矩阵。研究任务已支持Sol实施。
2. 除实际工程错误外，继续完成用户原授权的16K阶段：继承Cosh8K checkpoint-300的adapter/Adam，固定tau1，真实16K200步，保持全层QKVO+FFN及同训练目标，使用新16K池并尽量保持8K阶段来源配比。4K NIAH0不是禁止该阶段的新增门禁；该阶段是否有效由结果决定，不由当前假说保证。
3. 用同一4K/8K/16K简单检索、自然LM和简短QA观察变化；已有可比结果直接复用，32K/大面板随实际收益再扩。若有任务收益则保留及解释代价；若仍全部复读，停止当前具体配方的机械续训，由研究任务读回结果选择下一实质改动，不自动转专项监督或无限扫参数。

Sol唯一调度GPU，研究任务每30分钟做决策复核，常规问题自行解决，不等用户醒来。没有新证据不宣布已定位唯一根因。新的研究假说不能触发无边界计算；当前这条链完成后可继续已明确且未重复的非LoRA任务，同时研究任务选择后续方案。

**随后新增明确例外：** 用户授权8K 200个optimizer更新→保存/评测→继承权重16K 200更新→评测，tau分别sqrt(2)/1。该项必须读取已READY的`v2_data_full/manifest.json`扩充池（8K自然SFT3914条、16K4654条、短SFT165968条），禁止用R0仅70条16K的旧包。只复用R0模块/loss框架，不复用其小池；不是自动启动完整v2或参数扫描。具体见[最新LoRA决定](OLMO_LORA_V2_20260912.md)。Sol统一调度，已有健康非LoRAarm先收尾。

2026-09-12用户要求先把GPU投入其他实验，不逐项消耗GPU调tau、FFN、数据配比或监督。该阶段追加的E3强基线现已完成，不重复运行：同980条、七任务等权official，16K C42V24=50.0786%、BM=35.7071%、C42=35.2857%、MrPro=7.4857%；C42V24减BM为+14.3714pp，任务内配对bootstrap 95%区间[11.8784,16.8357]pp。4K四者分别68.8036%、66.6964%、71.9286%、30.3750%；C42V24相对C42的短端代价继续报告。新两臂耗时2003.5秒；四臂汇总owner在服务器`/root/autodl-tmp/rope_fast_5090_20260912/e3_strong_baselines_run_20260913/four_arm_summary.json`，研究任务已读COMPLETE汇总。这支持C42V24在该面板有实际竞争力，不能转称CoshDeploy-LoRA的收益。后续LoRA按上方最新已授权决定执行，Sol唯一调度GPU；旧暂停记录与旧入口不覆盖最新安排。

## 跟进原则

- 先报告当前arm、有效tokens/行数、最新checkpoint/输出和实测剩余时间。
- 只做与实际问题相关的检查；技术错误修复后继续，结果阴性完整保留。
- LoRA的32Mi是共同首段、8Mi中点同一schedule；每臂按8K/8K/16K轮转3072updates，两臂共6144。128Mi延续另行决定，不能无声拉长预算。
- E2/E3总7,406次生成，G1只有20次backbone forward；原计划E5crossing/MLA缺资产时不让本机空等。
- 原始数据齐全不等于已处理面板或模型运行已通过；下载完成、分词完成、训练完成各如实写一句即可。
- 工时与数据实际下载结果见准备记录，首批暂留1–3天，开卡后以真实速度更新。

可直接给新session的首条消息：

> 当前5090为westd:24904，用户保证克隆资产，不做SHA校验；新启动使用prepared_code_no_sha_20260912，保留当前健康进程。在现有hybrid-rope工作区读取 docs/research/next_stage_20260912/5090_SOL_HANDOFF_20260912.md 并接管。OLMo优先，E2/E3/G1都放5090，不等4080。原始数据已授权下载到服务器；无卡仅0.5核，本机不做重活。先读下载回执和最新源码；有卡后短probe通过就执行，遇到问题修复继续，不扩大检查流程。保留模型权重，不动4080，不启动Llama。

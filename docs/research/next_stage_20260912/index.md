# 下一阶段：从分配自由度到可学习、可解释的设计

更新：2026-09-13。**按科学价值和判别力安排，时间与机器资源由作者协调。** 本文件是主代理综合计划，吸收理论、实验、论文三个独立子代理的审查；计划与已完成结果严格分开，当前执行回执见“状态与依赖”。零训练区间选表的候选与判别实验合同见 [CODEX_INTERVAL_PROMPT_20260912.md](CODEX_INTERVAL_PROMPT_20260912.md)。

## 当前最高优先级：单固定表的模型条件化区间优化（2026-09-13）

作者已锁定问题：给定Native长度L和horizon H=sL，整个会话、全部层只能安装一张
固定Native-relative RoPE表；设计倍率S与实际运行倍率r分开，不允许按输入长度
动态换表。目标按Native保留、固定强基线endpoint floor、内部worst regret和
log-length AUC共同描述，不能用AUC掩盖中段深坑。

[4080S固定表结果与下一轮求解合同](4080_FIXED_TABLE_RANGE_RESULT_20260913.md)
已完成Llama g8的8/16/32/48/64K小面板、CPU OOD/SEP Pareto审计和磁盘整理。
[CPU理论分层测量](CPU_THEORY_LAYERS_RESULT_20260913.md)
给三层理论（几何/engagement/位置结构）配上零GPU仪器：权重级engagement跨模型
r=0.970、位置结构模型特异≈0、gamma3-vs-BM分岔日程预测48K平/64K崩成立、暴露
解释崩塌方向但不排序分数。
[跨模型判决与无固定边界求解流水线](FIXED_TABLE_CROSS_MODEL_PIPELINE_20260913.md)
记录逐实验目的、成功/失败价值、baseline复用、里程碑与当前服务器下一跳；最终
搜索空间不固定YaRN的`l/h`，band-remap只作机制控制。
[Winding-Matched RoPE作者方案](WINDING_MATCHED_ROPE_AUTHOR_PROPOSAL_20260913.md)
记录作者于2026-09-13提出的整数绕圈闭式全频表；Codex只负责审计、实现和实验，
CPU/GPU完成前不把精确算术性质升级为任务有效性结论。
[Band位置最小筛选](BAND_POSITION_MINIMAL_SCREEN_20260913.md)
用S=2/S=4的PPL与passkey/NIAH直接放开low/high：Llama S=4小面板当前最佳为
`[16,34]`，但原样迁入OLMo后失败；OLMo局部最佳basin为low=14、high=31--32。
该结果把band锁定为checkpoint/倍率条件化变量，并给出Qwen四臂最小统一判决。
当前三任务开发面板上，BM频率配区间均值固定gain把最弱点从72.92%提高到75.00%、
最大regret从10.42pp降到8.33pp，但AUC略低BM；只作为后续确认候选。
gamma3和BM/MrPro表空间中点均失败。严格OOD-max+SEP-min出现22个几何支配/任务
反转，停止从纯几何Pareto继续求表。

冻结OLMo、固定S=4及band[14,32]的首轮模型条件化求解已经完成。BM初始化未接受
局部步；C42V24初始化得到一个同时改善全fit区间regret、端点、source margin和
Native项的SolverC42候选。独立select/confirm没有单一胜者；350行七任务16K面板
上候选比C42高6.71pp、比C42V24低4.01pp，当前分类为Pareto。391行自然QA复核
与BM基本持平（+0.17pp），比MrPro高3.65pp，但任务间有正负重分配且该面板不是
独立确认。当前分类仍为Pareto；下一步拆分allocation与gain贡献。完整Hessian/
QCQP、Llama64K反向和random-simplex没有进入首轮；旧强基线在每个benchmark只
运行一次并永久复用。

候选判定不使用单一硬阈值提前筛除：协议/实现错误与任务层明确支配分开；开发集
不达线、代理冲突、小样本单任务损伤只标记为Pareto或未决。静态候选表继续进入
独立生成、宽任务和自然QA复核，避免把真实有用但收益分布不同的方案误删。

最新Llama跨模型结果：OLMo full-profile按Llama Native频率与S=8重建后，
8/16/32/48/64K为95.83/97.92/100/95.83/85.42，五点AUC96.91；但将内部
shape机械重映射到Llama YaRN band `[18,35]` 后64K降到45.83。前者进入八任务
64K复核；后者作为反例保留。最终求解不固定`l/h`，改为全64槽单调allocation。

本轮5090收尾结果与停止决定见[OLMo BM适配与Llama 8B倍率交叉报告](5090_FINAL_EXPERIMENT_REPORT_20260912.md)：
BM在4×目标和g8前半段强，MrRoPE-Pro在g8的4×/8×合成检索小幅领先，自然
任务排序不同；作者决定完成最后诊断后停止堆实验，转向能预测该交叉的理论突破。

## 最新执行顺序：零训练选表，再微调（2026-09-12）

作者在阅读Pro的Beta Bridge方案后明确调整顺序：先回到零训练，找出在
1–sL整体更好的方法，再用所选方法微调。已有BM及adapter作为可复用资产保留；
当前健康作业正常完成，下一阶段算力用于零训练比较，不是停工或取消结果。
不把附件中的“先通过原生非劣、再通过端点非劣”变成门槛，允许原生段合理损失。

本轮采用一个有明确变量含义的小比较：
`epsilon_q ∝ [q(N+1-q)]^(gamma-1)`，累加为m，再按Native*s^(-m)部署。
先补gamma=1.5与3两张候选；gamma=1是Uni、gamma=2是BM，复用现有基线。
原频带、scale和gain先沿用当前设置以看清集中度效果，这是本次比较选择，
不是禁止联合优化其他量。OLMo N18时该对称族均有sum(m)=40.5、增量中心q=9.5；
改变集中度不会像旧b轴那样同时移动中心和总减速量。

已有实现为`experiments/curvature_20260910/tables.py:m_incr_beta`，传入
`shape_a=gamma-1,b=gamma-1`即可；旧b扫描主要固定shape_a=1，不能把其结果
当成新对称方向的完整测试，也不能把已有Beta参数化宣传为本轮新发明。
有限离散gamma=3只是趋向连续五次smootherstep，不能混用连续采样表替换离散表。
BM基线沿用实际已部署FP32数组，不用近似连续smoothstep重新生成身份不同的基线。

Sol负责将两候选接入已有1–sL面板并执行，复用Native/Uni/MrPro/BM/C42V24
已有可比结果，不重新建立大矩阵。按逐长度及逐任务表现、短端代价、区间整体
表现选择当前最有用方案，再用于后续微调；没有单项分数的通过/晋级规则。
主区间包含原生短段，不能套附件只覆盖L–sL的目标。极远端只作已有压力结果参考。

附件中的Fisher加权建议与前两天的已测失败相重叠：到Native分布的距离不等于
任务损伤，见[既有记录](../../../ds_workspace/recon_20260910/theory/WHY_THE_FISHER_ROUTE_DIED_20260911.md)。
本轮先做上述可复用的对称方向，不额外开启Fisher探针/优化器。相位暴露阈值、
曲率最优与8K几何中点均是设计解释，不替代实际任务结果。

## 最新作者目标：现代Agent的1–sL全范围能力（2026-09-12）

**以本节作为5090后续工作的目标；不把下文历史优先级当成当前调度。**
**作者最新纠正：研究代理不得新增实验准入或晋级门槛。** 以下长度曲线、
AUC、最弱点、原生保持和任务分数用于描述结果，不作为继续训练、开展后续
评测或执行已授权任务的通过条件。按作者在执行任务中明确的顺序继续，
不因研究代理的单次判读自动停训、取消队列或换路线。
作者明确：z的实质价值已有证据，下一重点是从最短可用上下文一直到目标sL，
包含原生窗口内的可靠表现，而非只在L–sL取点，更不是追求100倍外推的单点记录。
L内需要保障但允许小幅损失，报告损失的长度、任务和大小；不设逐格零退化门槛。
当前OLMo以L=4096、s=4、sL=16384为主区间。概念上的1–sL不要求构造
1-token问答；各任务从能容纳有效输入的最短长度开始。

固定端点是为进一步区分指数整体范围(a,R)与内部位置z的控制手段，不是实用
方法必须满足的条件。固定base也不自动固定频率端点，因为指数首末值仍可变。
允许范围、gain、架构与分配联合设计以取得更好的系统结果；如改变了多个因素，
按组合方法报告，已有控制证据承担z独立作用的识别，不要求每次都重新做全套消融。
当前优先使用已有BM及adapter，不能把这个许可变成无依据的架构/大模型训练扩张。

### 5090的具体后续安排

1. Sol保持唯一GPU调度。当前BM-8K200的完整E3/E2及用户此前安排的32K/64K
   小面板正常收尾；后两者作为主区间外压力测试，不自动扩展远端大面板。
2. 下一项优先补已有BM frozen与BM-8K200的全范围曲线：短自然指令按实际长度；
   LM/可缩放任务以512/1K/2K/4K/6K/8K/10K/12K/14K/16K为覆盖点。
   已有同合同结果直接复用；不为点数重新跑已完成面板。RULER先用single_1、
   multikey_1、multivalue三类，每类每长度16例、分布在不同深度。跨长度保持
   核心问题与答案、主要改变背景长度；各模型在同长度使用完全相同的输入。
   模板放不下的短长度不伪造任务，短指令承担最短端能力评价。
3. 每个任务分别给逐长度值，原生段与外推段均值、最弱已测长度、相对对应基线
   的差值。log-length梯形AUC只作所声明有效取样范围的摘要；不能从1 token
   任意积分后当唯一排名，更不能用AUC掩盖中段深坑。NLL、official任务分数、
   规范化完整答案+EOS、截断率分别报告，不把异类量硬合成一个总分。
4. 已有8K与后续16K checkpoint均保留，以整条曲线记录收益和损失。
   16K训练及其他后续工作按作者在执行任务中明确的顺序推进，不由本节的
   任何分数决定是否“晋级”。当前BM阶段比较沿用同表/gain；作者另有设计时
   按其指令执行并记录变化，不新增审批环节。
5. Agent相关能力优先关注长历史中的当前状态更新、跨工具回执取证、早期约束
   保留及最终任务完成。先查可复用的历史/来源面板，设计小规模可判定任务，
   不另造重型框架。单针和静态QA是组成能力证据，不冒称完整多步Agent成功。
6. 先复用已有冻结强基线；需要同预算微调比较时优先选一个强对照，而不是把
   附件Native/YaRN/Uni/Pro/BM五臂全部训练当成硬要求。完整曲线上的系统收益
   和分配的独立归因可以分别建立，不让形式对称吞掉主要算力。

附件公式判读：离散BM有效首末epsilon并非零，N18时均为6/(19*20)=0.015789；
零端点属于外侧边界值或连续极限。`r*s^(-m)`是相位暴露比例的恒等式，
`r*=s^m`不是槽位任务能力保证。8K为4K–16K的几何中点，构成设计动机，
不能据此宣布8K是最佳训练长度或BM已经全范围最优。

## 夜间结果后的主线复核（2026-09-12，历史分析）

**本节更新科学优先级；下文初始计划保留作依据，不代表全部仍应执行。** 本轮依据是执行任务“5090实验负责agent-5.6sol”（任务ID `01a0958c-2936-7332-99d9-ea27cde4b42e`）的已完成结果回报、[LoRA机制owner](LORA_MECHANISM_AND_RECIPE_20260912.md)与当前论文源码。本轮未重新读取服务器原始行；下列新增数字属于执行报告依据，不能直接升级为已完成的论文数据核验。4080S近期任务读取未返回正文，本地S1索引仍停留在运行阶段，因此暂不判定scratch结果，也不把旧“尚无端点”当成实时状态。

**作者后续纠正：** 昨夜训练本身可能存在问题，当前已确认的是这些运行尚未达到EVQ LoRA的适配目标。先前“针对性实现复核未发现不对称”只是执行任务的报告，不能代替训练合同、损失/监督、预算与评价的独立审查，也不足以排除训练问题。以下负数值保留为运行观察；把它们归因于Cosh构造本身的判断暂缓，待训练有效性核对后再决定。

### 需要改变的研究目标

当前全局anchored Cosh适配的连续负结果，削弱了“解析构造自然转成成熟模型长上下文收益”这条桥梁。不能继续把主要预算用于解释或修复Cosh，也不能只把标题改成allocation后保留原先的方法成功叙事。中心问题应改为：**有限频率的内部组织，怎样同时影响位置表达、学得的使用方式与部署后的上下文能力；哪些可检验的结构选择确实有用？** Cosh是其中一个有明确数学先验的构造；BM/C42承担成熟模型中的另一组设计证据。两者的关系要由实验建立，不能由名字或统一坐标代替。

### 当前证据改变了什么

| 证据（执行报告依据） | 对主线的影响 | 不应推出的结论 |
|---|---|---|
| R0匹配32Mi：Cosh τ2相对Native，4/8/16/32K NLL差约+0.710/+0.514/+0.312/+0.123，每档24篇均更差；任务记录称针对性实现复核未发现不对称 | 当前成熟模型Cosh适配不是可以默认成立的正收益支柱 | 不把这一配方外推为scratch、所有强度或所有分配都无效 |
| anchored两阶段与渐进安装后，4/8/16K单针仍全部0/16；匹配Native300的4K为16/16 | 不再以“模型太小”“只有EOS严格”或“只需渐进安装”解释已测失败；停止围绕这些已测配方机械续训的科学建议 | 长阶段与Native300预算/长度暴露不同，长端NLL差不能当匹配方法优势 |
| CoshDeploy经过8K200→16K200，4/8K规范化完整答案+EOS保持100%，16K仍0；16K NLL约3.619 | 保留真实8K收益及16K能力边界；明确PPL与精确检索分离 | Deploy同时改变support、profile和gain，不能声称单独识别了减速或兼容性机制 |
| E3：每臂700条16K、280条4K，16K任务等权official分数C42V24/C42/BM/MrPro为50.08/35.29/35.71/7.49% | 将同总位移、同质心的形状对照及强基线结果提升为优先核验、整合的主文候选；优先级高于继续修复Cosh | 不混用旧350条开发面板分数，不把单模型七任务子集称完整通用benchmark |
| 631条长输入自然QA：C42V24/BM/MrPro为25.80/25.33/21.94%；C42V24对BM的+0.47pp区间跨0，对MrPro的+3.86pp簇区间[1.26,6.44]pp | 支持成熟相对分配的实际用途，尚未证明C42V24在自然QA优于BM；新复评与旧25.44/21.62单独记账 | 旧输入池再次评价不是全新独立测试，不能只取较高版本覆盖旧数 |
| C42V24来源反事实pair-follow为4K1.56%、16K0%，执行回报称主要存在绑定错误 | 撤去“强检索/QA已经验证来源绑定机制”的预期；这是候选机制解释的实际缺口 | 不以高QA分抵消反事实失败，也不把更难任务的地板解释为全部QA收益无效 |

### 优先级重排与论文影响

1. **先完成已有结果到主张的核验。** 整理E3四臂逐任务结果、配对区间、共同gain/端点/范围、开发与确认样本身份；将C42形状对照与BM自然QA构成“受控变化→任务后果”的主证据。先复用已有输出，不为归档重跑。不能因为C42V24分数最高就预设它是通用最终方法。
2. **S1仍有独立的识别价值，但任务是判别学习选择，不是证明Cosh。** 先取得当前Geo/Cosh/full-z共同端点与完整长度曲线；若Cosh败而full-z胜，强化可学习分配；若两者均不优于Geo，则应承认该真实2K/训练预算下尚无设计收益，重新衡量旧短窗、MLA及续训证据可承载的概括。保留已约定配对重复的意义；本节不修改其他任务队列。
3. **理论服务可验证差异。** full-pair几何说明提供了什么；crossing/slot说明权重与表的关系；Cosh定理给出指定先验的解。三者目前没有共同证明“更好几何必然带来更好任务”。优先在成功与失败的成熟表之间提出能预测排序或反转的结构解释，并用同约束反例检验。保持高频、相位扰动、内容Q/K均是待检验解释，不宣称已经识别唯一机制。
4. **BM或其他有效部署表的适配是实用收益候选，不是第二轮护航。** 先使用已有冻结结果和已授权实验；若主张训练增益，比较同表训练前后；若主张优于其他方法，必须有相同长度暴露、token预算和监督的对照。Native300不能自动成为任意8K200→16K200候选的同预算基线。训练臂由执行任务按用户最新安排管理，本节不增加或恢复训练。

论文下一版应从“识别内部结构→区分学习前设计与已有权重下调整→用受控成功和失败解释设计选择”推进。Cosh定理及历史正结果保留，但不再独占构造价值与机制解释。**风险没有因改叙事自动解除：** 如果S1在实际窗口缺乏收益、成熟方法也没有足够明确的新设计原则或可迁移收益，仅有“allocation matters”与若干负结果仍不足以支撑原先期望的贡献力度。当前先重排证据和研究决定，不用尚未核验的执行回报直接改论文数字。

## 先读结论

LoRA后续决策见[历史失败的机制分析与小步配方](LORA_MECHANISM_AND_RECIPE_20260912.md)：区分PPL与读出、换表适配成本、真实长依赖、短LM保留与模型能力；补充8K/16K来源比例漂移，按1.485B基线的相对收益判断，不增加全胜门禁。

当前论文已建立：内部指数分配组织有限位置基，权重学得使用它，具体构造能产生实际收益。下一阶段最值得增加的认识是：

> **在相同频率支持内，解析先验和训练损失分别会选择怎样的位置基？模型怎样学得使用这种分配，哪些结构能够迁移到新的长度和任务？**

保留现有论文标题和三段主线，推进“可识别的变量 → 可学习的设计对象 → 可检验的使用机制”。资源优先投入完整配对、充分训练、机制干预和独立验证；不因假定截止压力退回短smoke，也不因机器多堆曲线和benchmark。

## 三份独立报告与取舍

| 方向 | 报告 | 主要发现 | 综合采用 |
|---|---|---|---|
| 理论 | [theory.md](theory.md) | full-pair方向与raw系数代价不同；有限相位改变可精确重放来源odds；锚定密度和有限目标可补清；建议Geo/Cosh初始化×冻结/可学 | 模型机制复用主训练；先给准确对象，再用真实干预检验；锚定等式主要服务附录，不凭公式数量增加贡献 |
| 实验 | [experiments.md](experiments.md) | full-z代码及成熟联合学习已存在；缺scratch匹配比较；MLA原始评价JSON本地hash相符；C2/C42和gain需分清确认对象 | 主训练优先实际2K、两个support、三seed、共同成熟度轨迹；记录追回和MLA独立复评单列 |
| 论文 | [paper.md](paper.md) | 几何→设计、Cosh↔BM、C2↔C42关系还可讲得更充分；EOS可见度不足；实际窗口严格识别有桥接价值 | 立即加强已有成果的三处连接；4K作为明确窗口因素扩展，C42独立确认优先于再搜索形状 |

三方的不同建议有信息：论文审查倾向2K/4K窗口矩阵，实验审查倾向2K的support矩阵，理论审查希望补Cosh-init learned臂。综合选择如下：**先把训练目标与support依赖分辨清楚；窗口和初始化分别作为明确扩展，不一次混成一个“更多实验”表。** 这些比较可以在多机器上并行；统计解释和协议仍保持分组。

实现线索以实验报告的逐文件核查为准：`scripts/lib/rope/fixed_support_z.py`是完整参数化；`knot_allocation.py`只有少量knots；`phase11b_125m_dape.py`含Kerple/MLP改算子路径，不能因为名称或论文报告中的指针就复用为所需full-z基线。旧learnedfreq、learnable-tau、成熟z oracle和新scratch合同分别登记。

## S1 主实验：固定支持下的解析分配与直接学习

### 科学问题

A01已证明只改内点有作用，A02表明多个形状有价值。新实验要回答：标准短窗语言建模目标会把内点学到哪里；它与解析Cosh相比在训练窗口、长端及训练成熟度上有什么差异。重点是学习选择和长度行为的关系，不能只以learned是否击败Cosh判成败。

### 推荐主合同（待实施前锁定）

| 因素 | 推荐设计 | 判别作用 |
|---|---|---|
| 模型 | 151.9M量级、K32，实际连续L=2048 | 桥接256严格识别与实用学习窗口；不注入虚拟长位置 |
| 表 | fixed Geo、fixed anchored Cosh、Geo-init full-z与全部权重联合学习 | 解析先验与目标驱动学习；共享跨层/头的K−2自由度 |
| support | 预指定B=2048与B=500000两个block；每block内最高/最低频率、log-span、K完全一致 | 分辨接近训练窗的较窄支持与慢端更长的支持依赖；两个block之间不声称纯z |
| Cosh强度 | 初始建议tau=64/sqrt(2048)=sqrt(2)，两support相同，开封OOD前锁定 | 固定参考构造，不用每个block事后最优tau |
| 重复 | seeds42/137/256；同seed×support的初始权重、文档顺序、优化器、全局token batch、预算配对 | 训练seed是独立重复；提示不能冒充seed |
| 成熟度 | 建议1B-token预定轨迹，保存共同500M与1B checkpoint；所有臂相同最终budget/schedule | 直接检验早期增益是否随学习持续，不能跨旧实验归因；1B不是收敛保证 |
| full-z实现 | 正gaps归一化累积、端点精确、次序固定；allocation LR/decay/数值保护预先锁定 | 不隐形继承旧oracle的±2投影；说明是否限制设计空间 |
| 评价 | 独立文档/pack，2/4/8/16K全序列NLL和末128 NLL，预指定OOD等权汇总及窗内代价 | 不选最佳长度；重复文本或共享训练stream不能冒充独立holdout |

两support×三方法×三seeds = **18条完整训练轨迹**；若锁定1B合同，为18B训练tokens。这里只登记可调度的科学工作量，不估未经实测的GPU小时。确切架构/head_dim、数据和训练版本需从恢复后的实现核对；若d_head不同，tau公式必须明示使用哪个量。

**运行准备有明确终点**：uniform初始化与固定Geo前向一致、端点/排序正确、allocation梯度有限非零且optimizer更新、sin/cos缓存保留计算图、实际token吞吐/显存可行。通过后进入完整训练，不反复以CPU小检查代替模型结果。共同训练预算和LR在OOD开封前确定，不能只延长落后臂或按第一个seed输赢挑超参。

### 资源增加时最值得扩展什么

1. **初始化与可学习性的2×2**：补Cosh-init full-z，与现有Geo/Cosh fixed以及Geo-init learned构成因子设计。它区分解析初始化、自由度与训练路径，比再加一条手工曲线更有内容。若在两个support/三seed全补，为6条额外轨迹；是否作为主合同一次锁定由希望回答的路径依赖问题决定，不等看到不利结果才补救。
2. **4K实际窗口**：在预指定B=500000 block复做三表×三seed，保持support和tau=sqrt(2)，评价4/8/16/32K，增加9条轨迹。检验窗口因素；若tau改为1，则明确检验随窗口变化的默认设计规则，不能同时声称只改变window。
3. **重复与独立数据**：效应区间仍同时容纳有用收益和实质伤害时，增加训练seed或独立文档。不要用更多模型替代不充分的配对重复。

扩展由科学问题决定，可利用多机同步推进；既不因“太晚”自动删掉有价值的对照，也不因某个seed阴性取消预先确定的比较。

### 不同结果增加什么知识

| 观测 | 可以推进的认识 | 还不能说什么 |
|---|---|---|
| learned同时改善窗内/长端 | 分配可由任务目标进一步优化 | 不是所有support/模型都存在同一最优表 |
| learned窗内好、长端弱于解析 | 学习目标与长度设计先验偏好不同 | 不能只凭此认定梯度机制或短窗loss是唯一原因 |
| Cosh与learned相近且区间够窄 | 简单解析构造有实际竞争力 | 点估计相近不等于统计等价 |
| 不同support偏好不同 | 分配设计需要联系供给尺度与运行范围 | 两support之间的总效果不是pure-z估计 |
| 500M优势到1B缩小/扩大 | 同合同下的训练成熟度关系 | 中途500M点不是旧500M终止schedule的严格复现 |
| 新设置Geo更好或差异小 | 确认已有结论的条件结构，保留完整结果 | 不扩大为内点无用或整个解析构造无效 |

## S2 机制主线：在同批模型中连接位置基与实际使用

机制工作与S1共享模型、数据身份和早/中/末checkpoint，避免另起一套玩具模型给论文主结果作解释。

1. **供给层**：同一表计算raw Gram、block-whitened overlap、去掉低阶公共方向后的剩余谱；固定uniform/causal-triangular等预定距离测度。它们回答方向数与系数尺度，不直接预测loss。
2. **学习层**：做weights×table crossing；fixed Geo/Cosh/learned三表与三个配对权重的3×3是冻结推理比较。保留完整矩阵和本表结果，避免只解释赢家；learned最终表由对应seed定义。
3. **使用层**：在实际能完成基础检索的同一模型/匹配读出协议上，固定内容、答案与解码，改变证据距离。记录pre-RoPE Q/K、精确有限相位logit变化及evidence/distractor odds，同时记录完整答案与终止。全局平移所有position IDs不能替代相对gap干预。
4. **因果确认**：独立样本上，使用预定的band/层组双向移除与恢复、sham和路径对照，检查任务收益是否随干预改变。选择head/层用独立开发集并锁定，不能按测试答案挑贡献；禁止把答案方向logit强推当“机制恢复”。

odds公式本身是标准softmax代数，价值来自模型中何种频率—内容关系确实承载收益。只有attention图或首token odds变好，完整输出没改善，不能宣称机制闭环。任务地板先修具体的读出/数据问题，不把地板当理论否定；若新匹配readout训练引入了新适配器，明确它是独立协议。

理论报告的三个可交付件分别处理：**系数预算下的可表达性**有机会连接学习，完成严格条件和实际系数检验；**锚定后的准确密度身份**提高方法清晰度，主要入附录；**有限Cosh目标**只有导出并验证有限K的新预测才升为主文贡献。不要为了理论数量增加一个无法检验的泛函。

## S3 既有核心结果的完整性与独立验证

这是与S1/S2并行的证据工作，不因“只是复评”而忽略其科学价值。

- **151M绝对四格**：定向追回原evaluation JSON、anchors和checkpoint，按现有raw hash/科学合同核对。找到checkpoint而缺JSON时可重评原anchors；没有原件就保留已确认配对差，不从差值解出四个绝对值。
- **MLA独立语料复评**：本地`results/eval_3seeds_full_results.json`的SHA已与curated owner对上，但这没有证明旧cache独立。若六个final checkpoint可用，锁定能证明未参与训练/选型的文档/pack、版本与token hash，复评8/16/32K，保留原生窗代价。先每seed聚合NLL再指数化；seed与文档不确定性分开。
- **原始生成归档**：沿EOS/QK/C2回执找token IDs、EOS/cap、表与适配器身份。可达性、哈希核验和任务重跑是不同状态，不假装compact receipt含全部原始生成。

资源重点是原件、checkpoint和独立长文档，不默认重训所有历史模型。旧实验目录的服务器状态本轮未查询。

## S4 成熟设计结构：两个互补问题分别确认

**C42与C2不是竞争结论。** C42说明同一总量未涵盖全部形状信息；C2说明一个有效表仍可以有紧凑描述。下一阶段分别检验它们在开发面板外是否稳定。

| 比较 | 问题与必要控制 | 结果如何进入论文 |
|---|---|---|
| C42/C42V24独立生成 | 锁原两构造、同support/band/Σm/质心/实际gain/decoder；新提示与旧350选择面板隔离；同任务分布先确认，任务分项和EOS完整报告 | 方向稳定则把同预算结构从开发发现推进为独立确认；反转则报告新结果并限制该具体预测。step42历史反转不是此对照的负复现 |
| C2固定规则任务迁移 | 固定两参数、64点参照表、gain和transport，Qwen不重拟合；独立检索与自然QA，原生32K及64/128K长端分列 | 检验紧凑描述能否保留新的长任务用途，同时报告native代价；不以重新调参数挽救失败 |
| BM/MR×gain完整析因 | untouched同一面板四格；单位gain与登记YaRN gain；Native/officialYaRN作为有用参照，各自身份固定 | 逐提示difference-in-differences识别表与gain交互；350/180面板不拼接。Qwen Native/Index旧代码不是BM/MR对照 |

资源足够时可并行，但各项各自锁定问题和判据。C42优先服务“总量之外的形状结构”，C2优先服务“可迁移的紧凑设计”，gain服务“部署条件的交互”；哪项成为正文重点由完成后的实际证据决定。无信息地板、宽区间和实质负结果分别处理，不重开任意curve/gain搜索。

## 现在就能改善的论文连接

- §3→§4：已有满秩parity表的远距复现、静态排序随窗口变化和crossing共同说明“方向数量、尺度覆盖、学得使用”不同，几何不只是一幅低频冗余图。
- §5→§6：学习前设计供模型学习的位置基，冻结后在已学坐标关系上调整目标尺度；这是Cosh与BM的关系，不需要新造包办一切的目标函数。
- 生成证据：把8B来源使用、OLMo完整答案+EOS、selective-QK自然QA并列为不同协议对连续科学问题的回答，提升EOS可见度，不画成同一adapter的因果修复链。
- C2与C42：明确“可有紧凑描述”与“一个总量不够”共同说明设计结构；完整FullLagP2为另一几何构造实例提供指针。

本轮只形成计划与证据整理，不改动已经验收的TeX/PDF。未来写作按新完成结果更新，既有正结果不因新方向尚未完成而降级成半成品。

## 状态与依赖

5090 新 session 的用户范围、原计划完整责任表和轻量准备回执见
[5090 OLMo 恢复与计划准备](5090_recovery_preparation.md)。新增[LoRA v2与公开数据扩容](OLMO_LORA_V2_20260912.md)，依据[Pro专项方案](EVQ_LORA_PRO_GUIDANCE_20260912.md)与[历史简报](LORA_HISTORY_BRIEF_20260912.md)。OLMo LoRA 为新增 Top1；
当前5090入口为`westd:24904`；可信克隆不做SHA门禁，后续端口以用户最新消息为准。
不重复本页已登记的 4080 S1 链。Llama 后置，0.5 核无卡实例及本机都不运行重准备；用户已授权原始数据提前下载到服务器。OLMo长训练采用实际8K/16K混合，4K内样本仅用于能力保留。

| 层 | 当前完成 | 后续交付 |
|---|---|---|
| 资产 | A01–A27来源/作用/可达性已登记；新增A28成熟z学习历史 | 原件回收和匹配新合同 |
| 研究判断 | 三个独立子代理报告、主代理综合与用户资源纠正 | 明确实际机器、数据/权重可达性后形成运行合同 |
| 新理论 | 提议和CPU代数核验（见theory报告） | 完整证明及机制验证，尚非新论文实验结论 |
| 新模型实验 | S1首个`B=500000, seed=42`三臂配对block已启动；尚无科学端点 | 完成Geo/Cosh/full-z共同500M/1B checkpoint与独立评价；首block不能冒充三seed结论 |

### S1当前执行回执（2026-09-12）

远端配对预检已确认Geo/Cosh/full-z的151,898,880个模型权重初始化SHA完全一致；full-z额外31个gap logits、30个有效自由度。十个update的丢弃式资格检查通过：实际L=2048、Flash-only BF16，allocation梯度每步有限非零，首步参数确实更新，峰值CUDA显存11,791,566,336 bytes，去除编译步后的吞吐约69,055 tokens/s。随后服务器监督器已自动启动Geo正式臂，实测稳态约74K tokens/s。

当前1B轨迹明确由冻结的499,974,144-token前缀训练两epoch组成，每epoch使用独立但三臂共享的确定性排列；因此记为999,948,288个训练tokens，不写成10亿unique tokens。共同500M和1B checkpoint均保存。首block完成前没有S1质量结论，后续support与seed不得按该单seed的OOD输赢选择。

服务器串行链已预排到`B=500000, seed=137`：seed42三臂完成后先对500M/1B两个共同checkpoint做独立2/4/8/16K评价，再运行seed137三臂并做同合同评价。该重复在读取seed42的OOD胜负前锁定；评价器工程失败不会取消已经声明的第二训练seed，但首个训练block不完整会阻止下游。明日换机前不自动扩到350M或大模型。

时间由作者把控。本计划不以D6/D14自动砍实验或降低标准。为避免遗忘提交事项，仅保留官方日期参考：摘要2026-09-18 23:59 AoE，全文2026-09-25 23:59 AoE；确切政策见[ICLR 2027官方作者指南](https://iclr.cc/Conferences/2027/AuthorGuidelines)（2026-09-12核对）。摘要先用已确认贡献，未完成结果不预写。

## Pro领域分析的补充吸收

[2026-09-12完善记录](../../../paper-2027/research/PRO_FIELD_MAP_REFINEMENT_20260912.md)增强supplied basis与learned usage的区分。S1继续作为主训练；S2的usage/相位测量必须与任务干预连接；S3/S4仍有独立科学价值，不将learnable-z称作唯一有用的新实验。新增M4工作点图是已有结果复算，不是新训练，也不证明连续Pareto前沿。Agent-range保持独立研究身份，当前不加为第四条论文主线；这不否定另行授权的工作。

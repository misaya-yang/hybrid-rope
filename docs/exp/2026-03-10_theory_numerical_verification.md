# 理论数值验证报告（M4 Max 本机）

> **日期**: 2026-03-10
> **状态**: PENDING — 需人工分析确认
> **脚本**: `scripts/m4_max_36gb/theory_numerical_verification.py`
> **结果**: `results/m4_max_36gb/theory_verification_results.json`
> **环境**: M4 Max, CPU only, numpy + scipy, 运行时间 < 10s

---

## 0. 目的

对 EVQ-Cosh 的 6 条核心理论命题做纯数值验证，产出直接用于论文 appendix / rebuttal 素材。不涉及模型训练。

---

## 1. Test 1: Theorem 2 — Geometric Recovery (τ→0)

**命题**: φ_k(τ) → u_k (Geometric) as τ→0, 收敛阶 O(τ²)

**结果**: ✅ PASS

| τ | max\|φ_evq - φ_geo\| | 收敛倍率 |
|---|---|---|
| 1e-1 | 6.41e-04 | — |
| 1e-2 | 6.41e-06 | ×100 |
| 1e-3 | 6.41e-08 | ×100 |
| 1e-4 | 6.41e-10 | ×100 |
| 1e-6 | 6.42e-14 | ×10000 |
| 1e-8 | 2.22e-16 | (机器精度) |

- O(τ²) 收敛完美：每减 10× τ，误差减 100×
- Taylor 二阶展开预测 vs 实际误差 ratio = 0.999
- **论文可写**: "The degenerate limit is verified to machine precision with the predicted O(τ²) convergence rate."

---

## 2. Test 2: CDF 反演精度

**命题**: φ_k(τ) = 1 - (1/τ)arcsinh((1-u_k)sinhτ) 是纯 tether ODE 的精确 CDF 反演

### Part A: 纯 tether (γ=0) — 精确解验证

**结果**: ✅ EXACT (全部 τ 值误差 ≤ 1e-15)

| τ | max\|F(φ_k) - u_k\| |
|---|---|
| 0.5 - 8.0 (全部) | ≤ 1.11e-15 |

CDF 反演公式在纯 tether 模型下**精确到机器精度**。

### Part B: 含 Fisher 项 (γ≠0, base=500K)

| τ | max\|φ_cdf - φ_ode\| | mean\|Δ\| |
|---|---|---|
| 0.50 | 0.092 | 0.060 |
| 1.00 | **0.001** | 0.001 |
| 1.50 | 0.138 | 0.086 |
| 2.00 | 0.285 | 0.179 |
| 2.83 | 0.480 | 0.326 |
| 4.00 | 0.639 | 0.486 |

**⚠️ 需分析**:
- τ=1.0 时偏差极小 (0.001)，但 τ=0.5 和 τ≥1.5 偏差较大
- τ=1.0 的异常低值可能是 Fisher 项和 tether 项恰好抵消的巧合
- Fisher 项贡献 O(γ/τ²) 修正 — 但 γ = -2lnb ≈ -26.2 (base=500K)，所以修正并不小
- **疑问**: 论文的推导链中，CDF 公式是从完整 ODE（含 Fisher）导出的还是仅从 tether 导出的？如果只是 tether 导出，大偏差是预期的；如果声称是完整 ODE 的解，则需要解释为何偏差大
- **TODO**: 确认 CORE_THEORY.md 中推导链第 5→6 步的精确假设

---

## 3. Test 3: Broadband 近似 R²

**命题**: K ≈ αδ(φ₁-φ₂) + βmin(φ₁,φ₂), R²_mid > 0.99

**结果**: ⚠️ R² 低于论文 claim，但可能是先验不同

### 数据 (D(Δ) = Δ^{-1.5} 合成 power-law)

| base | L | R²_full | R²_mid |
|---|---|---|---|
| 10K | 256 | 0.795 | 0.716 |
| 10K | 2048 | 0.811 | 0.736 |
| 100K | 2048 | 0.743 | 0.587 |
| 500K | 256 | 0.662 | 0.350 |
| 500K | 2048 | 0.697 | 0.454 |
| 10M | 2048 | 0.619 | 0.139 |
| 10M | 256 | 0.580 | **-0.050** |

**⚠️ 关键问题**:

1. **R²_mid 最高 0.74，远低于论文的 ">0.99"**
   - 论文的 R²_mid > 0.99 是否使用了不同的距离先验 D(Δ)？
   - 合成 power-law Δ^{-1.5} 可能不是论文中使用的先验
   - **TODO**: 用 `learnable_evq.py` 的 `measure_distance_distribution()` 从真实 FineWeb-Edu 数据测量 D(Δ)，然后重跑

2. **α fitting 全部为负值（被 clamp 到 1e-10）**
   - 导致 τ_fit = √(β/α) 炸到 ~10 万，完全不可用
   - 两步拟合方法（先 off-diagonal → β，再 diagonal → α）在合成先验下可能不稳定
   - **TODO**: 对齐 `learnable_evq.py` 中 `estimate_tau_from_distance_prior()` 的实现，用同样的拟合方法重做

3. **R² 随 base 递减**: base 越大 R² 越低
   - 物理解释: base 越大 → 频率更分散 → 核矩阵结构更复杂 → 二参数近似更差？
   - 还是 base 越大时 δ 脊宽度相对更大？

4. **β 值稳定在 0.85-1.05**，与论文的 "典型 β≈1/2" 不同
   - 可能是先验 D(Δ) 不同导致

### 结论

Test 3 目前**不能直接用于论文**。需要:
- [ ] 用真实数据的 D(Δ) 重测
- [ ] 对齐 Algorithm 1 的拟合方法
- [ ] 搞清楚论文中 R²_mid > 0.99 的精确测量条件

---

## 4. Test 4: Waterbed 不等式

**命题**: ∫ln E(φ)dφ ≥ lnb - lnc，等号当且仅当 ρ≡1 (Geometric)

**结果**: ✅ PASS (40/40 全部满足)

| base | τ=0 (Geo) | τ=1.5 | τ=2.83 | τ=4.0 |
|---|---|---|---|---|
| 10K | 9.210 (=lnb) | 7.763 | 5.563 | 3.994 |
| 500K | 13.122 (=lnb) | 11.076 | 8.019 | 5.878 |
| 10M | 16.118 (=lnb) | 13.612 | 9.900 | 7.320 |

- τ=0 时 ∫ln E = lnb **精确**（Jensen 等号成立，spacing entropy = 0）
- τ 增大 → ∫ln E 单调下降（水床体积减小 = 分配更均匀）
- D_KL(Uniform ∥ ρ) 随 τ 单调增大（偏离 Geometric 越远）
- **论文可写**: "The waterbed inequality is verified for all 40 (base, τ) configurations, with exact equality at τ=0 confirming the Geometric degenerate point."

**⚠️ 待确认**: 水床的 ∫ln E 随 τ **下降**，意味着 EVQ 的全局对数误差体积**更小**而非更大。这与 CORE_THEORY §7 "EVQ 使误差趋于均匀但总体积增大" 似乎矛盾。需要仔细核对定义：是 ∫ln E 还是 exp(∫ln E) 作为"体积"度量？

---

## 5. Test 5: 碰撞块分析

**命题**: 碰撞通道 k 满足 λ_k = 2πb^{2k/d} > L_train，占比 c = lnL/lnb

**结果**: ✅ 数值正确，但定义方向需核对

### 精确碰撞通道数 (d_head=64, n=32)

| base | L=512 | L=1024 | L=2048 | L=4096 |
|---|---|---|---|---|
| 10K | 17/32 | 15/32 | 12/32 | 10/32 |
| 100K | 20/32 | 18/32 | 16/32 | 14/32 |
| 500K | 22/32 | 20/32 | 18/32 | 17/32 |
| 10M | 24/32 | 22/32 | 21/32 | 20/32 |

**⚠️ 碰撞定义方向问题**:

CORE_THEORY §9 写:

> base=10K, d/2=32: 碰撞块占比 **9.7%**, 碰撞通道数 **~3**

但我的计算（L=4096, base=10K）得到碰撞通道 = **10/32 = 31.3%**，或者用 c 定义得到 90.3%。

**可能的解释**:
1. 论文的 c 定义和我的相反：论文的 "碰撞块占比" 可能是指 1-c（可优化通道占比），而不是 c（碰撞占比）
2. 论文用的 L=4096 但碰撞条件不同（可能没有 2π 因子）
3. 论文表格中 base=10K 的 "碰撞块占比 9.7%" 对应的 L 值不是 4096

**TODO**: 回查 CORE_THEORY §9 的精确定义，确认 c 是 colliding 还是 non-colliding 的占比

### 相对增益 (1-c)/lnb 趋势

| base | L=2048 相对增益 |
|---|---|
| 10K | 0.0187 |
| 100K | 0.0293 |
| 500K | 0.0319 |
| 10M | 0.0327 |

增益随 base 递增 — 与 CORE_THEORY "base=500K 净增益是 base=10K 的 2.7×" 方向一致（0.0319/0.0187 = 1.7×，比例不同可能因为 L 不同）。

**Phase 18（正在跑）将提供实验验证**：如果 5 个 base 的实际 PPL gain 与 (1-c)/lnb 呈正相关，碰撞块理论得到定量确认。

---

## 6. Test 6: 密度比下界

**命题**: ρ(0)/ρ(1) ≥ cosh(τ)，纯 tether 模型取等号

**结果**: ✅ PASS

### 连续精确恒等式

| τ | sinh(τ)/tanh(τ) | cosh(τ) | 差异 |
|---|---|---|---|
| 1.0 | 1.5430806348 | 1.5430806348 | 0 |
| 2.0 | 3.7621956911 | 3.7621956911 | 0 |
| 4.0 | 27.3082328360 | 27.3082328360 | 0 |
| 8.0 | 1490.4791612522 | 1490.4791612522 | 2.3e-13 |

**解析证明**:
- dφ/du|_{u=0} = tanh(τ)/τ
- dφ/du|_{u=1} = sinh(τ)/τ
- ρ(0)/ρ(1) = [dφ/du|_{u=1}] / [dφ/du|_{u=0}] = sinh(τ)/tanh(τ) = cosh(τ) **Q.E.D.**

连续恒等式精确到机器精度。离散近似 (n=32) 在 τ≤2.83 时偏差 <5%，τ>4 时因有限通道数而发散。

**论文可写**: "The density ratio identity ρ(0)/ρ(1) = cosh(τ) holds analytically and is verified to machine precision. For τ=1.5 (typical operating point), the high-frequency density is 2.35× the low-frequency density."

---

## 7. Test 7: Taylor 分支连续性

**命题**: τ < 1e-4 使用 Taylor 展开，τ ≥ 1e-4 使用完整公式，边界处连续

**结果**: ✅ PASS

- 分支切换处 max|Δφ| = 2.57e-11
- 导数连续性: max|Δ(dφ/dτ)| = 1.15e-09
- **论文可写（如需要）**: 实现细节，证明数值稳定性

---

## 8. Bonus: 间距重分配量化

| τ | 低频间距放大 | 高频间距压缩 | 物理解读 |
|---|---|---|---|
| 0.0 (Geo) | 1.000× | 1.000× | 均匀 |
| 1.0 | 1.170× | 0.791× | 轻度重分配 |
| 1.5 | **1.401×** | **0.637×** | 典型工作点 |
| 2.0 | 1.750× | 0.513× | 中度重分配 |
| 2.83 | 2.577× | 0.375× | 强重分配 |
| 4.0 | 3.950× | 0.267× | 极端重分配 |

τ=1.5 时低频间距扩大 40%、高频间距压缩 36%。与 CORE_THEORY §3 的描述完全一致。

---

## 9. 总结：可直接进论文 / 需后续工作

### 可直接用 (5/8)
- [x] Test 1: Geometric recovery O(τ²)
- [x] Test 2A: CDF 纯 tether 精确性
- [x] Test 4: Waterbed 不等式 + Jensen 等号
- [x] Test 6: 密度比恒等式
- [x] Bonus: 间距重分配量化表

### 需后续工作 (3/8)
- [ ] **Test 3: Broadband R²** — 用真实 D(Δ) 重测 + 对齐 Algorithm 1 拟合
- [ ] **Test 5: 碰撞块** — 核对定义方向，与 Phase 18 实验交叉验证
- [ ] **Test 4 补充**: 核对 waterbed 体积的 ∫ln E 下降 vs 论文 "体积增大" 是否矛盾（可能是定义不同）

### 下一步
1. Phase 18 完成后，用其 5 个 base 的实验数据交叉验证 Test 5 碰撞块预测
2. 从 FineWeb-Edu 提取真实 D(Δ) 重跑 Test 3
3. 将确认无误的 Test 1/2A/4/6 结果写入 appendix LaTeX

# 2026-09-18 仓库收口

本轮让仓库与已封板论文保持一致，并支持另一台PC上的新会话接续。没有修改论文科学内容、PDF或投稿源码包，也没有运行模型、连接服务器、提交或推送Git。

## 更新了什么

| 区域 | 处理 |
|---|---|
| 根入口与README | 修正旧标题和旧方向链接；当前封板状态、研究／论文／实验入口一致 |
| 论文修改历史 | HANDOFF、REVISION_BRIEF、主张映射只保留当前有效说明；混合旧“当前”的完整原文进入导航快照；日记继续追加 |
| 本轮修改owner | 区分证据整合、逐章节、摘要封板三阶段；各自基线、审稿和最终交付不再混用 |
| 作者纠错与操作习惯 | 汇入AUTHOR_WORKING_CONTRACT，覆盖科学主线、Cosh／NCP、自然任务、写作风格、版本和实验边界 |
| 仓库内技能 | 新增改稿、双模型审稿、实验工作流三个技能；继续复用既有evidence技能，不依赖Mac个人技能目录 |
| 实验入口 | 46个家族按当前结果／复用、近期探索、历史专项分层；旧runner和原始路径保留，历史队列退出当前导航 |
| 过时状态 | strong-evidence入口不再写GLM刚启动；旧服务器层级和准备计划明确为历史；Native-QA两种聚合口径分开 |
| 证据索引 | 由asset_registry生成，修正旧图号／附录位置；注册表仍保留65项证据身份和原始来源哈希 |
| Pro材料 | 两份9月17–18日原文复制进external-reviews，保留SHA；最后一周问题和原文／采用结论分开 |
| 审稿可移植性 | 三份Mac临时PDF复制进现有reviews目录，保留相同哈希和三轮映射；history没有新增版本 |

## 保留边界

实验代码、完整raw、模型、历史结果和仍有依赖的路径没有重排或删除。
目录角色不等于实验的成功／失败状态，正式分数与协议继续由各自结果owner维护。
外部报告与历史脚本不产生新的授权。当前分支及未提交工作必须一起考虑，不能只按HEAD判断PC是否同步。

归档前原文见[导航快照](../archive/navigation_20260918/index.md)；接续方法见[跨机器指南](CROSS_MACHINE_CONTINUATION.md)。

## 验证与转移

验证记录由本目录的 `repository_refresh_20260918_validation.json` 保存，包含文档链接、技能校验、稿件保护和仅使用Git可见文件的跨机器模拟结果。

本机交接覆盖包生成在Git忽略的 `internal/local_snapshots/continuation_20260918/`。
它包括当前修改／新增的Git可见文件及基线提交清单，不含Git数据库、模型、凭据或ignored原始流。
使用者应在基线提交的隔离工作树应用并核查，不能覆盖家里PC尚未保存的工作。
这是可转移交付物，不表示已同步到PC或已推送远端。

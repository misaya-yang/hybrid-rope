# Native follow-up execution owner

The A/B/C evaluation is complete. See the [result owner](RESULT.md) and its
local raw/summary mirror. Only B (Native mass projection) survives as a near-tie
with NCP; A is closed and C does not improve NCP.

The current CA safety round remains authoritative and must finish without
interruption. This directory prepares the next zero-training attention-operator
experiments from the Pro five-scheme package. It does not treat all five schemes
as equal candidates.

Execution order is fixed:

1. eight-row canaries for B, B_raw, A, A_odd, and C;
2. Wave 1: B, B_raw, A in three parallel workers;
3. Wave 2: A_odd and C, only after C's full-key stable-sort runtime passes its
   own memory canary;
4. D only if A/B/C have a Full-13 signal but Natural-QA remains weak;
5. E only with explicit authorization for target-conditioned calibration.

Native/NCP baselines, the Full-13x10 panel, model, tokenizer and NCP table are
reused. The package's CPU mathematics is a reference, not a completed GPU
implementation. Every command defaults to PLAN_ONLY.

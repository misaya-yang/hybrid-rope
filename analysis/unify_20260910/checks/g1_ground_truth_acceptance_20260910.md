# G1 地面真值表 v1 —— 独立验收记录

日期：2026-09-10（09:2x，验收于 G1 最终清稿前；针对 `tables/ground_truth_tables.json` v1）。
方法：完全独立重算（numpy f64/f32，不复用 G1 脚本），核 G1 的核心声明。

## 验收通过项

| 声明 | 独立重算结果 | 判定 |
|---|---|---|
| MrPro 公式重建 `ν_j=ω_j·4^{−q(q+1)/306}` | 与 JSON 逐槽 max rel err **8.37e-08**（fp32 舍入域内）；单调递减 ✓；j≥40 m=1 ✓；槽0–23 与 Native 值**完全相等**（max rel=0.0） | **PASS** |
| Σm(MrPro)=29.3333、过渡段 B=16/3 | 29.333333 / 5.333333 | **PASS** |
| E1 手术臂 | s28_less 仅差槽28（m=0.065359=m27）；s29_more 仅差槽29（0.183007=m30）；pair28_29 恰差{28,29} 且=两手术叠加值 | **PASS** |
| **水床恒等式**：端点固定表 Σ_trans=ln4 | 24 张端点固定表（m23=0∧m40=1）**零违例**，全部 \|Σ−1.386294\|<2e-4 | **PASS** |
| 违例者定位 | HighGapToLong/HighGapToMid ΣT=1.60216=ln4+ln(b)/64（m_23=−0.155715=−(ln b/64)/ln4，**从带外 gaps0–22 注入**的定量表达 ✓）；E2 m_40=1.1557（尾部违例 ✓）；P2 不在固定集（m23≠0，ΣT=1.3855 ✓ 与 plnd_ctl 双源闭合一致） | **PASS** |
| 126 锚点/18 bit-exact 的账面 | 未逐字节复验 fp32 张量镜像（在 results/ 镜像里，G1 已列 sha）；值级已覆盖 | 抽查级 |

## 待 G1 最终版澄清的一项张力

README §4-5 说"全部面板表 fast 段（槽0–23）与原生**逐位**相同"，而 JSON 内各表 `endpoint_delta_m.fast_band_bitwise_equal_native=False`（但值级 max rel=0.0）。推测 flag 语义 = "公式重建字节串 vs native 部署字节串"（两条 fp32 pow 路径末位可差 1 ULP），README 说的是"部署 vs 部署"。终稿需要 flag 与 README 二选一改口径，**不是数值问题**。

## 结论

v1 可作为下一次推导的地面真值层使用；INTEGRATION §9 交付表"具体频率表"行的完成条件已满足其重建部分（新表资格门另论）。G1 workflow-2 的下游（D1–D4/T1–T3/V×4）以本 JSON 为输入。

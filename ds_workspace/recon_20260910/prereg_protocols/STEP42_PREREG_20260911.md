# 预注册：`step42` 的 out-of-sample 检验（写在读数之前）

**2026-09-11 ~11:15** · OLMo-2-0425-1B-Instruct · 面板 `prepared_holdout_union`（180 行）
对照 `holdout180/beta_b1p0.jsonl`（同面板已测的部署表）

> **本文档在【`step42` 在该 180 行面板上的任何一行结果被读取之前】提交。**

---

## 一、为什么做：它是第一个"两种口径都显著"的表，但有一个明确的红旗

`pro_step42`（Pro §9.3，`m_j = 1[j≥22]`，**S = 42.000000 精确**）在 350 行**选择面板**上：

| 口径 | acc | Δ vs BM | t | W/L/T |
|---|---|---|---|---|
| 分数值 | 0.4893 | **+7.26pp** | **+3.39** | 66/42/242 |
| 整行答对 | 0.4343 | **+6.00pp** | **+2.48** | 47/26/277 |

**这是本 campaign 第一个在两种口径下都对部署 BM 显著为正的表。**

**逐任务分解里的红旗**：

| 任务 | n | BM | step42 | Δ |
|---|---|---|---|---|
| **`niah_single_3`** | 50 | 0.500 | **0.820** | **+32.0pp** |
| `niah_multikey_1` | 50 | 0.600 | 0.680 | +8.0 |
| `niah_single_1` | 50 | 0.900 | 0.980 | +8.0 |
| `niah_multivalue` | 50 | 0.605 | 0.645 | +4.0 |
| `niah_multikey_3` | 50 | 0.000 | 0.000 | 0.0 |
| `qa_2` | 50 | 0.260 | 0.260 | 0.0 |
| `cwe` | 50 | 0.052 | 0.040 | −1.2 |

**`niah_single_3` 正是 held-out 测试判定为"虚假 +12～14pp 来源"的那个任务**
（当时结论：那 +12～14pp 集中在 `niah_single_3`，它单独贡献 +42～46pp）。
**现在 `step42` 的 +7.26pp 里有 +32pp 来自同一个任务。**

> **⟹ 强先验：这很可能是【同一个选择效应】的又一次出现，而不是一个真实增益。**
> **⟹ 必须用 held-out 检验，且判据写死在读数之前。**

## 二、材料：为什么这 180 行是干净的 out-of-sample

`prepared_holdout_union` 的 6 个任务（`niah_single_2` / `niah_multikey_2` /
`niah_multiquery` / `vt` / `fwe` / `qa_1`）是**选择面板从未用过的**，
**与 `niah_single_3` 无重叠**（已程序化核验，重叠 0）。
**部署表在该面板上已实测**（`beta_b1p0.jsonl`，180 行），所以不需要重跑对照。

## 三、臂与成本

| 臂 | 构造 | 成本 |
|---|---|---|
| `s42_h` | `--pro-tables step42` | **≈8 分钟**（对照已存） |

## 四、★ 判据（写死）

**主统计量**：分数值 Δ（与选择面板同口径），配对 SE。**次要**：整行答对口径。

| Δ（分数值） | 判定 |
|---|---|
| **≥ +5pp 且 t ≥ 2** | **out-of-sample 成立**。`step42` 是**本 campaign 第一个在从未用于选择的 180 行上显著超过部署 BM 的表** ⟹ 值得加量、值得最终测试 |
| **≤ +2pp** 或 **t < 2** | **不成立**。选择面板的 +7.26pp 是选择效应，与平台成员的 +12～14pp 同类 |
| 其它 | **仍未决**：报区间并停止 |

**必须同时报的**：**逐任务分解**——若增益仍然集中在一个任务上，即使总分显著也要标注；
**整行答对口径**（`TRADEOFF_ASYMMETRY` 的教训：两个口径可以符号不同）。

## 五、我的预测（写下来）

**我预测 Δ 落在 +0 ～ +3pp，即【不成立】。**
理由：选择面板的增益有 +32pp 来自 `niah_single_3` 一个任务，而该任务不在本面板里；
剩余部分（+8/+8/+4/0/0/−1.2）本就薄弱。

**若预测错了（≥ +5pp 且 t≥2），那是一个重要的正面结果**——
本 campaign 将第一次有一张表在从未用于选择的样本上真正超过部署表。

## 六、同时**更正**我此前的一个预判

`RUNBOOK_OFFICE_20260911.md` §3 我预判 `step42`"**应当仍差**"（依据是 `step_hi25`(S=39) 只有 0.1121）。
**该预判是错的**：`step42` 在选择面板上是 0.4893，比部署表高 +7.26pp。
**预算匹配（S=42 精确）显然比 S=39 好得多**——这一条本身值得记下来。
（这**不**改变本预注册的判据，只是如实记录我赛前判断错了。）

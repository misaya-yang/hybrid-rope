# 四个角：YaRN / MrRoPE / 部署表只是同一个单纯形的三个顶点

**2026-09-11** · 零训练 · OLMo-2-0425-1B-Instruct
状态：结构**已机器精度验证**；预测臂**已在跑**（`corner_lo14/15`, `b3_lo14/15`, `b2_lo15`）

---

## 一、这个框架没有形状参数

增量坐标 `ε_k = m_k − m_{k−1}`（`m` 为压缩指数），**零边界 Dirichlet Laplacian**

```
(Lε)_k = 2ε_k − ε_{k−1} − ε_{k+1},    ε_0 = ε_{n+1} = 0
```

**实测（机器精度）**：

| 表 | 频带 | `Lε` 的形状 |
|---|---|---|
| 部署 BM | [15,32], n=18 | `+0.00175` **逐点常数**（min == max） |
| MrRoPE | [15,32], n=18 | `0,0,…,0,` **`+0.1111`**（只在末端） |

> **MrRoPE 的「中频改进」在理论上就是：把 forcing `f = Lε` 从常数改成慢端的一个点源。**

## 二、三条命名方法 = 三个顶点

`ε ∝ k^a (n+1−k)^b` 上：

| forcing 支撑 | ε 形状 | (a,b) | 是谁 | `Lε` 实际读数 (n=18) |
|---|---|---|---|---|
| **两端** | 均匀 | (0,0) | **YaRN** | `+0.056, 0,…,0, +0.056` |
| **慢端** | 递增 `∝k` | (1,0) | **MrRoPE** | `0,…,0, +0.111` |
| **全带** | 凸包 `∝k(n+1−k)` | (1,1) | **部署 BM** | `+0.002 ×18` |
| **快端** | **递减 `∝(n+1−k)`** | **(0,1)** | **★ 从未试过** | `+0.111, 0,…,0` |

> **整个频率分配文献占据了单纯形的三个顶点。第四个顶点是空的。**
> 而且它不是一个新参数——它就是 `m_incr_beta(shape_a=0)`，一个已存在的开关。

## 三、预算与形状的精确关系（频带 [15,32]）

```
S = 31 + Σ_{i=1..18} (19−i) · ε_i
```

**十个 case 机器精度核验**（BM 9.5→40.5、MrRoPE 6.667→37.667、b2 11.379→42.379、
b3 12.637→43.637、corner 12.333→43.333，及 low=15 的五个）。

> **`S=42` ⟺ `Σ(19−i)ε_i = 11`。而缺角是 12.333。**
> **赢家们（C42V24 / a1_b64 / a2_b32 / C42 全部 S=42）已经被压到距缺角 89% 的地方。**
> 这解释了它们为什么全卡在 42：**在同一频带上已经快到不能再快。**

把同一个缺角形状往慢端推一格（low=15，带 [16,33]）就得到 **S = 42.333**——
**正好落在赢家的预算上**。这是本框架的 headline 预测。

## 四、实测的 `g = w·(Lε)` 质心指向缺角

`w_j = ½ F_jj (ln4)²`（实测窗内 output-Fisher 对角）。质心（带 [15,32]）：

| 表 | 质心 | 连续 NLL | RULER |
|---|---|---|---|
| **MrRoPE** | **32.00** | 3.6887 | 0.0709 |
| 部署 BM | 22.37 | 2.8627 | 0.4167 |
| `turns_a2_b32` | 21.88 | 2.8846 | 0.5100 |
| `turns_a1_b64` | 19.71 | 2.8402 | 0.5384 |
| `ctl_C42` | 18.74 | 2.9417 | — |
| **`ctl_C42V24`** | **15.47** | **2.8309** | — |
| *缺角* | *15.05* | — | — |

**赢家的质心单调向快端移动，C42V24 已经几乎落在缺角上。**
沿 `b` 轴（a=1）：MrRoPE 26.33 → BM 23.50 → b2 21.62 → b3 20.36 → b4 19.47 → … → 缺角 15.05。

## 五、诚实标注

- ✅ **已验证**：Poisson 结构（机器精度）、四顶点的 `Lε` 支撑、`S = 31+Σ(19−i)ε_i` 恒等式、
  10 个 case。
- ⚠ **依赖实测 Fisher**：§四的质心用 `F_jj` 加权。`F_jj` 有孤立尖峰（槽 24=7842、
  槽 35=1.21e4），**若那是噪声则质心方向也脆弱**。`fisher_stability.py` 正在跑
  （三个 delta + 一组不相交文档），跑完即判。
  注意 §一、§二、§三 **不依赖 Fisher**——它们是纯几何，已独立成立。
- ❌ **不是**"缺角一定更好"。它只是**唯一没被试过的顶点**，且实测质心方向指向它。
- ⚠ 质心**不是**顺序统计量：`ctl_C42`（18.74）比 `turns_a1_b64`（19.71）更靠前却更差。
  所以本框架给的是**方向**，不是标量。

## 六、由本文档派生的臂（已在跑，全部闭式无扫描）

| 臂 | 内容 | S | 判什么 |
|---|---|---|---|
| `corner_lo15` | 缺角形状，带 [16,33] | 42.33 | **本框架的 headline 预测**：同一预算下的缺角 vs ramp |
| `corner_lo14` | 缺角形状，带 [15,32] | 43.33 | 缺角的纯粹形态 |
| `b3_lo14` | b 轴外推过 b=2 | 43.64 | b 轴是否继续改善 |
| `b2_lo15` / `b3_lo15` | 同带 [16,33] 的 ramp 对照 | 41.38 / 42.64 | **受控对照**：只有形状不同 |

先上连续仪器（每臂 25 秒）。**只有赢的才升到 350 行 RULER。**

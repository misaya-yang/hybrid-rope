# 走线结果 + 内点的 out-of-sample 确认（预注册，写在确认读数之前）

**2026-09-11 ~11:26** · OLMo-2-0425-1B-Instruct

---

## 一、走线结果（`walk_out/`，180 行面板，判据 `82dc5e9`）

**两个端点对照都过了**（这是曲线可比的前提）：

```
walk_a0 vs 已存 beta_b1p0(部署 BM) : 180 行，文本与 correct 全同  OK
walk_a1 vs 已存 wide_b1p0(a1_b64)  : 180 行，文本与 correct 全同  OK
```

| a | Δ4096 | t | Δ16384 | t | 换算率 |
|---|---|---|---|---|---|
| 0.25 | −4.56 | **−3.30** | +9.82 | **+3.73** | 2.16 |
| **0.50** | **−2.69** | −1.51 | **+10.87** | **+3.28** | **4.04** |
| 0.75 | −6.36 | **−2.42** | +9.42 | **+3.00** | 1.48 |
| 1.00 | −4.11 | −1.81 | +7.83 | **+2.16** | 1.91 |

**二值化（整行答对）**：

| a | Δ4096 | t | Δ16384 | t |
|---|---|---|---|---|
| 0.25 | −8.33 | −2.32 | +3.33 | +1.42 |
| 0.50 | −10.00 | −1.94 | +3.33 | +1.07 |
| 0.75 | −13.33 | **−3.01** | +2.50 | +0.90 |
| 1.00 | −8.33 | −1.69 | −0.83 | −0.26 |

### 判决：**PURE TRADE**（按预注册规则）

**每一个 a>0 都在窗内输、长程赢。** 按写死的规则：
不存在 Δ4096(a) ≥ 0 的内点 ⟹ **本段无 DEVELOPMENT_WIN**.

### ★ 但**线性零假设被拒绝**——这是比"纯交易"更有内容的部分

预注册的强零假设是**线性**（过原点）。实测：

| cap | 线性斜率 | χ²/dof | 最大残差 | 观察值 |
|---|---|---|---|---|
| 4096 | −6.482pp/单位 a | 1.50 | 2.94pp | 0 / −4.56 / −2.69 / −6.36 / −4.11 |
| 16384 | +13.102pp/单位 a | **2.50** | **6.54pp** | 0 / **+9.82** / **+10.87** / +9.42 / +7.83 |

> **⟹ 长程增益【一开始就饱和】（a=0.25 已拿到 +9.82，最好的是 a=0.50 的 +10.87），
> 而窗内代价随 a 继续累积。响应不是直线，是【先陡后平】。**
> **⟹ 因此 a=0.50 在两个端点上都更优：长程最好（+10.87）、窗内最轻（−2.69，t=−1.51 不显著）。**

**逐任务看，长程增益的来源与 `TRADEOFF_ASYMMETRY` 一致**：
`niah_multiquery`(4 参考) +31.2 / +31.2 / +27.5 / +30.0 —— **几乎所有 a>0 都一样好（已饱和）**；
`vt`(5 参考) +6/+14/+9/+12；而二值任务 `niah_multikey_2`(1 参考) 在 a=1.00 变成 **−10.0**。

## 二、⚠ 两个口径对这个内点的判断**不一致**（必须一起读）

| 口径 | a=0.50 长程 | a=0.50 窗内 | 结论 |
|---|---|---|---|
| 分数值 | **+10.87pp (t=3.28)** | −2.69pp (t=−1.51) | 看似一个好内点 |
| **整行答对** | **+3.33pp (t=1.07)** | −10.00pp (t=−1.94) | **不是** |

**⟹ 按 `TRADEOFF_ASYMMETRY` 的教训：这个"内点"的长程那一半主要是逐项召回的部分分。**
**所以 pre-registered 的 PURE TRADE 判决成立，而"a=0.5 更好"只是一个分数值口径下的现象。**

## 三、★ 预注册：内点的 out-of-sample 确认（**写在读数之前**）

按 `WALK_PREREG_20260911.md` 第六·七条：**本实验只定位，不下结论；
任何内点必须在没拟合过的数据上复现才能称为结果。**

**材料**：`prepared_natural_union`（**391 行，5 个自然问答族，从未跑过**，
与选择面板重叠 0）。取证已有 `nat_bm`（同链在跑）。

**臂**：`walk_a0p5`（走线中点，**是一张新表**）。

**★ 判据（写死）**

| 观察（pooled Δ vs BM，分数值） | 判定 |
|---|---|
| **≥ +5pp 且 t ≥ 2** | 走线内点的长程增益**跨任务族泛化** ⟹ 值得做最终测试 |
| **\|Δ\| ≤ 1.5pp 且 SE ≤ 2.0pp** | **不泛化**，内点只是 180 行上的选择效应（与平台成员的 +12～14pp 同类） |
| 其它 | **仍未决**：报区间并停止 |

**参考锚**：`a1_b64` 与另两个平台成员在本面板上的 pooled Δ 由 `chain_natural` 给出
（预注册 `NATURAL_PREREG`，预测 0～+2pp）。**若走线中点不优于这几个成员，那它就没有新内容。**

**我预测**：`walk_a0p5` 落在 **+1 ～ +4pp**（即达不到 +5pp 的判据）。
理由：本面板 391 行里 346 行（89%）只有 1 个参考 ⟹ 二值口径，
而走线中点在该口径下的长程增益只有 +3.33pp（t=1.07）。
**⟹ 我预测它【不泛化】。**

**成本**：1 臂 × 391 行 ≈ 26 分钟。**现在就跑**（卡上只剩 ~7 GB 占用，25 GB 空闲）。

## 四、这份文档**不**改变什么

- `WALK_PREREG` 的 PURE TRADE 判决**不变**（那是判据驱动的结论）
- **不**把 a=0.5 称为"找到了更好的表"——它只在 180 行上被定位，且二值口径下不成立
- 不追加行数去移动任何已判定的统计量

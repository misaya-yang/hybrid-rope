# 2026-09-11 行动索引

按"先读哪一份"排序。桌面汇总 `~/Desktop/RoPE_KKT_核心结论_20260911.md`。

---

## 一、结论（先读这三份）

| 文件 | 一句话 |
|---|---|
| **`HEADLINE_20260911.md`** | **`ctl_C42V24` 0.5440 是全战役最高分（+12.73pp, t=+6.08）；三个赢家互相打平；C42/C42V24 受控对在两台仪器上同向验证** |
| **`FOUR_CORNERS_20260911.md`** | **YaRN / MrRoPE / 部署 BM 是同一个 forcing 单纯形的三个顶点；第四顶点（快端）从未试过；机器精度验证** |
| **`CONSTRAINT_IS_SLACK_20260911.md`** | **带内重分配免费，代价只在带外 ⟹ 用户的 KKT 在这个表族里退化；单槽梯度 SNR<1** |

## 二、被证伪的（收缩）

| 文件 | 内容 |
|---|---|
| **`WHY_THE_FISHER_ROUTE_DIED_20260911.md`** | `F_jj` 量的是"离 native 的 KL 距离"，而 native 不是最优 ⟹ **符号相反**。测量本身可复现（跨文档 r=0.76） |
| **`NO_STATIC_FUNCTIONAL_20260911.md`** | **12 个静态泛函全部出局**（`S`, `S_in`, `S_entry`, `C`, `D_fish`, `N`, `dev`, `cv`, `ks`, `span`, `Σν`, `n_plateau`） |
| **`PLATEAU_20260911.md`** | 连续仪器上**前五名统计打平**（跨度 0.019 nats）；已到停止条件 |

## 三、本轮新工具

| 脚本 | 作用 |
|---|---|
| `fisher_stability.py` | `F_jj` 四探针——**已完成，判出"测量可复现但对象错"** |
| `marginal_damage.py` | 真实逐槽代价 `D_j(m)` + 等边际剖面——**已完成** |
| `kkt_gradient.py` | 实测两侧梯度 `∂D/∂m`, `∂R/∂m`——**已停（SNR<1）** |
| `kkt_step.py` | 沿实测梯度走（备用） |
| `qwen_longnll.py` | Qwen 连续长程仪器（2× 无信号已废；4× 跑中） |
| `ruler_paired.py` | 跨臂 RULER 配对检验（**服务端跑**） |
| `patch_evq_geom.py` | **修 `--evq` 的几何 bug**（原用 Qwen 的 θ 构造、跑在 OLMo） |
| `patch_wide.py` | 给 `olmo_beta.py` 加 `--wide-betas` |
| `arm_queue.py` | 重排 RULER 队列为**严格串行**（32GB 卡，四个并发会 OOM） |

## 四、尚未落地

| 项 | 状态 |
|---|---|
| `b3_lo14` RULER 350 行 | ~260/350 |
| `b4_wide` RULER 350 行 | ~70/350 |
| **EVQ τ=0.5/1/2 首次长程** | 排队中。**预注册预测：差**（§5 桌面文档） |
| `gain 1.0 vs 1.1386` | 排队中 |
| **Qwen 4× 仪器** | 基线 2.9486 @131073 已出，臂跑中 |

## 五、本轮我犯并已更正的错误（留档）

1. **EVQ 读法用错**：用了 `m_evq_deployed`（代码注释明说"NOT this"）。
   canonical 是 `m_evq_shift`（`ν=θ^(−φ)`）。据此我先前"更正"掉的"EVQ span-守恒"**原本是对的**。
   已恢复，并用正确读法重算（桌面 §5）。
2. **`kkt_gradient` 的梯度少乘 2**（前向商+后向商 ≠ 中心差分）。`ρ` 不受影响，报告值已修。
3. **`qwen_longnll` 2× 版本**：拼接文档破坏长程结构 ⟹ 无信号。已重建为 4× + PG19 连续文档。
4. **`--evq` 缺几何守卫**（唯一没有守卫的 builder）。已补。
5. **三处切片/索引错误**（`Lop(m)` 应为 `Lop(e)`；Qwen 频带 [24,40] 切成 [15,33]；`turns` 臂误用 `ramp="mrpro"`）。
   **教训：每次从表构造量时，先跑一个已知答案的自校验。**
6. **`chain_holdout.sh` 里 `$ROOT` 未定义** ⟹ `--root "$ROOT/holdout"` 展开成 `/holdout`，
   最终测试的输出写到了文件系统根目录。**数据完好**，已建软链 `phase1_20260910/holdout -> /holdout` 修正。
   **教训：chain 脚本里每个变量都要么显式定义、要么在启动时 echo 出来核验。**


---

## 七、`PLAN_20260911.md` 的处置（2026-09-11 晚）

那份计划（本会话早期产物）的判决树**已被后来的实验覆盖**：

| 计划项 | 状态 |
|---|---|
| **KNIFE 2×2**（3 臂） | ✅ **已跑**。三个细胞对 BM 是 −1.99 / −2.30 / −3.21pp（配对 t = −1.25 / −1.98 / −1.85；符号检验 p = 0.03 / 0.003 / 0.015）。**行效应与列效应量级相近且都为负**——落在计划 §8.3 预判的"两个都在起作用"分支 |
| **砍掉 `beta_b3/b4/b6/b8`** | ❌ **后来被跑掉了，而且是对的**：`b3_lo14` 是全战役最高分（0.5587, +14.20pp）。计划当时判它"方向已被 β 族覆盖"——**方向分析正确，但漏了一点：同一方向上走到更远处仍有回报** |
| **砍掉 Qwen b 扫描** | ✅ **正确**。本轮独立证实：Qwen 4× 仪器上表效应只有 0.006 nats |
| **排序实验** | ✅ 已跑（Qwen 侧 `ord_*`），带内、代价 ≲0.009 |
| **OLMo 侧窗内代价曲线** | ⏸ **仍未做**。但 §4.0 已证明带内重分配免费（≲0.01），**信息量低** |
| **计划的停止条件 #4** | ⚠ **已被触及**：最优臂（`b3_lo14`）比 `turns_a1_b64` 只高 2.03pp、t=−1.11，**未达 2 配对 SE**。按计划原话，应报"部署 BM 与最优在该仪器下**不可区分**"——**但四个冠军全部显著优于 BM（t≈+6），所以正确的措辞是"存在一个平台，其成员互相不可区分、且全部显著优于 BM"** |

**计划没有预见到的**：平台（四个成员）、四个角的单纯形、`Lε` forcing 框架、
以及"所有建模推导都失败、只有受控扰动成功"这条元结论。

## 八、当前最高 ROI 的三件事（都在跑）

1. **holdout 最终测试**（4 臂 × 72 行 × 6 个从未用于选择的任务）——检验"增益集中在单一任务"的限定
2. **EVQ τ=0.5/1/2 首次长程**——用户自己的方法，预注册预测已写下
3. **跨模型**——Qwen 两台仪器都判不了；需要换判据（检索型连续仪器）或建 13-task macro 面板

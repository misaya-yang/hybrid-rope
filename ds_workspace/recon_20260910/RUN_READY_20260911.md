# GPU 就绪清单（2026-09-11，实例无卡模式下核实）

实例：`ssh -p 53405 root@connect.westc.seetacloud.com`（**可达**；GPU=0，无卡模式）
工作目录：`/root/autodl-tmp/phase1_20260910`（293 文件，运行器与 run 目录完好）
`PYTHONPATH=/root/autodl-tmp/nongeometric_screen_20260909/code:$D/repoharness:$D`（**必需**，否则 import 失败）

---

## A. Qwen 4× 有功效判决（task #18）—— **只差一个臂，一句话可跑**

| 项 | 状态 |
|---|---|
| 已落盘 | `qwen4x_power/rows.jsonl`：**native 2.6921983**、**mrpro 2.5358660**，delta −0.1563324，se 0.0069087（**与预注册记录逐位一致**） |
| 缺 | **只有 `beta_b1_BM` 臂** |
| 仪器 | `qwen_longnll.py`（接口 `--root --model --nll-dir --far --tail --only` 齐备；`build_arms()` 含 `native`/`mrpro`/`beta_b1_BM`——**已在实例上逐行核实**） |
| 语料 | `/root/autodl-tmp/longtext/prepared_pg19_4x/` —— **30 篇 × 32769 tok** |
| 模型 | `/root/autodl-tmp/qwen25_1p5b_32k` |

> ⚠ **2026-09-11 深夜修正（上卡第一动作）**：该模型目录当时**没有权重**——
> config/tokenizer 在（12M），`model.safetensors` 缺失，**照原样跑必挂**。
> 成因不是删除，是 **9/11 换实例后从归档恢复时大文件静默丢失**
> （目录 `Birth: 2026-09-11 18:01`，内含文件却是 8/23 的 mtime = tar 恢复签名）。
> 8/23 下载日志与 `.hfd/manifest`（期望 `3087467144` 字节）证明当初下载成功过。
>
> **恢复命令**（已验证 hfd.sh + aria2c 在位）：
> ```bash
> cd /root/autodl-tmp && HF_ENDPOINT=https://hf-mirror.com \
>   bash hfd.sh Qwen/Qwen2.5-1.5B-Instruct --include model.safetensors \
>     --tool aria2c --local-dir /root/autodl-tmp/qwen25_1p5b_32k
> ```
> **落盘核验（勿信返回码）**：`stat -c %s .../model.safetensors` 期望 `3087467144`。
> 尺寸不符**不要跑 A 臂**——它会把"加载失败"伪装成"实验结果"。
>
> 注：盘上另有 `rope_qwen_baseline_20260907/model`，但那是 **Qwen2.5-3B**
> （36 层 / hidden 2048），与 A 臂的 1.5B 仪器**不是同一台**，不可替代。

```bash
PYTHONPATH=... setsid nohup $PY qwen_longnll.py \
  --root $D/qwen4x_power --model /root/autodl-tmp/qwen25_1p5b_32k \
  --nll-dir /root/autodl-tmp/longtext/prepared_pg19_4x \
  --far 131073 --tail 512 --only native,beta_b1_BM >> qwen4x_bm.log 2>&1 &
```
读数：`python qwen4x_power_read.py`
（**已修正其反向判决**——原版在 `dd<0` 时印"MrRoPE 赢"，而 NLL 越低越好，即 BM 更好。
修复版在实例上，见 `CORRECTION_SIGN_20260911.md`）

---

## B. 振幅处方 @4×（**优先级最高**，见 `AMP4X_PRESCRIPTION_PREREG`）

| 项 | 状态 |
|---|---|
| 表 | `tables/amp4x_1p13.json`(47.46)、`amp4x_1p30.json`(54.60) —— **已同步，md5 与本地逐位一致** |
| `--m-file` 支持 | ✅ **已打好并验证**：argparse 接受、`--dry-run` 通过、插入块单元测试通过（含负对照，32 槽被拒） |
| 面板 | `prepared_ruler_newtasks_02/screen.jsonl` —— **350 行 / 7 任务 / cap 16384** |
| 模型 | `olmo2_1b_longalign_assets/models/OLMo-2-0425-1B-Instruct` |
| 对照 | 已测 `BM` = 0.4167（锚点，不重跑） |
| 成本 | 2 臂 × 350 行 |

```bash
PYTHONPATH=... setsid nohup $PY olmo_beta.py --root $D/olmo_amp4x \
  --model .../OLMo-2-0425-1B-Instruct \
  --panel .../prepared_ruler_newtasks_02/screen.jsonl \
  --archive .../run_ruler_newtasks_01 --betas '' --turns '' \
  --m-file $D/tables/amp4x_1p13.json:amp4x_1p13 \
  --m-file $D/tables/amp4x_1p30.json:amp4x_1p30 >> amp4x.log 2>&1 &
```
**判据四行写死于预注册**。同时必报 4096 行。

---

## C. 振幅 @8×（AMP8X，优先级低于 B）

表已同步：`tables/scale8x_wide.json`（=`1.5×wideBM`，与另一条线主臂**逐位相同**）、`shift8x_bm.json`。
面板 `prepared_s8_01`（72 行）。`s=1.00` 是已测锚点（0/48，不重跑）。

---

## D. 中断未跑完的（原样保留，可续跑）

| run | 现状 | 说明 |
|---|---|---|
| `olmo_lb_h` | 无 rows.jsonl | LongBridge `faster` 臂 60/180（符号对主统计量） |
| `olmo_gsweep` | 无 rows.jsonl | gain 1.20 上界（**杠杆已证实 1.1386 为最优**，价值下降） |
| `qwen4x_power` | 2/87 行 | 见 A |

---

## E. 审稿人点名的 OLMo 官方 YaRN 同 panel 基线 —— ★ **不需要写新代码**（我先前判断错了）

我原以为"实例上没有对应评测器，需新写一个"。**核实后是错的**：harness 是**表无关**的——
`runtime.install(model, table)` 接受任意表（`values_float32` / `gain` / `tensor_sha256`），
`run.py --prepared <dir>` 从 `<dir>/tables.json` 读具名表、从 `spec.json` 读方法队列。
**所以缺的只是一张表条目，不是评测器。**

**已完成（纯 CPU，零 GPU）**：
```
新目录 /root/autodl-tmp/olmo_fast_screen_20260908/prepared_natural_yarn_01
  tables.json : Native, MrPro, MrProBM, YaRN
    YaRN: gain=1.138629（与 MrPro/BM 匹配，同振幅对照）
          sum_m=42.9117, tensor_sha 由 harness 自己的 tensor_sha() 计算
  spec.json   : methods 增加 YaRN × 16（与 MrPro/MrProBM 同队列）
  其余文件（screen.jsonl/generation_config.json/manifest.json）与原目录逐字节相同
```

**约定守卫**：构造脚本在写入前先反推存储的 MrPro 表，要求
`j<=14 上 max|m|=4.97e-08`、`j>=32 上 max|m-1|=5.94e-08`、`sum_m=37.6667`
—— **37.6667 正是 `m_mrpro(n=18, low=14)` 的已知签名**，与 my m-坐标一致。
守卫第一次以 1e-9 容差拒绝（float32 存储），说明它确实在拒而不是在走过场。

工具脚本：`code/yarn_baseline/build_yarn_entry.py`、`code/yarn_baseline/add_yarn_spec.py`

**一条命令即可跑**（需卡）：
```
PYTHONPATH=... python scripts/experiments/olmo_fast_screen/run.py \
  --prepared /root/autodl-tmp/olmo_fast_screen_20260908/prepared_natural_yarn_01 \
  --out <新目录>
```

**未验证**：模型前向（需卡）。表条目本身已通过约定守卫与哈希计算。


---

## F. amp4x 读数器（已入库 + **已传到实例并验证**）

`experiments/zerotrain_20260910/amp4x_read.py`：实现预注册 §三 的四行判据，
逐项打印、**显式写出符号约定**（`correct` 越高越好 ⟹ 正 delta 支持该臂），
并**无条件报 4096 行**（预注册要求）。

配对基线 = `run_ruler_newtasks_01/MrProBM.jsonl`（**350 行，实测确认**）。
⚠ 注意：部署 BM 在 350 行面板上的名字是 **`MrProBM`**（archive 里），
不是 `beta_b1p0`——后者在实例上只存在于 72/180/391 行的面板。

**内置两条已付代价的纪律**：
- **拒绝部分读取**：面板不足 350 行即拒报（此前有读数器在 391 行中的 112 行上
  报 +1.34pp，而那 112 行恰是文件序中准确率最高的两个任务族）
- **符号约定显式写出**，不裸印带符号差

**传输状态：已完成。** 实例恢复后已 `scp` 并核验：落盘、`--panel {350,180}` 两面板
均干净拒绝（`REFUSING: no amp4x arms found yet`）。

**★ 自测（用已知答案验证验证器）**：让它配对 archive 里真实的 MrPro vs MrProBM，
输出 `delta=−34.59pp, t=−13.18, W/L/T=9/156/185, base=0.4167, arm=0.0709`
——**正是论文发表的那组数字**。配对、统计、符号约定全部验证正确；

**★ 自测暴露并已修的判据缺陷**：350 行主面板 caps 实测 `{16384: 350}`，
**里面没有 4096 行**，而预注册把"必须同时报 4096 行"写成硬判据 ⟹ 原设计不可满足。
已在读数前修正：4096 报告改由 180 行 held-out（4096×60 + 16384×120）提供，
其 16384 行**顺带成为主结论的 held-out 复现**。

## 上卡第一件事（建议顺序）

0. **核验 A 臂模型权重落盘**（见 §A 修正框）——`stat -c %s .../model.safetensors`
   必须 = `3087467144`。**这一条在开卡之前、在任何臂之前。** 已于 9/11 深夜启动重下；
   若尺寸不符，先补下再跑，否则 A 臂的失败会伪装成实验结论。
1. **B（amp4x）** —— 最便宜、350 行，且机制已全验证。
   ⚠ **判决读法已修正**（见预注册 §七）：1.13 是 **reach-only 上界**，不是精确最优。
   真最优落在 $\ge 1.0$ 一侧（依据：`DOSE_RESULT` 在 $m\le1$ 内单调、最优在最大压缩端、
   内点被 $|t|\ge5.6$ 拒绝）。**4096 行是判据的一半，不是附带报告。**
2. **A（qwen4x BM 臂）** —— 补齐杠杆表最后一格
3. **E（官方 YaRN 基线）** —— **已就绪，一条命令**（表条目已构造并过约定守卫），
   直接回答审稿人的"BM 是否只是修了 checkpoint mismatch"
4. D 的 `lb_hold` —— 补齐唯一有符号对

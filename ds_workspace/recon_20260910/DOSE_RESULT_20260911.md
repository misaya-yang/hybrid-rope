# 剂量曲线结果：a\*(L) **恒为 1**——最优点是**按模型**的常数，不是按长度的函数

**2026-09-11** · OLMo-2-0425-1B-Instruct · 连续仪器，16 篇配对 · 判据见 `DOSE_PREREG_20260911.md`（读数前提交）

---

## 一、结果

`m(a) = (1−a)·m_MrRoPE + a·m_BM`（端点逐位核验等于已测的 `mrpro` / `beta_b1_BM`）

| a | 1× (4096) | 2× (8192) | 4× (16384) |
|---|---|---|---|
| 0.00 MrRoPE | 3.209991 | 3.256920 | 3.688689 |
| 0.25 | 3.047836 | 3.085254 | 3.239292 |
| 0.50 | 2.994739 | 3.022102 | 3.181752 |
| 0.75 | 2.969728 | 2.983759 | 3.049953 |
| **1.00 BM** | **2.955406** | **2.956303** | **2.862685** |

**配对统计（vs a=1，16 篇配对）**

| 长度 | a=0 差 | t | a=0.25 | a=0.5 | a=0.75 | **a\*** |
|---|---|---|---|---|---|---|
| 1× | +0.254585 | **+6.24** | +4.55 | +4.65 | +2.54 | **1** |
| 2× | +0.300617 | **+5.59** | +3.87 | +3.03 | +2.20 | **1** |
| 4× | +0.826004 | **+6.68** | +8.12 | +8.31 | +5.70 | **1** |

**三个长度上单调递减；a\*(L) = 1；每个内点都显著差于 BM（所有 t ≥ +2.2）。**

## 二、★ 结论

> **在本模型上，最优点【不随长度移动】。随长度变化的是【边际】：0.255 → 0.301 → 0.826。**
>
> **⟹ 杠杆故事适用于【跨模型】，不适用于【跨长度】。**

**这与预注册的第二个分支完全一致**（我预测 a\*(L) 恒为 1，边际随长度增大）。**预测正确。**

## 三、goal 第 1 条（寻找最优化策略）的**已测答案**

之前只有方向（"取决于基座需要多少救援"），现在是：

> **最优压缩量是按模型的常数，不是按长度的函数。**
> - **OLMo**：1×/2×/4× 全部最优在**最大压缩端**，且每个长度都显著（t ≥ 5.6）
> - **Qwen 4×**：最优**翻到轻压缩端**
> - 跨长度只换**幅度**

**⟹ 可操作策略（有证据的决策规则）**：
1. 测 `native − BM`（一次前向的救援量）：OLMo **4.35**、Qwen **0.18**
2. 救援大 ⟹ 用最大压缩（BM 端）
3. 救援小 ⟹ 用轻压缩（MrRoPE 端）

**这不是拟合出来的阈值，是"表在做的事（拿分辨率换跨度）"的直接后果**：
基座已经用不上分辨率了（崩了），就全换成跨度；基座还好，就别换。

## 四、与前一条的关系

`THE_ANSWER` 说"最优点取决于基座需要多少救援"。**本文档把它精确化**：
依赖的是**模型（的崩坏程度）**，在**长度轴上是常数**。
**两者合起来：`a* = f(模型)`，`margin = g(长度)`。**

## 五、边界

1. **只在 OLMo 一个模型上测了长度轴。** "跨长度不移动"目前是单模型结论。
   Qwen 上没做长度轴（需要 4× 的长文本语料，本轮才刚建好）。
2. **只用了一条段（MrRoPE→BM）。** 别的方向上 a\* 可能不同。
3. 16 篇配对文档；每点 ~7–37 s。

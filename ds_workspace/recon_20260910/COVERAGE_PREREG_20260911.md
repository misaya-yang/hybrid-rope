# 预注册：天花板/覆盖理论的五条可证伪预测

写入时间：**2026-09-11T22:5X EDT（本地）**，在任何下述未读结果落地之前。
伴生理论：`COVERAGE_CEILING_THEORY_20260911.md`（先读它的 §1 两条公理）。
数值脚本：`code/coverage_theory_20260911.py`（复算本页每个数）。

已确认部分（写理论时已知，不算预测）：s8 面板 32768 行四个 m≤1 臂全部
0.0/48（拆分读数于 2026-09-11 22:35 EDT 完成）；evq_shift 三臂 0.0；step_hi25 0.1121。

---

## P1（主判决）8× 振幅缩放表在 32768 上非零

**臂**：`scale8x_wide` = `1.5 × m_incr_beta(1.0, n=21, low=11)`（数组见
`code/tables/scale8x_wide.json`；sum_m=63.0，ν 严格递减，最大频谱间隙 1.69× 原生，
n(1.5)=7）。构造逻辑：天花板理论说 m≤1 家族在 u=log4(8)=1.5 处覆盖为 0
（实测全零），唯一修复是把天花板剖面整体抬高到目标深度——振幅 = log4(目标倍数)。

**判据（面板 = prepared_s8_01，72 行，与已测四臂同 row_id 配对）**：

| 结果 | 判定 |
|---|---|
| 32768 行正确数 ≥ 5/48 且 >0（任何配对显著性都不要求） | 理论主预测成立：天花板可以靠振幅外移 |
| 32768 行 = 0/48 | **理论在其主可操作处方上被证伪**（m≤1 全零另有原因，不是覆盖上限） |
| 0 < 正确数 < 5/48 | 弱支持：覆盖存在但每槽质量或窗内代价是次级瓶颈 |

同时报告 4096 行：预测落在 **[0.50, 0.78]**（窗内覆盖 uw(−2) 从 BM 的 15/25
降到 12/25，允许中等损失；若 <0.40 说明 +0.5 振幅的窗内代价被非可加性放大——
像 step_hi25 那样，同样是有信息的否定）。

**次臂（若愿意加）**：`shift8x_BM`（+0.5 平移版，`code/tables/shift8x_bm.json`）。
理论预测它 32768 同样非零但**不应优于 scale8x_wide**（它带 4.49× 频谱空洞，
step 家族证据说空洞有害）。两臂都测则构成 shift-vs-scale 的内部对照。

## P2 step42（Pro §9.3，RULER 350 跑中，未读）

`m_j = 1[j≥22]` 是**阶跃/空洞表**（gap 7.76× 原生，与 step_hi25 同类）。
预测 RULER ∈ **[0.10, 0.35]**，显著低于 BM 0.4167；若 ≥0.40 则空洞判据被证伪。

## P3 condEVQ（同跑，未读）

平滑无空洞（gap ~1.6×），带内二阶形状差异。预测 RULER ∈ **[0.40, 0.55]**，
落在 BM 平台内；不预测它超过平台成员（理论对二阶形状明确无分辨率）。

## P4 walk 剂量响应（chain_walk 排队中，未读）

BM→a1_b64 的 a∈{0.25,0.5,0.75}：两端的覆盖曲线差异是**同机制的渐进**
（n_int 12.0→13.5，各深度差 ≤1 槽）。预测：

- **单调、近线性，无内部拐点**（无 DEVELOPMENT_WIN）；
- interior a 相对两端连线的偏离 ≤ 3pp（超出即出现理论未预言的交互）。

## P6（P2 反例后新增，2026-09-11T11:30+08:00 写）阶跃位置扫描

P2 的实测（step_hi25 0.1121 < step42 0.4893）与覆盖读数（n_int 10.5 < 13.5）
同向。覆盖理论对整条 hi 轴的预测（脚本 §step-curve，可复算）：

| hi | 19 | 20 | 21 | **22（已测 0.4893）** | 25（已测 0.1121） |
|---|---|---|---|---|---|
| n_int | 16.5 | 15.5 | 14.5 | 13.5 | 10.5 |

**预测（350 行 16K 面板，`--steps 19,20,21`，runner 旗标已存在）**：
- `step_hi19/20/21` ∈ **[0.48, 0.62]**，不低于 step42，且至少一个超过 0.52
  （平台水平）；沿 hi 单调。
- 若实测随 hi 下移**变差**（<0.44），则"覆盖一阶"在阶跃族内部被证伪，
  阶跃的窗内职责代价超出 n_int 的收益——同样是干净判决。
- 4K 行（若加测）：预测随 hi 下移**恶化**（uw(−2) 从 25 掉到 19），
  与 holdout180 分层同型。

**注意**：P6 是 P2 失败后由修订理论派生的预测——它检验的是修订版
（覆盖一阶），不是原始版（空洞一阶）。chain_pro 里已排的 `--steps 22`
与已完成的 step42 同表，跑不跑皆可。

## P5 yarn_uniform（未排队，理论派生的新臂）

`m = uniform ε`（线性 m，band [15,32]，`m_turns(1,32,"linear")` 即近似）。
理论预测 RULER ∈ **[0.32, 0.48]**——与 BM 打平，**不是**介于 MrRoPE(0.07) 与
BM(0.42) 之间。理由：均匀 ε 的天花板铺开（n_int=11.99 = BM），MrRoPE 的凸剖面
才是覆盖灾难（n_int=9.08）。若实测 ≤0.20，则"MrRoPE 的失败=凸剖面覆盖空洞"这一
归因被削弱，YaRN→MrRoPE 在零训练下的解释需要另找。

---

## P4 判分（2026-09-11T11:40+08:00，walk 900 行全部落地后）

180 行面板（4096:60 / 16384:120），对照与端点复现全部逐字一致（walk_read.txt）。

| a | 0.25 | 0.50 | 0.75 | 1.00 |
|---|---|---|---|---|
| Δ@16384（分数） | +9.82 (t=3.73) | **+10.87** (t=3.28) | +9.42 (t=3.00) | +7.83 (t=2.16) |
| Δ@4096（分数） | −4.56 | −2.69 | −6.36 | −4.11 |

**判分：按字面失败，按精神部分成立。**

1. "interior 相对两端连线偏离 ≤3pp"——**失败**：a=0.5 处高于连线（+3.92）
   达 **+6.95pp**。我的预注册没写显著性限定，这是我的错。
2. 但内部最大值（a=0.5，+10.87）对端点 a=1（+7.83）的差只有 +3.04pp，
   配对 SE≈3.3 ⟹ **不显著**；16K 曲线是"平坦倒 U"，无显著拐点——
   与并发会话 WALK_PREREG 自己的"无 DEVELOPMENT_WIN"判定一致。
3. n_int 沿 a 单调（12.0→12.7→13.5）而 16K 分数在 ±3pp 内不单调：
   内部驼峰落在理论声明的二阶无分辨区。**该驼峰（中段混合带在 16K 上
   略优于全宽带端点）是理论未解释的真实点估计**，留作开放项。

## P7（跨模型判决，2026-09-11T12:00+08:00 写）Qwen 检索式仪器上的覆盖排序

**动机**：理论判定 Qwen 几何下 9 臂覆盖展幅与 OLMo 同量级（n(.5) 8→14，
sum_m 逐位吻合实测），几何不解释 Qwen 零效应；2× tail-NLL 是流畅度仪器
（`NLL_VS_TASK` 已证 NLL 可与任务反向）。**覆盖→分数的响应函数在 Qwen 上
是否平坦，是跨模型主张的唯一待决问题。**

**仪器（二选一，均为新代码，GPU 需持写会话执行）**：
(a) 检索式连续仪器：teacher-force **答案 token** 的 NLL（不是 tail-512），
    用现有 8 篇 65538-token 文档构造needle问答对，同进程 native 配对；
(b) Qwen 13-task RULER macro ~300 行（上游经 HF 镜像下载）。

**预测（排序，与效应大小无关）**：
```
mrpro (n_int 8.6)  <  beta_b1_BM (11.4)  <  beta_b3 (14.4) ≈ corner_a0b1 (14.1)
```
- 四臂排序与前两名间隔显著 ⟹ 覆盖机制跨模型成立，Qwen 零效应归因于
  tail-NLL 仪器（`NLL_VS_TASK` 机制适用）。
- 全平（配对 SE 内无序）⟹ **覆盖理论降级为 OLMo 的响应函数**，
  跨模型需要别的机制——同样按判决记录。
- 归档 36 行的深度侧符号（MrRoPE 在 131072 上 −7.3pp）已与该排序同向，
  但 SE 13pp 不足以作数。

## 纪律

- 本文件在服务器 `olmo_pro`（condEVQ/step42）、`chain_walk`、`chain_natural`
  产出读取之前提交；P2/P3 若在提交前已可读，以提交时间为准作废该条。
- 判定只按区间与方向读，不追加事后判据；区间外结果按表内语句诚实归类。
- GPU 只读：所有新臂只提供就绪命令与表文件（`code/tables/*.json`、
  `code/patch_mfile.py`），由持写权限的会话决定是否排队。

## 收尾时的时钟核对（2026-09-11T11:00+08:00 补记）

- 提交时刻：服务器钟 2026-09-11 11:05+08:00（本地 2026-09-10 23:05 EDT）。
- `pro_condEVQ` 于服务器钟 **10:24:11** 完成，早于提交 41 分钟 ⟹ **P3 按纪律作废**
  （预测为盲写：落点 0.4380 确实在预测区间 [0.40, 0.55] 内，但不计分、不宣传）。
- `pro_step42` 提交时 276/350 行在跑、`walk_a0p25` 100 行在跑 ⟹ **P2、P4 有效**。
- P1（scale8x_wide）、P5（yarn_uniform）未排队，有效。

## P2 判分（2026-09-11T11:15+08:00，结果落地后）

**预测 [0.10, 0.35]，实测 0.4893 —— 预测失败**；且对 BM 66胜/42负/242平（+7.26pp，t≈+2.4）。

失败方式的信息量：

1. **频谱空洞作为独立杀手的判据被证伪**——step42 与 step_hi25 同带 7.76× 空洞，
   一个 +7.3pp、一个 −30.5pp。空洞最多是二阶惩罚（step25 偏离家族趋势约 2×），
   不是一阶变量。
2. **我自己算出的 n_int 其实排对了这对**（13.5↔0.489 落在家族趋势中点）；
   预注册时用"空洞"的口头判据覆盖了自己的定量信号。教训入档：
   有数量时就赌数量，不要用故事推翻自己的数。
3. 修订后的理论（覆盖一阶 / 空洞二阶）已回写 `COVERAGE_CEILING_THEORY` §2；
   该修订是 P2 反例驱动的**事后**更新，不冒充事前预测。

新事实入 index：**零形状参数的阶跃表 m=1[j≥22]（S=42）显著超过部署 BM** ——
"必须平滑 ramp"不是定律；位置（=哪些高κ槽进平台）才是。

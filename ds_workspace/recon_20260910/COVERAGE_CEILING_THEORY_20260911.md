# 天花板/覆盖理论：用两个常数解释整场战役

**2026-09-11** · 伴生代码 `code/coverage_theory_20260911.py`（纯 numpy，复算本页每个数）
· 预注册 `COVERAGE_PREREG_20260911.md`（P1–P5，在未读结果前写入）

回答的问题：**YaRN→MrRoPE 是一个很小的改动，为什么效应巨大？
能不能用一个不含复杂参数的理论解释它，并从理论推出比 MrRoPE 更好的表？**

---

## 0. 一页答案

冻结模型每根槽的能力在训练时就封顶了：槽 j 只认识**转数 ≤ t_j(W)** 的相位模式
（W=4096 训练窗；t_j(W) = 651.9·θ^(−j/64)，从 652 转单调降到 0.13 转）。
压缩表 m_j 不改变槽认识什么，只改变**这份能力被投放到哪个距离段**：

```
槽 j 在深度 u = log4(d/W) 处可用  ⟺  m_j − min(κ_j, L) ≤ u ≤ m_j
κ_j = log4(t_j(W)/δ)     信号余量（δ=0.25 转≈90°，唯一标定常数；L≈2）
```

**m_j 就是天花板的位置**：每根活槽覆盖一段以 u=m_j 为顶、向下延伸 min(κ_j,L)
的深度窗。一张表 = 深度轴上的一组窗。任务分 ≈ 外推区 [0,1] 被多少窗覆盖
（记 n(u)）；快槽钉在 m≈0 保窗内局部职责是另一条硬约束。
（频谱空洞最初被列为结构性约束，**当晚即被 P2 的实测证伪并降级**，见 §2 与
预注册判分：带 7.8× 空洞的 step42 得 0.4893 > BM。）

**YaRN→MrRoPE 为什么"小改动大效应"**：ε 的形状就是天花板的分布。
ε 从均匀（YaRN，天花板铺满全区，n_int=11.99）改成 ∝k（MrRoPE，凸剖面，
70% 的带内天花板挤在 u<0.5，n_int=9.08，中段覆盖 12→8）——公式只动了一个
因子，整张 tiling 被重排。**部署 BM 的抛物线 ε（n_int=11.99）恢复了铺开
的 tiling，再加宽带（n_int 13.5→18.3）继续加密**。实测分数沿 n_int 单调：
0.07 → 0.14 → 0.23 → 0.42 → 0.50 → 0.54–0.56。

**比 MrRoPE 更好**：这个问题战役已经解决（BM/宽带家族 +34~+48pp）；本理论给出
的是**为什么**，以及下一代：**振幅 = log4(目标长度/W) × 剖面**——表应当随目标
外推倍数整体抬高。8×（32768）上现有全家 = 实测 0.0/48×4臂，理论处方
`scale8x_wide`（=1.5×宽带BM）是唯一有 8× 覆盖（n(1.5)=7）的无空洞表，已预注册。

---

## 1. 两条公理与两个常数

**公理 A（能力封顶）**：槽 j 在训练中见过的相位差全部满足转数 τ ≤ t_j(W)。
超出即分布外（OOD），模型对它的行为未定义——实测表现为噪声。

**公理 B（信号下限）**：槽在距离 d 上有信息量，需转数 ≥ δ（相位差 <90° 的槽
对任意两个位置给出几乎相同的内积，无分辨力）。

由 A、B，槽 j 压缩 m_j 后在深度 u 可用 ⟺ T_j = t_j(W)·4^(u−m_j) ∈ [δ, t_j(W)]。
移项即开头的窗口式。两个常数 δ=0.25 转、L=2 在敏感性网格
（δ∈[0.125,1]，L∈[1,10]）里 rho 变动 <0.02（脚本 §sensitivity）——**结论不依赖
它们的精确值**。

活槽共 39 根（j≤38；j≥39 的 t_W<δ 在训练中就近乎退化，压不压都一样）。
其中 j≤14 是窗内主力（t_W ∈ [37,652] 转，κ≥3.7），j∈[15,32] 是 18 根自由槽
（κ 从 3.45 递减到 0.61），j∈[33,38] 是 6 根"顶灯"槽（κ ∈ [0.03,0.61]）。

**外推区 [W,4W] 的深度宽度恰好 = 每根槽窗口的对数宽度量级** —— 所以表设计的
本质就是：把 18+6 根自由/顶灯槽的天花板摆进这一段，摆得越满、越用高 κ 槽，
分数越高。

## 2. 理论逐条对上已有实测（17 个 RULER 臂 + 14 个连续 NLL + s8）

全部数字由脚本重算，表数组与运行记录的 sum_m 逐位核对通过（9/9）。

| 实测事实 | 理论读法 | 状态 |
|---|---|---|
| **s8 面板 32768 行四臂全部 0.0/48**（BM/b3/wide_b1/wide_b4，4× 上相差 +12~14pp 的形状在 8× 全部归零、完全无差异） | m≤1 ⟹ n(1.5)=0：**m 的盒子是绑定约束，带内形状在 8× 无关紧要** | ✅ 理论在拆分读数前给出（部分样本外：理论只用 4× 数据标定） |
| evq_shift τ=0.5/1/2 全部 0.0/350 | m<0 ⟹ n(u)=0 对全部 u∈[0,1]（天花板在区外）＋慢槽加速 OOD | ✅ 完全崩溃被预测为零覆盖 |
| native 不可用（4×） | m≡0 ⟹ n(u>0)=0。EVQ_LIMIT 的 N=21 反例正是被 per-slot 天花板修掉的：native 的"可读槽"全部超训练边界 | ✅ |
| interp（m≡1）不可用 | n(u) 反而最大（~35）——被**窗内侧**杀死：u=−2 处 native 有 25 槽覆盖，interp 全部过度压缩归零，局部精细结构（相邻 token 相位差被压 4×）塌掉 | ✅（窗内侧是本理论最弱一环，见 §5） |
| step_hi25 = 0.1121（vs BM 0.4167），单槽代价却是免费的 | n_int 10.5 → 家族趋势期望 ~0.22，实测 0.112 —— 在趋势下方但同量级；**空洞（gap 7.76×）是二阶惩罚**：P2 实测后判明 | ✅ 修订（见下） |
| **step42（m=1[j≥22]，同 7.76× 空洞）= 0.4893，对 BM 66胜/42负 +7.26pp（t≈+2.4）** | n_int 13.5，恰在 BM(12.0↔0.417) 与 a1_b64(13.5↔0.538) 的趋势中点——**覆盖把阶跃族排对了**；空洞与平台级分数共存 ⟹ 空洞不是一阶杀手。阶跃位置 22 vs 25 的差别 = 槽 22–24（κ≈2.0–2.3 的长窗）进不进平台 | ✅ **P2 反例驱动的事实修订**：一阶=覆盖，二阶=空洞（step25 在低覆盖处偏离趋势 ~2×） |
| MrRoPE 0.0709 → b0.25 → b0.5 → BM 0.4167 → b2 → b3 0.5587 单调 | n_int 9.1 → 9.9 → 10.7 → 12.0 → 13.9 → 15.2 同序（rho=+0.926；n(0.3) rho=+0.941 为最佳单量） | ✅ |
| 宽带（lo 14→11）+12pp | 加进来的 3 根槽 11–14 是高 κ（2.4–2.9）长窗，加密中段 tiling：n_int 12.0→13.5 | ✅ |
| 平台五成员互相打平（C42V24/wide_b4/a1_b64/b3 差 ≤1.5 SE） | 平台成员 n_int 差 ≤1.7（13.5–15.2 内），理论分辨率本来就 ~±1 槽 | ✅（不自量力地分辨二阶） |
| holdout180 分层：4K −5.37pp / 16K +6.54pp | 振幅定则：m 幅度应 = log4(目标/W)。4K 目标 u=0，任何幅度>0 都是纯窗内代价；16K 目标 u=1，幅度 1 的表正好。**平台成员在用原生能力买外推能力——这正是振幅定则的内容** | ✅ |
| Qwen 两台仪器零效应（0.008 nats） | Qwen 侧 κ/几何不同（λ=0.3114），但更本质：Qwen 训练窗 32768、θ=1e6，其 4× 区的可用槽结构完全不同；理论对 Qwen 的预测（BM N=16 族内）与归档一致，跨模型比较被仪器分辨率卡住（CROSS_MODEL） | ○ 不新增主张 |
| C42 0.4367 vs C42V24 0.5440（同 S 差 10.73pp） | n_int 13.5/13.5 完全无分辨 | ❌ **理论明确不覆盖这对**（二阶形状效应，与 NO_STATIC_FUNCTIONAL 一致；诚实边界） |

## 3. "为什么这么牛逼"：形状=tiling，小公式≠小改动

ε ∝ k^a(n+1−k)^b 家族里，(a,b) 从 (1,0)（MrRoPE）到 (1,1)（BM）只是给 ε
乘了个对称抛物线因子——**但 m 是 ε 的累积和**，ε 的二阶变化直接变成 m 剖面的
曲率变化，而 m 剖面就是天花板分布：

| 表 | m 剖面形状 | 天花板分布 | n_int | 实测 |
|---|---|---|---:|---:|
| MrRoPE (1,0) | 凸（≈(k/n)²） | 70% 挤在 u<0.5，中段 8 槽 | 9.08 | 0.0709 |
| YaRN (0,0) | 线性 | 均匀铺开 | **11.99** | 未测 → **P5 预测 ≈0.42** |
| BM (1,1) | S 形 | 铺开且中段加密 | 11.99 | 0.4167 |
| wide BM | 同上+3 高κ槽 | 中段再加密 | 13.48 | 0.5384 |

**MrRoPE 论文语境（训练时用表）与我们的零训练语境不同**：零训练换表时，
凸剖面把中段覆盖砍半，就是灾难。YaRN 的均匀 ε 反而有与 BM 相同的 tiling——
所以理论给出一个未测的可证伪预测（P5）：**零训练 16K 上 yarn_uniform ≈ BM
（0.32–0.48），不介于 MrRoPE 与 BM 之间**。若实测 ≈0.2 以下，本归因被削弱。

## 4. 从理论推出的表（比 MrRoPE 好，且按目标长度参数化）

**设计规则只有一条：振幅 = log4(目标长度 / W)，剖面用已验证的宽带 BM。**

```
m*(j) = log4(s) · m_wideBM(j),   s = 目标长度 / 4096
```

- s=4（16384）：**精确等于已测的宽带赢家**——理论解释了它为什么赢。
- s=8（32768）：`scale8x_wide` = 1.5×m_wideBM。sum_m=63.0，ν 严格递减，
  无空洞（最大间隙 1.69× 原生；对照 +0.5 平移版有 4.49× 空洞，被判为劣——
  这正是 step 家族教训的应用），n(1.5)=7、n(0.95)=11，同时保住 4× 区。
  **这是全战役第一张在 8× 有非零覆盖的表**，就绪命令与表文件见
  `code/tables/`、`code/patch_mfile.py`，判据见预注册 P1。

为什么不是继续在 m≤1 盒子里调形状：**8× 全零已经判决了那条路**（§2 第一行）。
盒子的边界不是形状的边界，是覆盖范围的边界。

## 5. 边界与不自量力的部分（照旧不藏）

0. **P2 反例（当晚）**：预注册预测 step42 ∈ [0.10,0.35]，实测 **0.4893**（对 BM
   +7.26pp）——频谱空洞作为独立杀手被证伪（step25/step42 同 7.76× 空洞，一个
   −30.5pp 一个 +7.3pp）；覆盖 n_int 把这对排对（10.5/13.5）。**修订：覆盖一阶、
   空洞二阶**。同一晚的 `NLL_VS_TASK_20260911.md`（并行会话）给出互补约束：
   带内重分配在窗内**均值 NLL 上改善**（−0.023, t=−2.9）而**任务准确率变差**
   （−10.6pp, t=−2.45）——本理论的"窗内职责"一侧必须按任务口径、不能按
   NLL 口径陈述，§2 的"约束松弛"行也应读作 NLL 口径。
1. **二阶形状效应无分辨率**：C42/C42V24、平台成员互比、condEVQ±——理论只给
   一阶（谁有覆盖、覆盖多少），±2pp 以内明确放弃。
2. **窗内侧（u<0）的定量化最弱**：interp 的失败读法用了过度压缩界 L≈2；
   δ、L 的敏感性稳定，但"过度压缩=浪费/干扰"的机制解释停留在模型层面。
3. **n_int 与 S 在家族内共线**（rho 0.926 vs 0.924）：理论的证据力不在相关性，
   而在结构预测（零 vs 非零、空洞、8× 塌零、分层交换）——这些是 S/N/12 个
   静态泛函全部给不了的。
4. **8× 全零的预测部分样本外**：理论标定只用 4× 数据（holdout180 分层在先），
   但我在读 s8 拆分前只知道合并分 0.2447——诚实记为"半预期"。
5. P1–P5 全部未读；理论的可操作部分押在 P1（scale8x_wide 8× 非零）上，
   0/48 即证伪处方。

## 5b. 跨模型（2026-09-11 补，回答"更好必须跨模型成立"）

**先认账**："+34~+48pp" 是 OLMo-only。Qwen 侧两台仪器的读数是**零**
（连续 0.008 nats vs OLMo 0.83；36 行 RULER 归档自判 NO_LONG_GAIN，SE≈13pp）。
"跨模型更好"目前**未证明**——但也未被否定，是卡在仪器上。以下用理论把它拆开。

**理论对 Qwen 的数值判定（`qwen` 段可复算，sum_m 与实测报告逐位吻合）**：
Qwen2.5-1.5B 几何（θ=1e6，W=32768，带[23,40]，λ=0.3114，活槽 47 根）下，
9 个已测臂的覆盖展幅**与 OLMo 同量级**：n_int 从 8.59（mrpro）到 14.35
（beta_b3）/14.10（corner），n(.5) 从 8 到 14——正是 Qwen 2× 仪器实际采样
的深度。**⟹ 几何不解释 Qwen 零效应：表与表之间的覆盖差一样大。**
零只能来自响应侧：要么 (a) 仪器测错了东西（tail-512 NLL 是"流畅度"不是
"检索"；当晚 `NLL_VS_TASK` 恰好证明 NLL 与任务可以反向——而 Qwen 仪器跑在
2× 而非 4×，还是语料妥协的产物），要么 (b) Qwen 冻结权重对覆盖差的响应
本身平坦（学习到的位置处理更强）。**(a) 与 (b) 当前不可分。**

**归档 36 行里被淹没的一个理论一致信号**：分长度看，MrRoPE−BM 在
cap=32768（窗内）+0.044、cap=131072（4×）**−0.073**——深度侧的符号与
覆盖排序一致（mrpro n_int 8.6 < BM 11.35），窗内侧是 BM 付压缩代价的
交易（与 OLMo holdout180 分层同型）。 pooled +3.38pp 由窗内侧驱动，
SE 13pp 下两者都不显著。

**⟹ 跨模型主张的正确形态**：
1. 覆盖/天花板框架本身是模型无关的（两条公理只用训练窗与 θ）；
   **待验证的是"覆盖→分数"的响应函数在 Qwen 上是否平坦**。
2. 判决实验 = P7（检索式连续仪器：teacher-force 答案的 NLL，不是 tail NLL；
   或 ~300 行 13-task RULER macro——上游数据经 HF 镜像下载）。**预注册
   排序预测**：mrpro(8.6) < b1_BM(11.4) < b3(14.4) ≈ corner(14.1)——
   若 Qwen 检索仪器上仍全平，则覆盖理论降级为"OLMo 的响应函数"，
   跨模型需要别的机制；这也是干净判决。
3. 在 P7 之前，"比 MrRoPE 更好"的跨模型版本**只能写成开问题**，
   不能引用 +34~48pp 外推。scale8x/step42 的处方形式是模型无关的
   （带由模型自己的 turn-window 规则、振幅由目标定），但同样继承
   这个未验证的响应问题。

- **四角/Lε forcing**（FOUR_CORNERS）：forcing 是 m 的差分算子，本理论是它的
  语义层——forcing 形状 ⟺ m 曲率 ⟺ 天花板分布。"形状不预测"（NO_STATIC
  §六）说的是 forcing 坐标下的静态形状；天花板分布是**带 per-slot 训练边界
  解释的**形状，在家族内给出单调序（且不与"形状相关 0.899 结果相反"那对
  冲突——那对本理论也不分辨，属二阶）。
- **EVQ_LIMIT 的可读窗 N**：N 是 n(u) 在 u=1 的单深度切片＋全局带界；本理论
  把它推广成整条 n(u) 曲线，修掉它的 native 反例（per-slot 边界），并加上
  空洞与窗内两个约束。他们的"条带斜率 ≡ ν 退化斜率"恒等式就是本理论里
  窗口向下延伸率与 κ 的关系。
- **约束松弛**（CONSTRAINT_IS_SLACK）：带内窗内代价平坦 ⟸ 活槽在窗内
  深度的覆盖弹性大；代价在带外 ⟸ 破坏窗内主力槽或制造空洞。同一个事实，
  本理论给出坐标。

## 7. 出处表

| 陈述 | 状态 |
|---|---|
| 窗口式 m_j−min(κ_j,L) ≤ u ≤ m_j、两公理、两常数 | 推导＋机器复算 |
| 表数组重建（9 个 sum_m 逐位对上运行记录） | 证 |
| n_int/n(.u) 与 17 臂 RULER 的单调与 rho | 量（含共线警告） |
| gap（频谱空洞）分离 step 家族 | 量（7.8× vs ≤1.9×） |
| 8× 全零、evq_shift 全零、native/interp 不可用 | 实测（前者半预期） |
| yarn_uniform ≈ BM、step42 差、condEVQ 平台内、walk 无拐点 | **未测**（P2–P5） |
| scale8x_wide 8× 非零 | **未测**（P1，主判决） |

# 预注册：长程增益的**跨任务族泛化**（写在读数之前）

**2026-09-11 ~10:30** · OLMo-2-0425-1B-Instruct · 面板 `prepared_natural_union`（391 行，**从未跑过**）

> **本文档在【该面板任何一行结果被读取之前】提交。**

---

## 一、要问的问题

180 行 RULER 面板（6 个**合成**任务）分层给出：@16384 **+6.54pp**（t=+2.41），@4096 **−5.37pp**（t=−2.23）。

> **这个长程增益是【一般能力】，还是【合成检索任务】的特性？**

**为什么这是现在最该问的**：`verdict()` 把三个成员判为 TRADEOFF——交易本身已测到。
但一个交易值不值得，取决于**长程那一端是不是真的能力**。若它只在 NIAH 类合成任务上出现，
那它就是检索基准的产物；若它在**自然阅读理解**上也出现，那它是一个带代价的真实长上下文能力。

**这个区分决定整个结论的性质**，而它可以用**从未跑过的数据**直接回答。

## 二、材料：`prepared_natural_union`（391 行，全 @16384）

| 任务族 | 行 | max_new_tokens |
|---|---|---|
| `multifieldqa_en` | 150 | 64 |
| `narrativeqa` | 61 | 128 |
| `qasper` | 60 | 128 |
| `2wikimqa` | 60 | 32 |
| `hotpotqa` | 60 | 32 |
| **合计** | **391** | |

来源：`prepared_natural_01`(180) + `prepared_natural_extra_01`(211) 的并集，**行 id 去重后 391**。
**与本 campaign 用过的任何面板重叠：0**（此前从未被任何臂跑过，已核验）。
全部是 LongBench 风格**自然**长文问答，**与 RULER 的合成检索完全不同族**。

**打分**：用与 RULER 完全相同的 `ruler_bench.score`（reference 子串匹配；多参考取命中比例）。
**同一个打分器对所有臂**，所以比较有效。346/391 只有 1 个参考。

**功效**：按 180 行面板的 16384 分层 SE≈2.71pp（n=120）外推，
**391 行 ⟹ SE ≈ 2.71·√(120/391) = 1.50pp**，可判 **~3pp**。

## 三、臂（4 个，全部是**已存在的**表——不引入任何新表）

| 臂 | 构造 |
|---|---|
| `nat_bm` | `--betas 1.0`（部署表端点，本进程现测） |
| `nat_b3` | `--betas 3.0`（`b3_lo14`） |
| `nat_a1b64` | `--wide-betas 1.0`（`turns_a1_b64`） |
| `nat_b4w` | `--wide-betas 4.0`（`wide_b4`） |

## 四、★ 判据（写死）

**主统计量**：**pooled Δ = 逐行取三成员 (member − BM) 的平均**，配对其 SE
——**与 180 行 RULER 完全相同的统计量**，只是换了任务族。可比性是设计的。

**门控（先过才看主统计量）**：
> **BM 的绝对准确率必须 ≥ 0.10。**
> 若 BM < 0.10，任务对这台 1B 模型已在地板上，Δ 由"两边都答不出"的噪声支配，
> **本实验判为不可判，报告并停止**。

**主判据**：

| pooled Δ | 附条件 | 判定 |
|---|---|---|
| **≥ +3.0pp 且 t ≥ 2** | 门控过 | **长程增益跨任务族泛化**——它是（带代价的）真实长上下文能力，不只是检索基准的产物 |
| **\|Δ\| ≤ 1.5pp 且 SE ≤ 2.0pp** | 门控过 | **增益不泛化**，是合成检索任务族特有的 ⟹ TRADEOFF 只在 RULER 上成立 |
| 其它 | — | **仍未决**：报区间并停止 |

**必须同时报的**：每任务族单独的 Δ（**5 个族各自的方向**比合并数更说明问题——
若只有一个族在动，合并数会掩盖）；三成员互比；绝对准确率。

## 五、我的预测（先写下来）

我预期 **Δ 为正但小于 RULER 的 +6.54pp**，大概 **+1 ～ +4pp**。
理由：自然问答对**精确位置**的依赖弱于 NIAH（NIAH 是纯粹的位置检索），
所以同一个机制（压缩慢槽以延长可用跨度）在这里的收益应该更小。
**若落在判据的中间带（+1.5 ～ +3.0），我会如实报"弱正向但达不到判据"，不追加行数。**

## 六、成本与排队

4 臂 × 391 行 ≈ **105 分钟**，排在 `chain_walk` 之后（卡上已有三条链）。

## 七、本实验**不**做的事

- 不引入新表、不做形状搜索
- **不因为结果是负的就去换任务**——391 行是"现成且从未跑过"的全部自然数据
- 不改判据（本文档先提交，`git log` 可查时间戳）

---

## 八、补：按 TRADEOFF 分解**下调**我的预测（面板仍未读）

`TRADEOFF_ASYMMETRY_20260911.md`（事后分解，零 GPU）发现：**@16384 的 +6.54pp 几乎全部来自
"逐项召回"的部分分**——把每行二值化（整行答对）后合并只剩 **+1.11pp（t=+0.47）**。
增益集中在多参考任务（`fwe` 3 项 +13.3、`niah_multiquery` 4 项 +27.9），
而二值任务（1 个参考或取 max）平均只有 **+0.6pp**。

**而本面板 391 行里 346 行（89%）只有 1 个参考 ⟹ 基本是二值口径。**

> **⟹ 我把 §五 的预测从 "+1～+4pp" 下调为 "≈ 0 ～ +2pp"，即预测落在"不泛化"那一格。**

**这不是改判据**（§四 的三个阈值一字不动），是**改我的预测**，且改在读数之前。
若结果真的落在 +0～+2pp，那它同时确认两件事：
（a）长程增益确实只是逐项召回现象；（b）"不泛化"作为结论成立。
**若反而 ≥ +3pp，则说明二值化分解低估了增益，那本身是更强的结果。**

---

## 九、补：BM 基线已测完，门控通过，但**两个族在地板上**（**成员臂仍未读**）

`nat_bm` 跑满后（330 行时的读数，最终以 391 行为准）：

| 族 | n | BM acc |
|---|---|---|
| `hotpotqa` | 60 | 0.3167 |
| `2wikimqa` | 60 | 0.2500 |
| `qasper` | 60 | 0.1528 |
| **`multifieldqa_en`** | **150** | **0.0225** |
| **`narrativeqa`** | **61** | **0.0164** |
| **宏平均** | 391 | **0.1517** |

**门控（§四：BM ≥ 0.10）⟹ 通过。**

> **⚠ 但 211/391 = 54% 的行来自两个几乎在地板上的族**（0.0225 / 0.0164）。
> **这削弱了功效**：§二 的 SE≈1.5pp 是假设全部行都携带信号估的。
> 若只有三个非地板族（181 行）携带信号，**有效 SE ≈ 2.7·√(120/181) ≈ 2.2pp，
> 可判 ~4.4pp**——仍足以看见 RULER 那个 +6.54pp，但**看不见更小的效应**。

**处置：判据阈值一字不改**（§四 三个阈值照旧）。**但报告时必须逐族给出**
（§四 本来就要求），**并且要说明宏平均被两个地板族稀释了多少**。
**若结果落在"仍未决"，这一条就是"为什么仍未决"的解释之一**，而不是再跑一次的理由。

# 180 行 held-out 预注册判定：**仍未决**（+2.57pp, t=+1.27）——但分层揭示「原生 vs 外推」交换

**2026-09-11** · OLMo-2-0425-1B-Instruct · 180 行 × 6 个**从未用于选择**的任务（每任务 30 行），同 row_id 配对
预注册 `62cef88`（首个臂完成之前提交）· 读取脚本 `9182732`（`holdout180_read.py`，服务器副本 md5 与仓库一致 `7b84ed1e…`）

---

## 一、主判定（预注册写死，不得事后改）

| | 值 |
|---|---|
| **pooled Δ（三成员 − BM，逐行平均）** | **+2.57pp** |
| SE | 2.02pp |
| t | **+1.27** |
| W/L/T | 44/43/93 |
| 95% CI | **[−1.4, +6.5]pp** |

**判定分支：「其它 → 仍未决：报告区间并停止。不得为了移动它而再加行数。」**

- 「≥+5pp 且 t≥2」未触发（2.57 < 5，1.27 < 2）
- 「≤+1.5pp 且 SE≤3pp」未触发（2.57 > 1.5）
- **⟹ 诚实的答案就是这个界：pooled +2.57pp，CI [−1.4, +6.5]。此问题按预注册纪律关闭，不再加行。**

**预注册 §五 的自评预判核对**：我写「pooled Δ 会落在 **+2～+5pp**，弱正向但达不到判据」——
**实测 +2.57pp，命中。**（这是本战役少数命中的预测，且是事前写下的。）

## 二、逐成员（次要，预注册允许报告）

| 臂 | acc | Δ vs BM | SE | t | W/L/T | 72 行时 |
|---|---|---|---|---|---|---|
| `b3_lo14`（beta_b3p0） | 0.5714 | **−0.41pp** | 2.25 | −0.18 | 32/30/118 | −0.00pp |
| `turns_a1_b64`（wide_b1p0） | 0.6140 | +3.85pp | 2.56 | +1.50 | 43/19/118 | +4.40pp |
| `wide_b4`（wide_b4p0） | 0.6181 | **+4.27pp** | 2.50 | +1.71 | 34/30/116 | +6.94pp |
| 部署 BM（beta_b1p0） | 0.5755 | — | — | — | — | 0.5350 |

成员互相：`a1_b64` − `wide_b4` = −0.42pp（t=−0.16，**打平**）；
`b3_lo14` − `wide_b4` = **−4.68pp（t=−2.16，显著更差）**；`b3_lo14` − `a1_b64` = −4.26pp（t=−1.83）。

## 三、★ 分层（次要，预注册明确警告过「合并数可能掩盖分层结构」）

| 分层 | n | BM acc | pooled Δ | SE | （t） |
|---|---|---|---|---|---|
| **cap=4096（原生长度）** | 60 | **0.797**（近天花板） | **−5.37pp** | 2.41 | （−2.23） |
| **cap=16384（外推长度）** | 120 | 0.465 | **+6.54pp** | 2.71 | （**+2.41**） |

逐任务×cap（Δpp vs BM）：

| task | cap | BM | b3_lo14 | a1_b64 | wide_b4 |
|---|---|---|---|---|---|
| fwe | 4096 | 0.600 | −10.0 | +3.3 | −10.0 |
| fwe | 16384 | 0.417 | +13.3 | **+15.0** | +11.7 |
| niah_multikey_2 | 4096 | 1.000 | +0.0 | +0.0 | +0.0 |
| niah_multikey_2 | 16384 | 0.400 | −10.0 | −10.0 | **+10.0** |
| niah_multiquery | 4096 | 0.900 | −7.5 | −10.0 | **−32.5** |
| niah_multiquery | 16384 | 0.412 | +23.8 | **+30.0** | +25.0 |
| niah_single_2 | 4096 | 1.000 | +0.0 | +0.0 | +0.0 |
| niah_single_2 | 16384 | 0.900 | −10.0 | +0.0 | +10.0 |
| qa_1 | 4096 | 0.600 | **+10.0** | +0.0 | **+10.0** |
| qa_1 | 16384 | 0.550 | +5.0 | +0.0 | +10.0 |
| vt | 4096 | 0.680 | **−26.0** | −18.0 | −6.0 |
| vt | 16384 | 0.110 | −9.0 | **+12.0** | −9.0 |

**读法（假设生成，不是判定——分层是次要统计量，且逐层 t 是事后选择）**：

> 零训练换表在 held-out 上呈现**方向相反的两侧**：
> **原生长度（4K，BM 已近天花板）付出 −5.4pp；外推长度（16K，BM 只有 0.465）拿到 +6.5pp。**
> 这与 §4.0 理论一致：宽带表把压缩推进槽 11–14（原生尺度的位置槽，带外代价），
> 换来慢端展开（远距离可分辨性）。**平台成员是在用原生能力买外推能力。**
> 若成立，部署规则应当是**长度条件的**，而 P1 的 d×b×n 机制实验正是对此的受控检验
> （Y1：只拉远距应保持局部；Y3：局部间隔 vs 全局跨度应有不同敏感响应）。

注意反面细节：4K 侧的损失集中在 **BM 饱和的任务**（multiquery@4K −32.5 是最痛的一格；
multikey_2/single_2@4K 全为 0.0——天花板下无处可去只能掉）。16K 侧的收益集中在
**检索型任务**（fwe/multiquery +11.7～+30.0），与选择面板的机制签名（远距离检索）同型；
`vt@16384`（BM 0.110）对所有人仍近地板。

## 四、解空间收缩（本轮判决买到的）

1. **`b3_lo14`（窄带 b 轴内点、「唯一理论闭环」）出局**：两块独立 held-out（72 行 −0.00、180 行 −0.41）
   都判它与 BM 无差，且 180 行上**显著差于** `wide_b4`（t=−2.16）。
   **「四角框架 → b 轴内点 → 面板确认」的闭环降级为选择面板伪影。**
2. **幸存者共性是「频带加宽 lo 14→11」，不是 ramp 形状**：`a1_b64`（b=1）与 `wide_b4`（b=4）
   在两块 held-out 上互相打平、方向一致为正；b 轴在 held-out 上不可分辨。
3. **「平台超过部署 BM」的头条维持【未被证明】**，且按预注册**此问题已关闭**——
   界就是 +2.57pp [−1.4, +6.5]。仍成立的头条只有：**MrRoPE 显著差于部署 BM**（跨仪器）。
4. **EVQ 冻结替换全线死亡**（同进程实测，见 §五）：τ=0.5 与 τ=1.0 都是 **accuracy 0.0**
   （比 MrRoPE 的 7.09% 还低）。核心结论 §5 的预注册预测（外推要更慢时钟而 EVQ 加速时钟；
   全局分布外改动）**确认**。EVQ 仅存路线 = 匹配训练（E3 → 两轨计划 P2）。
5. **连续仪器 @16384 与 held-out 16K 层同向**（宽带臂显著优于 BM）——
   「连续仪器比 RULER 选择面板更会选」（index #8b）获得第二次支持，但仍待预注册检验。

## 五、EVQ τ 扫描实况（P0-C / 增量 E，`chain_serial`）

| 臂 | acc | sum_m | eos | vs MrProBM | 秒 |
|---|---|---|---|---|---|
| `evq_shift_t0p5` | **0.0** | −6.157 | 26/350 | 0胜/174负/176平 | 1893 |
| `evq_shift_t1p0` | **0.0** | −22.952 | 50/350 | 0胜/174负/176平 | 1642 |
| `evq_shift_t2p0` | 跑中（表构造 sum_m=−72.22） | | | | |

**两臂 0 胜、逐行全零——不是「差一点」，是输出层崩溃**（eos 只有 26–50/350，多数行不收敛于答案格式）。

## 六、队列事故与修复（2026-09-11 09:47）

- **`chain_protables.sh` 死锁**：其等待条件 `pgrep -f "chain_holdout18[0]"` 被**监控 tail 进程**
  （命令行含 `chain_holdout180.log`）永久命中 → holdout180 09:35 完成后 pro_tables 不接力。
- **`chain_pro.sh` 的同类隐患**：等待模式 `chain_seria[l].sh` 会命中 07:57 遗留的启动器
  bash -lc（命令行含该字符串）→ chain_serial 排空后 chain_pro 也永远不会醒。
- **处置**：杀掉三个 stale 进程（44051/34952/39750；两个 chain 工作进程 34954/39752 因 setsid
  挂到 init 下存活），**直接启动 pro_tables**（PID 45781，`--pro-tables condEVQ,step42`，
  首臂 condEVQ sum_m=42.0 与表构造自检一致）。GPU 恢复双进程 100%。
- **教训（写进纪律）**：`pgrep -f` 等待模式必须排除监控/启动器进程（用 `[x]` 括号技巧不够——
  tail 的**参数**里也有完整文件名）。后续链改用「检测产物文件存在」或 `flock` 而不是 pgrep。

## 七、仍在队列（P0 收尾）

| 项 | 位置 | 状态 |
|---|---|---|
| `evq_shift_t2p0` | chain_serial 跑中 | ~09:57 完 |
| **gain 1.0 vs 1.1386（RULER 350）** | chain_serial 下一站 | 排队 |
| `pro_tables condEVQ,step42`（RULER 350） | 直接启动 45781 | 跑中，2 臂 ≈1h |
| `h22`（--steps 22） | chain_pro（等 chain_serial） | 排队 |
| `gain2x2`（a1_b64/mrpro/b3 × g1.0/gY） | chain_pro | 排队 |

以上全部跑完后 P0（Pro 的纯 GPU 增量）关闭；下一杠杆按两轨计划是 **P1 机制实验（d×b×n）**——
它检验的正是本文 §三 的交换假设。

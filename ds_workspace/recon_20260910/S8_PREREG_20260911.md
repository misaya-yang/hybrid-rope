# 预注册：长度轴检验（32768，写在读数之前）

**2026-09-11 ~10:00** · OLMo-2-0425-1B-Instruct · 面板 `prepared_s8_01`（**从未被读过**）

> **本文档在【任何一行 `prepared_s8_01` 的结果被读取之前】提交。** 判据在此写死。

---

## 一、为什么要做：180 行给出的是一个**交易**，不是一个空结果

预注册（`62cef88`）的主统计量判为 **UNRESOLVED**：pooled Δ = **+2.57pp**，SE 2.02，t=+1.27。
按规则"报告区间并停止"——**主统计量到此为止，不再加行**。

**但分层（次要项，预先就要求报）给出了完全不同的图景：**

| length_cap | n | BM acc | pooled Δ | SE | t |
|---|---|---|---|---|---|
| 4096 | 60 | 0.797 | **−5.37pp** | 2.41 | **−2.23** |
| 16384 | 120 | 0.465 | **+6.54pp** | 2.71 | **+2.41** |

**而且不是任务构成造成的——同一个任务内部翻号：**

| 任务 | cap | BM | `b3_lo14` | `turns_a1_b64` | `wide_b4` |
|---|---|---|---|---|---|
| `niah_multiquery` | 4096 | 0.900 | −7.5 | −10.0 | −32.5 |
| `niah_multiquery` | **16384** | 0.412 | **+23.8** | **+30.0** | **+25.0** |
| `fwe` | 4096 | 0.600 | −10.0 | +3.3 | −10.0 |
| `fwe` | **16384** | 0.417 | **+13.3** | **+15.0** | **+11.7** |

**三个成员在同一个任务内、跨长度一起翻号。** 这不像抽样噪声（噪声不会让三个独立构造的表同向翻转）。

> **⟹ 假设：平台成员在【短上下文】上损失、在【长上下文】上收益——它们把预算从窗内换到了窗外。**
> **这恰好就是用户提的那个 KKT 权衡本身（窗内损失 vs 外推能力），它不是抽象，是可测的。**
> **而且它解释了为什么 180 行的合并数是零：两个 cap 被合在一起，符号相反、互相抵消。**

## 二、材料：`prepared_s8_01`（零新数据生成）

| | 行 | 任务 | 与已读数据的关系 |
|---|---|---|---|
| `cap=4096` | 24 | 同六任务各 4 | **与 `holdout180` 行 id 重叠 24/24 ⟹ 内置复现对照** |
| `cap=32768` | **48** | 同六任务各 8 | **完全未读**（与选择面板重叠 0，与 180 行面板重叠 0） |

**这是唯一现成的第三个长度点**，且它是为 s=8 YaRN 对照准备的、从未被这个 campaign 跑过。

**为什么 4096 的 24 行重叠是好事而不是污染**：它们给出**同面板内的复现检验**——
如果这 24 行重跑得不到 ≈−5.37pp，说明面板/仪器在两次之间变了，
**那么 32768 的任何数都不可比**。这是免费的自校验，见 §四。

## 三、臂与预测（写在读数之前）

| 臂 | 构造 |
|---|---|
| `s8_bm` | `--betas 1.0`（部署表端点，**本进程现测**，该面板无归档端点） |
| `s8_b3` | `--betas 3.0` |
| `s8_a1b64` | `--wide-betas 1.0` |
| `s8_b4wide` | `--wide-betas 4.0` |

**我的预测（先写下来，事后核对我的直觉）**：
pooled Δ(32768) **≈ +4 ～ +12pp**，即**不弱于** 16384 的 +6.54pp。
理由：如果这是长度交易，收益应随外推倍数（4× → 8×）单调。
**我也有一个具体的反面担忧**：32768 = 原生窗口的 8×，BM 可能已接近地板，
那时 Δ 可能由"两边都退化、退化速率不同"造成，而不是可用的检索能力。

## 四、★ 判据（写死，事后不得改）

**主统计量**：pooled Δ = 逐行取三成员 (member − BM) 的平均，配对其 SE —— **与 180 行那次同一个统计量**，只是换长度。

**门控（先过才看主统计量）**：
> **`cap=4096` 的 24 行必须重现 −5.37pp 的量级**（落在 −5.37 ± 2·SE 内，SE≈2.4 ⟹ 约 [−10.2, −0.6]）。
> **不过门 ⟹ 面板或仪器变了，本实验作废，报告并停止。**（这是防"两次跑的不是一回事"）

**主判据**：

| pooled Δ(32768) | 附条件 | 判定 |
|---|---|---|
| **≥ +6.0pp 且 t ≥ 2** | **且**成员绝对准确率 ≥ 0.15 | **长度交易成立**，第三个长度点确认；180 行的合并零是 cap 混合造成的 |
| **≤ +2.0pp** | — | **长度单调性不成立**；16384 的 +6.54pp 是任务抽样产物，假设死 |
| 其它 | — | **仍未决**：报告区间并停止 |

**绝对准确率地板（不是装饰，是判据的一部分）**：
若 BM@32768 < 0.15，则**加一条**：成员绝对准确率必须 ≥ **0.15**，否则判"两边都崩"，
**即使 Δ 很大也不得称为收益**。——在 0.05 的地板上 +20pp 不是能力。

**次要（报告，不作判据）**：
- 每任务 × 32768
- 三成员互比（区分"共享一个效应"还是"某一个扛着"）
- 4096 与 32768 的**同面板**对比（这是内生的长度轴，无面板混淆）

## 五、成本

4 臂 × 72 行（48@32768 + 24@4096）。32768 的 prefill 约 8× 于 4096，
估计 **≈45 分钟**。与 `chain_serial`/`chain_pro`/pro 表并行时需盯显存（32768 激活更高）。

## 六、本实验**不**做的事

- **不移动 180 行的主统计量**（那个已判 UNRESOLVED，规则说停止）
- **不加任何行**给已有问题；这问的是一个**新问题**（Δ 是否依赖长度）
- 不改判据（本文档先提交，`git log` 可查时间戳）
- 不构造任何新表

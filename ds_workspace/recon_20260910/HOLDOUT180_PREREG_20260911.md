# 预注册：180 行 held-out（写在读数之前）

**2026-09-11 ~09:05** · OLMo-2-0425-1B-Instruct · 链 `chain_holdout180.sh`

> **本文档在【任何一行 180 行面板的结果被读取之前】提交。** 判据在此写死，事后不得改。

---

## 一、为什么要做

72 行 held-out 给出：

| 臂 | Δ vs BM | t | SE |
|---|---|---|---|
| `b3_lo14` | −0.00pp | −0.00 | — |
| `turns_a1_b64` | +4.40pp | +1.11 | ~4.0pp |
| `wide_b4` | +6.94pp | +1.59 | 4.36pp |
| **合并（三成员）** | **+3.78pp** | **+1.22** | — |

**这足以杀掉选择面板的 +12～14pp，但不足以判断一个更小的效应是否存在**——
最大臂的 SE 是 4.36pp，**72 行排除不了 ±10pp 以内的任何东西**。

而 72 行也不是谁选的数——它是**一个面板碰巧有的行数**。

## 二、材料

**同样那 6 个选择面板从未用过的任务**（`niah_single_2` / `niah_multikey_2` /
`niah_multiquery` / `vt` / `fwe` / `qa_1`），三个现有面板的并集：

| 来源 | 行 |
|---|---|
| `prepared_ruler_holdout_01` | 72 |
| `prepared_ruler_holdout_02` | 72 |
| `prepared_ruler_06` | 36 |
| **并集** | **180**（每任务 30，caps 60×4096 + 120×16384） |

**与选择面板任务重叠：无**（已程序化核验）。
**行 id 冲突：0**（已在合并时消歧并加前缀，避免配对静默丢行）。

**SE 外推**：按 1/√N，4.36pp / √2.5 = **≈2.8pp** ⟹ **t=2 可判 ~5.5pp。**

**为什么不更多**：三个面板就是全部现有的；再造就得重新生成 RULER 提示，
**那会换掉任务抽样、破坏"同样这六个任务"的可比性**。

## 三、臂（4 个，总成本 ≈30 min）

| 臂 | 构造 |
|---|---|
| `h180_bm` | `--betas 1.0`（部署表端点，**本进程现测**，并集面板无归档端点） |
| `h180_b3` | `--betas 3.0`（`b3_lo14`，选择面板冠军） |
| `h180_a1b64` | `--wide-betas 1.0`（`turns_a1_b64`） |
| `h180_b4wide` | `--wide-betas 4.0`（`wide_b4`） |

## 四、★ 判据（写死）

**主统计量**：**pooled Δ = 逐行取三成员 (member − BM) 的平均**，配对其 SE。
（合并是合法的：**被检验的假设"平台成员优于部署表"在合并之前就已陈述**。）

| pooled Δ | SE | 判定 |
|---|---|---|
| **≥ +5pp 且 t ≥ 2** | — | **效应是真的**；72 行那次是**功效不足**，不是阴性 |
| **≤ +1.5pp 且 SE ≤ 3pp** | ≤3pp | **效应在噪声内**。**"调参不能泛化"从"没测出来"升级为【已确立的结论】** |
| 其它 | — | **仍未决**：报告区间并停止。**不得为了移动它而再加行数**——到那一步，诚实的答案就是那个界 |

**次要（报告但不作判据）**：每个成员单独、以及成员之间的对比——
它们区分「三成员共享一个效应」还是「某一个成员扛着它」。

**还必须报的**：4096 vs 16384 分层。72 行上面板的分层差异极大
（`vt` 0.700→0.100、`niah_multikey_2` 1.000→0.375），**合并数可能掩盖分层结构**。

## 五、我预判会看到什么（也写下来）

**我先写自己的猜测，以便事后核对我的直觉有多准**：我认为 pooled Δ 会落在
**+2 ～ +5pp**，即「弱正向但达不到 +5 的判据」。理由是 72 行上三个臂给出 −0.00/+4.40/+6.94
的**梯度**（不是零），但选择面板的 +12～14pp 明显被放大了。

**如果落进"仍未决"，我不会再加行数**——按 §四 的规则，那时的答案就是那个界。

## 六、本实验**不**做的事

- 不引入任何新表（三个臂都是已测过的）
- 不做任何形状搜索
- 不改判据（本文档先提交，`git log` 可查时间戳）

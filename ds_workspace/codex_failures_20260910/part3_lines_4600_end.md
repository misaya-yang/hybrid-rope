# Codex transcript failure mining — PART 3 (lines 4600–6796, end of file)

Source (strictly read-only): `/Users/yang/.codex/sessions/2026/09/08/rollout-2026-09-08T09-59-44-01a08151-4eb4-7572-8865-b321956dda94.jsonl`
Shard time span: **2026-09-08T21:30:23.829Z → 2026-09-09T00:59:59.063Z** (~3.5 h). All timestamps are UTC as written in the file.
Sibling shards cover lines 1–4600; nothing in this file is duplicated from them.

---

## (a) 10-line summary

1. The shard's own arc: final hours of the *native sparse-position* line, then a hard user-imposed deadline, then a GPU-off literature/CPU-theory phase, then a forced stop.
2. **Deadline**: user set a two-hour window at 2026-09-08 22:13:46 UTC ending 2026-09-09 00:13:46 UTC (L5067 / restated L6282). It was not to be reset (L6166, L6278).
3. **Consequential failure #1 — the final frozen 32-row assay killed the quality–cost claim.** PostMetric4 **10/32**, PreMetric4 **11/32** vs the strong baseline QuestFine32 **13/32** at "roughly half" the descriptor memory (L6089, L6148, L6164).
4. **Consequential failure #2 — the whole PSR / position-aware-summary line had no answer-level effect.** On the exact-ranking diagnostic, RoPE and NoPE oracle outputs were identical at raw-token level on **15/16** pairs; manual correctness Dense 13/16, RoPE 13/16, NoPE 14/16 (L5673, L6704; agent's retrospective L6715 point 1).
5. **Consequential failure #3 — the user-supplied RefCarry proposal's theory was corrected twice**: the group-moment formula exactly factorizes the *mean logit* but its softmax is a geometric-mean, not a marginalization (TV counterexample **0.5761169** vs **0.0001233946**); and the claimed bound `r >= rank(Phi)` is wrong (L6499, L6520, L6562).
6. **Novelty veto**: clustering / centroid / count-weighted summaries are already covered — ClusterKV, SAAP, Multipole Attention §3.3/§3.2, MuSe §3. RefCarry's core operator is a special case of TAPE §3.2 Eq.7 (identity error **0.0**) and NTM §3.3.2 (2014) already saved read weights for relative shift.
7. **Infrastructure**: CUDA `torch.linalg.eigh` crash in `pair_envelope.build_pair_envelope` (CUSOLVER_STATUS_INVALID_VALUE), a missing-assistant-header bug that changed 4/8 → 0/8, a missing `einops`, an InfLLM-V2 cutlass header syntax error, one ValueError, one SSH drop.
8. **The strict endpoint was repeatedly poisoned by format**: comma-vs-space and a dropped assistant header. "The fixed old comma-format contract still fails even for Dense" (L6282).
9. **Final state**: goal left **unachieved**; agent marked it `blocked` at 00:15:35 UTC with `tokensUsed 1815270`, `timeUsedSeconds 16534` (L6358); user rejected that framing (L6368 "什么意思？你在干嘛，没找到办法？").
10. User then said stop ("别找了", L6594) → commit/push `3dc3527` → but continued asking *why*; the last assistant message (L6793) states the requirement for the next theory: "在同一套假设下，解释一个已有成功与一个有判别力的失败；指出两者之间可计算、可干预的区别；据此给出一条尚未看过结果的预测。"

---

## (b) Failure-case table

Numbers are verbatim. "Reason not stated" means the transcript gives none.

| ID | Lines | What was tried | What happened (verbatim numbers) | Reason | Rule it implies |
|---|---|---|---|---|---|
| **F1** | L5673, L6282, L6704, L6769 | PSR (Phase-Stratified Routing): farthest-first phase grouping, B=64, R=4, per-query-head block selection | Remote prob. retained / relative attention-value error — Qwen3.5-0.8B: RoPE mean `.783644`/`.018219`, NoPE mean `.774587`/`.019021`, **PSR `.791685`/`.017753`**, Contiguous R=4 `.792752`/`.017686`, Random R=4 `.786830`/`.018039`. Qwen2.5-3B: RoPE mean `.606341`/`.120433`, NoPE mean `.292119`/`.150254`, **PSR `.609223`/`.119691`**, Contiguous `.612167`/`.120067`, Random `.609048`/`.119043` | Stated: "increasing the number of summaries explains the observed mean-summary improvement at least as well" | A PSR win is only admissible if it beats **both** count-matched contiguous **and** random partitions; if contiguous ≥ PSR the method claim is void |
| **F2** | L5673, L6704 (doc), L6715 | Use exact block-ranking oracles to test whether the Qwen2 RoPE/NoPE *local fidelity gap* is the answer bottleneck | 16 previously inspected 16K event questions; all 48 completions terminated. "RoPE and NoPE oracle outputs are identical at the raw-token level on **15/16** pairs." Manual whole-answer correctness Dense **13/16**, RoPE **13/16**, NoPE **14/16**. Strict full answer + EOS **0/16** for every method. Case-insensitive whole-string + EOS **2/16, 3/16, 3/16** | Stated: "This fails to establish that the large Qwen2 local fidelity gap is binding for these generated answers." Agent's own retrospective (L6715): "这个关键判断应该更早完成" | An attention-fidelity / local-metric difference may not be used as evidence of an answer-level effect unless the generated answers actually differ |
| **F3** | L4644, L4932, L5737 | 24 untruncated natural QA (8 HotpotQA + 8 Qasper + 8 MultiFieldQA-en), 384-token cap, whole-answer F1 | Qwen3.5-0.8B: Dense `.376799`, RoPE oracle `.343573`, NoPE oracle `.368684`, R/N identical **15/24**. Qwen2.5-3B: Dense `.234505`, RoPE `.254752`, NoPE `.248081`, R/N identical **14/24**. Trimmed exact+EOS **3/2/3** (Qwen3.5) and **1/1/1** (Qwen2.5-3B). N_vs_R_F1 wins/losses/ties 3/6/15 | Stated: length-sensitive F1; "the largest Qwen3.5 R/N difference is `qasper_196`: both give the correct 0.6103, but R includes a surrounding sentence. Length-sensitive F1 cannot be interpreted as a retrieval gain in that pair." | F1 differences driven by answer length are not retrieval evidence; require whole-string+EOS as the primary endpoint |
| **F4** | L5433, L5450, L5706 | SupportRepair oracle on 4 frozen RULER 32K rows (niah_multikey ×2, niah_multiquery ×2) | COMPLETE, **77.484 s**, 16 outputs, all EOS. Dense and SupportRepair strict `full_exact_and_eos` both **0/4**. Dense itself wrong on both multikey rows (e.g. `niah_multikey_2_32768_1` Dense `4996680`, SupportRepair `4989051`, RoPEMean `7831250`). multiquery_0: Dense/SupportRepair both give the 4 correct values but with spaces, not the prescribed commas | Stated: "两条multikey的Dense数值本身错误" + template change confound ("至少operator与tail两因素") | A 0/N strict score may not be used to declare a selector mechanism refuted; first separate prompt-template change from a model capability gap, and check whether Dense itself passes |
| **F5** | L5675, L5763 | Primary-RULER input generation | "落地prompt以 `<\|im_start\|>assistant` 结束，缺少原生chat模板的末尾换行；8条生成都先补一个 `\n`。当前text_exact用answer.strip()，所以记录**4/8**通过，但未strip、只去terminal EOS的raw_body_exact实际**0/8**。" After fix: "短样本重放为 **4/8** 完整字符串加 EOS 正确，另 4 条仍有漏数或顺序错误" | Stated: generation template lost the native assistant header newline; `strip()` hid it | Assert the complete native assistant header before tokenizing; record `trimmed-full-string` and `raw-body+EOS` as separate metrics, never folded by `strip()` |
| **F6** | L5657, L5740 | Build the rotary-pair envelope cache on the GPU | `torch._C._LinAlgError: cusolver error: CUSOLVER_STATUS_INVALID_VALUE, when calling cusolverDnXsyevBatched_bufferSize(...)` at `pair_envelope.py:158` in `build_pair_envelope`; no result produced. All-key-gather bitwise parity had already passed (`RoPEMean/Quest/PairEnvelope: true`) | Stated (inferred fix path): batched small-matrix `eigh` unusable on CUDA. Fix = equivalent 2-D analytic solution; "**22项CPU测试全部通过**" (L5740) | Do not call batched `torch.linalg.eigh` on small (2×2) covariance matrices on CUDA; use the closed-form 2-D solution |
| **F7** | L5795, L5889, L6704 | RPEE (rotary-pair envelope selector) on the fixed failing example | `envelope_causal_02` local/remote raw SHA match. RPEE output `2608476 4102310 3192420 3192420`; RandomPair also wrong; Quest first two numbers wrong; 5 arms all EOS, none restored the full content. "这不是纯逗号问题。" Source-block selection **69/1200** (RPEE) vs **55/1200** (Quest); candidate bound inflation still ≈26. Full diagnostic (L5795): RoPEMean source_selected 65/1200, Quest 55/1200 inflations_mean `25.302002984384696`, PairEnvelope 69/1200 `25.130839864561956`, RandomPair 39/1200 `25.655916535605986`, QuestSplit32 56/1200 `21.955338515654827`; `source_max_rank_top16: 57` for **all** methods; `Pair_over_Quest_source_admissions: 34`, `Quest_over_Pair_source_admissions: 20` | Stated: bound-inflation reductions are correlated diagnostics; common-position-shift invariance is not unique to RPEE (origin-rebased Quest achieves it); "‘对称性恢复’不等于‘能力修复’" | Symmetry/bound properties may never be promoted to a capability claim without complete-answer recovery on the same reader and budget |
| **F8** | L5180 | Orbit-regression key MSE reduction | "key MSE 降低约 **67%**，却产生超出原 key 范围的虚拟峰值，使真实块排名翻转" | Stated: the projection property does not bound virtual peaks | Any summary claim must be checked for out-of-range virtual extrema, not just MSE |
| **F9** | L5358, L5929, L5968, L6704 | Novelty of "cluster real post-RoPE keys + centroid summaries + count weighting" | ClusterKV §IV-B clusters keys *after* QKV projection and RoPE; Multipole Attention §3.3 has time-blocked online clustering updating only the last block, §3.2 has `count × exp(q·centroid)`; MuSe §3 has positional-block semantic summaries + exact retrieval; SAAP has de-RoPE clustering with asymmetric query indexing | Stated: "this known clustering-family implementation is not an independently novel algorithm" | The only remaining differentiator is "fixed 64-token physical page + no output compensation / global re-ranking"; that alone is insufficient for a novelty claim |
| **F10** | L6284 | Descriptor-cost comparison used to argue the mixture cache was near-Quest-cost | Retraction by the delegating agent: "正式mixture_frozen_01的build回执显示4均值实际为**FP32**（首行首层 **2,059,872** bytes），QuestFine32为无损BF16端点 **1,022,976** bytes，确实约一半。我之前沿用早期mixture草稿的BF16设定说‘开销接近’" | Stated: the earlier claim used a draft's assumed dtype, not the actual build receipt | Descriptor bytes must be read from the run's own build receipt; an untested dtype variant is not an existing result |
| **F11** | L5334 | `mixture_native.py` reader cost | "interface 无条件执行 `ks=k[0].float()`；但 PostMetric4/PreMetric4/MatchedContiguous4 分支只用缓存means，完全不使用ks。这会在每层每个问题/生成token把全部原始K读出并转FP32，抵消缓存摘要的读取节省" | Stated: dead read of the full K tensor defeats the cache's read saving | Cached-summary branches must not materialize the full raw K; move the read into the single-mean branch only |
| **F12** | L5828 | Diagnostic aggregation key | `summarize_envelope.py` joined key contained only `(layer, position, head, block)` and omitted `row_id`; multi-row runs would silently overwrite and could flip the `Pair_over_Quest` / `Quest_over_Pair` admission counts | Stated | Per-cell diagnostics keys must include `row_id` (plus prefix/input identity) |
| **F13** | L6166 | Control matching for PreMetric4 | "当前MatchedContiguous4的counts来自PostMetric4，只能说与Post严格匹配counts；Pre是同摘要容量，不是它的独立counts-matched控制" | Stated | A counts-matched control must be matched to *the arm it controls*; equal capacity is not the same control |
| **F14** | L5376 | `prepare_support_strict.py` input selection | `ValueError: Evidence must stay remote throughout the continuation` (line 45); later, an oracle-prep issue was found where `support ∩ eligible` silently drops blocks, and up to 63 tokens near the local boundary belong to neither legal-remote nor mandatory: "若证据恰好跨这条边界，交集会把必要证据删掉，使‘support oracle’并未完整修复，可能造成假阴性" (L5307) | Stated | Assert per-source-record-span that every required token is in the actual mandatory-read set; otherwise mark the oracle inapplicable rather than reporting a false negative |
| **F15** | L6499, L6544, L6520, L6562 | RefCarry "Position Is a Retrieved State" — group-moment address interface | (1) The factorization holds exactly for the **mean logit**: `group_logit_factorization_max_abs_error 2.1094237467877974e-15` (100 random CPU cases max error `3.885780586188048e-16`), but "该式没有 softmax，也没有 value readout"; the moment softmax is a normalized weighted *geometric* mean. Legal counterexample, frequency π/2, anchors at 0 and 1, post-RoPE keys `(10,-10),(-10,10),(1,1)`: moment logits `(0,0,1)` put probability **0.5761169** on the third item vs **0.0001233946** for the true read mixture. (2) State collision: `mu_A=(δ0+δ2)/2` vs `mu_B=(δ1+δ3)/2` have identical first moments; actual marginal reads `(0.49101, 0.49101, 0.01798)` vs uniform, **total variation 0.3153530** | Stated: "不能称其精确保留一般地址分布的读取" | Do not claim a moment sketch preserves a read distribution; state explicitly whether the task needs single-reference, consensus, or marginalized reads |
| **F16** | L6499, L6520, L6562 | RefCarry's claimed minimal linear-sketch bound `r >= rank(Phi)` | Wrong unconditionally. Correct form: `r_min = rank(Phi\|_H) = rank([1ᵀ; Phi]) - 1`. CPU counterexample: two addresses 0/1, frequency 1, uncentered `rank_Phi = 2`, yet `sufficient_linear_sketch_dimension = 1` (reconstructed_moment `[0.37, 0.63]`, recovery error < `1.2e-16`) | Stated: the simplex constraint `1ᵀμ = 1` must be centered before taking rank | Any "minimal sufficient state" theorem on a probability simplex must be stated on the centered subspace; the general case may still be 2K |
| **F17** | L6544, L6562, L6569 (agent), L6577 | RefCarry novelty vs nearest neighbours | TAPE §3.2 Eq.7 already weights the position matrix by actual attention weights; under shared writer weights + frozen key positions + query-only update it reduces exactly to the RefCarry formula (`fixed_key_TAPE_style_operator_identity_error: 0.0`). RoVE's fixed-payload rotary value read yields the address Fourier moment exactly. RePo's one-hot reference is the single-sided dynamic-position case. NTM §3.3.2 (2014) already saves the previous read weighting and does a relative shift after content addressing | Stated: "RefCarry有受限适配器的研究价值，但现有理论新颖性主张过强"; "不能把这个接口概念本身当作首次发现" | Novelty must be carried by the *restricted* settings (frozen original key coords, probabilistic-moment constraint, specific bridge schedule, actual cost) against a function-matched TAPE variant — not by the "read produces address state" idea |
| **F18** | L6562, L6520 | RefCarry oracle identification | "Gold-reference 是一种带正确地址的 phase 干预，不是恢复上界." `native_query_rebasing_counterexample`: current_position 10, gold_anchor 6, desired_offset -1, native_argmax 5, gold_reference_argmax 1 — "Oracle rebasing failure need not mean reference information was absent." `shift_reference_counterexample`: reference_positions `[0,1,2,3]`, selected_positions `[7,7,7,7]`, **slope 0.0**. `zero_entropy_gate_cancellation`: reference_entropy 0.0, g 0.5, query_norm_ratio `6.123233995736766e-17` | Stated | A gold-reference failure kills only *this interface* on this checkpoint, not the reference-bottleneck hypothesis; address shift does not generally move the target with slope 1 |
| **F19** | L6562, L6554 | RefCarry causal interventions ("Writer-value-zero", "No-target-KV") | "只清一个 writer 的语义输出不清更早层或残差中的内容；只删除 reader 的原始 target KV 不删除后续 token 已经携带的答案。失败也可能来自破坏正常计算" | Stated | Use same-length same-format donor/recipient target-value exchange (Q/K/non-target V/upstream state held fixed), calibrated first with Gold-target; deletion controls are not path cuts |
| **F20** | L6520, L6544, L6562 | Claim that a recurrent/compressed bridge necessarily loses the reference | Official Qwen3.5 `DecoderLayer` keeps a per-token residual after the GDN branch; a layer-19 query token still has a per-token residual path to layer 23. `nonlinear_residual_address_switch`: max_abs_error `2.220446049250313e-16` over `q ∈ [-1,1]^2` | Stated: "沿序列的 recurrent 压缩不能直接推出这段沿深度的表征必然丢失" | Sequence-direction compression ≠ depth-direction loss of reference; an affine-map impossibility does not extend to gated MLP+residual blocks |
| **F21** | L6769 (failure-synthesis table) | "Forcing the gold block into the candidate set ≈ perfect routing" as a way to prove attention is not the bottleneck | The control was still wrong: "原代码仍保留其他块并用普通QK softmax。该控制的无效不能关闭所有注意力处理" | Stated | A routing oracle is only valid if it also neutralizes the other blocks and the QK softmax |
| **F22** | L4604 | `git ls-remote` of the InfLLM-V2 CUDA repo | `fatal: Remote branch feature_infer not found in upstream origin` (exit_code 128) | Reason not stated | — |
| **F23** | L4920 (agent), L4909 (install) | First launch of the native dense baseline | Blocked by FlashAttention missing the `einops` dependency; "原生基线首次启动被 FlashAttention 缺少 `einops` 依赖挡住，尚未产生答案；依赖正在补齐，失败记录已保留" (L4920) | Stated (missing dep). Fix: `pip install einops==0.8.1` (L4909) | Environment deps must be installed before queueing the qualification run |
| **F24** | L4788, L4880 | Build the official InfLLM-V2 kernel | Official dependency header syntax error in cutlass `cuda_host_adapter.hpp` (inspected L4788); minimum patch applied and kept ("此前遇到的官方依赖头文件语法错误已做最小修复，补丁也已保留", L4880). Compile was still ongoing at 23:15 and MiniCPM native sparse was never qualified in this shard | Stated | Do not report a PyTorch reconstruction as the official fused kernel without checking the causal/masking/precision contract |
| **F25** | L4604 | Install `transformers==4.56.2` inside the MiniCPM runtime | `huggingface_hub 1.28.0` "Not uninstalling ... outside environment /root/minicpm41_native_20260908/runtime" — version conflict between the miniconda and the isolated runtime | Stated (environment isolation) | — |
| **F26** | L6103/L6105 | SSH session to `connect.westc.seetacloud.com` | `Connection to connect.westc.seetacloud.com closed by remote host.` mid-run | Reason not stated | — |
| **F27** | L6219 | `envelope_causal_01` reconciliation | `envelope_causal_01 reconciled FAILED, zero answers` | Reason not stated | — |
| **F28** | L6176 | MiniCPM weight download | `"status": "PAUSED_FOR_NO_GPU_SWITCH"`, partials `model-00002-of-00002.safetensors.partial 1317011456`, `model-00001-of-00002.safetensors.partial 1166016512` | Stated: no-GPU switch | — |

---

## (c) Repeated failures (same failure hit ≥ 2×)

| Repetition | Where (both/three occurrences, verbatim) |
|---|---|
| **R1 — "local proxy improves, complete answer does not"** | (i) L5180 orbit regression: "key MSE 降低约 **67%**，却产生超出原 key 范围的虚拟峰值，使真实块排名翻转"; (ii) L6704/L6282 PSR: better retained-probability `.791685` vs contiguous `.792752`, no answer recovery; (iii) L5889 RPEE: inflation `25.130839864561956` vs Quest `25.302002984384696`, no recovery. Agent's own summary at L6781: "证明代理量变好了，没有证明答案会变好" |
| **R2 — the old comma-format contract poisons the strict endpoint** | L5450: "multiquery_0的Dense/SupportRepair均完整给出4个正确数值，但用空格而非规定逗号"; L5500: "模型用了空格而非规定的逗号，所以这一轮的严格字符串指标仍记为不通过"; L6282: "The fixed old comma-format contract still fails even for Dense" |
| **R3 — generation-template / header bug** | L5675: missing native assistant trailing newline → recorded 4/8 but raw_body_exact 0/8; L5763: "数据生成器删掉了 assistant 头的换行；补回后，短样本重放为 4/8 完整字符串加 EOS 正确，另 4 条仍有漏数或顺序错误" |
| **R4 — switching models / environments instead of running the decisive experiment** | User at L4999: "你搞那么多模型下载不要时间吗？pro模型那么多思考不要时间吗？...谁让你收窄研究的？"; user again at L5065: "你一直在违背我的核心原则，一直在浪费我的GPU"; the delegation at L5099 explicitly orders "停止重复24条弱任务、格式F1主导的'验证'以及无新预测的模型切换" |
| **R5 — nothing beats a plain strong baseline once it is finally run** | L6026 (interim, 24 rows: Dense 13, RoPEMean 0, Quest 1, QuestSplit32 5, PostMetric4 7, PreMetric4 7, MatchedContiguous4 0) → L6095 "32 条完整比较已结束：Dense 17/32；单均值 0/32；Quest64 3/32；两段摘要的 Quest 8/32；旋转后／旋转前分组分别 10/32、11/32；匹配连续分组 1/32"; then L6172/L6268: "最后的强基线是 13/32，候选为 10/32 和 11/32；强基线的摘要内存约为候选的一半" |
| **R6 — "the theory is complete" turns out to be complete only on a simplified object** | Named by the agent itself as the month-long loop at L6755/L6781: "因为我们反复把'能够推导出一个自洽的数学方案'，当成了'已经找到能改善模型的机制'"; the same structure is visible in-shard: L5180 (orbit regression), L6520 (RefCarry rank theorem wrong), L6562 (moment semantics wrong) |

---

## (d) Vetoes / "do not restart" (verbatim quotes with line numbers)

1. **L6562 — do not start the proposal's training.** "不启动提案中的 13 GPU 小时训练。" (RefCarry audit doc, §5)
2. **L6469 — no GPU, no second queue.** "我不启动 GPU 或第二条候选队列。"
3. **L5530 — do not build another GPU matrix for the shift-invariance point.** "不必为此额外铺GPU矩阵；只需把逻辑边界写清，继续同预算Quest/真实pair/随机pair的现有实测。"
4. **L5889 — do not widen the RPEE matrix before the target is shown attainable.** "若精确max也不能，继续优化矩形/轴/阈值并无这个失败的目标依据；不应直接扩大RPEE矩阵。"
5. **L6166 — do not change R, budget, or model in the last stage.** "请保持原两小时截止，不在最后阶段改R、改预算、换模型寻找胜例，也不把这轮诊断/局部收益标为完成论文目标。"
6. **L6282 — do not relabel and do not auto-restart.** "Do not relabel this as a solid-accept result or substitute this report for the requested finished paper." / "Do not reset it or automatically restart GPU work after it."
7. **L6380 — do not package a local repair as "a method found".** "不要把局部修复包装成已经找到办法，也不要暗示只要再给固定几小时就能保证找到。"
8. **L5134 — kill rule for the selector-only route.** "若此修复都无恢复，当前selector-only修复路线没有继续优化的依据。" (kill condition, same shape at L5216: "若无恢复kill selector-only机制")
9. **L6028 — do not switch the main method mid-run.** "也不要在32条尚未完成时按Pre/Post暂时分数切换主方法"
10. **L5450 — do not upgrade a parsed pass to a strict pass, and do not use 0/4 to kill.** "不能据0/4宣布selector机制被oracle否定，也不能把multiquery_0解析后升格strict exact成功。"
11. **L5307 — do not kill a mechanism from a single inapplicable example.** "不满足就标为oracle不适用，不能据该例kill机制。"
12. **L6520 — a negative oracle result kills only the interface.** "负结果只能杀此接口，不能杀reference瓶颈。"
13. **L6166 — "不是统计上证明它普遍更差，而是这轮没有通过晋级条件"** (failing to promote ≠ proving inferiority).
14. **L6284 — an untested variant is not a result.** "不能把尚未测试的BF16均值版本当作既有结果。"
15. **L5358 — do not download models or port the whole system now.** "不现在下载模型或移植全部系统。"
16. **L5940 — do not open a larger pair-method search.** "不建议另开更大pair等新方法。"
17. **L6781 — no new GPU night on a promise.** "也没有依据让你仅凭这样的承诺再投入一晚 GPU。"

---

## (e) FINAL STATE (as the transcript leaves it)

**Belief.**
- The research goal (a solid, validated, publishable core result on positional encoding in modern sparse/compressed/hybrid attention) is **unachieved**. Stated by the agent at L6268: "**我没有做出你要求的 solid accept 结果。**" and in the frozen HANDOFF text (L6282, L6335, L6354): "The broad research goal is **not complete**."
- What *is* believed and evidenced:
  - The fixed development example `niah_multiquery_32768_0` (native Qwen2.5-3B, m16/B64/local2048/sink64, sparse from the first question token) has a **real, autonomous repair**: exact block-max, exact block-logmass, PostMetric4 and PreMetric4 all reproduce Dense **token for token including EOS** (`2608476 9403234 4105180 3192420`). RoPEMean fails (`2608471`). Privileged source-page repair reproduces Dense; same-budget wrong-page repair fails (`2631010`). Quest64 reference `26081234 94032010 4105180 3192420`; native rotary-pair envelope `2608476 4102310 3192420 3192420`; random-pair envelope `2608476 4105180 3192420 3192420`; QuestSplit32 `2608476 9405180 4105180 3192420`; MatchedContiguous4 `2608476 9403234 3187540 4192730`. (L6704, doc `RESULT_20260908.md` §1)
  - Frozen 32-row assay (seed 20260911, primary endpoint = raw entire body + EOS): Dense **17/32**, RoPEMean **0/32** (30/32 EOS), Quest64 **3/32**, QuestSplit32 **8/32**, PostMetric4 **10/32**, PreMetric4 **11/32**, MatchedContiguous4 **1/32**. Strong frontier baseline QuestFine32 (actual 32-token Quest pages, lossless BF16 extrema) **13/32**, 32/32 EOS, 210.494 s, SHA `a642b957a04671baf68db790cc8e55049b2bd31f90e9fe493496050570ff58d8`. Paired: PostMetric4 vs QuestFine32 **3 wins / 6 losses**, p=0.5078125; PreMetric4 **3 wins / 5 losses**, p=0.7265625. PostMetric4 vs RoPEMean **10/0**, p=0.001953125; vs Quest **8/1**, p=0.0390625; vs QuestSplit32 **5/3**, p=0.7265625; Post vs Pre **2 wins / 3 losses**. (L6089, L6148, L6164)
  - Therefore: **“failed quality–cost improvement claim”**; "No quality-cost superiority or sufficiently novel algorithm was established."
  - Position-specific advantage: **not established** ("Post/Pre do not demonstrate a position-specific advantage").
  - Novelty: **not established** — clustering family overlap (ClusterKV / SAAP / Multipole §3.3 / MuSe §3).
  - RefCarry: "值得继续研究的是'模型是否能组合内容寻址与参照相对读取'。现有材料尚未证明一个现代 hybrid 架构特有的 reference 丢失瓶颈，也没有证明所给 group-moment 接口具有独立新意或生成优势。" Judgement: "RefCarry有受限适配器的研究价值，但现有理论新颖性主张过强" (L6569).
  - From the retrospective failure synthesis (L6776): "尚无已证实的方法突破、超越 MrRoPE 的结果或可直接升格的论文主贡献。准备、文档与代码完成不能替代这三项。"
  - The one strong positive carried forward in the doc: the frozen three-seed EVQ result — anchored EVQ-Cosh minus FMRoPE tail NLL `-0.28073/-0.17599/-0.14571` at `512/1K/2K`, **3/3 seeds** favoring EVQ at every OOD length, with `+0.02619` at 256; but when both grids are target-matched the ordering **reverses in all three seeds** (`+0.06032/+0.22720/+0.45959`), so EVQ's gain is condition-dependent. (L6776, doc `EXACT_RANGE_151M_3SEED_RESULT_20260820.md`)
  - Paper: `paper-2027/main.tex:74` is still `\title{RoPE Has a Spectral Budget}`; "稿件、当前假说和实验已经脱节" (L6748). Stated next to that: the six-arm budget experiment needs ≈**33.5 GPU hours** not including evaluation, and is "尚未含评测" (L6748).

**Open.**
- Whether *any* position-specific or reference-handoff bottleneck exists in a modern hybrid, and whether it is reachable by a method that beats a plain strong baseline at equal cost — open.
- The RefCarry line: only the *restricted* interface (frozen original key coords, query-only update, probabilistic-moment constraint, specific bridge schedule, real cost) is left to test; the theory needs its probability semantics and minimality conditions rewritten first.
- "在已核对的近邻、现有因果证据和剩余约三十多分钟的条条件下，**我没有识别出一个同时满足'重要、真正未被近邻覆盖、一次现有资产实验即可决定性检验'的新位置编码问题**" (L6412, doc "最后独立研究判断", timestamped 2026-09-08 23:42 UTC) — explicitly: this is a judgement about the current proposals, "不是证明该研究领域不存在新问题".

**What the agent says should happen next.**
- L6748 (5-point plan): (1) before opening the GPU, deliver a one-page reviewable core plan — the real failure, the closest existing method, the specific difference, one falsifiable prediction, and what result is worth continuing; (2) the first round must put the core hypothesis against a competitive control (original model + relevant strong baselines + necessary mechanism controls), checking task-solvability, template correctness and a complete generation metric; (3) allocate ~2 h core / 6 h independent verification / 2 h key supplementation, not pre-filled; if stage 1 has no supporting signal the remaining 8 h do not auto-start; (4) keep only one core claim in flight; (5) update the paper with the evidence, never write conclusions in advance. And: "**我的建议是暂时不要因为我承诺'这次一定行'而重新开 GPU。先要求我拿出一个经得起反例和强基线检查的核心方案，再把 10 小时投入其中。**"
- L6781 (this **corrects** the above): "我需要纠正上一条回答：**只保留一个主线、分配好 10 小时，只能减少浪费，不能解决根本问题。根本要求是，一个候选必须比已有解释多预测出一件具体东西，并且实验能够判断这件事情。**"
- L6793 (final requirement): "在同一套假设下，解释一个已有成功与一个有判别力的失败；指出两者之间可计算、可干预的区别；据此给出一条尚未看过结果的预测。"
- Execution status: GPU off (user, L6406 "那现在我关掉GPU了"), goal `blocked` (L6358), heartbeat `rope` PAUSED (L6237), work committed and pushed as `3dc35278e256c27566250c6656d0d8d564f9cee4` (L6667), research stopped per user (L6594/L6599).

**Artifacts left on disk (referenced, not verified by me).**
`experiments/native_sparse_position/RESULT_20260908.md`, `.../CORE_DIAGNOSIS.md`, `.../METHOD_INDEPENDENT_20260908.md`, `.../COHERENT_MIXTURE_PROTOCOL_20260908.md`, `docs/research/COMPRESSED_POSITION_INDEPENDENT_20260909.md`, `docs/research/REFCARRY_INTERFACE_AUDIT_20260909.md`, `experiments/refcarry_audit/{README.md,checks.py,results.json}`, `experiments/native_sparse_position/aux_refcarry_math_checks.py`, `results/position_observability_20260908/evidence_manifest_20260909.json` (20 hashed artifacts, 256 complete streams, input_sha256 `046d3e90b399c6084d688aba96b19140a6667fe866b94180e86acd38c709eaee`), `docs/research/ROPE_LOCAL_FAILURE_SYNTHESIS_20260908.md`.

---

## (f) Retractions and contradictions (later statement wins; both quotes + both line numbers)

| # | Earlier claim | Later correction |
|---|---|---|
| **C1** | **L6284 (delegation), quoting the agent's own earlier auxiliary report**: "我之前沿用早期mixture草稿的BF16设定说'开销接近'" — i.e. the mixture cache was described as BF16 and near Quest cost. | Same line, correction: "正式mixture_frozen_01的build回执显示4均值实际为**FP32**（首行首层 2,059,872 bytes），QuestFine32为无损BF16端点 1,022,976 bytes，确实约一半... 已在辅助报告更正。...不能把尚未测试的BF16均值版本当作既有结果。最终32行质量判决不变，候选尚未胜过强基线。" |
| **C2** | **L6748**: the 5-point 10-hour plan ("约 2 小时核心检验、6 小时独立验证、2 小时关键补证"), presented as how to avoid repeating the failure. | **L6781**: "因此，我需要纠正上一条回答：**只保留一个主线、分配好 10 小时，只能减少浪费，不能解决根本问题。根本要求是，一个候选必须比已有解释多预测出一件具体东西，并且实验能够判断这件事情。**" And L6793 re-frames the cause: "我前面强调'数学正确不保证实验成功'，如果让你听成'理论解决不了，所以只能试'，那是我的表达和判断都偏了。" |
| **C3** | **L5075 / L6361**: the agent framed the situation with the two-hour budget ("按你给的两小时推进"; goal marked blocked at the deadline). | **L6392 user**: "为什么找不到办法？你不是跑了两个小时，你是跑了一个晚上" → **L6399**: "你说得对，**投入的是整个晚上，最后两小时只是最后一个阶段。**我刚才没有把总投入说清楚。... 所以，**不能把失败归因于最后两小时不够。是我用了一晚，没有把研究方向和关键实验组织好。**" |
| **C4** | **User-supplied RefCarry proposal (pasted at L6437, echoed in the search output at L6446)**: the group-moment interface exactly preserves the address read, and `r >= rank(Phi)` is a minimality bound. | **L6499/L6520/L6562**: "群矩公式确实精确因子化 E[logit]，100次误差<4e-16；但其 softmax 是各anchor读取分布的归一化几何平均，并非读取概率的边缘化... 不能称其精确保留一般地址分布的读取" and "原文无条件写 `r>=rank(Phi)` 不成立". Corrected bound: `r_min = rank(Phi|_H) = rank([1ᵀ; Phi]) - 1`. |
| **C5** | **RefCarry's claimed novelty** ("真正可以承担理论 novelty 的不是'有一个 gate'，而是：1. 该地址状态能够**精确因子化**后续 anchor-relative attention；2. 在自然线性 sketch 类中，它是**最小充分状态**...", L6446). | **L6554 / L6569**: "TAPE最接近：共享writer权重、冻结key位置、仅更新query字段后，Eq.7＋Eq.6化为RefCarry公式" (identity error `0.0`); "查新还应补NTM原文§3.3.2：保存上次读取权重、内容定位后再作相对shift，是2014已有接口；它不等同具体RefCarry，但不能以'读产生地址状态'概念本身承担novelty." |
| **C6** | **RefCarry §"Qwen3.8 将 Gated DeltaNet 与少数 full-attention/QSA 层交替"** used to motivate a general reference-loss bottleneck (L6446). | **L6520/L6562**: "官方Qwen3.5 DecoderLayer在GDN分支后仍是residual+update，query token在19→23层有逐token残差通路；不能把沿序列的recurrent压缩直接等同于沿深度必然丢reference." |
| **C7** | **L6446 (proposal)**: the gold-reference / shift / no-target / writer-zero interventions identify reference handoff. | **L6562**: "Gold-reference 是一种带正确地址的 phase 干预，不是恢复上界"; "Writer-value-zero 与 No-target-KV 不是完整路径切断"; "地址平移不普遍保证目标按斜率 1 移动" (slope 0.0 counterexample). |
| **C8** | **L6715 point 1**, the agent's own retrospective on the earlier reasoning: early on it saw a RoPE/NoPE **attention-fidelity** difference and pursued position-aware summaries on that basis. | Same turn: "但后来用精确排序排除摘要误差，16 个问题中两者有 **15 个输出逐 token 相同**。这说明此前观察到的局部差异，在这些样本上并没有形成相应的答案差异。这个关键判断应该更早完成。" (Cross-ref F2; the original 16-pair result is at L5737.) |
| **C9** | **Earlier-session claims corrected inside the in-shard failure synthesis (L6776)** — these retract claims made before line 4600, so a reader of the earlier shard would be misled: | (i) **YaRN identity**: "7月已区分官方线性ramp与repo smoothstep，9月又把sqrt增益Y2称忠实... 官方cos/sin幅度为a=1+.1ln(s)，logits乘a²。**Y2是变体；不能用它的零分证明官方方法上限。**" (ii) **Phase16 99-run "near-optimal law"**: "混合pilot/confirmation评分与种子数，宣称near-optimal law → 7月原始重分析已否定该排名。公式对独立邻点只赢3/9配置；本次给仍可检索的3月报告补撤回提示". (iii) **LoRA capacity bound withdrawn**: "LoRA容量的错误界已撤回。rank-r全局更新的每头切片可以各有rank-r... best-found残差只给该代理最小误差的上界，不能变成不可修复下界". (iv) **ALS false convergence fixed**: "`previous=inf`使首轮比较成为`inf<=inf`... 旧首轮2.848193371；修后43轮2.836061317". (v) **Early shape/theta "PASSED"**: "sigmoid短/长PPL为37648/62474，因比值小而写'更稳定、PASSED' → 低崩溃比可以来自短端已经坏掉，不能作能力收益". (vi) **Static-geometry family**: "Gram、曲率、重构残差、phase risk、覆盖、平滑性变好⇒生成变好 → 已有直接反例；18样本64维行为梯度也已失败，不重新包装成'功能需求恢复'". |
| **C10** | **L6361**: "目标已标记为阻塞" — the agent chose the `blocked` label as its answer to the user. | **L6373**: "刚才'阻塞'是我按两小时时限停止自动续跑的状态。**实际问题是我花了大量时间做诊断和候选验证，却没有交出核心方法。**这是我的执行没有达到要求，不能靠状态标签或实验报告交差。" |

---

## Notes on method / confidence

- Every line number above was read directly from the JSONL; no line number is inferred. Where a fact came from a document *printed into* a tool output, I cite the line of the tool output and name the document.
- All metric values are copied character-for-character from the transcript. Where the transcript itself printed a rounded form (e.g. the failure-synthesis "Z短训20/32与完整输入23/32"), that is a reference to pre-shard work and is quoted, not re-derived.
- **Do not treat this as a result**: `12/32` appears at L5542 only inside a `tqdm` progress bar, not as a metric. `20/32` at L6776 is a retrospective reference to earlier (pre-shard) work.
- Reasoning records in this shard are mostly `encrypted_content` with empty `summary`; the agent's reasoning is therefore recovered mainly from its assistant messages, its delegation inputs to the `independent_method` subagent, and the documents it wrote.

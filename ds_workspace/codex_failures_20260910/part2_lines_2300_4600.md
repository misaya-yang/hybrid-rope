# Codex transcript failure mining — part 2, lines 2300–4600

Source (read-only): `/Users/yang/.codex/sessions/2026/09/08/rollout-2026-09-08T09-59-44-01a08151-4eb4-7572-8865-b321956dda94.jsonl`
Shard: lines **2300–4600** of 6796. All `L####` below are **line numbers in that JSONL file**. Payload timestamps in this shard run **2026-09-08T17:05:57.307Z → 2026-09-08T21:30:23.829Z** (≈4 h 24 min, UTC). All numbers are copied verbatim; nothing is rounded.

---

## (a) 10-line summary

1. This shard is the second half of a single overnight run: a zero-training RoPE-table-transfer hunt ("BM" vs MrRoPE/MrPro) that dies, then a user-forced pivot (L3123, L3200, L3275) to a K-budget training experiment + paper rewrite, then a second pivot (L3439, L3744) to "does modern sparse/hybrid attention still need position encoding".
2. **Most consequential failure #1 — the Qwen transfer wall.** The BM table that beat MrPro on OLMo scores **32K 80.00% vs 83.33%, 128K 71.11% vs 84.44%, paired_wins 0 / paired_losses 3** on Qwen2.5-7B (`status: "NO_LONG_GAIN"`, L2304), after Qwen2.5-3B had already failed (**32K 91.67% vs 87.22%; 128K 70.83% vs 78.13%; long-end 4W-4L-16T, mean −7.29 pp**, L4322). Four separate repair attempts then failed in a row (F3, F4, F5, F7).
3. **Most consequential failure #2 — the entire pre-pivot goal was cancelled.** L3159 heartbeat text: the zero-training-beat-MrRoPE objective is cancelled, "不再启动其候选或恢复旧队列". Every candidate built in the first half of this shard (two-stage handoff, bias-position repair, K/V mixing, native windows, selective gain, GapCapped, CausalGain) is thereby retro-vetoed.
4. **Most consequential failure #3 — the replacement theory's own headline prediction was retracted before it was ever tested on GPU.** The monotone claim Δ(K=64)≈0 → Δ(K=32)>0 → Δ(K=16)≫0 → Δ(K=8) 更强 is labelled at L3128 **"这是一个错误的论文组织目标。"**, refuted by the K=2 degenerate case, and the softmax stress test shows P16/U16 beating E16 (L3134).
5. Practically nothing in this shard produced a *new* positive method claim: the only surviving positive result is the OLMo static-S4 BM table (+13.47 pp long-end over MrPro on the 72-row confirm panel, L2824), which the same shard shows does not transfer.
6. The last ~2 h of the shard is the new direction (PSR / phase-stratified routing): first real-activation test **negative** (PSR ≤ matched contiguous control), second model also **negative** (L4154, L4224), exact-ranking oracle **fails to establish** the bottleneck (L4391), 24-case natural QA shows **no consistent selector gain** (L4583).
7. Infrastructure bit hard, repeatedly: a dtype-cast bug invalidated the OLMo stage diagnostic (L2304), the training data had been deleted off the server and had to be re-derived (L3308, L3417), an old arch gate rejected the 5090 (L3426), and three tools were missing (`nvcc`, `scipy`, `flash_attn`).
8. The user intervened three times on process: cancel the MrRoPE chase (L3123), "不是上来就六臂" (L3489), and "'不要浪费GPU'是你不要空转" (L3721).
9. Two process rules survived to the end of the shard: every candidate needs a mechanism/derivation basis that can be explained in a paper (L4322: "方法不能猜"), and a negative result must not be chased by sweeping coefficients (L4322).
10. Net solvable-space contraction from this shard: **frozen-table reader-side swaps are dead; amplitude re-allocation is dead; 32K retrieval on this checkpoint family is at floor; Phase-Stratified Routing is not supported; the "mean pooling destroys RoPE position" framing is taken by Prism.**

---

## (b) Failure table

| ID | Lines | What was tried | What actually happened (numbers verbatim) | Stated/inferable reason | Rule it implies |
|---|---|---|---|---|---|
| **F1** | L2302, L2304, L2305, L2310, L2537 | BM (boundary-matched) static table transferred to **Qwen2.5-7B-Instruct**, 6-task RULER dev subset at 32K/128K, vs MrPro. | `"status": "NO_LONG_GAIN"`; `macro_delta_by_length` `"32768": -0.03333333333333344`, `"131072": -0.1333333333333333`; candidate `score_sum 13.333333333333334` vs baseline `15.133333333333333`; 32K macro `0.7999999999999999` vs `0.8333333333333334`; 128K macro `0.7111111111111111` vs `0.8444444444444444`; `paired_wins: 0`, `paired_losses: 3`; 128K vt `0.6` vs `0.9`, qa_1 `0.0` vs `0.5`; 32K vt `0.8` vs `1.0`; `eos_count` 10 vs 11; 18 rows. Cost: 30 new generations + 6 reused, 1380.62 s, peak allocation 27.38 GiB. | "BM 在 OLMo 上的优势目前没有迁移到两个 Qwen 模型" (L2310). | Do not promote a frozen-model table swap to a second architecture family on one model's evidence; a screen that returns 0 wins / 3 losses must stop the expansion, not trigger a repair hunt. |
| **F2** | L2304 (FAILED), L2335 (fix), L2338 | OLMo two-stage diagnostic's own internal consistency check: rebuild a same-table key from raw K and compare bitwise. | `"status": "FAILED"`, `"error": "same-table key reconstruction is not bitwise exact"`, traceback `cross_cache.py line 157`. Root cause found in `rotated_key`: it returned `raw * cos.unsqueeze(1) + rotate_half(raw) * sin.unsqueeze(1)` without casting back. Fix comment verbatim: "OLMo2 supplies FP32 cos/sin and casts the rotated result back to K dtype. Qwen2 supplies K-dtype cos/sin, so this preserves its existing arithmetic." | dtype-cast order differed from the model's own rotation path. | Any re-derivation of a model's own rotation must reproduce the model's exact cast order; the reconstructed key must be bitwise-equal to the model's cached key before that diagnostic is allowed to produce a result. |
| **F3** | L2304 (stage_run_01 FAILED), L2380, L2626–L2628 (stage_run_02) | Two-stage handoff on OLMo: form the long prefix with the **MrPro** table, then let **BM** read it (and the cross-product: BM prefix / MrPro read). | `niah_single_1_16384_1`: MrPro-src/MrPro-read `0.0` `' 16843.'`; MrPro-src/BM-read `0.0` `' 1686.'`; BM-src/BM-read `1.0` `' 1685436.'`; BM-src/MrPro-read `0.0` with degenerate repetitive output (`': 16843. Here we go. The grass is green. The sun is yellow. …'`). `qa_2_16384_13`: MrPro/MrPro `0.0` `' the UDA'`; MrPro-src/BM-read `0.0` `' the Irish Republican Army (IRA)'`; BM/BM `1.0` `' a united Ireland'`; BM-src/MrPro-read `1.0` `' a united Ireland'`. | "两条 OLMo 正例都无法由'MrPro 前缀、BM 读取'救回" (L2380); outcome follows the **prefix formation** source, not the reader. | The formation table is the causal variable for a cached prefix; reader-side table swaps cannot rescue a wrongly-formed cache. Kill the arm on a source×reader 2×2, not on single swaps. |
| **F4** | L2480, L2530, L2536, L2540, L2601, L3116, L2707 | Repair Qwen's failure by restoring only the **learned bias–bias position term** to its MrPro form (Qwen2 has learned Q/K biases, OLMo does not), plus a **192-dim zero-pad control** (`PadBM192`) to separate method effect from kernel-dimension effect. | `vt_32768_1` M `0.8` / B `1.0` / new `0.0`, `same_B_tokens: False`, `eos: False`. `fwe_32768_1` M `0.6666666666666666` / B `1.0` / new `1.0`. `niah_multikey_2_131072_1` M `1.0` / B `0.0` / new `0.0` (**unrecovered**). `vt_131072_1` M `1.0` / B `0.2` / new `0.2` (**unrecovered**). `niah_multiquery_131072_3` M `0.75` / B `1.0` / new `0.5`. Control rows `same_B_tokens: True` on both. | "bias 项构造也未通过：两条负例都没恢复，还损失了短端 VT 和长端多查询的已有收益。" (L2601) | A repair must both recover the known negatives **and** retain the known positives. After one fixed-condition test that does neither, the construct is closed: no coefficient search, no layer-range extension. |
| **F5** | L2628, L2680, L1570–L1580 (dump) | K/V **source** experiment: form the cache from one table, read with another; test K-only and V-only replacement. | 12 generations, `elapsed_seconds 18.381786171346903`, `peak_memory_bytes 7191751680`. `niah_single_1_16384_1`: MrPro-K/MrPro-V `0.0`; BM-K/BM-V `1.0`; MrPro-K/BM-V `0.0`; BM-K/MrPro-V `0.0`. `qa_2_16384_13`: MrPro/MrPro `0.0`; BM/BM `1.0`; MrPro-K/BM-V `0.0`; BM-K/MrPro-V `1.0`. | "K/V 混合也未能继承这两例的 BM 收益；单独替换 K 或 V 没有形成值得部署的组合。" (L2680) | Neither K nor V alone carries the effect — a mechanism that fails both single-factor arms is closed before any combination is tried. |
| **F6** | L2681–L2813, L2885, L2902 | Native-window formation: cut the prompt into blocks of the model's native length (OLMo 4096, Qwen3B 32768), encode every prefix token with an **empty cache** at local positions, then globally rephase all keys to the MrPro/BM frame for reading. | OLMo 72 rows: `NativeWindowMrPro score_sum 20.28333333333333` (4K macro `0.5152777777777778`, 16K macro `0.16493055555555555`) vs `MrPro score_sum 10.416666666666666` (`0.37847222222222215` / `0.027777777777777776`) vs `MrProBM score_sum 44.266666666666666` (`0.8180555555555555` / `0.5131944444444444`). Qwen 36 rows (32K/128K): `NativeWindowMrPro 17.966666666666665` (`0.6222222222222222` / `0.4375`) vs `MrPro 29.216666666666665` (`0.8722222222222222` / `0.78125`) vs `MrProBM 28.0` (`0.9166666666666666` / `0.7083333333333334`). | "它修复了 OLMo 的单键例；Qwen VT 这例得到完整召回并以 EOS 结束，但多键仍失败，也未保住 BM 的 QA 正例。" (L2813) — cancelling cross-block prefill dependence fixes some cells and loses others. | Partial-credit repairs that trade one task family for another do not beat the incumbent on the same panel; require the aggregate panel to beat the incumbent before any further investment. |
| **F7** | L2822, L2489, L4322 | **Selective / matched amplitude gain** re-allocation on top of BM (instead of a single global gain). | "选择性gain：4K80.69%、16K44.79%；统一匹配gain：79.44%/44.24%；均低于原BM开发16K49.03%，不晋级、不再调系数。" | Reason not stated beyond the numbers; the implication is the amplitude degree of freedom is not where the effect lives. | Amplitude re-allocation is a closed arm; do not tune gain coefficients again on this panel. |
| **F8** | L2824 (doc), L4322 | Static **S8** (scale 8) table and the two 32K recovery arms on OLMo. | S8 transfer MrPro `4.17%` / long-end `0.69%` (32K cap), S8 MrProBM `23.89%` / `6.94%`. 32K follow-ups: "BM原S4频率/gain在同32K输入上为0%，仅将S8的gain降到S4为5.56%，均低于完整S8的6.94%。两项不进入进一步32K确认。" | Static S8 is near the long-end floor; lowering scale or gain does not restore 32K ability. | 32K retrieval/tracking on this OLMo checkpoint family is at floor. Do not restart these, and do not re-label short-end recovery as 32K gain. |
| **F9** | L4322 | BM transfer to **Qwen2.5-3B-Instruct**, smoke first then full. | "32K BM91.67%对MrPro87.22%；128K BM70.83%对MrPro78.13%，长端4胜4负16平、均分下降7.29pp，未晋级全量。72次生成全部正常完成，约29.44分钟". 1.5B aborted at "MrPro19/36条" on user instruction, "不作方法结论". | Long-end mean fell 7.29 pp; 4W-4L-16T is not a win. | Do not promote to a full panel without a long-end win; an aborted run yields no method conclusion. |
| **F10** | L3154 (cited), L4322 (cited), L4400 | **GapCapped** (clamped-gap) candidate. | "32K84.44%对Mr87.22%，128K62.15%对78.13%，0胜7负29平。只跑本方36条、复用原Mr基线，899.87秒" | Reason not stated in this shard beyond the counts. | 0W-7L-29T closes the arm; "不扫描cap" — no cap sweep. |
| **F11** | L4322, L3154 | **CausalGain** and **K/V factor** candidate reruns after GapCapped. | "CausalGain代码虽已通过2项CPU测试但暂停未运行，K/V亦暂停" / "GapCapped失败、CausalGain和K/V因子暂不自动运行". | Author warning against a "改一个破坏一个、十分钟一个猜想" loop (L4322). | Passing CPU tests is not a licence to spend GPU; a candidate needs a mechanism basis first, and the author forbade the patch-one-break-one loop. |
| **F12** | L4322, L3154 | **Double-orthogonal-projection residual** BM (mean-based / "均值双投影") route. | "双正交投影残差BM虽精确保留公共方向，却不是正交相位算子；实际均值产生方向增益最高约1.65，不能忽略。若进一步要求同维正交群在所有lag完整保留循环公共向量轨道，通常迫使退回MrPro。均值核统计亦不支持BM普遍增大背景。故该路线未上GPU，不根据均值代理选表". | The construction is not an orthogonal phase operator; the mean introduces a direction gain up to ≈1.65. | Analytic refutation is enough to veto; "不能把另一几何最优解自动上GPU" — a second geometric optimum is not auto-promoted to GPU. |
| **F13** | L4322 | **18/18 mixed-layer schedule** (mixed native/long layers). | "18/18混层因缺乏构造依据已撤回，STOP已生效，仅两次uniform资格回放，没有混合候选结果" | Lack of a constructive basis ("缺乏构造依据"). | Retracted before it ran; "方法不能猜" — no method may be launched on a guess. |
| **F14** | L4154, L4187, L4224, L4391 | **PSR (Phase-Stratified Routing)**: farthest-first phase grouping of native RoPE phases, B=64, R=4, post-RoPE mean log-mixture selection, then exact read of original KV; tested against **count-matched contiguous and random partitions**. | Real-activation table (L4391): Qwen3.5-0.8B — RoPE mean `.783644`/`.018219`, NoPE mean `.774587`/`.019021`, **PSR `.791685`/`.017753`, Contiguous R=4 `.792752`/`.017686`**, Random R=4 `.786830`/`.018039`; Qwen2.5-3B — RoPE mean `.606341`/`.120433`, NoPE mean `.292119`/`.150254`, **PSR `.609223`/`.119691`, Contiguous R=4 `.612167`/`.120067`**, Random R=4 `.609048`/`.119043`. Per-cell `PSR-contiguous mass` deltas printed at L4187 (12 cells): `-0.00026237964630126953`, `-0.0001399517059326172`, `-0.002800464630126953`, `-0.00018066167831420898`, `-0.00042313337326049805`, `-0.0025951266288757324`, `0.00026166439056396484`, `-0.0048566460609436035`, `0.0024780333042144775`, `-0.0015911459922790527`, `-0.014093995094299316`, `0.0001347064971923828` (9 negative / 3 positive on the printed label). | "PSR does not establish an advantage over its matched controls; increasing the number of summaries explains the observed mean-summary improvement at least as well." (L4391). Later: "这一批查询中，PSR 所针对的相位变化只占很小的分数方差，内容残差占主导。" (L4494) | A phase-design claim must beat a **count-matched and size-matched** contiguous/random control with the same bytes; the sign convention of the printed delta metric must be stated before the delta is used as evidence. |
| **F15** | L4224, L4391, L4299 | Exact-ranking **oracle** diagnostic: rank remote B=64 blocks by exact logsumexp over all keys with native-RoPE vs pre-rotation Q/K, keep 32 remote blocks/query head + 2048 local + 64 sink, then generate the real answer. | Status COMPLETE, 16 questions, 48 outputs, `seconds 69.43104282394052`, `peak_cuda_bytes 12452600832`, `block 64`, `local 2048`, `remote_topk_per_query_head 32`, `max_answer_tokens 48`. "all 48 completions terminated. RoPE and NoPE oracle outputs are identical at the raw-token level on 15/16 pairs. The remaining pair changes a wrong violet assignment to the correct orange one. Manual whole-answer content correctness is Dense 13/16, RoPE 13/16, NoPE 14/16; these are annotations, **not exact-string scores**. Strict full answer plus EOS is 0/16 for every method… Case-insensitive whole-string plus EOS is respectively 2/16, 3/16, 3/16." | "This fails to establish that the large Qwen2 local fidelity gap is binding for these generated answers. It also does not establish equivalence, positional redundancy, or a NoPE method improvement." | An oracle whose per-cell local metric differs but whose generated answers do not differ is not evidence of a bottleneck. The oracle "scans all keys; NOT a runtime/efficiency method" and cannot support any efficiency claim. |
| **F16** | L4471, L4494, L4583 | 24-case natural QA (8 hotpotqa + 8 multifieldqa_en + 8 qasper, 16/32/64K prefixes), Dense vs RoPE-oracle vs NoPE-oracle, whole-completion F1, Qwen3.5-0.8B then Qwen2.5-3B. | Qwen3.5-0.8B `totals.ALL`: Dense n=24 `whole_answer_f1 0.3767993514899828`, trimmed_exact_and_eos 3, normalized_exact_and_eos 5, eos 24; **RoPEOracle `0.34388687705067933`**, 2, 4, eos 23; **NoPEOracle `0.3723293828116474`**, 3, 5, eos 23. hotpotqa: Dense `0.4649436090225564`, RoPE `0.4694078947368421`, NoPE `0.4649436090225564`. multifieldqa_en: Dense `0.3713425631151779`, RoPE `0.37408049597430426`, NoPE `0.37709206466936523`. qasper: Dense `0.294111882332214`, RoPE `0.18817224044089179`, NoPE `0.27495247474302065`. `R_N_identical_tokens: 15`; `N_vs_R_F1: {wins: 3, losses: 4, ties: 17}`. Largest single-instance gap is qasper_196, where both oracles emit `qa_f1 0.11764705882352941`. | "尚未出现一致的选择器收益：Qwen3.5 的 NoPE 平均 F1 较高，Qwen2.5-3B 则较低，差异中还混有措辞和截断影响。" (L4583). Also "最大的单例差异只是'0.6103'与包含同一数值的完整句子，主要反映回答长短" (L4494). | With 15/24 outputs token-identical and a paired record of 3W-4L-17T, a mean-F1 difference between selectors is not a retrieval result; report the paired count and the identical-output count next to every mean. |
| **F17** | L3280, L3308, L3417, L3419, L3214 | Server-side data/artifact availability for the six-arm K-budget experiment. | `ls: cannot access '/root/autodl-tmp/fineweb_edu/sample/10BT/': No such file or directory`; `du: cannot access '/root/autodl-tmp/rotary_budget_20260908/data/train.partial.npy': No such file or directory` with `{"status": "FAILED", "returncode": 1, "pid": 16847}`; `scp: /root/autodl-tmp/hybrid-rope/rebuttal/rebuttal_0723/experiments/fmrope_125m_l256_500m/{protocol.py,run_experiment.py}: No such file or directory`; "原训练数据清单还在，但 token 数组和源 parquet 已不在服务器上" (L3308). | The old experiment's token arrays and source parquet had been deleted from the server; only the manifest survived. | Before scheduling a training run, verify every referenced asset exists and hashes to the recorded value; treat a manifest as an index, never as proof the payload survives. |
| **F18** | L3426, L3434, L3432, L3486, L4539, L4541, L8687–L8692 (dump) | GPU/toolchain qualification for the six-arm run. | `RuntimeError: PyTorch build lacks native sm_89; compiled for ['sm_70', 'sm_75', 'sm_80', 'sm_86', 'sm_90', 'sm_100', 'sm_120']`. A Flash-only SDPA gate raises `RuntimeError(f"Flash-only SDPA gate failed: {result}")` if any of mem-efficient/math/cudnn SDPA is enabled. On the server: `flash_attn None`, `infllm_v2 None`, `triton` present, `FileNotFoundError: [Errno 2] No such file or directory: 'nvcc'`. Locally: `ModuleNotFoundError: No module named 'scipy'` breaks `tests/test_psr_groups.py` collection. | Old registered trainer required an exact native `sm_89` arch entry; the 5090's build lists `sm_120` but not `sm_89`. | Replace exact-arch equality with compatible-cubin checking plus an actual Flash forward/backward probe; qualify the toolchain (nvcc, flash_attn, scipy) before scheduling, not after. |
| **F19** | L3154 (cited), L3159 (cited), L4322 | **The pre-pivot objective itself**: autonomously find a zero-training method that beats MrRoPE-Pro. | "目前最有价值的结果是：BM 在 OLMo 上取得了检索、自然问答和 NLL 的一致改善，但迁移到 Qwen 3B、7B 后出现退化，所以还不能算解决。" (L3116). User: "给你1000000个小时你也做不出来超过mrrope 别浪费我的钱" (L3123). Heartbeat: "旧零训练超越MrRoPE目标已取消，不再启动其候选或恢复旧队列。" (L3159) | User cancelled the goal; the OLMo-only win did not transfer. | Close the objective and all of its candidate arms explicitly; do not restart them under a new name. |
| **F20** | L3206, L3208 | **KLD v2**: a two-slot "erase-sensitivity" memory state (H = ∂S/∂z at z=0) claimed to recover "entity-internal order" information. | "原 KLD 不宜直接作为主投稿方向重投入。正交地址下的历史队列、状态非扩张和共享三角分解，没有证明真实混合模型存在对应瓶颈，更没有证明同状态预算下超过强基线。" The general interpretation is withdrawn: "本版保留有用的算子，但撤回通用'实体内部序位'的解释。" Numeric: with d=128, b=1, after 128 independent interferers current-slot coefficient `0.3664`, history-slot `0.3693`; after 512, `0.01803` and `0.07269` (CPU check JSON: `random_interference.128.S 0.3664377159220373`, `.128.H 0.3693230522678801`, `.512.S 0.018030205213609263`, `.512.H 0.07268870133360585`; `linear_capacity 3.9304255356400377`; `state_budget_bytes_per_sequence_fp32 1048576`; `limitation: "No trained checkpoint, full-network generation, or GPU-kernel performance tested."`). | H is exactly the first Taylor coefficient of S in erase strength, so it "并没有凭空创造一个普通多状态动力学无法逼近的函数族"; and the synthetic-task pass does not prove a real-model bottleneck. | A state-augmentation claim must beat an equal-state-budget ordinary extra memory and a multi-erase-rate memory; an exact-derivative reinterpretation is not new capability. |
| **F21** | L3128, L3134 | The K-budget paper's organising claim: Δ(K) increases monotonically as rotary budget K shrinks (`K=64: Δ≈0`, `K=32: Δ>0`, `K=16: Δ≫0`, `K=8: Δ更强`). | Labelled **"这是一个错误的论文组织目标。"** Refuted by the two-endpoint degenerate case: "固定两个端点、`K=2` 时根本没有内部自由度，anchored EVQ 与 Geo 完全相同。因此收益不可能随着 K 减少一直增大。" Also "**不能从量化误差的 `K^-2` 上界，推出'真实模型中 K 越小，EVQ 收益必然越大'**". | A quantization-error upper bound does not imply a monotone benefit in K. | Never pre-register a monotone ordering in a design variable; test the interaction, and state the degenerate endpoint that makes monotonicity impossible. |
| **F22** | L3134, L3128 | Softmax stress test as evidence that higher static rank ⇒ EVQ wins at half budget. | "**这组结果不支持'EVQ的静态rank更高，所以它一定最佳'。** 在4096的窄响应上E16优于G16，但宽响应相反；P16在2048很强，4096宽响应则明显受损；U16在4096等权KL下优于E16。E16没有达到G32的整体拟合水平。" Also "附录B的本轮受控读取检查进一步显示：P16在2K的测试目标上很强，4K时窄响应和宽响应的排序又不同；E16并没有达到G32的总体拟合水平。**因此不能把'E16大概率全面胜出或半预算无损'作为已经得到的预测。**" Also "不能只根据§4.3的谱表就为18次完整训练提供'基本必胜'的承诺，也不能把等权混合的KL当作自然语言任务权重。" | Rank/spectral proxies do not order the task-relevant objective, and the equal-weight KL mixture is not a natural-language task weighting. | No proxy (static rank, equal-weight KL, controlled read) may be used to pre-commit a training budget; the strongest expected outcome must be written as a hypothesis to be tested, not a prediction already obtained. |
| **F23** | L2660 (dump) | A previously-completed frozen-model **boundary-slope arm**. | "whose boundary-slope arm scored zero in its stated core-4 protocol. The algebra explains the mismatch; it does not by itself explain the behavioural score." | The alignment algebra explains the mechanism but the behavioral zero is unexplained. | An algebraically-clean construction that scores exactly 0 in its own registered protocol is a failure, not a pending result; do not retrofit the algebra as an explanation of the score. |
| **F24** | L4322 | Qwen 128K failure diagnosis. | "C/P均满分；保留完整预填充KV的L多键仍0%、VT仅60%，O与原冒烟逐token一致。说明原长背景形成的状态参与退化，不能仅归因于128K坐标或单一竞争key；层/头/槽机制尚未识别。" Also (L4322) "VT/MrPro缓存query与完整O仅第30token有差异，因此另立匹配cached基线、同表重建逐token核对，不能宣称原输出完全一致或100% EOS exact。" | The degradation is caused by the state formed over the long background, not by the 128K coordinates or a single competing key — but the layer/head/slot mechanism was never identified. | A "reproduces the baseline" claim requires token-exact equality against a matched cached baseline; a 1-token divergence at token 30 invalidates it. |

### Verbatim evidence blocks (long JSON)

**F1** `verdict(rows['MrProBM'], rows['MrPro'])` at L2304, abridged to the decision fields:

```
  "status": "NO_LONG_GAIN",
  "macro_delta_by_length": { "32768": -0.03333333333333344, "131072": -0.1333333333333333 },
  "paired_wins": 0,
  "paired_losses": 3,
  "evidence_scope": "Six-task RULER development subset; not full RULER or independent confirmation"
```

**F1 doc text written at L2537** (the file `docs/research/ROPE_QWEN7_BM_TRANSFER_20260908.md`):

> "32K BM80.00%对Mr83.33%，128K BM71.11%对Mr84.44%；0胜、3负、15平。128K三个检索任务均满分，FWE同66.67%；VT从90%降至60%，QA从50%降至0%。短端VT从100%降至80%。这是有限筛查，不足以宣称所有任务失败，但本轮没有新增赢过Mr的样本，不能把OLMo收益推广至这个Qwen模型。停止未具收益依据的全量扩展。run_screen_02新增30次生成、复用6条，1380.62秒，峰值分配27.38GiB；首轮OOM仍保留。"

**F4 control result** (L2540): `{'method': 'PadBM192', 'row': 'niah_multikey_2_131072_1', 'M': 1.0, 'B': 0.0, 'new': 0.0, 'same_B_tokens': True, 'output': ' 9289114.', 'eos': True}` and `{'method': 'PadBM192', 'row': 'vt_131072_1', 'M': 1.0, 'B': 0.2, 'new': 0.2, 'same_B_tokens': True, 'output': ' ZOFPD.', 'eos': True}`.

---

## (c) Repeated failures

A repeat is a much stronger signal than a single occurrence. Ordered by strength.

**R1 — Post-hoc repair of the Qwen transfer failure failed four consecutive times.** F3 (prefix/reader handoff, L2380/L2628) → F4 (learned bias position term, L2601) → F5 (K/V source mixing, L2680) → F7 (selective gain, L2489). Four distinct mechanisms, four CLOSED verdicts, four explicit stop instructions ("暂停这个两阶段候选" L2380; "停止这个构造，不再调整其系数或层范围" L2601; "没有形成值得部署的组合" L2680; "不晋级、不再调系数" L2489). Rule: the Qwen transfer failure is not a repair target.

**R2 — The same failure appeared on two Qwen model sizes and was then re-tested on a third.** F9 (Qwen2.5-3B: 32K 91.67%/87.22%, 128K 70.83%/78.13%) and F1 (Qwen2.5-7B: 32K 80.00%/83.33%, 128K 71.11%/84.44%) both fail at the long end with a short-end win. Agent summary at L2310: "BM 在 OLMo 上的优势目前没有迁移到两个 Qwen 模型". Rule: two Qwen sizes is a family-level negative, not a sample-size problem.

**R3 — PSR lost to its matched control in two different models.** L4154 (Qwen3.5-0.8B/partial-RoPE): "PSR 比单均值好，但没有显示出优于同容量连续分组的优势". L4224 (full-dimension RoPE checkpoint): "全维 RoPE 模型上的检查也没有显示 PSR 的独立优势，先不扩大这条方法." Rule: the phase-grouping effect is not present in either the partial-RoPE or the full-RoPE regime on these checkpoints.

**R4 — "No selector gain" repeated across two model families with opposite sign.** F16: Qwen3.5-0.8B NoPE mean F1 higher (0.3723293828116474 vs 0.34388687705067933), Qwen2.5-3B lower; `N_vs_R_F1` 3W-4L-17T. Rule: a sign flip across models is a null result, not a small win.

**R5 — Expected server-side artifacts were missing in three separate places.** F17: `fineweb_edu/sample/10BT/` absent (L3280); `rotary_budget_20260908/data/train.partial.npy` absent → `{"status": "FAILED", "returncode": 1, "pid": 16847}` (L3417); the two reference `.py` files absent from the scp glob (L3214). Rule: asset existence is a prerequisite gate, and it failed on 3 of 3 checked paths in one session.

**R6 — "32K is at floor" repeated across three configurations.** F8: S8 MrPro `0.69%`, S8 MrProBM `6.94%`, BM S4 freq/gain on the same 32K input `0%`, S8-gain-lowered-to-S4 `5.56%`; plus F9's 32K was the *winning* cell for Qwen3B (91.67%) but F1's 32K is a loss on 7B. Rule: 32K results must be reported per model family; there is no shared 32K story.

**R7 — The same "mean-based table swap" idea was killed twice, once empirically and once analytically.** F6 (native-window mean/rephase route, aggregate below BM in both panels) and F12 ("均值双投影路线已因不充分机制收回" L3154; "均值核统计亦不支持BM普遍增大背景" L4322). Rule: mean-based pooling as a table-construction primitive is closed on both evidence and algebra.

**R8 — The same "more summaries must be better" error, in the metric and in the interpretation.** F14's own table shows `Contiguous R=4` (`.792752`, Qwen3.5) above `PSR` (`.791685`) and `Contiguous R=4` (`.612167`, Qwen2.5-3B) above `PSR` (`.609223`); the agent's stated reason is that "increasing the number of summaries explains the observed mean-summary improvement at least as well" — i.e. a confound it had pre-registered as a control (L4391). Rule: any multi-summary method must be reported against the count-matched control first, in the same table row order.

---

## (d) Vetoes and "do not restart" — verbatim

| Line | Verbatim quote |
|---|---|
| **L3159** | "旧零训练超越MrRoPE目标已取消，不再启动其候选或恢复旧队列。" (heartbeat/automation update that replaced the objective; mirrored at L3154 and L3160) |
| **L4322** | "**适用边界：** 静态S8在32K BM6.94%、MrPro0.69%，检索/追踪地板；同32K使用S4 BM为0%，S8频率+S4gain为5.56%，两个恢复方案均不晋级。Native短端也有逐任务取舍。**不要重启这些固定负结果**，或把短端恢复当32K收益。" |
| **L4322** | "**后续研究边界：** …现有结果不支持继续调失败gain系数、盲增scale或无条件扩大32K矩阵。" |
| **L4322** | "作者警告不要"改一个破坏一个、十分钟一个猜想"循环，因此CausalGain代码虽已通过2项CPU测试但暂停未运行，K/V亦暂停；不扫描cap。下一阶段先建立能同时分析原生关系保留与长距区分、对选法有判别力的机制依据，再针对验证；不能把另一几何最优解自动上GPU。" |
| **L4322** | "作者强调方法不能猜：18/18混层因缺乏构造依据已撤回，STOP已生效，仅两次uniform资格回放，没有混合候选结果，当前GPU空闲。方法需有机制/推导依据且能在论文讲清楚。" |
| **L3154** | "均值双投影路线已因不充分机制收回，GapCapped失败、CausalGain和K/V因子暂不自动运行。" |
| **L2601** | "两条零补齐对照与原 BM 的完整输出逐 token 相同，因此停止这个构造，不再调整其系数或层范围。" |
| **L2380** | "修正精度后，阶段诊断完成了：两条 OLMo 正例都无法由"MrPro 前缀、BM 读取"救回，因此暂停这个两阶段候选。" |
| **L2489** | "均低于原BM开发16K49.03%，不晋级、不再调系数。" |
| **L4224** | "全维 RoPE 模型上的检查也没有显示 PSR 的独立优势，先不扩大这条方法。" |
| **L2537** | "停止未具收益依据的全量扩展。" |
| **L4322** | "若用户叫停须同时停止等待队列。" (process veto on the queued OLMo stage_run_01) |
| **L3128** | "**不能从量化误差的 `K^-2` 上界，推出"真实模型中 K 越小，EVQ 收益必然越大"。**" |
| **L3128** | "这是一个错误的论文组织目标。" (immediately after the K=64/32/16/8 Δ table) |
| **L3134** | "**因此不能把"E16大概率全面胜出或半预算无损"作为已经得到的预测。**" |
| **L3134** | "不要删除P16或U16。它们防止把"缩小K后错误地重填宽support"产生的损失误当成领域普遍问题。" |
| **L3128** | "不能临时换曲线、换主指标或把P16/U16移出主表。" |
| **L3206 / L3208** | "本版保留有用的算子，但撤回通用"实体内部序位"的解释。" |
| **L3206 / L3208** (KLD v2 doc, `# KLD v2：关键审查、修正推导与最小真实模型实验`, section 0 投入结论) | "原 KLD 不宜直接作为主投稿方向重投入。" |
| **L3884** (assistant message; the quoted Pro/ChatGPT reply reappears in the accessibility tree at L3934/L3935/L3941/L3942) | "**Prism 已经分析了 RoPE 与均值池化之间的相消，并提出分频段评分和能量校准。**所以，不能再把"池化破坏位置信息"当作我们的发现。" |
| **L3884** | "**COBS 已经用块内协方差改进 attention mass 估计，而且明确讨论了 NoPE、RULER-style SFT 与实际运行时间之间的混淆。**所以，"增加二阶统计量""更准确估计块的重要性"也不是空白。" |
| **L4058 / L4061** (doc `native_sparse_position/DESIGN_SOURCE.md`) | "**Quest** … 必须比较成熟 decode 方法，不能只打一个均值基线。" / "**RNoPE-SWA / HyPE** … 不能把分支分工或"全局层去 RoPE"当新意。" |
| **L3128** (paper text read from the repo) | "Partial-RoPE与MHA2MLA… 因此"只有一部分维度需要旋转"以及"选择策略在小维数时重要"都不能作为新发现。" |
| **L3489 (user)** | "我再重申一次实验原则，不是上来就六臂，我们完整性是其次，一定是先把我们最solid的实验做出来，再去和别人比，你自己决定吧，不要浪费我的GPU时间" |
| **L3123 (user)** | "算了，你调转方向，… 给你1000000个小时你也做不出来超过mrrope 别浪费我的钱，继续实验，监控，并重构论文" |
| **L3721 (user)** | "我的不要浪费GPU是你不要空转" (agent restated at L3726: "'不要浪费GPU'指**不要空转，思考与有效实验并行**，我刚才理解偏了。") |
| **L3672 (user)** | "这个不需要，最多20min，你就可以读取，定时任务够了，你继续不要浪费GPU很重要" |
| **L4125/L4128/L4400** | "已有Qwen2.5-3B与Qwen3.5-0.8B短/长完整输出检查均完成，保留格式/答案/EOS差别：3B长80条只有79条EOS，不能说全部终止" |
| **L4322** | "不要再定时轮询该回复，不向任何其他对话发消息。" (appears in the L4400 heartbeat text) |

---

## (e) What DOES work, with numbers and configuration

These are the only positive, number-backed configurations in the shard.

**W1 — BM (boundary-matched) static S4 table on OLMo-2-0425-1B-Instruct (the one real win).**
Config: `allenai/OLMo-2-0425-1B-Instruct`, revision `48d788eca847d4d7548f375ad03d3c9312f6139e`, 1,484,916,736 parameters, BF16, Flash SDPA, greedy, `repetition_penalty=1`, **no training**. Six tasks (niah_single_2, niah_multikey_2, niah_multiquery, VT, FWE, SQuAD QA), equal weight within each length, official per-item answer budget and match-all/match-any scoring, inputs not truncated. Confirm-set actual lengths: 4K `2262–4065`, 16K `14839–16354`. QA uses `pre_samples=64`.

| Panel / method | 4K mean | long-end mean | cap |
|---|---:|---:|---:|
| dev MrPro | 37.22% | 14.93% | 16K |
| dev MrProBM | 79.44% | 49.03% | 16K |
| confirm MrPro | 37.85% | 2.78% | 16K |
| **confirm MrProBM** | **81.81%** | **51.32%** | 16K |
| confirm MrUni (control) | 76.88% | 32.12% | 16K |
| confirm OfficialYaRN (control) | 54.38% | 6.94% | 16K |
| S8 MrPro | 4.17% | 0.69% | 32K |
| S8 MrProBM | 23.89% | 6.94% | 32K |

Prompt-level counts: dev BM vs MrPro **24胜1负11平**; confirm **44胜0负28平**; confirm vs MrUni **22胜6负44平**; confirm vs Official YaRN **40胜2负30平**. Dev panel = 36 rows (seed 20260909); confirm = 72 rows (seed 20260910), "两组prompt SHA无重叠".

**W2 — The BM API and install recipe that produced W1.** `scripts/lib/rope/boundary_matched.py::boundary_matched_inv_freq(native_fp32_inv_freq, base, reference_length, scale)` returns FP32 frequency table + cos/sin amplitude; "其输出与本轮S4实际数组逐位一致"; it does not train, change positions, handle KV cache, or choose its own scale. Install recipe (verbatim, from L2824): set `rotary.inv_freq`, clone to `rotary.original_inv_freq`, set `rotary.attention_scaling = gain`, after the model has been converted to BF16 — with the constraint "不得叠到已经scaled/dynamic的checkpoint或中途更换已有KV cache的频率".

**W3 — OLMo natural-text and QA extensions of W1.** Three natural QA tasks, 567 rows/arm: extended-length F1 macro mean **BM 26.01% vs Mr 21.37%**. New 7 RULER tasks × 50 rows/arm: 16K macro mean **41.67% vs 7.09%, 156胜9负**. Natural NLL (16 documents, natural prefix, last-512 next-token): 4K `Native 2.835 / Mr 3.211 / BM 2.955`; 8K `Mr 3.257 / BM 2.955`; 16K `Mr 3.688 / BM 2.862`. Caveat recorded by the agent: "NLL仅最后512下一token，不是生成证明".

**W4 — Per-task trade-off disclosure that must accompany W1 (this is the honest boundary).** On the same confirm 24 rows at 4K: `Native 75.63%`, `BM 81.81%`; but BM's QA fell `100% → 75%`, multiquery `93.75% → 87.5%`, VT rose `10% → 70%`. "均分提高不等于原生逐任务保持。"

**W5 — A control that worked and is reusable.** `PadBM192` (192-dim zero-padding of the rotary table to exercise a different Flash path) reproduced BM's outputs **token-for-token** on both test rows (`same_B_tokens: True` for `niah_multikey_2_131072_1` and `vt_131072_1`, outputs `' 9289114.'` and `' ZOFPD.'`). This is what let the agent conclude F4's failure was a method failure, not a kernel-dimension artifact — a control worth re-using.

**W6 — Determinism / regression evidence for W1.** "扩展幅度运行器后，重复BM开发36条的生成token与原运行器完全一致。累计针对性测试当前29项通过（原20项、长度主终点1项、幅度3项、BM部署5项）". Also "7个GPU监督阶段全部正常完成，共780次完整生成（包含对照与重放，不是780条独立样本），监督阶段合计1475.99秒，含模型加载".

**W7 — A working GPU qualification path (after F18).** Replace exact-arch equality with compatible-cubin checking plus a real Flash forward/backward probe. The probe that eventually ran: `NVIDIA GeForce RTX 4080 SUPER`, `capability [8, 9]`, `native_arch_listed: false`, `compatible_cubins: ["sm_80","sm_86"]`, torch `2.8.0+cu128`, CUDA `12.8`, `flash_forward_backward: "PASS"`, micro_batch 8, compile default → `steady_tokens_per_second 74649.81574553056`, `conservative_tokens_per_second 74513.70966140887`, `peak_cuda_bytes 11182305280`, `estimated_18_arm_training_hours 33.48796905971831`, 151,898,880 params, 7,629 updates, 499,974,144 input tokens. **This record carries `"discarded": true` and `completed_updates: 10`** — it is a 10-update probe, not a training run.

**W8 — The six-arm K-budget design itself (a deliverable, never run in this shard).** Fixed total head width 64, 12 heads; arms G32/E32/G16/E16/P16/U16; seeds 42/137/256; primary endpoint = document-mean NLL on the last 256 identical target tokens at 4096, from a table frozen at 2,048 training length; retention condition = same-target NLL at 2048 plus full-window 2048 NLL; importance threshold `0.02 nat/token` (≈2% PPL ratio) and native non-inferiority margin also `0.02`; same `tau` for all K; attention denominator stays `√64`; the same relative position must give the same rotation inner product before and after translation; interleaved layouts keep fixed `(2j, 2j+1)` pairs. **Status at the end of the shard: data prepared, probe complete, no formal six-arm run started.**

**W9 — Natural-QA data prep that passed.** "自然问答的 24 条输入已按源数据哈希顺序固定，三类任务各 8 条；在两个模型的分词器下都满足 8K–16K 长度，未截断文档。" (L4407). And the Qwen3.5 native-path qualification: "让选择器保留全部 key 时，两种实现都与原生 dense logits 逐位一致" (`all_key_gather_bitwise_logit_parity: {"RoPEOracle": true, "NoPEOracle": true}`, L4448/L4299). This is the gate that makes the F15/F16 negatives trustworthy.

---

## (f) Cases where I think the transcript's own stated reason is wrong or unsupported

**G1 — The "learned Q/K bias is the cause of the Qwen failure" reason is refuted by the shard's own next experiment.** At L2380 the agent states the reason: "Qwen 的 Q/K 投影含有学习到的 bias，OLMo 没有。BM 会改变它们形成的位置项", and designs F4 around it. F4 then fails on exactly that prediction, and at L2601 the agent closes the construct. Any later document (or handoff) that still explains the Qwen transfer failure by the learned Q/K bias is contradicted by F4's own numbers. The transcript later offers the correct, narrower statement at L2707: the outcome follows the prefix-formation table.

**G2 — "NoPE 的平均 F1 高于 RoPE oracle" (L4495) reads as a NoPE advantage that the same output's own numbers do not support.** The full 24-case JSON printed at L4471 has `R_N_identical_tokens: 15` and `N_vs_R_F1: {wins: 3, losses: 4, ties: 17}`, and the per-task picture is mixed (hotpotqa: RoPE `0.4694078947368421` > NoPE `0.4649436090225564`; multifieldqa_en: NoPE `0.37709206466936523` > RoPE `0.37408049597430426`; qasper: NoPE `0.27495247474302065` > RoPE `0.18817224044089179`). The "ALL" gap (NoPE `0.3723293828116474` vs RoPE `0.34388687705067933`) is an unpaired mean over 24 cases. The agent does flag the largest single case ("0.6103") but the sentence's framing survives into L4583.

**G3 — The stated reason for PSR's failure is directionally understated.** L4391 says PSR "does not establish an advantage over its matched controls; increasing the number of summaries explains the observed mean-summary improvement at least as well." But the PSR row is *below* the contiguous control in both models (`.791685` vs `.792752`; `.609223` vs `.612167`), and 9 of the 12 printed `PSR-contiguous mass` cells are negative. "Not better, and more summaries explain it" is not the same claim as "worse in a majority of cells". **Caveat: I could not find the sign convention of the `PSR-contiguous mass` metric stated in any printed line of my shard** — so I flag the direction, not a contradiction. Whoever owns this should state the sign convention before the deltas are used again.

**G4 — "对无 bias 的 OLMo，它严格等于原 BM" (L2380) was never tested in this shard.** The BiasPosition run at L2540 exercises Qwen rows only (`vt_32768_1`, `fwe_32768_1`, `niah_multikey_2_131072_1`, `vt_131072_1`, `niah_multiquery_131072_3`). I saw no OLMo row for this construct in lines 2300–4600, so the "strictly equals original BM on OLMo" property is a design claim, not a measured one, in this shard.

**G5 — "short training probe17018 is COMPLETE" (L4322) can be misread.** The underlying record (L3486) is `"discarded": true`, `"completed_updates": 10` out of `"updates": 7629`, and it ran on an RTX 4080 SUPER, not the training target. Read it as "the throughput probe finished", never as "training completed".

**G6 — The reason given for the 128K OOM is operational, not methodological.** L4322: "run_screen_01首128K OOM后保留6条短MrPro；run_screen_02/PID9764用expandable_segments恢复并校验复用这6条". The fix is stated (`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` plus 4096-token MLP chunking, L2381/L3154) but the reason the first attempt OOM'd is not diagnosed in this shard. The 6 reused rows are flagged "已校验复用" and "**勿重跑基线**" (L3154) — a legitimate reuse, but it means F1's 18-row panel is 30 new + 6 reused, not 18 independent.

---

### Method note (for the next miner)

- 187 `custom_tool_call` + 28 `function_call` records, 491 reasoning summaries (all short titles like `**Preparing bias-term context**` — they carry no content; the substance is in tool outputs).
- The richest single records for failures are the **automation/heartbeat prompt texts** (L3154, L3159, L4322, L4400) and the **goal-state audits** — they contain verbatim vetoes and the consolidated negative ledger. Mine them first in any other shard.
- Files the agent wrote that consolidate these negatives (all referenced, none re-read here): `docs/research/ROPE_GAP_CAPPED_RESULT_20260908.md`, `ROPE_BM_SELECTIVE_GAIN_20260908.md`, `ROPE_BM_SCALE_CAP_20260908.md`, `ROPE_BM_128K_DIAGNOSIS_20260908.md`, `ROPE_QWEN7_BM_TRANSFER_20260908.md`, `experiments/native_sparse_position/CORE_DIAGNOSIS.md`.

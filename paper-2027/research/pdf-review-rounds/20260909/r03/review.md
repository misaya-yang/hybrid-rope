# Round 3: independent PDF-only review

Input SHA256: `309e140c784305e6fb07e59f02b9a342f17631a32c28f9d27633dc1a9253ad3a`. Fresh Sol, high reasoning; PDF only. Core contribution read as range, allocation and learned compatibility, supported by fixed-endpoint controls and complete sin/cos geometry. Verdict: borderline; medium-high confidence. No decisive main-theorem or fixed-range error found.

1. **Llama temporal protocol missing** (pp5,33–34). Accepted. Restored the exact historical numerical artifact (SHA256 `0335415a2245e1fb31149705342e975a016ddddb557a79c364fc4a98c3f89001`) from `main_0726`. Recomputed each domain's loss sums/counts, all 24 pack differences, and the equal-domain NLL macro. Added sources, date bounds, LongAlpaca adaptation data, disjoint-document packing, nested prefixes, cross-document attention, masks for document starts and EOS separators, aggregate formula, valid-token totals/ranges, and pack-delta distribution. The source archive includes the pack loss/count summary. LongAlpaca's public source and paper citation were checked.
2. **Cosine-only diagnostic cannot justify the full-subspace construction** (pp4,17,19–20). Accepted the structural criticism. Removed the optional historical cosine-slice table and its surrogate-validation subsection. The full-subspace analysis, its explicit counterexamples, and the Cosh criterion remain separate, clearly defined results; no new optimizer-of-rank claim or model-loss theorem was added. The existing full-subspace plots remain. Deleted material is preserved in Git.
3. **Index/YaRN amplitude attribution** (pp7,34). Main text now explicitly states that the installed table and amplitude together give the +6.09-point comparison. Same-amplitude direct-gap/index experiments remain the placement controls. Added the 95% confidence level at first report.
4. **Whitening removes physical scaling** (pp4,14–16). Added the exact self-Gram eigenvalues `(1±|sinc(omega L)|)/2`, small-frequency minimum eigenvalue `(omega L)^2/12`, and condition number `12/(omega L)^2`. Main text identifies r2 as directional overlap after within-pair normalization; raw feature energy and coefficient magnitudes have their own roles. This is algebraic clarification, not a model experiment.
5. **Appendix figure/float readability** (pp15,19,25). Moved the 750M result table next to its protocol; increased the frequency-geometry figure's labels, shortened titles, and enlarged its height. Removed the redundant cosine-slice pages as part of item2.

The independent source-archive smoke check also passed: five regenerated PNG figures and four table sources match exactly; the compiled PDF has identical extracted text. The eight finite-basis calculations agree with independent quadrature to 3.55e-14.

Output is the round4 PDF.

# Round 2 integration

Review: [fresh independent PDF-only report](review.md), internal 6/10, weak accept,
confidence 4/5. The reviewer received only the round-1-optimized PDF. Its score
applies to that input, not the subsequent edits or newly completed QA result.

| Finding | Integration | Status |
|---|---|---|
| W1 matched T/C | Preserve the measured primary contrast and runtime-qualified role; describe TailSpline as an effective chosen boundary prior. The exact C-only repair remains in the executable experiment arrangement. | No new GPU run launched by this review task. A stronger residual-shape result is not claimed. |
| W2 MLA data provenance | Put the absent historical document-exclusion receipt directly in the Figure 3 caption, next to the observed paired-cache gain. Keep τ and shared-cache/offset identity in the main paragraph. | Existing six-checkpoint disjoint-data reevaluation is a valuable follow-up if those checkpoints are available; it is not represented as completed. |
| W3 native uncertainty | Keep the main 2.33% observed relative difference, its absolute difference and paired interval, and the distinct panel contracts together. Remove repeated exact native percentages from introduction/conclusion in favor of the measured trade-off narrative. | Retains direct, positive description of the clean gain; does not convert the interval into a retention guarantee. |
| W4 construction interpretation | Retain explicit frequency-space objectives, reference strengths, favorable retained-support and unfavorable retargeted-support regimes, and the existing parity-lattice counterexample. Clarify that the dose/centroid identity assumes a fixed band and s>1. | Existing theory and controls supply this clarification. No additional optimizer or speculative mechanism introduced. |
| W5 historical permutation | Keep its disclosed missing run identity and secondary role; the main figure uses the fully specified crossed-table control. | Exact historical replay remains a provenance gap, not a fabricated successful new intervention. |
| Narrative heading | Rename to “Learned compatibility and slot assignment,” since the paragraph contains both different-spectrum crossings and same-spectrum permutations. Point the retained-support result to main Figure 1b. | Implemented. |
| Clean selection timeline question | Describe first-200 source-order preparation, fixed extra QA-1 source fragment, preparation before arm execution, no generated-answer/score input, and exact reuse of classic TailSpline tensor/gain metadata. | Verified in the existing preparer/runner and saved classic/clean contracts. No stronger timestamped preregistration claim manufactured. |

## Newly available high-value evidence: complete Natural-QA631

While the independent reviewer was reading its frozen input, the existing
Natural-QA queue completed both arms. This result was **not** sent to the reviewer
or substituted into its PDF. It is integrated during this optimization step.

- Read the completed V2 report and 631 raw generations per arm; checked COMPLETE
  statuses and raw hashes, paired prompt/reference identities, and run contracts.
- Recomputed all 1,262 answer F1 scores locally. The five-task primary point and
  20,000-draw source-context cluster interval reproduce the source report exactly.
- Main result: T/P F1 41.0791/40.8834%; T−P +0.1957pp, CI [−1.5311,+1.8864]pp.
- Full pool has 524 source-context clusters. The >8192-token stratum has 315 rows:
  T−P −0.9383pp, CI [−4.1494,+1.9539]pp. The 316 within-native rows give
  +0.6998pp [−1.2478,+2.8073]pp. All strata compare the two extension tables.
- T/P EOS counts are 625/624; caps 6/7; empty outputs 0/0. All responses retained.
- Added a main table for the full pool and both length strata, and an appendix
  with all five task means, source counts, output health and document-equal
  sensitivity. The original primary estimand was not changed.
- Portable score-only records now reproduce the clean 2,600-pair and natural
  631-pair results. Large prompt/token streams remain on the existing server.

The result makes the application evidence more complete: TailSpline's clear
clean-RULER improvement and small/uncertain natural-QA contrasts are both visible.
It neither borrows BM's QA gain nor converts a zero-crossing interval into proof
of equivalence. The abstract continues to make its positive task claim explicitly
about RULER on Llama.

## Scientific follow-ups kept concrete

1. Matched-runtime C-only repair on the frozen classic panel if stronger
   residual-shape attribution is pursued: 390 generations, and at most 138 LM
   rows unless valid equivalent LM rows can be reused.
2. Existing 432M checkpoints evaluated on a pinned, documented disjoint corpus,
   with common native/extended offsets; first establish checkpoint availability.
3. Optional clean YaRN single arm for baseline breadth. No curve/gain sweep.
4. Clean native task inputs if a precise native-retention tolerance becomes a
   target; define tolerance before inspecting results.

These are future model evaluations, not claimed outcomes of the two completed
review/optimization cycles. Existing GPU queue ownership is preserved.

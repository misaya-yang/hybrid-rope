# ICLR 2027 whole-paper revision plan

> **SUPERSEDED (2026-08-27):** the revision roadmap moved to
> [`REVISION_BRIEF.md`](../REVISION_BRIEF.md) v2 (five-seat-panel-grounded).
> Retained for routing history; do not execute from this file.

- **Date:** 2026-08-26
- **Role:** internal execution plan for the 2026-08 whole-paper writing pass
  (revision cycle closed 2026-08-28)
- **Authority:** narrative and file-routing only; this file owns no fact,
  number, protocol identity, evidence tier, or mutable manuscript state
- **Objective:** maximise ICLR 2027 acceptance probability by making a reviewer
  recover one claim--evidence--implication chain without reading the paper as a
  historical rebuttal

Facts and claims defer to `AGENTS.md`, `INDEX.md`, `paper-2027/HANDOFF.md`,
`paper-2027/NARRATIVE_GUIDE.md`, and the canonical owner routed by
`paper-2027/research/README.md`.

## Reviewer memory to optimise

> Finite RoPE has a third design coordinate. At fixed support, pair count, and
> operator, interior allocation `z` determines which phase scales are sampled
> and which effective positional basis the nominal rotary budget realises.
> Exact geometry diagnoses that basis; a fixed-support three-seed intervention
> identifies its behavioural effect; three protocol-separated routes show that
> the control remains consequential through training, adaptation, and frozen
> deployment. EVQ-Cosh is one bounded closed-form witness on this axis.

## What the NeurIPS panel actually remembered

| Reader | Successful reading | Failure that limited the score | Structural repair in this pass |
| --- | --- | --- | --- |
| `Dz6s` (4) | Understood operator/table separation and saw a practical conceptual framework. | Read the evidence as diagnostic-heavy and too far from mature tasks/models. | Route completed 432M/750M/1.485B/8B, RULER, QA, remote-source, and frozen evidence through one lifecycle map while preserving endpoint identities. |
| `27bE` (3) | Rewarded the distinction among exact statements, surrogate assumptions, and operating choices. | Remembered the surrogate, finite-`tau` caveats, and layered qualifications more strongly than the scientific consequence. | Make exact diagnosis and bounded witness two explicit questions; reduce body weight on operating-rule plumbing; lead with the basis and causal consequence. |
| `zWsa` (2) | Recognised allocation as potentially valuable. | Missing citation and matched control collapsed the paper into an FMRoPE repetition claim. | Keep FMRoPE as related work and the paper-faithful fixed-support protocol control; make the third coordinate, not the comparison, the reviewer memory. |
| AC | Saw a careful EVQ-Cosh paper with small-model diagnostics. | Did not retain the third coordinate or an evidence ladder with mature capability endpoints. | Re-route the whole body as finite-table coordinate -> exact theory -> causal identification -> three lifecycle routes -> field implication. |

The old scale/RULER/real-task gaps were real. The current manuscript now owns
evidence at 432M, 750M, 1.485B, and 8B, plus RULER, QA, causal remote-source
use, and frozen-checkpoint interventions. The present bottleneck is reader
routing and evidence legibility, not new compute.

## Five modules and their unique responsibilities

### 1. Introduction -- gap and reviewer memory

- **Files:** `sections/00_abstract.tex`, `sections/01_intro.tex`, Figure 1 caption.
- Establish `x=a+Rz`, the `46 -> 2.00` hook, and `z` as phase-resolution
  placement within a fixed nominal rotary budget.
- Keep one sentence promising the fixed-support three-seed causal result, one
  mature frozen-checkpoint hook, one three-route preview, and three
  contributions.
- Remove the isolated `It does.` transition, the full frozen score/interval
  ledger, and the theorem/table checklist.
- State EVQ-Cosh once as a closed-form zero-learned-parameter witness for the
  stated surrogate.

### 2. Identification -- primary causal owner

- **File:** `sections/02_identification.tex`.
- Preserve held fixed -> only 30 interiors moved -> 3/3 at every OOD length ->
  identified implication.
- Keep FMRoPE only as the paper-faithful geometric fixed-support control.
- Remove the frozen-checkpoint duplicate; route it to the lifecycle section.
- Keep 50.9M as one configuration/shape-breadth sentence, never a scale point.
- Keep target-matched evidence in Appendix E and its canonical owner, not the
  remembered body story.

### 3. Theory -- exact object and co-adaptation boundary

- **Files:** `sections/03_theory.tex`; `appendix/a1_proofs.tex` only for moved
  material and references.
- Open with two separate questions: (i) what positional basis does `z` change,
  and (ii) what bounded analytic witness follows from a modelling surrogate?
- Preserve full sin/cos subspaces, phase-invariant canonical correlations, the
  exact spectral-budget identity, slow collapse, the weights-by-table crossing,
  and the transplant obstruction.
- Keep the theorem/corollary defining EVQ-Cosh, but reduce body weight on
  empirical `tau`/grid plumbing and repeated scope explanations.
- Move `fig_frequency_geometry.pdf` from the body to Appendix A exactly once;
  Figure 1 already carries the body-level geometry hook.
- Preserve every correct proof and introduce no new proposition.

### 4. Experiments -- lifecycle persistence

- **Files:** `sections/04_experiments.tex`; one minimal related-work edit only
  if needed for LeRoPE attribution.
- Keep the three routes strictly separate: from-training/co-adapted, matched
  low-rank adaptation, zero-training frozen checkpoint.
- Implement the evidence map as three bold route leads rather than a cramped
  results table: when allocation changes, what is fixed, the question answered,
  and one headline anchor per route.
- State once that metrics and uncertainty are protocol-specific and not pooled;
  keep detailed boundaries in the route subsections.
- Preserve the 1.485B pre-specified early-training trend, 8B adaptation label,
  frozen derived/ramp identity, and DiT as modality breadth.

### 5. Discussion -- field implication

- **File:** `sections/05_discussion.tex`.
- Do not repeat numbers or reconstruct the theory section.
- Establish support selection, interior allocation, and range transport as
  distinct interacting intervention levels.
- State `z` as a general finite-table coordinate that reallocates positional
  capacity in-window and out-of-window; the strongest completed causal evidence
  is long-context.
- Position learned allocation as compatible use of the same design space, not
  validation of EVQ-Cosh or this mechanism.
- End with the field implication first, followed by one short protocol scope
  clause.

## Visual carrier plan

1. Keep Figure 1 as the only body overview: allocation coordinate, exact
   slow-block hook, and fixed-support empirical identification.
2. Move the overlapping `fig_frequency_geometry` visual to Appendix A, retaining
   its label and one appendix reference from the body.
3. Use three bold lifecycle route leads instead of a multi-column table after
   the table proved too ledger-like at readable one-column type size. Do not
   repackage the same endpoints as a new lifecycle figure.
4. Do not regenerate existing figures or add a heavy plotting dependency.

## File-level edit order

1. Tighten abstract and Introduction around the reviewer-memory chain.
2. Restore Identification to the sole 151.9M causal owner.
3. Rebalance Theory; move the overlapping figure and operating details.
4. Build the lifecycle route map without a second meta-explanation paragraph.
5. Rewrite Discussion as implication, not recap.
6. Make only locator/input/page-break changes required in `main.tex` or
   appendices.
7. Compile, inspect pages, and iterate only on demonstrated layout defects.

## Repository gap-audit conclusion

The page limit is a ceiling and page count is an outcome. The repository audit
found no missing body-essential science in the completed Introduction,
Identification, Related Work, or exact Theory. Only two appendix-owned results
add body value without repeating an existing endpoint: the fresh FineWeb-Edu
pure-$z$ natural-NLL control and quantified two-seed video-DiT modality breadth;
both are now routed compactly in Experiments. Other audited material remains in
its appendix owner or excluded from the body. No filler, lifecycle replot, new
meta-explanation, or page-count-driven expansion is warranted.

## Post-submission stronger-model theory audit

After submission, a stronger reasoning model may audit a possible unification
of the exact pair-exposure identity
`lambda_±(S_w)=(1±|chi_mu(2w)|)/2`, the real/full RoPE kernel, and learned
allocation objectives for in-window behaviour. That audit is an internal theory
program, not part of the current manuscript pass and not a claim owner.

The audit must not revive superseded or untrained routes as paper claims:

- a single `tau` power law or the historical `p≈0.85` fit;
- `tau_surr = tau_deploy`;
- `K_cos` as the full RoPE kernel;
- `C_app` as derived from the LM objective or full kernel;
- static rank as an LM-quality predictor;
- slow channels as dead, unused, or reclaimable;
- Cosh as a universal optimum;
- EVQ-Cosh-R, phase-isotropy, or other untrained internal candidates.

## Acceptance gates

- Body at most 9 pages; page count follows complete, non-redundant content.
- Section 2 on page 3; Theory on page 4 unless a demonstrably better reader map
  justifies a change.
- Undefined references/citations `0`; worst overfull `0pt` target and `5pt`
  hard ceiling.
- US Letter, anonymous metadata, `\iclrfinalcopy` commented, Type-3 and
  unembedded fonts `0`.
- Figure 1 and lifecycle route leads readable at final size; no orphan,
  caption spill, float-only body page, or large blank region.
- `pdftotext` recovers the five-module reader map in order.
- Full-text searches find no isolated `It does.`, body target-matched/FMR
  subplot, LeRoPE over-attribution, scaling-law wording, or defensive paragraph
  led by an unproved limitation.
- `git diff --check` passes; complete status is reported; `paper/` status and
  `paper/main.pdf` hash remain unchanged.

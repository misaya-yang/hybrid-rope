# Mature-checkpoint retrofit archive

Enter this archive only after [`../../../INDEX.md`](../../../INDEX.md) routes a
mature-checkpoint question. It owns no separate agenda or authorization.

Current boundary:

`FROZEN_PURE_Z_EFFECT_ESTABLISHED /
STATIC_Z_NATIVE_TO_LONG_TRANSPORT_OPEN / NO_SOTA /
NO_ACTIVE_GPU_RUN`

- Fully frozen pure-`z` controls establish that allocation can strongly change
  mature-checkpoint behaviour with no weight updates.
- Ordered frequency/dilation-to-subspace pairing matters because mature weights
  co-adapted to the Native table.
- The open method question is whether one deterministic static `z` table can
  transport existing short capability to long positions at small Native cost.
- The 9/2 natural-QA/headwise corrections remain in the root `INDEX.md`; they do
  not redefine the paper or the author-ordered research direction.

## Open locally only as needed

| Need | Start here |
| --- | --- |
| Full result catalogue and status | [`results/README.md`](results/README.md) |
| Result receipts | [`evidence/`](evidence/) |
| Mechanism/falsification analysis | [`analysis/`](analysis/) |
| Frozen historical protocols | [`preflights/`](preflights/) |
| Current and retracted theory notes | [`theory/README.md`](theory/README.md) |

The root `INDEX.md` owns current claim status and scoped negatives. A result
file owns only its exact protocol; an analysis or preflight is never an action
queue.

If research is explicitly reopened, derive the deterministic static pure-`z`
candidate before LM evaluation, without learning or loss-based frequency
search. Then validate Native/long assay configuration, controls, scorer,
executed identity, raw-owner output, budget, and stop condition before compute.

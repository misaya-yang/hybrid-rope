# Attack 4 on SOLVER_F_CANDIDATE_SOLUTION_V2_20260904 (protocol-feasibility and decision audit)

- **Date:** 2026-09-04 (wave 2b)
- **Role:** independent hostile reviewer; veto-seeking; decision-analyst severity
- **Target:** `SOLVER_F_CANDIDATE_SOLUTION_V2_20260904.md` (S0–S15, §2–§8) — including whether each §0 disposition actually closes the round-1 hole it claims to close
- **Verification actually run:** full read of V2, round-1 memo, ATTACK_1, handoff, preflight, all three observation owners, and `SCALE_CONSISTENT_LOG_PROFILE_RESULT_20260831` (hash lines); read of `scripts/train/train_log_p2_phase_transfer_lora.py`, `scripts/eval/target_free_ruler_smoke.py`, `scripts/eval/run_log_p2_frontier_and_transfer_4080.sh`, `tests/test_log_p2_phase_transfer_lora.py`, `rebuttal/rebuttal_0723/experiments/olmo2_phase_adarope_5090/prepare_identifiable_data.py`, `rebuttal/rebuttal_0723/experiments/olmo2_1b_evq/contract.py` + `evaluate_ruler.greedy_generate`; local CPU recomputation (§F). No GPU used.

## 1. VERDICT: **VETO**

Four FATALs stand (A1′-panel, B1-veto-contradiction, C1-task-transport, C2-authorization-regression), each striking a load-bearing element of V2 as the *protocol* it claims to be: the reveal rule contradicts itself; the gate that the reveal rule depends on is built on a data panel the repo's own data contract cannot supply; the band machinery fits on one task family and falsifies against another without naming it; and the E2/E3 arm set silently breaches the standing QK-first authorization whose disclosure round 1 carried and V2 deleted. Owner-citation arithmetic (surfaces D/E) I re-derived in full and it is **clean** — I say so plainly in §4; the memo's failures are not in its numbers but in what it claims those numbers and modules can do.

## 2. Attacks, ranked by severity

### F1 — FATAL — T1's redesigned power (the A8 "fix") is built on a row panel the repo's data contract cannot legally supply, and the memo states two panel sizes a thousandfold apart

**Target statements:** §0 A8 row ("pooled within-row LSE-vs-ln n regression with SE across 12–16K rows … slope ∈ 1±0.15 on the CI"); S5 ("(12–16K panel)"); S6(i) (reveal opens **iff** T1 passes); §8.5 ("powered … (20+20/cell)").

**Demonstration.**
1. *Internal inconsistency.* S5 sizes the T1a panel at "12–16K rows" of bootstrap rows; §8.5 sizes the E1 powering at "20+20/cell" (≤40–80 rows). Both cannot be the protocol. Under the §8.5 reading the A8 replacement test runs on ~4× the row count round-1's deleted Spearman ran on (4 bands × 20+20); the "12–16K" figure exists nowhere else in the repo and no owner supports it.
2. *The larger reading is contractually impossible.* T1a/P̂ require **held-out ≤4x pair-view rows** (the only rows that carry a same-target counterfactual view — see F3). The pair-view builder (`prepare_identifiable_data.py`) ships splits `train` (128 rows, 16 teacher-forced queries), `train_eos` (32), `component_gate` (default **32** rows, single query, `method_selection_allowed=True`), `final_validation` (default **16** rows, `method_selection_allowed=False` — the builder *hard-raises* "final validation cannot select a method", lines 87–88). V2's own S5 forbids reusing train rows for the fit. The selection-legal held-out panel is therefore ~32 rows/length ≈ **96 margin–accuracy pairs** across 1x/2x/4x as built; reaching 12–16K rows requires a new natural-source corpus (the builder's pool is bounded by `source_rows`; the `--component-gate-rows` flag cannot exceed the source supply, and the supply is capped by the hash-bound corpus the Stage-F contract binds via `EVQ_IDENTIFIABLE_*_DATA`). No repo file evidences a >10²-row held-out pool, and growing one is an unpriced data-contract extension (handoff §5's "stronger future data contract" — nonces, route-explicit eval — is explicitly not implemented).
3. *Consequence for the decision spine.* S6(i) makes the single authorized reveal conditional on T1. Either T1 is run at the legal size — where "slope ∈ 1±0.15 on the CI" is dominated by P̂/Ĝ fit noise on ~96 binary-outcome rows (a 2-parameter logistic on ≤96 points; per-band row-level residuals of natural cue-continuation logits are plausibly O(0.2–0.5) nats, giving slope SEs comparable to the entire ±0.15 window — i.e., the test can neither reliably reject nor reliably pass), or T1 is run at the claimed size, which requires inventing a corpus that does not exist. The A8 disposition ("Redesigned … SE across 12–16K rows") therefore does not close the aired power hole — it replaces an underpowered statistic with one whose stated power is unsupported by the memo's own data base.
4. *Module existence.* The smoke evaluator (`target_free_ruler_smoke.py`) exposes exactly: prediction tokens, official score, reference recall, EOS flag, prompt token count, elapsed time (lines 717–732). **No logits, no margins, no band LSE.** The trainer computes margins only inside the loss (`paired_objective`, lines 125–174). So T1a/T1b/Ĝ/ρ̂/P̂ are three to four new inference modules plus truncated-render data (E1's n′∈{4K,8K,12K,16K} truncations do not exist as assets). This is buildable — chunked diagnostic QK-recompute of ≤16K-token rows is *not* the banned attention fallback (the model path stays Flash; only diagnostic math is added) — but V2 deleted even round 1's disclosure of that route ("documented chunked recompute ≤16K rows only", round-1 §5 item 3) while keeping the dependency, and nowhere prices any of it. The handoff is explicit that the trainer is "a prototype, not a scientifically complete LoRA screen" and lists what is *not* verified; V2's protocol silently inherits all of it and then gates the reveal on modules that are neither built nor costed.

**Repair that must NOT count:** relabeling "12–16K" to "the component-gate pool" without recomputing what slope-CI ±0.15 actually attains at ~96 rows; declaring T1 "advisory" while S6(i) still reads "iff … T1 passes"; proposing a new corpus in prose without supply verification or GPU-cost estimate. What must be re-derived: T1's threshold and panel from the assets that exist, with an explicit power calculation, or T1 removed from the reveal-gate entirely.

### F2 — FATAL — the reveal rule is self-contradictory (S6(i) "iff" vs §5.7/S13 "skip veto, reveal anyway"), and the veto's stated justification collapses on cost figures the memo never has to invent — they are in the owners

**Target statements:** S6(i) ("Open the single blind reveal **iff** both retention gates pass, T1 passes, and â_hi(8x) ≥ τ=0.40"); S13 last bullet ("If GPU is cheap, the protocol's recommended default is to reveal anyway and let the falsification test run — the veto exists to protect a scarce single-reveal seal, nothing more"); §5 item 7 ("Recommended default when blind reveal is cheap: skip veto, reveal, and let the falsification test decide").

**Demonstration.**
1. *Literal contradiction.* A biconditional opening rule and a recommended default that skips its decisive condition are not both the protocol. The memo never defines who declares "cheap", nor states a single cost number anywhere. Following S6(i) and following §5.7 produce different actions on the same pre-reveal state — that is a decision-affecting contradiction, not wording.
2. *The scarcity premise is false on the repo's own receipts, and misidentified anyway.* The thing the veto is said to protect is "a scarce single-reveal seal". The reveal is 60 greedy decodes (20 rows × 3 lengths, core-4 per handoff Stage F order). Owner cost anchors: Obs3's entire 96-step r8 training took **402.56 s** at 28.2 GiB peak (`LOG_P2_QK_LORA_GAIN_MATCHED_RESULT` §2); the smoke evaluator records per-row `elapsed_seconds` and ran 240-row core-4 batteries on the same machine. Even at 128K (single-row KV = 16.0 GiB on this config — verified from `contract.py`: 16 layers × 16 kv-heads × 128 head_dim × 2 tensors × 2 B = 128 KiB/token; ×131,072 = 16.0 GiB; +2.77 GiB weights ⇒ the runner's own comment "131K (32x) requires 32GB+ VRAM" is arithmetically consistent and the 32 GiB passport is feasible), the full reveal is hours of GPU-time. So the veto declines the protocol's *own* flagship deliverable (the unconditional demonstration S1-ii/S13 says only measurement can supply) to save hours. And the single-reveal rule is not a cost constraint at all — it is the firewall discipline of S12 (repeated peeks would contaminate conditional claims). The veto thus protects a self-imposed rule while *destroying* the one object S1-ii licenses unconditionally: by the memo's own epistemics, "reveal anyway" is the dominant strategy, which is exactly what §5.7 says — and what S6(i) forbids.
3. *Answer to the value question (band vs bare reveal).* With the veto optional-by-default, the band's remaining decision role is nil; its remaining evidential role ("measured ≪ â_lo ⇒ P1–P4 falsified *with direction*", S6(iii)) is voided by F1 (â can't be built at stated power) and F3 (the band compares across task families, so a falsification has five exits, not three). Under the memo's own concession that P3 carries 100% of blind readout queries, a bare "train → gates → reveal" path — which the memo itself recommends as default — strictly dominates the conditional-prediction apparatus: same unconditional demonstration, no fictitious panel, no contradiction. As it stands, the four-clause premise set + band + E1 + decision files are **ceremony in the recommended branch and a blocker in the binding branch**.
4. *A second, quieter contradiction:* S6(i)'s open-condition includes T1, whose *fitting data* (held-out pair-view margins) is also what makes S6(iii) interpretive readings legal. If T1 is skipped per §5.7, â's endpoints are unpriced and S6(iii)'s "premise set survives falsification" reading has no licensed estimator — yet §5.7 invokes precisely that reading ("let the falsification test run"). The recommended default consumes the machinery the recommended default skips.

**Repair that must NOT count:** rewording S6(i) to "open if …, unless cheap" (that is the contradiction, restated); moving τ; pricing the veto after seeing â_hi (the veto's whole content is pre-registration); converting §5.7 into an appendix recommendation while keeping "iff" in S6(i). What must be re-derived: one reveal rule, a stated GPU-cost measurement (the owners already supply it), and a demonstration that the band changes some decision the bare reveal does not make strictly better — or the band machinery is withdrawn from the protocol spine (it may survive as pre-registered *commentary*).

### F3 — FATAL — â is fit on the cue-continuation pair-view family and falsified against the RULER core-4 family; the cross-family transport is a fifth un-named premise, and it silently corrupts the memo's quantified-honesty claim (S3) and its falsification semantics (S6(iii))

**Target statements:** S4 ("Ĝ is the inference-time same-target counterfactual margin on 1x/2x/4x held-out rows, P̂(·) … greedy-exact correctness on **those same rows**"); S12 (sealed rows = the physical RULER core-4 set of Stage F); S6(iii) (measured a(S) vs band ⇒ P1–P4 verdict "with direction"); S3 (dependence quantification).

**Demonstration.**
1. *Ĝ requires pair views; only the pair-view corpus has them.* The same-target counterfactual margin needs a correct/deranged source twin of the *same row* (`paired_objective` swaps only source content, trainer lines 137–143). Such twins exist exclusively in the identifiable pair-view corpus (`PAIR_STATUS OLMO2_PHASE_ADAROPE_IDENTIFIABLE_PAIR_VIEW_V2`, physical lengths 4K/8K/16K — the builder hard-codes `PAIR_LENGTHS=(4096, 8192, 16384)`). That corpus's task is **8-token continuation of numbered anchors/cues in natural passages** (`marker_format="\nAnchor {number:02d}: "` / "Continue the quoted cue with exactly the next eight passage tokens").
2. *The endpoint that gets measured at 8x/16x/32x is a different task family.* S12/Stage F step 2/step 6 and the runner bind the blind set to **RULER core-4** (`niah_single_1, niah_multikey_2, niah_multikey_3, vt` — `target_free_ruler_smoke.py` DEFAULT_TASKS; `prepare-ruler` generates exactly these at 32K/64K/128K). There is **no counterfactual-view machinery for RULER rows anywhere in the repo** (grep across `scripts/` and `rebuttal/` for pair-view producers returns only the two trainers + `prepare_identifiable_data.py`; the smoke evaluator has no deranged path). So S4's "same rows" forces P̂ to be fit on cue-continuation (margin, exact) pairs.
3. *Therefore â(S) predicts cue-continuation accuracy; a(S) measures niah/vt accuracy.* For the comparison a(S) ∈ [â_lo, â_hi] to mean what S6(iii) says it means, the pair-view margin→exactness map must transfer to core-4 — a **task-family transport premise**, call it P5, which is nowhere in P1–P4 and has no visible-consequence column. This is not a quibble about emphasis: at *identical* lengths and the *same* substrate the two families differ by tens of points — Obs3's own table: c=.074 frozen core-4 4K = **.8375** vs five-task natural 1x macro = **.315833** (handoff §3.1, same table row V2 itself cites); Obs2's RULER official-macro 4K 42.44 vs its 2Wiki exact 21.5 — same rows' neighbors, same lengths. P̂ fit on family A margins cannot carry family B's base rates; the band's *location* (not just its blind-length extension) is biased by an unstated amount.
4. *Damage to the memo's central honesty claim.* S3 quantifies untested dependence as "100% of blind readout queries … ride on P3". The dependence accounting is done entirely in the length direction and silently assumes the family direction is free. The correct statement of the memo's own construction is: 100% of the *blind comparison* rides on P3 **and** P5, and P5 is *also* violated-by-construction in the visible world (it is the clause that the ≤4x panel cannot check without a second family's labeled margins, which cannot be built without new counterfactual infrastructure). The A1-disposition ("quantified, conceded, not concealed") is thus misquantified: the concession names the biggest load-bearing untestable clause and misses a second one created by the fit/test population mismatch the concession's own numbers gloss over.
5. *Falsification semantics fail.* S6(iii): "measured a(S) ≪ â_lo(S) ⇒ P1–P4 falsified jointly *with a direction worth reporting (distractor nonstationarity beyond 16K, numerator drift > B, or transport failure)*". With P5 unpriced, the same outcome has four exits, and P5 (the boring one: different task, different base rate) will typically dominate — the repo then records a "premise-set falsification with direction" that is in fact a family-mismatch artifact. That is a mechanism story produced by the protocol as written, i.e., the failure mode S15 promises ("no new mechanism story") and S13 polices.

**Repair that must NOT count:** adding "P5 (task transport)" as a fifth bullet without fixing the estimator (the band remains fit on A, tested on B — naming does not un-confound); declaring â a within-family prediction and quietly comparing it to core-4 numbers anyway; fitting Ĝ on RULER rows (impossible — no counterfactual twins of RULER rows exist, and building them is new data infrastructure F1 has already shown unpriced). What must be re-derived: â compared to the *same family* it is fit on (then the reveal endpoint must move to pair-view strict-generation rows — which the pair-view corpus caps at 16K, so the blind set must be *generated* for that family: new builder lengths, untested), or the endpoint family gets its own counterfactual twin generator, or the band is withdrawn from decision-making and interpretation both.

### F4 — FATAL — V2 asserts preflight compliance while its E2/E3 arm design requires the parameter-space opening the standing authorization forbids; the disclosure round 1 carried was deleted, and S11's ordering statement is false about its own protocol

**Target statements:** S11 ("QK runs first per the preflight ordering; E2 runs it against its crossed predictions"); §6 E2 ("fresh-QK vs prototype-QKVO vs D2-QKVO … C2-capacity: QK fails 4x, QKVO passes"); §6 E3 ("Native-table vs s4, identical D2-QKVO contract"); S15 (compliance statement of record).

**Demonstration.**
1. *The standing text.* Handoff §7 Stage F step 7: "Open parameter-matched QKVO **only if** QK routing succeeds but oracle-routed answer/EOS fails. Otherwise QKVO cannot identify the bottleneck." Preflight §4 repeats it ("QKVO opens only if causal routing succeeds but oracle-routed answer/EOS remains weak"). Step 8: the matched Native-table adapter runs "only if a non-Native adapter succeeds and causal attribution is needed".
2. *The design breaches it.* E2's discriminating reading "QK fails 4x, QKVO passes" (the C2-capacity cell of the §3 attribution table — the memo's headline attribution deliverable) is observable only if QKVO is trained and scored **when QK fails** — precisely the branch step 7 seals shut. E2's third reading and all of E3 additionally require D2-QKVO and a Native-table D2-QKVO arm without the step-7/8 preconditions. Attacker-1's clean list passed this because round 1 **disclosed the deviation**: "except that this solution's arm E2 (§6) runs both from the start *because* the attribution prediction is now explicit and two-sided" (round-1 S10). V2 deleted that sentence, kept the design, and *replaced* it with an affirmative compliance statement ("QK runs first per the preflight ordering") that is false of V2's own experiment set, next to an S15 "statement of record" of compliance.
3. *Severity argument.* This is not paperwork-by-another-name: (i) the attribution table (§3, labeled "Interpretation, falsifiable via E2/E3") is one of the memo's two promised deliverables, and it is produced by running arms the authorization bars, while the authorization's stated rationale for the bar ("Otherwise QKVO cannot identify the bottleneck"; preflight §4: "A raw QKVO task win alone does not identify V/O") is exactly the inference E2's reading draws; (ii) the undisclosed deviation + compliance assertion is the same chain-of-custody failure species as the A3 misquote the memo itself treated as mandatory-fix; (iii) as written, the protocol's first authorized run violates the standing contract at step 7, so under the veto standard ("protocol … violating standing authorization in a way that changes what gets decided") it cannot serve as the delivered strongest solution.
4. *Aggravating internal inconsistency:* S11 says "QK runs first per the preflight ordering" — but E2 runs its arms from the start (otherwise the crossed predictions have no observation model); §5 item 5 lists "Arms frozen/QK/QKVO/D2-QKVO param-matched + Native-table control on pass (E3)" — "on pass" for the Native control half-honors step 8 but silently drops it for QKVO, where the binding condition is routing-success + oracle-failure, not "pass".

**Repair that must NOT count:** re-adding the disclosure sentence while leaving S11/S15 asserting compliance with an ordering the protocol violates (the assertions become false in the other direction); claiming the solver-memo supersedes the handoff (only the author/adjudicator can re-authorize). Acceptable repair: either gate E2's QKVO arms by step 7's observed conditions (and accept the C2-capacity cell is then *only* observable in the routing-success/oracle-failure branch — which changes the crossed-prediction table), or carry an explicit amendment request to the author with round 1's rationale restored verbatim.

### M1 — MODERATE — E2's readings still smuggle the forbidden mechanism identification from confounded arms (the "机制故事" returns through the experiment table)

**Target:** §6 E2 ("Exposure-bias: QKVO(teacher) passes 4x but **D2's blind gap is the exposure-bias estimate**"; "E2/E3 arms' blind gap is *pure P1/P2/P3 content*: if D2 and prototype both pass 4x and differ only in blind outcome, the between-length mechanism claim is what is being tested"); S8 falsifier ("D2 passes 4x while prototype fails 4x ⇒ content-exposure component established"; "D2 ≡ prototype in-window ⇒ D2 mechanism refuted").

**Demonstration.** D2 differs from the prototype in **at least two treatment factors** — student-exposure content substitution *and* the terminal pre-EOS→EOS margin contrast (S8 itself lists both changes) — plus an uncontrolled third: any retrained adapter is a different weight vector (single seed; schedule is chaotic-sensitive). The blind D2-vs-prototype gap is therefore a confound of {content exposure, EOS-contrast, trajectory variance} ∪ {P1,P2,P3}: "is the exposure-bias estimate" is an identification the design does not license, and "pure P1/P2/P3 content" is false of it (it is not even pure-P content — it is arm content *and* P content). Worse, S8's falsifiers require detecting a **4x in-window pass/fail flip** (20 rows/cell; Obs3's own 4K core-4 delta CI is ±[−.0400,+.0100] — a one-cell pass/fail boundary flip is within noise of "≡") and an **in-window equivalence** ("D2 ≡ prototype") that equivalence-testing at n=20 can never establish. Note S13's (α) correctly conditions between-length mechanism language on P1–P4; E2/S8's sentences are not covered by that hedge — they assert the identification as an experiment *reading*, and S7's status line ("falsifiable via E2/E3") certifies them as tests. Patch: relabel every E2/S8 blind-gap reading as a composite (arm-difference + premises) and replace the ≡-style falsifiers with CI-intersection language at stated power.

### M2 — MODERATE — the 128K leg of the single reveal is unverified end-to-end (data generation and eval never run), the reveal's memory/cost was never priced, and the hardware naming in the repo is contradictory; V2 inherits all of it silently

**Demonstration.** Handoff §6 lists as *not verified*: "32K/64K/128K RULER data generation and evaluation" and "one-step BF16 Flash gradients and memory headroom". Verified: the evaluator's length allow-list does include ×16/×32 (lines 511–526) and `prepare-ruler` requests 131072 (runner lines 102–110), so the plumbing is *typed*, never *run*. Memory: single-row KV at 128K = **16.0 GiB** + 2.77 GiB weights (+overhead) on this config (16×16×128 MHA verified from `contract.py`; cross-check: Obs2's exactly-8,388,608 QK-r64 param count = 16·64·4096·2 confirms 16 layers × 2048 hidden). Obs3's 26.28-GiB training peak proves the work machine has >26 GiB despite the runner's "4080" name (a 16-GiB 4080 cannot reserve 26.3 GiB — the passport's 32 GiB and the "requires 32GB+ (RTX 5090)" comment are the consistent readings). Consequences: (i) OOM mid-reveal at 131K is an immediate-stop condition; the evaluator *resumes* per-row (completed-row skip, lines 642–659), which protects the seal **only if** the fix requires no code change — a code fix breaks `script_sha256`/run-manifest identity (lines 589–598) and the stop rule "tensor/data/hash drift" then makes a same-output rerun contract-illegal; (ii) a floor at 32x is survivable (handoff: "A 32x floor does not erase a valid lower-length result") but an *unrunnable* 32x is not the same as a floored one, and V2's band/reading table never distinguishes them. MINOR because every piece is patchable with one honest cost/feasibility paragraph and a fallback reading for a never-measured 32x; FATAL-adjacent if E-protocols continue to silently inherit it — the memo's "delivers at blind lengths" promise currently has one leg whose only evidence is "earlier adapter path has work-machine evidence; new lengths untested."

### M3 — MINOR — S3's headline concession mislabels which population its fraction describes

**Demonstration (derived).** The formula 1−(2D/n−(D/n)²) is P(|i−j|>D) for **uniformly random positions i,j** in a row of length n (two corner triangles of leg n−D over n²) — recomputed: 0.2500/0.5625/0.7656 at D=16K for n=32K/64K/128K (V2's numbers ✓). Attacker-1's 46.7/74.2/87.3% is recomputed here as (H−D)/H with H=n−2000: 0.4675/0.7421/0.8731 ✓ — the fraction of **(readout-query, key) pairs** with the query pinned at row end. Both derivations are right; the *label* is wrong at the sentence level: S3 says "most — **in the query-population sense**, all — of the blind prediction mass rides on P3" and immediately supports "most" with the uniform-pair 25% figure, while the query-population figure it is glossing is 46.7% — the *larger*, memo-adverse number. Since S3 is the memo's "conceded, not concealed" centerpiece, understating its own concession by ~1.9× at 8x while claiming the query-population frame is a labeling defect worth one correction (and it flows the honest way — V2's Δ_S·ρ̂ term genuinely operates on distractor mass ~uniform, so the 25% figure is *also* the right number for the band; it just isn't the one the quoted sentence's words describe).

### M4 — MINOR — E3's crossed predictions ignore the in-window asymmetry that Obs2's own numbers force (Native arm dominates in-window, fails blind)

**Demonstration.** Obs2 column-labeled correctly by V2: at 4K the *adapted* Native arm scores 72.19 vs EVQ's 42.44 (a 29.75-pp native advantage, CI [−34.42, −24.85]), while Native collapses at 8K/16K (2.02/0.38). Transposed to E3 (Native-table vs s4, D2-QKVO): the Native-table arm should be expected to *win* in-window (zero table damage; passes S9 gates trivially) while the s4 arm runs at the 0.875302 margin. E3's discrimination is blind-length-only, but the protocol can terminate before any blind length is seen: if the s4 adapter trips its marginal retention gate, the run is a "protocol negative" (S9) and E3 never executes — while the Native-table arm would have passed every gate and floored blind (consistent with the *carrier* hypothesis yet untested). E3's "rival: Native matches s4" must be restricted to blind outcomes and the attribution table needs the pre-blind dropout branch written down.

## 3. Disposition audit of the §0 table (do the claimed fixes close the aired holes?)

| Round-1 item | Claimed disposition | Independent verdict |
| --- | --- | --- |
| A1 | Re-derived via P1–P4 + λ + band; dependence quantified | **Partially closed** — premise honesty is real (P3 zero-visible-consequences, P4 named, minimality withdrawn). But the quantification is mislabeled (M3) and incomplete: the fit/test task-family gap (P5) is a second fully-un-named premise (F3), so "the band's honest worst case" is still not priced. |
| A2 | Conceded; veto = compute-avoidance; strings deleted; false-negative named | **Closed as to labels** (all three strings verified absent outside quoted deletion; "decision not evidence" in S6(i)/S13 ✓; false-negative named ✓). **Reopened as to function:** the veto's justification is a false scarcity claim and its binding rule contradicts the recommended default (F2). |
| A3 | Re-cited to Obs2 | **Closed** — 61.0% EOS and 200/200 empty are OLMo-2 owner lines 84–85 ✓; Obs1 no longer carries them. |
| A4 | Single-owner core; composite verification-gated; 0.8982 dropped; 0.03 relabeled | **Closed**, with F1 caveat: the "verification step" (Native row-set identity) is sound; the composite is now honestly a *derived-composite* with a protocol gate. |
| A5 | Re-attribution to protocol-negative; gate split (a′)/(a) | **Closed** — S9's failure-attribution sentence matches attack-1's demanded fix; frozen-passes-owner ✓ (0.875302 ≥ 0.875). |
| A6 | Two-pass student exposure; +50% wall; P3 half named | **Closed on cost and honesty** (decode at ≤16 tokens ≈ one extra prefill + negligible step-decodes ⇒ ≈+25–50%, honest; positional half conceded). Mechanism-reading of D2 still overclaims (M1). |
| A7 | Bias-free class; base-weights gate; value AND gradient | **Closed.** I verified the GeLU/GeLU′ tail bounds (Φ(−t) ≤ φ(t)/t ⇒ |GeLU(−t)| ≤ tΦ(−t) ≤ φ(t) ≤ (t+1)e^{−t²/2}; same for |GeLU′(−t)| for t ≥ 1), g=60 ⇒ log₁₀ ≈ −779.9 ✓, the threshold algebra (g+g′ ≤ W·k/16K with free W), and that the gate now sits in base weights (round-1's LoRA-bias hole removed). Internally consistent construction; §8.1's self-declared weakness (no instantiated 2-layer example) is the honest status label already given. |
| A8 | Redesigned regression "SE across 12–16K rows" | **NOT closed — F1.** New test inherits the hole in a different form; stated power has no data basis; §8.5 contradicts §5. |
| A9 | Column labels; Δ nominal-vs-effective via ρ̂ | **Closed** on citations ✓ (verified all four columns against owner tables; ρ̂ construction legal). M3 touches the S3 numbers A9's row also cites. |

## 4. NOT AN ATTACK (restated-aired, re-quoted-disclosed) or verified clean

- **Owner-forensics (surface D): every number in §2/§3/S9/S10/§0 matches its owner, to the column.** Obs1: hit@16 18.75→64.06; gold-deletion +1.5055 vs −0.0095; exact 0%; rank 2,043; QA 0.1126 vs 0.2110 ✓ (`EVQ_8B_ADAPTATION_EVIDENCE_20260724` Levels 2–3). Obs2: QA exact 21.5/17.5/4.0 vs 22.0/0/0; F1 24.84→21.48→8.57; RULER official macro 42.44/31.63/5.03 vs 72.19/2.02/0.38; all-refs 0%/0% vs 18.08%/1.54%; 8K EOS 61.0%; 200/200 empty at 16K ✓. Obs3: +0.014107 [+0.009616, +0.019405]; +0.018742; −0.00582 [−0.02054, +0.00352]; core-4 −.0100/+.0025/−.0225 with CIs; 16K frozen .4025 ✓; table hash `56ddfae2…` appears identically in Obs3 §2 and SCALE owner (line 123) ✓ — S9's "same NLL 3.104234 and table hash across owners" is true as stated. Handoff §3.1 retentions 0.875302/0.915103 ✓. The team-lead brief's hypothetical misread ("EVQ-*parent* 42.44") is not in V2 — V2's arms are both phase-adapted, exactly as the owner pairs them.
- **Arithmetic (surface E): re-derived all, zero errors** (my Python): pair fractions 0.2500/0.5625/0.7656 (uniform-pair population — correct for the Δ_S·ρ̂ distractor-mass role) and 0.4675/0.7421/0.8731 (query-at-end — attacker-1's population; M3 is about the label, not the math); frozen retention exp(−0.133187)=0.875302 ✓; 0.875302·e^{0.014107}=0.8877374 vs exp(−0.119079)=0.8877377 (memo's "to 7 decimals" claim is true — they differ at the 7th decimal, consistently rounded ✓); −ln0.88/−ln0.875 = 0.127833/0.133531 ✓; ln2/4/8 ✓; 1x-anchor polynomial value 2c at o=0, 0 at o=1,2 ✓; GeLU bound and g=60 ⇒ ~10⁻⁷⁸⁰ ✓; KV 128 KiB/token, 16.0 GiB @128K, weights 2.77 GiB ✓; QK-r64 = 8,388,608 exact ✓.
- **Lemma F1′ internals:** n-feature (curvature ⇒ mean shift Θ(1/n)), weight-carried threshold, order-statistic exactness on the realized finite sample, blind-flip β > margin, 18K–32K transition confined to blind-row prefixes — all consistent; the gradient-indistinguishability extension (A7's demand) is done and quantified. The ≤4x-unfalsifiability consequence is correctly scoped to "no test can" (S5's power-limit sentence) and is not contradicted by S1-ii (measurement ≠ identification). I could not break the construction.
- **Trainer-script facts V2 depends on:** `FAMILY_PATTERN=("near_2x","far_4x","near_2x","far_4x","replay_1x")` line 19 ✓ (test asserts it); `source_effect[:, :-1]` EOS exclusion line 167 ✓ (S8's fix targets the right line); counterfactual identity assert lines 142–143 ✓; (128,2,L) pair shape line 59 ✓; split-contract check lines 52–53 ✓ (and note: a "heldout"-split directory would be *rejected* by the trainer's loader — reinforcing F1's new-code point); QK/QKVO-only target validation lines 89–93 ✓; flash enforcement + math/mem-efficient disable lines 194–198 ✓; `evaluation_length_firewall [8,16,32]` line 121 ✓.
- **Firewall logic of S1-ii:** selection uses ≤4x gates and sealed geometry-only reads; post-reveal measurement is not an "identification" and S2's theorem does not bar it; §0/A2 string deletions verified; veto-declines-are-recorded-as-decisions text verified present in S6(i)/S13. (The firewall's *operational* enforcement is convention, as everywhere in this repo — not counted against V2.)
- **A2/A3/A4/A5/A6/A7/A9 dispositions** — closed as audited in §3 (no re-litigation).
- S14's "why not pure impossibility" argument is valid given S1-ii and survives independently of F1–F4 (the demonstration leg does not need â).

## 5. If the solver survives

F1–F3 all bite the same organ — the band machinery cannot be estimated, powered, or *interpreted* as a decision object from the assets the repo's own data contract permits — and F2 then removes the only role the band has left (the veto) by the memo's own recommended default. The repair is not another epistemic relabel: either â's estimators, panel, and endpoint family are rebuilt around what exists (≤96 selection-legal held-out pair-view rows; cue-continuation endpoints — which caps the reveal at 16K unless new blind-family data is generated, itself a contract change), or the protocol is honestly re-scoped to "train → gates → single measured reveal, with premises listed and no band" — which S1-ii already licenses and S14 already argues is the surviving leg. F4 is repaired by one disclosed amendment, but until then S11/S15 are false statements of record.

---
*No GPU was used. Everything asserted as measured traces to an owner file; everything asserted as possible-on-this-hardware was recomputed from `contract.py`/`prepare_identifiable_data.py`/`target_free_ruler_smoke.py`/`run_log_p2_frontier_and_transfer_4080.sh` on local CPU.*

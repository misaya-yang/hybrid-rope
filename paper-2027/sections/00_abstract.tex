RoPE gives each attention head a finite frequency budget, but geometric spacing
conflates support location and span with interior allocation. Writing
$x_k=-\log\omega_k=a+Rz_k$ exposes $z$ as a third finite-table design
coordinate. Under the stated uniform separation prior, $23$ slow pairs---$46$
nominal dimensions---have block-whitened effective dimension $2.00$ over
separations up to $4$K. Thus, at fixed support, $K$, and RoPE operator, $z$
determines where phase resolution is placed and which effective positional
basis the same nominal budget realises. On a frozen $1.485$B OLMo checkpoint,
replacing a same-support log-linear grid by a checkpoint-derived allocation
raises nine-task $16$K RULER from $0.56\%$ to $60.47\%$; a label-free
movement-profile ramp reaches $61.04\%$ (official Transformers YaRN at factor
four scores $7.94\%$ on the same rows), with no optimisation or learned
positional parameters. A separate paired three-seed training intervention moves only the
interior frequencies and improves every tested OOD length in every seed,
causally identifying the same coordinate during co-adaptation. A stated convex
surrogate yields \evq{}, a closed-form table unique for that surrogate. Across
zero-training frozen, matched low-rank adaptation, and from-training routes,
controlled interventions establish $z$ as an identifiable and actionable
determinant of long-context behaviour in the tested settings.

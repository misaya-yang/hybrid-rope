Equation~\eqref{eq:warp} specifies the default midpoint \evq{} table as a pure
function of $(\tau,K)$, with no learned parameters or auxiliary loss. Anchored
support embeddings, endpoint $u_k{=}k/K$ quantisation in the mature OLMo
protocols, and the inclusive static diagnostic are labelled where used; each
retains the same continuous Cosh allocation law but defines a different finite
grid or support.
Equation~\eqref{eq:evq-practical} is the full-RoPE reference rule; every protocol
reports its realised $\tau$ explicitly, including the separately fixed MLA and
video settings and the mature-model tables in App.~\ref{sec:mature-details}.
Complete proofs of Theorems~\ref{thm:budget}, \ref{thm:obstruction} and
\ref{thm:ode}, Proposition~\ref{prop:collapse},
Corollary~\ref{cor:quantiles} and Lemma~\ref{lem:budget-crossing}, together
with every stated assumption, are in App.~\ref{sec:proofs}. The identification protocol,
including how sampled extrema and log-span are pinned, the exact pre-specified
factorial design, and the corpora used by each arm, is in
App.~\ref{sec:identification-details}; the mature-scale adaptation protocols, module
budgets, phase-exposure mixtures, and evaluation harness settings are in
App.~\ref{sec:mature-details}; remaining architectures and hyperparameters are
in App.~\ref{sec:experiment-details}. Seed scope is stated with every result.
The anonymous supplement includes the exact-range training entrypoint and
three-seed aggregate, the frozen figure generators, curated mature-protocol
snapshots, and CPU tests, with a reviewer-facing file map and commands.

# Round 12 Phase 0 解读备忘（对照 Pro §11.3 决策表）

日期：2026-09-06 执行日。只读诊断，未启动任何新训练、未新增任何表/增益/头选择、未重排候选、未动容差。
范围：P0a 分叉 E/B（§11.2，5 系统 × 32 组 × 3 布局）、P0b 写/对数分解（§11.1，2 锁定实例 × 2 世界 × 5 层 × 16 头）、4080 探测。

## 0. 执行事实与保真度
- 卡：4080 SUPER 32GB（非预期 16GB），探测全过（7B bf16 载入 14.6GB；16K 前向 peak 16.4GB，3.6s）→ Phase 2 放行。
- 恒等式保真度（预注册容差，未放宽）：
  - fork E/B §6.2 paired-CE 残差 ≤1.1e-7。
  - wd 外重建 ~1.6e-6（门 5e-3）；§5.2 三项恒等式 ≤7.5e-5（门 1e-4）。
  - §2.3 logit 恒等式 rel_residual p99：Z0=6.3e-5、ZF=6.3e-5、ON=4.9e-4（门 1e-3，全过）。
  - 修复史（只修代码，不动容差）：①§2.3 直接/继承项误用绝对旋转，应为相对算子
    W_j=g²R((j−qpos)θ)；②交叉核对测试侧误比 W′k_t vs W′k0，已改正。
    两代缺陷现场存于 `diag/wd_preserved_buggy_runs/`。
  - 内部自洽验证：ON（表=T0 表）的 direct 项 **恰为 0.0**（M′−M=0），
    且 Z0 与 ZF 的 direct 项逐位相同（direct 只依赖表，LoRA 不进 M′）。

## 1. P0a 分叉 E/B（§11.2）
E=(d0−d1)/2 证据判别项，B=(d0+d1)/2 偏置项；E>|B| ⇔ 两世界同判（§6.1）。注册 strict 原样 join。

| 系统 | 布局 | pair-order | E | B | strict(注册) |
|---|---|---|---|---|---|
| T0 | compact | 31/32 | 9.77 | 0.96 | —(无回执) |
| T0 | far / near | 0/32 | 0.004 / 0.012 | −0.41 / −0.37 | — |
| Z0 | far / near | 18/32, 30/32 | 2.83 / 6.08 | 0.68 / 0.95 | 1/32, 3/32 |
| ZC | far / near | 17/32, 30/32 | 2.80 / 6.08 | 0.65 / 0.97 | 1/32, 4/32 |
| ZF | far / near | 22/32, 30/32 | 4.40 / 6.85 | 0.76 / 1.06 | 4/32, 16/32 |
| ON | far / near | 0/32 | 0.008 / 0.044 | −0.08 / −0.03 | 0/32 |

（compact 全系统 E≈9.6-9.8，31/32，strict 25-27/32：短上下文不是战场。）

读法：
1. **E 恢复完全依赖 Z 表。** ON（native 表 + 同款长输入 LoRA）far/near E≈0，与 T0 不可区分——
   LoRA 本身不产生证据判别信号，信号来自频率表。
2. **分叉判别与注册生成之间是质量分配瓶颈。** Z0 near：分叉 30/32 对，注册 strict 仅 3/32；
   ZF near：30/32 对 → 16/32，增益几乎全部来自 margin 翻正
   （mean_gap w0: 0.02→1.18，w1: −0.78→0.44），pair-order 不变
   → "质量分配/裕度"型增益，非"判别方向"型。

## 2. P0b §5.2 写分解（ũ−o⁰ = T1 值写 + T2 路由 + T3 新增竞争）
中位数（跨 2 实例×2 世界×5 层×16 头；β=远视图注意力落在插入背景 D 上的质量）：

| 系统 | Δo 范数 | T1 | T2 | T3 | β(mass_D) | mass_S |
|---|---|---|---|---|---|---|
| T0 | 1.558 | 0.422 | 0.955 | 1.060 | 0.982 | 0.018 |
| Z0 | 0.656 | 0.232 | 1.045 | 0.844 | 0.779 | 0.221 |
| ZF | 0.668 | 0.225 | 1.052 | 0.835 | 0.780 | 0.220 |
| ON | 1.561 | 0.408 | 0.959 | 0.948 | 0.980 | 0.020 |

读法：
1. **native 表下查询位把 98% 注意力质量倒给插入背景 D**（mass_S 仅 1.8%）；
   Z 表把 mass_S 提 12 倍（→22%），β 降 20 个点。写差 Δo 同步缩小 57%：
   Z 表让远输入行为向 compact 行为靠拢。
2. ON 与 T0 的写结构几乎逐项重合（β 0.980、三项范数、深度曲线）——
   在 native 表上，长输入 LoRA 没有改变写入结构；与 §1 的 ON≡T0 互证。
3. 深度结构：T1（值写变化）在 L0≈1e-7、随深度增长；T2（路由）在深层最大
   （L15：T0 T2=1.86，Z0 T2=2.37）。写差在深层以路由项为主。

## 3. P0b §2.3 对数分解（ds = direct 相位项 + inherited 表示漂移项）
对 T0 参考的差（同输入、同查询位；S=原位可见，D=插入背景；
跨 2 实例×2 世界×5 层×16 头的 Σ）：

| 系统 | Δlogit_S | Δlogit_D | direct_S | direct_D | inherited_S | inherited_D |
|---|---|---|---|---|---|---|
| Z0 | −4.27e5 | −1.74e7 | −4.47e5 | −6.46e6 | +1.91e4 | −1.10e7 |
| ZF | −4.10e5 | −1.71e7 | −4.47e5 | −6.46e6 | +3.67e4 | −1.06e7 |
| ON | +3.29e4 | +1.25e6 | 0.0 | 0.0 | +3.29e4 | +1.25e6 |

读法：
1. **换表引起的 logit 变化 97.6% 落在插入背景 D 上**（−1.74e7 vs −4.3e5）。
2. 深层以 inherited（q/k 表示漂移）为主；逐层看（Z0 w0 单实例）：
   L0 direct_D=−1.20e6≈全量（第一层无漂移，机制纯相位），
   L8 direct_D=+2.3e4 vs inherited_D=−1.08e6（深层 98% 来自表示漂移）。
3. S 侧（原位在位 token）则以 direct 相位为主（−4.47e5 vs +1.9e4）。
4. ZF 与 Z0 的 direct 完全相同，差别只在 inherited（LoRA 改表示不改算子）；
   ON 的 direct 恒为 0（表同），全部是 inherited——机械分离成立。

## 4. 对照 §11.3 决策表
| 行 | 观测假设 | 本轮证据 |
|---|---|---|
| 1 直接相位冲突 | 部分支持 | 相位冲突真实存在且是 L0 与 S 侧的主导通道，但不是总预算主体（深层与 D 侧由表示漂移主导）；本轮无任何调 gain 动作 |
| 2 源/查询表示已变 | **强支持** | 换表效应的 97.6% 在 D、深层 98% 为 inherited；仅在最终 readout 打补丁不够 |
| 3 新增背景写入 | **支持** | native 下 β=0.98 质量倒给背景；Z 表回收 20 点但 far strict 仍仅 1-4/32 → 需要学会筛选/no-op，虚拟 position exposure 不充分（ON 为直接证据：同款长输入训练、β 仍 0.98、E≈0） |
| 4 证据对比信号弱 | 支持 | 分叉判别 30/32 对但注册 strict 3/32（Z0 near）：信号存在、裕度不足；ZF margin 翻正 → 真值依赖远源的任务关系是正确战场 |
| 5 目标可满足、未见关系失败 | 支持 | ZF near strict 16/32 仍非 32/32；重点在关系泛化而非加参数 |
| 6 局部参数空间可行但慢 | 待验证 | Track B（CPT+SFT）对应此行；执行待用户当日确认 |

声明：任何后续方法必须指明解决上表哪一行已被观测支持的问题；本轮不派生新候选。

## 5. 本轮未做（依 §11/§12 纪律）
- 未重启 351 盲标与 teacher-prefix 面板（任务 #17/#20 保持 pending）。
- 未做候选重排、未以失败为由重选检查点、未放宽任何容差。
- 负面结果与缺陷现场全部保留：`diag/wd_preserved_buggy_runs/`（两代）、
  fork E/B 的 T0/ON 零信号原样在清单中。
